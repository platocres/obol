'use strict';
const fs=require('fs');
const path=require('path');
const root=path.join(__dirname,'..');
const p=rel=>path.join(root,rel);
const read=rel=>fs.readFileSync(p(rel),'utf8').replace(/\r\n/g,'\n');
const write=(rel,text)=>fs.writeFileSync(p(rel),text);
function replace(rel,from,to){let text=read(rel);if(text.includes(to))return false;if(!text.includes(from))throw new Error(rel+' missing expected text: '+from.slice(0,140));write(rel,text.replace(from,to));return true;}
function append(rel,needle,text){let current=read(rel);if(current.includes(needle))return false;write(rel,current.replace(/\s*$/,'')+'\n\n'+text.replace(/^\s+|\s+$/g,'')+'\n');return true;}
let changed=0;const c=v=>{if(v)changed++;};

// Keep the original migration applicator idempotent after its first contract-layout fix.
{
 const rel='tools/apply-v947.js';let text=read(rel);
 const from=`c(insertBefore('data/product-hardening/item-test-contracts.js',\" 'runtime-app-single-paint':{\",contract,\"'runtime-app-semantic-retirement':{\"));`;
 const to=`c(insertBefore('data/product-hardening/item-test-contracts.js',\"contracts['runtime-app-single-paint']={\",\"contracts['runtime-app-semantic-retirement']={\\n acceptance:[\\n  'The application ownership area uses a stable non-versioned semantic-delta-replay owner rather than an exact historical concatenation, while all 43 v9.43 surviving fragments remain traceable as the frozen semantic ledger.',\\n  'Historical application startup timers, intervals, hashchange listeners, MutationObservers, and delayed route repaint schedules are suppressed; one current router owns navigation and drains surviving historical decorator intent before current workflow/operator presentation commits.',\\n  'Cold boot and route navigation do not directly request versioned historical application files and do not expose a historical Home or Next Steps frame across the complete former timer horizon.',\\n  'README, Product Build Next, Dashboard, runtime consolidation projection, architecture guidance, and historical v9.43/v9.46 milestones distinguish request flattening, first-paint protection, and semantic application retirement without rewriting earlier completion counts.'\\n ],\\n validationCommands:['node tools/sync-app-current.js --check','node tools/validate-app-semantic-current.js','node tools/validate-app-current-equivalence.js','node tools/validate-app-dom-equivalence.js','node tools/validate-app-semantic-boot-browser.js','node tests/run-v9.47-tests.js'],\\n proofFiles:['data/runtime-manifest.js','assets/obol-app-current.js','tools/sync-app-current.js','tools/validate-app-semantic-current.js','tools/validate-app-semantic-boot-browser.js','data/product-hardening/product-hardening-queue.js','assets/product-hardening-dashboard.js','README.md','BUILDING.md','docs/RUNTIME-COMPACTION.md','docs/ARCHITECTURE.md','tests/run-v9.47-tests.js','docs/v9.47.md']\\n};\\n\",\"contracts['runtime-app-semantic-retirement']={\"));`;
 if(text.includes(from)){text=text.replace(from,to);write(rel,text);changed++;}
}

// The compatibility metadata must describe current execution ownership, not just the old delivery shape.
c(replace('data/runtime-manifest.js',
 "strategy:'domain-core-semantic-equivalence+script-exact-load-order+style-cascade-equivalence',\n  consolidation:'semantic-domain-snapshot+semantic-core-delta-replay+ordered-fragment-concatenation',",
 "strategy:'domain-core-app-semantic-equivalence+script-exact-ledger+style-cascade-equivalence',\n  consolidation:'semantic-domain-snapshot+semantic-core-delta-replay+semantic-app-delta-replay+ordered-fragment-concatenation',"));

c(replace('tools/validate-runtime-manifest.js',
 "assert.strictEqual(manifest.compatibility.strategy,'domain-core-semantic-equivalence+script-exact-load-order+style-cascade-equivalence','runtime compatibility strategy protects domain/core semantic equivalence, script order, and CSS cascade equivalence');\nassert.strictEqual(manifest.compatibility.consolidation,'semantic-domain-snapshot+semantic-core-delta-replay+ordered-fragment-concatenation','current runtime owners record mixed semantic/exact-concatenation strategies');",
 "assert.strictEqual(manifest.compatibility.strategy,'domain-core-app-semantic-equivalence+script-exact-ledger+style-cascade-equivalence','runtime compatibility strategy protects domain/core/application semantic equivalence, the frozen historical script ledger, and CSS cascade equivalence');\nassert.strictEqual(manifest.compatibility.consolidation,'semantic-domain-snapshot+semantic-core-delta-replay+semantic-app-delta-replay+ordered-fragment-concatenation','current runtime owners record mixed semantic/exact-concatenation strategies');"));
c(replace('tools/validate-runtime-manifest.js',
 "/* Current ownership: the browser loads one owner per area instead of one request per\n   historical fragment. Domain is a semantic graph snapshot, core is a semantic\n   delta replay, and the other five owners remain exact concatenations. Dedicated\n   validators own the proof for each strategy. */",
 "/* Current ownership: the browser loads one owner per area instead of one request per\n   historical fragment. Domain is a semantic graph snapshot; core and application are\n   semantic delta replays; the four route-lazy compatibility areas remain exact\n   concatenations. Dedicated validators own the proof for each strategy. */"));
c(replace('tools/validate-runtime-manifest.js',
 "assert.strictEqual(coreArea.fragments.length,69,'core semantic owner flattens the 69-fragment state/derivation chain');\nfor(const area of bundleAreas.filter(area=>!['domain','core'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation','non-domain/core area remains an exact ordered concatenation: '+area.id);",
 "assert.strictEqual(coreArea.fragments.length,69,'core semantic owner flattens the 69-fragment state/derivation chain');\nconst appArea=bundleAreas.find(area=>area.id==='app');\nassert(appArea&&appArea.strategy==='semantic-delta-replay','application area is flattened behind a semantic delta-replay current owner');\nassert(manifest.appCurrent&&manifest.appCurrent.owner===appArea.owner,'application semantic owner metadata must name the startup owner');\nassert.strictEqual(manifest.appCurrent.generator,'tools/sync-app-current.js','application semantic owner declares its generator');\nassert.strictEqual(manifest.appCurrent.semanticValidator,'tools/validate-app-semantic-current.js','application semantic owner declares its execution-ownership validator');\nassert.deepStrictEqual(Array.from(manifest.appCurrent.historicalFragments),Array.from(appArea.fragments),'application semantic metadata keeps the exact frozen v9.43 surviving ledger');\nassert.strictEqual(appArea.fragments.length,43,'application semantic owner accounts for all 43 surviving rendering deltas');\nfor(const area of bundleAreas.filter(area=>!['domain','core','app'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation','route-lazy compatibility area remains an exact ordered concatenation: '+area.id);"));

c(replace('tools/validate-runtime-loading.js',
 "/* Consolidation budget: the live historical fragment ledger is 215 scripts deep, and the\n   browser must fetch one current owner per ownership area rather than one file per\n   fragment. Domain is a semantic graph snapshot, core is a semantic delta replay,\n   and app remains an exact concatenation of the fragments that still contribute\n   behavior. */",
 "/* Consolidation budget: the startup semantic source ledger is 215 scripts deep, but the\n   browser fetches one current owner per ownership area rather than one file per\n   fragment. Domain is a semantic graph snapshot; core and application are semantic\n   delta replays. Historical application fragments remain regression/equivalence input,\n   not autonomous browser layers. */"));
c(replace('tools/validate-runtime-loading.js',
 "for(const area of (manifest.bundles.areas||[]).filter(area=>!['domain','core'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains an exact concatenation owner');\nconst appArea=(manifest.bundles.areas||[]).find(area=>area.id==='app');\nassert(appArea&&manifest.appCurrent&&manifest.appCurrent.owner===appArea.owner,'application owner metadata must name the startup owner');\nassert.strictEqual(manifest.appCurrent.equivalenceValidator,'tools/validate-app-current-equivalence.js','application owner declares its retirement validator');\nassert.strictEqual(manifest.appCurrent.domEquivalenceValidator,'tools/validate-app-dom-equivalence.js','application owner declares its browser-level validator');",
 "const appArea=(manifest.bundles.areas||[]).find(area=>area.id==='app');\nassert(appArea&&appArea.strategy==='semantic-delta-replay','application startup owner must be a semantic delta replay');\nassert(manifest.appCurrent&&manifest.appCurrent.owner===appArea.owner,'application owner metadata must name the startup owner');\nassert.strictEqual(manifest.appCurrent.generator,'tools/sync-app-current.js','application owner declares its semantic generator');\nassert.strictEqual(manifest.appCurrent.semanticValidator,'tools/validate-app-semantic-current.js','application owner declares its execution-ownership validator');\nassert.strictEqual(manifest.appCurrent.equivalenceValidator,'tools/validate-app-current-equivalence.js','application owner keeps its v9.43 retirement validator');\nassert.strictEqual(manifest.appCurrent.domEquivalenceValidator,'tools/validate-app-dom-equivalence.js','application owner keeps its browser-level retirement validator');\nfor(const area of (manifest.bundles.areas||[]).filter(area=>!['domain','core','app'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains an exact route-lazy compatibility owner');"));

// Historical release tests must freeze what their release accomplished, not forbid later semantic retirement.
c(replace('tests/run-v9.40-tests.js',
 " for(const area of areas.filter(a=>!['domain','core'].includes(a.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains an exact concatenation owner');",
 " const app=areas.find(a=>a.id==='app');assert(app&&['ordered-fragment-concatenation','semantic-delta-replay'].includes(app.strategy),'later releases may semantically retire the v9.40 application ledger');\n for(const area of areas.filter(a=>!['domain','core','app'].includes(a.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains an exact route-lazy compatibility owner');"));
c(replace('tests/run-v9.41-tests.js',
 " for(const area of manifest.bundles.areas.filter(area=>!['domain','core'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains exact-concatenated');",
 " const app=manifest.bundles.areas.find(area=>area.id==='app');assert(app&&['ordered-fragment-concatenation','semantic-delta-replay'].includes(app.strategy),'later releases may semantically retire application execution without rewriting v9.41 domain ownership');\n for(const area of manifest.bundles.areas.filter(area=>!['domain','core','app'].includes(area.id)))assert.strictEqual(area.strategy,'ordered-fragment-concatenation',area.id+' remains exact-owned route-lazy compatibility');"));
c(replace('tests/run-v9.42-tests.js',
 " /* Demoted in v9.43: later releases retire fragments out of the live areas, so these\n    become one-way ratchets. The 172 semantically flattened domain+core fragments are\n    v9.42's own contract and stay exact. */\n assert(p.consolidatedFragments<=297,'projection never regrows the live runtime ownership areas');\n assert.strictEqual(p.flattenedHistoricalFragments,172,'projection counts domain and core as semantically flattened');\n assert(p.liveHistoricalFragments<=125,'projection never regrows the exact-owned historical fragment count');\n assert(p.liveStartupHistoricalFragments<=64,'projection never regrows exact-owned startup runtime beyond the v9.42 application area');",
 " /* Later releases may semantically flatten additional ownership areas or retire\n    fragments, so v9.42 current-state values are one-way floors/ceilings. Its durable\n    fact is that at least the 103 domain + 69 core fragments are semantic. */\n assert(p.consolidatedFragments<=297,'projection never regrows the live runtime ownership areas');\n assert(p.flattenedHistoricalFragments>=172,'v9.42 establishes a permanent floor of 172 semantically flattened domain+core fragments');\n assert(p.liveHistoricalFragments<=125,'projection never regrows the exact-owned historical fragment count');\n assert(p.liveStartupHistoricalFragments<=64,'projection never regrows exact-owned startup runtime beyond the v9.42 application area');"));
c(replace('tests/run-v9.42-tests.js',
 " assert(/\\*\\*Current runtime ownership areas:\\*\\* 7 owners account for \\d+ historical fragments — 172 semantically flattened, \\d+ still exact-owned; \\d+ fragments stay retired in the frozen ledger\\./.test(readme),'README Product Build Next reports core flattening');",
 " assert(/\\*\\*Current runtime ownership areas:\\*\\* 7 owners account for \\d+ historical fragments — \\d+ semantically flattened, \\d+ still exact-owned; \\d+ fragments stay retired in the frozen ledger\\./.test(readme),'README Product Build Next reports the live projection without freezing v9.42 current-state values');"));
c(replace('tests/run-v9.44-tests.js',
 " assert.strictEqual(p.consolidatedFragments,272,'projection accounts for the live runtime ownership areas after retirement');\n assert.strictEqual(p.flattenedHistoricalFragments,172,'domain and core stay semantically flattened');\n assert.strictEqual(p.liveHistoricalFragments,100,'projection counts the remaining exact-owned historical fragments');\n assert.strictEqual(p.retiredFragments,55,'projection counts the grown retired ledger');",
 " assert.strictEqual(p.consolidatedFragments,272,'v9.44 permanently reduces the non-retired runtime source ledger to 272 fragments');\n assert(p.flattenedHistoricalFragments>=172,'later semantic ownership passes may grow the v9.44 domain+core flattened floor');\n assert(p.liveHistoricalFragments<=100,'later semantic ownership passes may reduce the v9.44 exact-owned ceiling');\n assert.strictEqual(p.retiredFragments,55,'v9.44 permanently grows the retired ledger to 55 fragments');"));
c(replace('tests/run-v9.44-tests.js',
 " assert(readme.includes('**Current runtime ownership areas:** 7 owners account for 272 historical fragments — 172 semantically flattened, 100 still exact-owned; 55 fragments stay retired in the frozen ledger.'),'README Product Build Next reports the retirement');",
 " assert(/\\*\\*Current runtime ownership areas:\\*\\* 7 owners account for 272 historical fragments — \\d+ semantically flattened, \\d+ still exact-owned; 55 fragments stay retired in the frozen ledger\\./.test(readme),'README Product Build Next preserves v9.44 retirement while allowing later semantic ownership changes');"));
c(replace('tests/run-v9.46-tests.js',
 "assert(/deeper semantic retirement remains future work|semantic retirement/i.test(read('docs/v9.46.md')));",
 "assert(/semantic application retirement/i.test(read('docs/v9.46.md')));"));

c(append('docs/v9.46.md','## Retrospective clarification after v9.47',`## Retrospective clarification after v9.47\n\nv9.47 later demonstrated an important limit in the v9.46 contract: proving that the **first visible** Home frame was current did not prove that the 43-fragment historical application chain could never repaint after that commit. The old application owner still contained delayed route/decorator schedules extending beyond the original browser proof's observation window. v9.46 therefore remains correctly recorded as the first-paint visibility milestone, while v9.47 separately owns semantic application execution retirement. Future agents must not merge those metrics or reinterpret v9.46 as having removed historical application execution.`));

console.log('v9.47 future-safety fixes applied: '+changed+' file mutation groups.');
