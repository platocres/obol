'use strict';

/* One-shot branch migration used to keep the cross-file v9.45 release wiring atomic.
 * This file is removed after the migration commit; it is not a runtime owner. */
const fs=require('fs');
const path=require('path');
const root=path.join(__dirname,'..');
const read=rel=>fs.readFileSync(path.join(root,rel),'utf8').replace(/\r\n/g,'\n');
const write=(rel,text)=>fs.writeFileSync(path.join(root,rel),text);
function replaceOnce(rel,find,replacement){
 const text=read(rel),matches=text.split(find).length-1;
 if(matches!==1)throw new Error(rel+': expected exactly one replacement target, found '+matches+' for '+JSON.stringify(find.slice(0,100)));
 write(rel,text.replace(find,replacement));
}
function replaceRegex(rel,re,replacement){
 const text=read(rel),matches=[...text.matchAll(new RegExp(re.source,re.flags.includes('g')?re.flags:re.flags+'g'))];
 if(matches.length!==1)throw new Error(rel+': expected exactly one regex replacement target, found '+matches.length+' for '+re);
 write(rel,text.replace(re,replacement));
}

/* Runtime manifest: the historical list stays frozen, but current CSS ownership is
   now explicitly semantic and independently proven. */
replaceOnce('data/runtime-manifest.js',
 "const styles=['assets/obol-current.css'];\n",
 "const styles=['assets/obol-current.css'];\nconst styleCurrent=Object.freeze({\n owner:'assets/obol-current.css',\n strategy:'semantic-cascade-snapshot',\n sourceRelease:'v9.45',\n historicalFragments:freeze(historicalStyles),\n generator:'tools/sync-current-styles.js',\n equivalenceValidator:'tools/validate-style-current-equivalence.js',\n visualEquivalenceValidator:'tools/validate-style-visual-equivalence.js'\n});\n");
replaceOnce('data/runtime-manifest.js'," schemaVersion:'1.12.0',"," schemaVersion:'1.13.0',");
replaceOnce('data/runtime-manifest.js'," styles:freeze(styles),\n scripts:freeze(scripts),"," styles:freeze(styles),\n styleCurrent,\n scripts:freeze(scripts),");

/* Current manifest validator stops protecting v9.40's exact concatenation shape and
   instead protects the semantic current owner plus the frozen compatibility ledger. */
replaceRegex('tools/validate-runtime-manifest.js',/\/\* The single stylesheet owner is now a flattened cascade rather than an @import chain:[\s\S]*?\nconst flattened=/,
`/* v9.45 keeps one stable stylesheet owner but retires the exact 69-fragment\n   concatenation shape. The historical list/order fingerprint above remains frozen;\n   tools/sync-current-styles.js now derives the semantic cascade snapshot and a real\n   browser proof compares it with the exact historical cascade. */\nconst styleMeta=manifest.styleCurrent;\nassert(styleMeta&&styleMeta.owner===manifest.compatibility.styleOwner,'runtime manifest declares stylesheet current-owner metadata');\nassert.strictEqual(styleMeta.strategy,'semantic-cascade-snapshot','stylesheet current owner is semantic rather than historical-fragment concatenation');\nassert.strictEqual(styleMeta.sourceRelease,'v9.45','stylesheet semantic owner records the flattening release');\nassert.strictEqual(styleMeta.generator,'tools/sync-current-styles.js','stylesheet semantic owner declares its generator');\nassert.strictEqual(styleMeta.equivalenceValidator,'tools/validate-style-current-equivalence.js','stylesheet semantic owner declares its static proof');\nassert.strictEqual(styleMeta.visualEquivalenceValidator,'tools/validate-style-visual-equivalence.js','stylesheet semantic owner declares its browser proof');\nassert.deepStrictEqual(Array.from(styleMeta.historicalFragments),Array.from(historicalStyles),'stylesheet semantic metadata preserves the frozen historical ledger');\nconst css=read(styleMeta.owner).replace(/\\r\\n/g,'\\n');\nassert(css.includes('Obol current stylesheet — semantic cascade snapshot.'),'current stylesheet identifies semantic cascade ownership');\nassert(!/obol-style-fragment:/.test(css),'semantic stylesheet no longer embeds historical-fragment delivery markers');\nassert(!/@import\\b/i.test(css),'semantic stylesheet remains a one-request owner');\nconst styleSync=require('./sync-current-styles');\nassert.strictEqual(css,styleSync.expected(),'current stylesheet is exactly reproducible from the frozen ledger through the semantic generator');\nconst styleStats=styleSync.projection().stats;\nassert(styleStats.removedDeclarations>0&&styleStats.removedRules>0,'semantic stylesheet must prove real cascade retirement rather than a renamed concatenation');\n\nconst flattened=`);

/* v9.40 keeps the durable one-request/frozen-ledger contract without pinning future
   releases to its temporary exact-concatenation implementation. */
replaceRegex('tests/run-v9.40-tests.js',/test\('v9\.40 stylesheet owner is one flattened cascade rather than an import chain',[\s\S]*?\n\}\);\n\ntest\('v9\.40 browser loads consolidated owners/,
`test('v9.40 stylesheet owner remains one request over the frozen compatibility ledger',()=>{\n assert.deepStrictEqual(Array.from(manifest.styles),['assets/obol-current.css'],'one stable stylesheet owner');\n assert.strictEqual(manifest.compatibility.historicalStyles.length,69,'frozen stylesheet ledger remains explicit');\n const css=read('assets/obol-current.css').replace(/\\r\\n/g,'\\n');\n assert(!/@import\\b/.test(css),'the stylesheet owner no longer chains fragment fetches');\n if(manifest.styleCurrent&&manifest.styleCurrent.strategy==='semantic-cascade-snapshot'){\n  assert(css.includes('semantic cascade snapshot'),'later semantic flattening may replace the exact v9.40 delivery shape');\n  assert(!/obol-style-fragment:/.test(css),'later semantic owner no longer needs fragment markers');\n  assert.deepStrictEqual(Array.from(manifest.styleCurrent.historicalFragments),Array.from(manifest.compatibility.historicalStyles),'semantic owner still points at the v9.40 frozen ledger');\n }else{\n  const markers=[...css.matchAll(/\\/\\* obol-style-fragment: ([^ ]+) \\*\\//g)].map(m=>m[1]);\n  assert.deepStrictEqual(markers,Array.from(manifest.compatibility.historicalStyles),'v9.40 exact owner preserves fragment order');\n }\n});\n\ntest('v9.40 browser loads consolidated owners`);

/* Queue and item Definition of Done. */
replaceOnce('data/product-hardening/product-hardening-queue.js',
 '["architecture-runtime","Architecture / runtime","Compact the sedimentary runtime without breaking historical behavior.",16,20]',
 '["architecture-runtime","Architecture / runtime","Compact the sedimentary runtime without breaking historical behavior.",17,20]');
replaceOnce('data/product-hardening/product-hardening-queue.js',
 '["runtime-style-flattening","architecture-runtime","queued",18.8,"Flatten the stylesheet ownership area"',
 '["runtime-style-flattening","architecture-runtime","complete",18.8,"Flatten the stylesheet ownership area"');
replaceOnce('data/product-hardening/item-test-contracts.js',
 "contracts['runtime-consolidation-sync']={",
 `contracts['runtime-style-flattening']={\n acceptance:[\n  'assets/obol-current.css remains the one stable non-versioned live stylesheet owner, while data/runtime-manifest.js declares it as a semantic-cascade-snapshot generated from the complete frozen 69-fragment compatibility ledger rather than an exact fragment concatenation.',\n  'tools/style-cascade-current.js removes only provably superseded declarations for the exact same selector/property/grouping-at-rule context, preserves important precedence and conservative vendor/fallback chains, and tools/validate-style-current-equivalence.js mutation-tests those safety boundaries while requiring deterministic regeneration.',\n  'tools/validate-style-visual-equivalence.js independently proves the semantic snapshot in Chromium by comparing layout geometry and computed styles for every rendered element and visible pseudo-element against the exact historical cascade across representative operator routes at desktop and mobile widths.',\n  'The frozen stylesheet files, v9.5 order/cardinality fingerprint, one-request runtime budget, accessibility owners, responsive behavior, browser-local workspace state, Evidence/proof semantics, command generation, recommendation logic, and reporting remain intact.',\n  'The Product Hardening queue, Dashboard, README handoff, architecture/runtime docs, and release history report stylesheet flattening complete, so Runtime Layer Consolidation has no live items and Product Build Next advances to the queued Evidence correctness defect.'\n ],\n validationCommands:['node tools/sync-current-styles.js --check','node tools/validate-style-current-equivalence.js','node tools/validate-style-visual-equivalence.js','node tools/validate-runtime-manifest.js','node tools/validate-runtime-loading.js','node tests/run-v9.45-tests.js'],\n proofFiles:['data/runtime-manifest.js','assets/obol-current.css','tools/style-cascade-current.js','tools/sync-current-styles.js','tools/validate-style-current-equivalence.js','tools/validate-style-visual-equivalence.js','.github/workflows/browser-smoke.yml','assets/product-hardening-dashboard.js','data/product-hardening/product-hardening-queue.js','README.md','CHANGELOG.md','docs/v9.45.md','docs/RUNTIME-COMPACTION.md','docs/ARCHITECTURE.md','docs/PRODUCT-HARDENING.md','tests/run-v9.45-tests.js']\n};\ncontracts['runtime-consolidation-sync']={`);
replaceOnce('data/product-hardening/item-test-contracts.js',"{version:'9.44.0',requiredForStatuses,contracts}","{version:'9.45.0',requiredForStatuses,contracts}");

/* Focused and release validation. The browser proof stays in browser CI because
   release preflight intentionally has no Playwright installation. */
replaceOnce('tools/scope-check.js',
 " ['tools/validate-runtime-manifest.js'],",
 " ['tools/sync-current-styles.js','--check'],\n ['tools/validate-style-current-equivalence.js'],\n ['tools/validate-runtime-manifest.js'],");
replaceOnce('tools/scope-check.js',
 " ['tests/run-v9.44-tests.js']\n];",
 " ['tests/run-v9.44-tests.js'],\n ['tests/run-v9.45-tests.js']\n];");
replaceOnce('tools/release-preflight.js',
 "'tools/sync-current-styles.js','tools/sync-current-release.js'",
 "'tools/style-cascade-current.js','tools/sync-current-styles.js','tools/validate-style-current-equivalence.js','tools/validate-style-visual-equivalence.js','tools/sync-current-release.js'");
replaceOnce('tools/release-preflight.js',
 "  run('current stylesheet synchronization',['tools/sync-current-styles.js','--check']);\n",
 "  run('current stylesheet synchronization',['tools/sync-current-styles.js','--check']);\n  run('stylesheet semantic equivalence',['tools/validate-style-current-equivalence.js']);\n");
replaceOnce('.github/workflows/browser-smoke.yml',
 "      - name: Upload browser screenshots\n",
 "      - name: Prove the v9.45 semantic stylesheet in a browser\n        env:\n          OBOL_SMOKE_BASE_URL: http://127.0.0.1:4173/index.html\n        run: node tools/validate-style-visual-equivalence.js\n\n      - name: Upload browser screenshots\n");

/* Product Dashboard reports the CSS semantic owner and proof rather than the retired
   exact-concatenation shape. */
replaceOnce('assets/product-hardening-dashboard.js',
 "  {area:'CSS / theme',historical:historicalStyles.length?historicalStyles.length+' fragments flattened into one cascade':'none',current:manifest&&manifest.styles&&manifest.styles.length===1?'yes — '+manifest.styles[0]:'partial',proof:queueStatus('runtime-css-consolidation')==='complete'?'manifest equivalence':'open',tests:'not yet'}",
 "  {area:'CSS / theme',historical:historicalStyles.length?historicalStyles.length+' frozen fragments retained as the equivalence ledger':'none',current:manifest&&manifest.styleCurrent&&manifest.styleCurrent.strategy==='semantic-cascade-snapshot'?'yes — semantic cascade snapshot':(manifest&&manifest.styles&&manifest.styles.length===1?'yes — '+manifest.styles[0]:'partial'),proof:queueStatus('runtime-style-flattening')==='complete'?'static cascade proof + Chromium visual equivalence':(queueStatus('runtime-css-consolidation')==='complete'?'manifest equivalence':'open'),tests:queueStatus('runtime-style-flattening')==='complete'?'currentized; historical ledger retained':'not yet'}");

/* Architecture/current-state docs. */
replaceOnce('docs/ARCHITECTURE.md',
 "v9.40 closes the delivery half. `assets/obol-current.css` is now the flattened ordered concatenation of the same 69 fragments rather than an `@import` chain, so the cascade is byte-for-byte what it was while costing one request instead of seventy. `tools/sync-current-styles.js` still generates it; the owner still adds no rules of its own; `tools/validate-runtime-manifest.js` still verifies exact fragment order against the frozen v9.5 fixture.\n",
 "v9.40 closed the delivery half by flattening the same 69 fragments into one exact-concatenation request. v9.45 closes semantic stylesheet flattening: `assets/obol-current.css` is now a generated semantic cascade snapshot rather than a historical-fragment delivery container. `tools/style-cascade-current.js` removes only superseded declarations for an identical selector/property/grouping context, while `tools/validate-style-current-equivalence.js` proves deterministic source transformation and `tools/validate-style-visual-equivalence.js` compares the semantic owner with the exact frozen cascade in Chromium across desktop/mobile operator surfaces. The 69 historical styles and their v9.5 order fingerprint remain the regression ledger.\n");
replaceOnce('docs/ARCHITECTURE.md',
 "This remains per-area work. Domain and core are flattened semantically and the application area is flattened by retirement; Evidence parsing and stylesheet flattening are still queued separately because each has a different equivalence and workspace-migration surface.\n",
 "The per-area flattening sequence is now complete. Domain and core use semantic owners, application and Evidence parsing were flattened by proven retirement, and v9.45 gives CSS its semantic cascade snapshot with independent Chromium visual proof. The frozen ledgers remain available for regression even though their delivery shape is no longer current architecture.\n");
replaceOnce('docs/PRODUCT-HARDENING.md',
 "v9.40 later closed the delivery half that v9.7 deliberately left open: the same owner is now the flattened concatenation of those 69 fragments rather than an `@import` chain, so the cascade is unchanged and costs one request instead of seventy.\n",
 "v9.40 later closed the delivery half that v9.7 deliberately left open by moving the 69-fragment cascade into one exact-concatenation request. v9.45 closes the semantic half: the same stable owner is generated as a conservative cascade snapshot, with superseded declarations removed only under exact selector/property/grouping-context identity and with Chromium visual equivalence against the frozen historical cascade.\n");
replaceOnce('docs/PRODUCT-HARDENING.md',
 "Current runtime accounting now distinguishes 172 semantically flattened domain/core fragments, 104 fragments still executing through exact current owners, and 51 fragments retired to the frozen ledger. Product Build Next therefore keeps the **Runtime Layer Consolidation** package active for the remaining ownership areas — Evidence parsing and stylesheet — each with its own equivalence, workspace-migration, and test-retirement proof. Do not flatten two areas in one pass.\n",
 "v9.44 then retires four unreachable Evidence overlays, leaving 37 reachable Evidence fragments and 55 total JavaScript fragments retired to the frozen ledger. v9.45 completes the last separate ownership-area pass by replacing the 69-fragment exact CSS concatenation with a semantic cascade snapshot backed by static reduction proof and Chromium visual equivalence. **Runtime Layer Consolidation now has no live items.** Product Build Next advances to the critical Evidence-chain restoration defect uncovered during v9.44 rather than opening another runtime-flattening layer.\n");
replaceOnce('docs/PRODUCT-HARDENING.md',
 "The application area shows that flattening has two shapes, not one. Domain and core were rewritten into semantic owners. The application area was flattened by *subtraction*: `tools/validate-app-current-equivalence.js` proves from source which fragments can no longer execute, `tools/validate-app-dom-equivalence.js` proves in a browser that removing them changes nothing, and the survivors stay byte-identical so Evidence, command, ranking, report, and workspace semantics cannot move. Prefer subtraction where it is available — it carries no rewrite risk.\n",
 "The ownership sequence shows that flattening has multiple safe shapes. Domain and core were rewritten into semantic owners. Application and Evidence used *subtraction* where reachability/supersession could be proven. CSS uses a narrow semantic cascade reduction and an independent browser comparison. Prefer subtraction where it is available, and require equivalence proof whenever current behavior is re-authored.\n");
replaceOnce('docs/RUNTIME-COMPACTION.md',
 "## Dependency and equivalence audits\n",
 `## Stylesheet flattening in v9.45\n\nv9.45 completes the final live Runtime Layer Consolidation ownership-area item. The stable owner remains \`assets/obol-current.css\`, but it stops being an exact formatted copy of all 69 historical stylesheet fragments. \`tools/style-cascade-current.js\` builds a semantic cascade snapshot from that frozen ledger and removes only declarations superseded by the same selector/property in the same ordered grouping-at-rule context. Important precedence is respected; shorthand/longhand relationships are not guessed; vendor/fallback chains and unknown at-rules are preserved conservatively.\n\nThe shipped v9.45 projection reduces 1,817 source style rules / 5,524 declarations to 1,809 rules / 5,496 declarations. The 69 source files remain on disk with the same path-order fingerprint, so historical source observation is retained even though the live owner no longer carries one marker/body per fragment.\n\nTwo independent proofs replace the retired delivery-shape assertion. \`tools/validate-style-current-equivalence.js\` proves deterministic regeneration and mutation-tests the reducer boundaries. \`tools/validate-style-visual-equivalence.js\` substitutes the exact historical cascade for the current owner in Chromium and requires matching layout geometry and computed style for every rendered element and visible pseudo-element across representative routes at desktop and mobile widths. The browser-smoke workflow owns that second proof.\n\nThis closes Runtime Layer Consolidation without deleting the historical ledger. Future style work should edit current stable owners or deliberately evolve this generator/equivalence boundary; it should not restore versioned stylesheet execution merely because an old v9.40 test once observed fragment markers.\n\n## Dependency and equivalence audits\n`);

/* Release history. */
replaceOnce('CHANGELOG.md',
 "## v9.44 — Evidence parsing ownership area flattening\n",
 `## v9.45 — Stylesheet ownership area flattening\n\n- Replaced the live 69-fragment exact CSS concatenation with a generated semantic cascade snapshot in \`assets/obol-current.css\` while retaining every historical stylesheet file and the frozen v9.5 order fingerprint as the compatibility ledger.\n- Added \`tools/style-cascade-current.js\`; the conservative reducer removes only declarations superseded under exact selector/property/grouping-context identity, taking 1,817 source style rules / 5,524 declarations to 1,809 rules / 5,496 declarations while preserving important precedence, fallback-sensitive chains, unknown at-rules, and non-identical contexts.\n- Added \`tools/validate-style-current-equivalence.js\` for deterministic regeneration plus mutation-tested cascade safety boundaries, and \`tools/validate-style-visual-equivalence.js\` for an independent Chromium comparison against the exact historical cascade across seven routes at desktop and mobile widths.\n- Currentized the v9.40 historical stylesheet assertion so it keeps protecting the frozen ledger and one-request owner without forcing exact-fragment delivery shape back into the current runtime.\n- Closed \`runtime-style-flattening\`, completing the Runtime Layer Consolidation package and advancing Product Build Next to \`cc-evidence-chain-restore\`.\n\n## v9.44 — Evidence parsing ownership area flattening\n`);

console.log('Applied v9.45 release wiring.');
