'use strict';
const assert=require('assert'),fs=require('fs'),vm=require('vm'),path=require('path');global.window=globalThis;global.DOMParser=function(){};
function run(file){vm.runInThisContext(fs.readFileSync(file,'utf8'),{filename:file});}const root=path.join(__dirname,'..');
for(const f of ['core-v2-base.js','core-v2.js','core-v2.1.js','core-v2.2.js','core-v2.3.js','core-v2.4.js','core-v2.5.js','core-v2.6.js','core-v2.7.js','review-v2.7.js'])run(path.join(root,'assets',f));
const C=global.OBOL_CORE_V2;let passed=0;function test(n,f){try{f();console.log('ok - '+n);passed++;}catch(e){console.error('FAIL - '+n);throw e;}}
test('same artifact value remains context-safe while preserving per-context provenance',()=>{const s=C.newState(),a=C.mergeHost(s,{ip:'10.10.10.10'}),b=C.mergeHost(s,{ip:'10.10.10.20'}),ca={type:'host',id:a.id},cb={type:'host',id:b.id};const x=C.addTypedArtifact(s,'files','C:\\Temp\\proof.txt',{context:ca,source:'first'}),y=C.addTypedArtifact(s,'files','C:\\Temp\\proof.txt',{context:cb,source:'second'}),x2=C.addTypedArtifact(s,'files','C:\\Temp\\proof.txt',{context:ca,producer:{cardId:'followup',source:'card-evidence'}});assert.notStrictEqual(x.id,y.id);assert.strictEqual(x.id,x2.id);assert.strictEqual(x.contextKey,C.contextKey(ca));assert.strictEqual(y.contextKey,C.contextKey(cb));assert(x.producedBy.some(p=>p.cardId==='followup'));});
test('typed artifact review gate suppresses unchecked intake proposals',()=>{const s=C.newState();s.ui.typedArtifactGate27=[C.artifactReviewKey27('hosts','10.10.10.10')];const kept=C.addTypedArtifact(s,'hosts','10.10.10.10',{source:'intake:v2.6'}),dropped=C.addTypedArtifact(s,'hosts','10.10.10.20',{source:'intake:v2.6'});assert(kept);assert.strictEqual(dropped,null);assert.strictEqual(s.typedArtifacts.hosts.length,1);});
test('network review gate suppresses unchecked network observations',()=>{const s=C.newState();s.ui.networkGate27=false;const added=C.recordNetworkObservations(s,[{type:'route',network:'172.16.50.0/24',gateway:'172.16.1.1'}],{source:'intake:v2.7'});assert.strictEqual(added.length,0);assert.strictEqual(s.networkModel.observations.length,0);});
console.log(`\n${passed} v2.7 hardening tests passed`);
