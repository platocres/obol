{
  "schemaVersion": "1.0.0",
  "release": "v9.11",
  "viewports": [
    {"id":"narrow-laptop","width":1280,"height":800},
    {"id":"exam-split","width":1024,"height":768},
    {"id":"tablet-portrait","width":768,"height":1024},
    {"id":"mobile","width":390,"height":844}
  ],
  "routes": ["#/home","#/boxes","#/intake","#/path","#/tools/nmap","#/report","#/dashboard"],
  "invariants": [
    "primary operator content remains readable without page-level horizontal scrolling",
    "wide tables and tab strips contain their own horizontal overflow instead of widening the page",
    "command text wraps while copy and action controls remain reachable",
    "forms and credential rows collapse without clipping inputs",
    "modals remain inside the dynamic viewport",
    "Product Dashboard summary, queue, and ledger remain usable at narrow widths",
    "keyboard focus treatment remains visible after responsive reflow"
  ]
}
