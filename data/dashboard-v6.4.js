// Obol v6.4 dashboard metadata — source fidelity becomes atomic and requirement-aware.
(function(root){
'use strict';
root.OBOL_DASHBOARD_V64={version:'6.4.0',releaseMilestone:{release:'v6.4',implemented:93,partial:34,gap:0,stale:0,coveragePct:73,representedPct:100,label:'atomic Orange source-fidelity accounting added'},source:'v6.2 source-depth baseline plus revised North Star requirements',sourceFidelityPhase:{title:'Source fidelity is deeper than canonical coverage',statement:'The Dashboard now tracks three separate layers: canonical breadth, source inventory depth, and per-unit North Star delivery readiness. A broad owner can represent an Orange section without proving that each subordinate branch is actually modeled end to end.',adcsExample:'AD CS is the first atomized source file. Seven broad canonical sections are decomposed into nineteen meaningful source units spanning enumeration plus the distinct ESC families and important variants in the pinned Orange 2025.03 source.',completionRule:'Orange is not exhausted until every methodology source file is atomized and every useful atomic unit has a terminal modeled, superseded, or rejected disposition with its required tool, Run, execution-context, Evidence, Next Steps, cleanup, and reporting dimensions accounted for.'}};
})(typeof window!=='undefined'?window:globalThis);
