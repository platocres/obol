'use strict';
(function(root){
const WAVE='v9.78-web-authz-idor-verb-cluster';
const ACTIVE_QUEUE_ID='source-note-cluster-web-authz-idor-verb-tampering';
const ACTIVE_CLUSTER_ID='web-authz-idor-verb-tampering';
const CARD_ID='web-authz-boundaries';
const ANALYZER_ID='web-authz-idor-verb-evidence-analyzer-v978';
const SOURCE_ROUTE='platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json';
const SOURCE_PACKETS=Object.freeze(['data/review-packets/htb-penetration-tester-01.json']);
const REVIEW_TEXT_CHARS=177587;
const TOTAL_NOTES=18;
const CORE_AUTHZ_NOTES=7;
const NEIGHBOR_NOTES=11;
const PUBLIC_NOTE_IDS=Object.freeze([
 'note-object-reference-discovery-v978',
 'note-authorization-differential-proof-v978',
 'note-http-method-replay-boundary-v978',
 'note-bounded-idor-enumeration-v978',
 'note-authz-report-boundary-v978'
]);
function fl(v){return Object.freeze((v||[]).slice());}
function fo(v){return Object.freeze(v||{});}
function uniq(v){return Array.from(new Set((v||[]).filter(Boolean)));}
function lanes(){return Array.isArray(root.OBOL_LANES)?root.OBOL_LANES:Array.isArray(root.LANES)?root.LANES:[];}
function liveCard(id){if(!id)return null;if(typeof root.liveCardById==='function'){try{const c=root.liveCardById(id);if(c)return c;}catch(_){}}if(root.CARDS&&root.CARDS[id])return root.CARDS[id];for(const lane of lanes())for(const card of lane.cards||[])if(card&&card.id===id)return card;return null;}
function ensureLane(){if(!Array.isArray(root.OBOL_LANES)&&!Array.isArray(root.LANES))root.OBOL_LANES=[];let lane=lanes().find(l=>l&&(l.id==='web-authorization'||l.lane==='web-authorization'||l.id==='web'));if(!lane){lane={id:'web-authorization',lane:'web-authorization',title:'Web Authorization',group:'Initial Access & Web',cards:[]};if(Array.isArray(root.OBOL_LANES))root.OBOL_LANES.push(lane);else if(Array.isArray(root.LANES))root.LANES.push(lane);}if(!Array.isArray(lane.cards))lane.cards=[];return lane;}
function replaceCard(card){const lane=ensureLane();const i=lane.cards.findIndex(c=>c&&c.id===card.id);if(i>=0)lane.cards.splice(i,1,card);else lane.cards.push(card);root.CARDS=root.CARDS&&typeof root.CARDS==='object'?root.CARDS:{};try{root.CARDS[card.id]=card;}catch(_){}return !!liveCard(card.id);}
const PUBLIC_NOTES=fl([
 fo({id:'note-object-reference-discovery-v978',title:'Find object references before calling IDOR',body:'Look for direct object references in URL parameters, request bodies, API routes, hidden form fields, cookies, and front-end JavaScript calls. Treat encoded or hashed-looking values as transform questions first: decode, compare structure, and only then replay a changed reference.',kind:'path-guidance',cardIds:fl([CARD_ID]),toolIds:fl(['Burp Repeater','curl','ffuf']),pathIds:fl(['path',CARD_ID]),tags:fl(['idor','object-reference','authorization','web']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-authorization-differential-proof-v978',title:'Prove authorization with role and object differentials',body:'A changed status code is not enough. Capture an authorized baseline, a same-role different-object replay, a lower-role or unauthenticated replay, and the relevant response body differences. Promote impact only when the returned object, state change, or function access crosses the role/object boundary.',kind:'evidence',cardIds:fl([CARD_ID]),toolIds:fl(['Burp Repeater','curl']),pathIds:fl([CARD_ID]),tags:fl(['idor','access-control','authorization','replay']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-http-method-replay-boundary-v978',title:'Replay methods as authorization states, not magic bypasses',body:'HTTP verb tampering is a method-consistency test. Replay the same function through allowed and alternative methods while preserving comparable authentication, object reference, headers, and body/query meaning. A bypass matters only when the alternate method reaches a protected function or downstream effect that the guarded method blocks.',kind:'tool-guidance',cardIds:fl([CARD_ID]),toolIds:fl(['Burp Repeater','curl']),pathIds:fl([CARD_ID]),tags:fl(['http-method','verb-tampering','authorization','replay']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-bounded-idor-enumeration-v978',title:'Bound IDOR enumeration and keep proof samples small',body:'When object IDs or file references are predictable, enumerate in a bounded range with rate limits, deduplicate discovered references, and keep representative proof samples. Do not turn a proof-of-access finding into wholesale data collection when a few reviewed examples prove the broken authorization boundary.',kind:'script',cardIds:fl([CARD_ID]),toolIds:fl(['curl','ffuf','Burp Intruder']),pathIds:fl([CARD_ID]),tags:fl(['idor','enumeration','script-guidance','evidence']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-authz-report-boundary-v978',title:'Report the failed authorization boundary',body:'Report the broken server-side check: object ownership, function role, method coverage, client-side-only control, or inconsistent route middleware. Unguessable IDs, disabled buttons, hidden JavaScript functions, or blocked POST filters are not fixes if the server accepts an equivalent unauthorized request.',kind:'report',cardIds:fl([CARD_ID]),toolIds:fl(['curl','Burp Repeater']),pathIds:fl([CARD_ID]),tags:fl(['reporting','authorization','idor','verb-tampering']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE})
]);
const PRODUCT_CHANGES=fl(PUBLIC_NOTE_IDS.map(id=>'field-note:'+id).concat(['live-card:'+CARD_ID,'evidence-parser-change:'+ANALYZER_ID,'cluster-ledger-completion:'+ACTIVE_QUEUE_ID]));
function redact(v){return String(v||'').replace(/HTB\{[^}]+\}|flag\{[^}]+\}/gi,'[flag-redacted]').replace(/Answer:\s*[^\n\r]+/gi,'Answer: [redacted]').replace(/\b\d{1,3}(?:\.\d{1,3}){3}(?::\d+)?\b/g,'[host]').replace(/\b[A-Fa-f0-9]{24,128}\b/g,'[encoded-or-secret-material]').slice(0,1400);}
function hash(v){let h=0,s=String(v||'');for(let i=0;i<s.length;i++)h=((h<<5)-h+s.charCodeAt(i))|0;return String(Math.abs(h));}
function analyze(text){const raw=String(text||''),matches=[];function add(id,label,fact){matches.push(fo({id,label,fact}));}if(/\b(?:idor|object reference|uid=|user_id|account_id|filename=|file_\d+|invoice|document|direct object)\b/i.test(raw))add('object-reference','Direct object reference or predictable object naming observed','web.authz.object_reference_observed');if(/\b(?:role|admin|user|employee|owner|same-role|lower-role|unauthenticated|access denied|forbidden|401|403)\b/i.test(raw))add('role-differential','Role or authorization differential clue observed','web.authz.role_differential_observed');if(/\b(?:GET|POST|PUT|DELETE|PATCH|OPTIONS|HEAD|Change Request Method|verb tamper|method override|X-HTTP-Method)\b/i.test(raw))add('method-replay','HTTP method replay or verb-tampering clue observed','web.authz.method_replay_observed');if(/\b(?:base64|hex|urlencode|decode|encode|hash|md5|CryptoJS|encoded|hashed)\b/i.test(raw))add('reference-transform','Encoded, hashed, or transformed reference clue observed','web.authz.reference_transform_observed');if(/\b(?:for i in|seq |Intruder|Fuzzer|ffuf|wget|grep -o|enumerat|mass|range|dedupe|sort -u)\b/i.test(raw))add('bounded-enumeration','Enumeration or object harvesting clue observed','web.authz.enumeration_observed');if(/\b(?:created|deleted|updated|changed password|downloaded|returned document|state change|file was successfully|bypassed|protected function)\b/i.test(raw))add('protected-effect','Protected object/function effect clue observed','web.authz.protected_effect_claim_observed');const outcomeFacts=fl(uniq(matches.map(m=>m.fact))),warnings=[];if(outcomeFacts.includes('web.authz.object_reference_observed'))warnings.push('Object reference control is only a hypothesis until compared across roles, owners, or sessions.');if(outcomeFacts.includes('web.authz.method_replay_observed'))warnings.push('Method replay needs comparable auth, object, headers, and body/query meaning before impact is claimed.');if(outcomeFacts.includes('web.authz.enumeration_observed'))warnings.push('Keep enumeration bounded and promote a few reviewed samples, not bulk data collection, into report proof.');if(outcomeFacts.includes('web.authz.protected_effect_claim_observed'))warnings.push('Protected effects require baseline, negative control, and role/object differential evidence.');return fo({id:ANALYZER_ID,matchCount:matches.length,matches:fl(matches),outcomeFacts,warnings:fl(warnings),snippetHash:hash(redact(raw)),snippet:redact(raw)});}
function upsertNotes(){const prev=root.OBOL_NOTE_INTEGRATION;if(!prev||!Array.isArray(prev.publicFieldNotes))return false;const byId=new Map(prev.publicFieldNotes.map(n=>[n&&n.id,n]).filter(p=>p[0]));for(const n of PUBLIC_NOTES)byId.set(n.id,n);root.OBOL_NOTE_INTEGRATION=fo(Object.assign({},prev,{publicFieldNotes:fl(Array.from(byId.values())),__webAuthzIdorVerbClusterV978:true}));return true;}
function cmd(tool,run,when,evidence,note){return fo({tool,run,when,evidence,note});}
function installCard(){const prev=liveCard(CARD_ID)||{};const prior=Array.isArray(prev.commands)?prev.commands:[];const add=[
 cmd('curl','curl -i -s -k -b "{{low_priv_cookie}}" "{{base_url}}/{{object_route}}?{{object_param}}={{owned_object_id}}"','Capture the authorized baseline for the object or function.','Status, headers, body marker, object owner, and response length for the object you are allowed to access.','Baseline first; this does not prove a vulnerability.'),
 cmd('curl','curl -i -s -k -b "{{low_priv_cookie}}" "{{base_url}}/{{object_route}}?{{object_param}}={{other_object_id}}"','Replay the same request with a peer or higher-privilege object reference.','Compare returned object, owner markers, body differences, and denial behavior against the baseline.','A different response is triage; unauthorized object data or state change is the proof.'),
 cmd('curl','for m in GET POST PUT DELETE PATCH OPTIONS HEAD; do printf "\\n== %s ==\\n" "$m"; curl -i -s -k -X "$m" -b "{{cookie}}" "{{url}}" -d "{{body_or_empty}}" | sed -n "1,20p"; done','Test method consistency after you understand the protected function.','A method matrix showing which verbs are routed, denied, or reach the protected action.','Keep request meaning comparable; do not call verb difference alone a bypass.'),
 cmd('Burp Repeater','Send authorized baseline, changed-object replay, unauthenticated replay, and alternate-method replay as four labeled tabs.','Use when headers/cookies/body shape are easier to preserve in Burp than shell templates.','Side-by-side request/response evidence with role, object, and method labels.','Promote only the response pair that crosses an authorization boundary.'),
 cmd('curl','for id in $(seq {{start_id}} {{end_id}}); do curl -s -k -b "{{cookie}}" "{{collection_url}}?{{object_param}}=$id" | grep -oE "{{object_reference_regex}}"; done | sort -u','Use only inside the authorized lab scope after a single object-reference hypothesis is proven.','Deduplicated references or document links from a bounded range, with representative samples reviewed manually.','Keep enumeration bounded and evidence-focused; do not collect unnecessary data.')
];const byRun=new Map();prior.concat(add).forEach(c=>{if(c&&c.run)byRun.set(c.run,c);});return replaceCard(fo(Object.assign({},prev,{id:CARD_ID,lane:prev.lane||'web-authorization',title:prev.title||'Authorization Boundary Replay',hypothesis:'Use this when a request appears to expose object, function, role, method, or client-side-control authorization behavior. Prove the boundary with baselines, changed-object replay, role/session comparison, method consistency, and downstream effect evidence.',commands:fl(Array.from(byRun.values())),expected:fl(uniq([].concat(prev.expected||[],['authorized baseline captured','changed-object replay compared','lower-role or unauthenticated control compared','HTTP method matrix reviewed','object/function effect boundary selected','bounded enumeration sample reviewed','report boundary selected']))),produces:fl(uniq([].concat(prev.produces||[],['web.authz.object_reference_reviewed','web.authz.method_matrix_reviewed','web.authz.role_object_differential_reviewed','web.authz.report_boundary_selected']))),tools:fl(uniq([].concat(prev.tools||[],['curl','Burp Repeater','Burp Intruder','ffuf']))),sourceAuthz78:fo({wave:WAVE,proof:'data/product-hardening/web-authz-idor-verb-cluster-v9.78.js',sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:TOTAL_NOTES,coreAuthzNotes:CORE_AUTHZ_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,publicNotes:PUBLIC_NOTE_IDS})})));}
function patchEvidence(){const T=root.OBOL_INTAKE_V21;if(!T||typeof T.analyzeTerminal!=='function')return false;if(T.analyzeTerminal.__webAuthzIdorVerbClusterV978)return true;const original=T.analyzeTerminal;try{T.analyzeTerminal=function(text){const result=original.apply(this,arguments)||{},a=analyze(text);if(a.matchCount){const acts=Array.isArray(result.activities)?result.activities.slice():[];acts.push(fo({id:'evidence-web-authz-'+a.snippetHash,cardId:CARD_ID,title:'Authorization replay evidence staged for boundary review',result:a.outcomeFacts.includes('web.authz.protected_effect_claim_observed')?'interesting':'triage',summary:a.warnings[0]||'Authorization evidence needs baseline and role/object comparison.',facts:a.outcomeFacts}));result.activities=acts;result.webAuthzEvidence78=a;}return result;};T.analyzeTerminal.__webAuthzIdorVerbClusterV978=true;return true;}catch(_){return false;}}
function completeCluster(){const prev=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!prev||!prev.status||!Array.isArray(prev.pendingClusters)||!Array.isArray(prev.reviewQueue))return false;if(prev.status.latestCompletedClusterQueue===ACTIVE_QUEUE_ID)return true;const active=prev.pendingClusters.find(c=>c&&c.id===ACTIVE_CLUSTER_ID)||{};const pending=fl(prev.pendingClusters.filter(c=>c&&c.id!==ACTIVE_CLUSTER_ID));const queue=fl(prev.reviewQueue.filter(i=>i&&i.id!==ACTIVE_QUEUE_ID));const remaining=pending.reduce((s,c)=>s+Number(c.pendingCount||0),0);const counts=pending.reduce((a,c)=>{a[c.readiness]=(a[c.readiness]||0)+1;return a;},{});const done=fo({id:ACTIVE_CLUSTER_ID,queueId:ACTIVE_QUEUE_ID,completedBy:'v9.78',sourceRoute:SOURCE_ROUTE,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:TOTAL_NOTES,coreAuthzNotes:CORE_AUTHZ_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,ownerCards:fl([CARD_ID]),productOutputs:PRODUCT_CHANGES,tags:fl(['web','idor','authorization','verb-tampering','replay']),rationale:'Whole-cluster packet text was re-read. Public-safe mechanics were folded into the existing authorization-boundary card, field notes, analyzer facts, and report guidance; neighboring non-authz material remains for its own cluster instead of becoming duplicate cards.'});const completed=fl([].concat(prev.completedClusters||[],[done]));const status=fo(Object.assign({},prev.status,{schemaVersion:'2.2.0',reviewedSourceNotes:233,pendingSourceNotes:remaining,clusteredPendingNotes:remaining,unclusteredPendingNotes:0,clusterCount:pending.length,readyClusterCount:counts['ready-to-mine']||0,privateHeavyClusterCount:counts['private-heavy']||0,needsSplitClusterCount:counts['needs-split']||0,latestCompletedClusterQueue:ACTIVE_QUEUE_ID,latestCompletedClusterId:ACTIVE_CLUSTER_ID,latestCompletedClusterReviewTextChars:REVIEW_TEXT_CHARS,latestClusterReviewWave:WAVE,nextClusterReviewQueue:queue[0]&&queue[0].id||null}));function validate(){const f=[],sum=pending.reduce((s,c)=>s+Number(c.pendingCount||0),0);if(sum!==status.pendingSourceNotes)f.push('pending cluster counts do not match pending source notes');if(queue.length!==pending.length)f.push('review queue length must match pending clusters');if(queue.some(i=>i&&i.id===ACTIVE_QUEUE_ID))f.push('completed active cluster still appears in review queue');if(pending.some(c=>c&&c.id===ACTIVE_CLUSTER_ID))f.push('completed active cluster still appears pending');if(status.pendingSourceNotes!==323)f.push('v9.78 pending source-note count should be 323');if(status.clusterCount!==16)f.push('v9.78 pending cluster count should be 16');if(/HTB\{|flag\.txt|Password123|94\.237|83\.136|Answer:|BEGIN RSA PRIVATE KEY|AKIA[0-9A-Z]{16}/i.test(JSON.stringify({pending,queue,done,status})))f.push('cluster completion metadata leaked private/source-specific material');return fl(f);}root.OBOL_SOURCE_NOTE_CLUSTERS=fo(Object.assign({},prev,{status,pendingClusters:pending,reviewQueue:queue,completedClusters:completed,latestCompletedCluster:done,validate}));return true;}
function updateProgress(){const p=root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS;if(!p||typeof p!=='object')return false;root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS=fo(Object.assign({},p,{reviewed:233,pending:323,latestWave:WAVE,latestClusterReview:fo({id:ACTIVE_QUEUE_ID,clusterId:ACTIVE_CLUSTER_ID,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,notesReviewed:TOTAL_NOTES,publicNotes:PUBLIC_NOTE_IDS,productChanges:PRODUCT_CHANGES,neighborNotesDispositioned:NEIGHBOR_NOTES})}));return true;}
function validate(){const f=[];const notes=root.OBOL_NOTE_INTEGRATION&&Array.isArray(root.OBOL_NOTE_INTEGRATION.publicFieldNotes)?root.OBOL_NOTE_INTEGRATION.publicFieldNotes:[];for(const id of PUBLIC_NOTE_IDS)if(!notes.some(n=>n&&n.id===id))f.push('missing public note '+id);const card=liveCard(CARD_ID);if(!card)f.push('missing live card '+CARD_ID);else{const runs=(card.commands||[]).map(c=>String(c&&c.run||''));if(!runs.some(run=>run.includes('{{other_object_id}}')))f.push('card lacks changed-object replay command');if(!runs.some(run=>run.includes('GET POST PUT DELETE')))f.push('card lacks HTTP method matrix command');if(!(card.expected||[]).includes('lower-role or unauthenticated control compared'))f.push('card lacks role/session negative control expectation');}const clusters=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!clusters||!clusters.status)f.push('cluster ledger missing after v9.78');else{if(clusters.status.latestCompletedClusterQueue!==ACTIVE_QUEUE_ID)f.push('latest completed cluster queue did not advance to v9.78');if(clusters.status.reviewedSourceNotes!==233)f.push('reviewed source-note count did not advance to 233');if(clusters.status.pendingSourceNotes!==323)f.push('pending source-note count did not reduce to 323');if(clusters.reviewQueue&&clusters.reviewQueue.some(i=>i&&i.id===ACTIVE_QUEUE_ID))f.push('completed v9.78 cluster still queued');}return fl(f);}
upsertNotes();
const cardInstalled=installCard();
const evidencePatched=patchEvidence();
const clusterCompleted=completeCluster();
const progressUpdated=updateProgress();
const failures=validate();
root.OBOL_WEB_AUTHZ_IDOR_VERB_CLUSTER_V978=fo({status:failures.length?'needs-attention':'live-integrated',wave:WAVE,activeQueueId:ACTIVE_QUEUE_ID,activeClusterId:ACTIVE_CLUSTER_ID,cardId:CARD_ID,analyzerId:ANALYZER_ID,sourceRoute:SOURCE_ROUTE,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:TOTAL_NOTES,coreAuthzNotes:CORE_AUTHZ_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,publicNoteIds:PUBLIC_NOTE_IDS,productChanges:PRODUCT_CHANGES,notesIntegrated:!!(root.OBOL_NOTE_INTEGRATION&&root.OBOL_NOTE_INTEGRATION.__webAuthzIdorVerbClusterV978),cardInstalled,evidencePatched,clusterCompleted,progressUpdated,analyze,failures});
})(typeof window!=='undefined'?window:globalThis);
