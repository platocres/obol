'use strict';
(function(root){
const W='v9.83-web-content-discovery-fingerprinting-cluster';
const ACTIVE='source-note-cluster-web-content-discovery-and-technology-fingerprinting';
const CLUSTER='web-content-discovery-and-technology-fingerprinting';
const NEXT='source-note-cluster-credential-dumping-lsass-and-windows-secrets';
const ROUTE='platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json';
const CARD='web-content-discovery-fingerprinting-workflow';
const UPLOAD='web-upload-inclusion-proof-chain';
const BURP='burp-intruder-fuzzing-workflow';
const ANALYZER='web-content-discovery-fingerprinting-analyzer-v983';
const REVIEW_TEXT_CHARS=758149;
const NOTE_IDS=Object.freeze(['offsec-pen-200-96c6dcd3d448c5b6','offsec-pen-200-a2ab44f2ef0d1117','offsec-pen-200-07c96c5ae331a6ff','offsec-pen-200-043a4ca517c8a4ba','offsec-pen-200-5f6d4e8a7b910233','offsec-pen-200-8df9f22d5dbb6e40','offsec-pen-200-45af3d6be011aa39','offsec-pen-200-26612d0c5d33f841','offsec-pen-200-19f57e5e2cfdcc28','offsec-pen-200-79fd23adfbc9214d','htb-penetration-tester-2f07120a4309c5cb','htb-penetration-tester-3b04e5eb3e7fd96a','htb-penetration-tester-8d62c16dd540b2a5','htb-penetration-tester-94f1d620cf348a4b','htb-penetration-tester-c57ed0a2f26bd652','htb-penetration-tester-5f2b87a918d42c2a']);
function freeze(list){return Object.freeze((list||[]).slice());}
function frozen(obj){return Object.freeze(obj||{});}
function uniq(list){return Array.from(new Set((list||[]).filter(Boolean)));}
function allLanes(){const out=[];if(Array.isArray(root.OBOL_LANES))out.push(...root.OBOL_LANES);if(Array.isArray(root.LANES)&&root.LANES!==root.OBOL_LANES)out.push(...root.LANES);return out;}
function ensureLane(id,title){let lane=allLanes().find(l=>l&&(l.id===id||l.lane===id));if(!lane){lane={id,lane:id,title:title||id,group:id,cards:[]};if(Array.isArray(root.OBOL_LANES))root.OBOL_LANES.push(lane);else root.OBOL_LANES=[lane];}if(!Array.isArray(lane.cards))lane.cards=[];return lane;}
function findCard(id){if(root.CARDS&&root.CARDS[id])return root.CARDS[id];for(const lane of allLanes())for(const card of lane.cards||[])if(card&&card.id===id)return card;return null;}
function upsert(card,laneId,title){const lane=ensureLane(laneId||card.lane||'web',title||'Web');root.CARDS=root.CARDS&&typeof root.CARDS==='object'?root.CARDS:{};for(const l of allLanes()){if(!Array.isArray(l.cards))continue;const idx=l.cards.findIndex(c=>c&&c.id===card.id);if(idx>=0)l.cards.splice(idx,1);}lane.cards.push(card);root.CARDS[card.id]=card;return true;}
function cmd(tool,run,when,evidence,note){return frozen({tool,run,when,evidence,useWhen:when,expected:evidence,note:note||evidence});}
function rows(a,b){const m=new Map();(a||[]).concat(b||[]).forEach(row=>{const key=row&&String(row.run||row.value||row.command||'');if(key)m.set(key,frozen(row));});return freeze(Array.from(m.values()));}
function list(base,extra){return freeze(uniq([].concat(base||[],extra||[])));}
function redact(value){return String(value||'').replace(/\b\d{1,3}(?:\.\d{1,3}){3}(?::\d+)?\b/g,'[host]').replace(/\b[A-Fa-f0-9]{24,128}\b/g,'[encoded-or-secret-material]').replace(/OS\{[^}]+\}|HTB\{[^}]+\}|flag\{[^}]+\}/gi,'[flag-redacted]').replace(/Answer:\s*[^\n\r]+/gi,'Answer: [redacted]').replace(/AKIA[0-9A-Z]{16}|ASIA[0-9A-Z]{16}/g,'[aws-key-redacted]').replace(/((?:secret_access_key|aws_secret_access_key|password|passwd|pwd|secret|token|cookie|hash|key)\s*[:=]\s*)([^;\s&]+)/gi,'$1[redacted]').replace(/[A-Za-z0-9._%+-]+@[A-Za-z0-9.-]+\.[A-Za-z]{2,}/g,'[email-redacted]').replace(/\b(?:offseclab|AIDevCorp|INLANEFREIGHT|Academy_student|HTB_@cademy|Nexus123|Welcome1|Password123|AIDevCorp)\b/gi,'[lab-redacted]').slice(0,1600);}
function stableSeed(id){if(id===BURP)return {id:BURP,lane:'web-fuzzing',phase:'web',title:'Burp Intruder Fuzzing Workflow',operatorGoal:'Review contextual web fuzzing with payload position, transform, response-delta, rate, scope, and manual replay evidence before reporting impact.',commands:[cmd('ffuf','ffuf -u {{base_url}}/FUZZ -w {{wordlist}} -mc all -fs {{baseline_size}}','For broad repeatable route sweeps after baseline behavior is known.','Status, size, word-count, and route deltas with the baseline preserved.','CLI fuzzing is best for broad repeatable sweeps.'),cmd('Burp Intruder','Send request to Intruder, mark one payload position, choose a bounded wordlist, preserve processing/encoding, then sort by status/length/words.','For short contextual tests where cookies, headers, and request shape matter.','Payload position, unchanged request context, transform chain, and response deltas.','Proxy-integrated fuzzing is evidence only after manual replay.')],expected:['request captured','payload position named','payload source bounded','processing and encoding recorded','candidate replayed manually'],expectedEvidence:['request captured','payload position named','payload source bounded','processing and encoding recorded','candidate replayed manually'],failureModes:['Status and size deltas are triage until manually replayed.'],nextSteps:['Replay interesting candidates before reporting.'],produces:['web.fuzzer.workflow_reviewed','web.fuzzer.manual_replay_required'],tools:['Burp Intruder','OWASP ZAP Fuzzer','ffuf','gobuster','dirb','wfuzz'],fieldNoteIds:[]};if(id===UPLOAD)return {id:UPLOAD,lane:'web',phase:'web',title:'Web Upload and Inclusion Proof Chain',operatorGoal:'Keep discovered upload, inclusion, traversal, served-file, and execution behavior as separate proof states.',commands:[],expected:['route discovered','request preserved','result replayed'],expectedEvidence:['route discovered','request preserved','result replayed'],failureModes:['A discovered route is not execution proof.'],nextSteps:['Record request/result evidence before moving to report.'],produces:['web.upload.route_reviewed'],tools:['curl','Burp Suite'],fieldNoteIds:[]};return null;}
const noteDefs=[['note-web-dns-cloud-boundary-v983','DNS and cloud clues are starting evidence, not total ownership proof','Nameserver records, registrar data, reverse DNS, IP ownership, TXT records, and cloud-provider hints should be recorded as scoped infrastructure clues. They guide the next enumeration step but do not prove every hosted asset belongs to the target.','evidence',[CARD],['host','dig','whois','dnsenum'],['web','dns','cloud','osint']],['note-web-resolver-baseline-v983','Resolver configuration is part of web recon evidence','When lab or engagement DNS changes are required, record the explicit resolver, the system resolver, and a control lookup. Otherwise false negatives and polluted DNS answers can poison every downstream discovery result.','path-guidance',[CARD],['host','dig','cat'],['web','dns','resolver']],['note-web-recursive-subdomain-discovery-v983','Subdomain discovery should recurse into route discovery','Treat each new subdomain as a new evidence branch: resolve it, fingerprint it, check headers, enumerate content, and decide whether it unlocks an auth, upload, injection, or exploit-review card.','path-guidance',[CARD],['dnsenum','ffuf','gobuster'],['web','subdomains','content-discovery']],['note-web-content-enum-delta-v983','Content discovery depends on status, size, and body-shape deltas','A found path is useful only when the method, status code, content length, redirect target, body fingerprint, and extension context are captured with a baseline. Noise filtering should preserve the reason a path mattered.','evidence',[CARD,BURP],['ffuf','gobuster','curl'],['web','ffuf','gobuster','baseline']],['note-web-tech-version-fingerprint-v983','Technology fingerprints need evidence source and confidence','Headers, HTML source, footer strings, framework paths, cookies, error pages, and tool banners carry different confidence levels. Record where the version came from before searching for matching exploit candidates.','evidence',[CARD],['curl','whatweb','nmap'],['web','fingerprinting','versioning']],['note-web-exploit-candidate-review-v983','Exploit candidates need precondition and source review before execution','A matching name and version is only a candidate. Confirm affected version, auth requirements, required path, upload behavior, command parameter, callback assumptions, and code safety before running public exploit code.','tool-guidance',[CARD],['searchsploit','ExploitDB'],['web','exploit-review','preconditions']],['note-web-authenticated-exploit-boundary-v983','Authenticated web exploitation is a multi-step proof chain','Keep credential validity, application access, upload behavior, command execution, user context, callback reachability, and reverse-shell proof as separate evidence states so report language matches what happened.','evidence',[CARD,UPLOAD],['curl','nc','searchsploit'],['web','authenticated-exploit','webshell']],['note-web-osint-metadata-identity-v983','People, email, and metadata clues feed hypotheses, not conclusions','Staff pages, document metadata, repository references, and public identities can seed username lists or credential hypotheses. Keep source provenance attached and avoid treating a public identity as valid access.','path-guidance',[CARD],['exiftool','cewl'],['web','osint','metadata']],['note-web-route-unlock-logic-v983','Discovery results should unlock the next card by evidence type','A hidden upload route should unlock upload proof. A reflected parameter should unlock input-boundary testing. A versioned app should unlock exploit-candidate review. The tool used to find it should not decide the branch.','path-guidance',[CARD],['ffuf','gobuster','curl'],['web','path-unlock','workflow']],['note-web-reproducible-request-notes-v983','Web findings need reproducible request notes','Record URL, method, headers or cookies when relevant, parameter changes, payload class, baseline result, observed result, and exact evidence artifact. Do not copy private lab answers into reusable product guidance.','report-guidance',[CARD],['curl','Burp Suite','Obsidian'],['web','reporting','reproducibility']]];
const PUBLIC_NOTES=freeze(noteDefs.map(d=>frozen({id:d[0],title:d[1],body:d[2],kind:d[3],cardIds:freeze(d[4]),pathIds:freeze(d[4]),toolIds:freeze(d[5]),tags:freeze(d[6]),sourceRefs:NOTE_IDS,reviewWave:W})));
function patchNotes(){const prev=root.OBOL_NOTE_INTEGRATION;if(!prev||!Array.isArray(prev.publicFieldNotes))return false;const map=new Map(prev.publicFieldNotes.map(n=>[n&&n.id,n]).filter(p=>p[0]));PUBLIC_NOTES.forEach(n=>map.set(n.id,n));root.OBOL_NOTE_INTEGRATION=Object.assign({},prev,{publicFieldNotes:freeze(Array.from(map.values())),__webContentDiscoveryFingerprintingV983:true});return true;}
function makeDiscoveryCard(){const base=Object.assign({},findCard(CARD)||{id:CARD,title:'Web Content Discovery and Fingerprinting Workflow',phase:'web',lane:'web-discovery',group:'web',commands:[],expected:[],produces:[],tools:[],fieldNoteIds:[]});const commands=[cmd('host','host -t ns {{domain}}','At the start of domain-scoped recon.','Authoritative nameserver records and DNS hosting clue.','Nameservers can hint at cloud or DNS provider but do not prove full asset ownership.'),cmd('host','host {{host_or_domain}}','After selecting a hostname or subdomain.','Resolved A/AAAA/CNAME result and whether the name exists.','Use this as a branch point for HTTP probing and recursive discovery.'),cmd('host','host {{public_ip}}','After resolving a public IP.','Reverse DNS pointer or explicit lack of PTR.','Reverse DNS is a clue that needs ownership and service validation.'),cmd('whois','whois {{domain_or_ip}} | egrep -i "Registrant Organization|OrgName|NetName|CIDR|Amazon|Azure|Google|Cloudflare"','When ownership or cloud-provider context changes prioritization.','Registrar, org, network, CIDR, and provider hints.','Cloud provider hints should shape method, not become a broad scope claim.'),cmd('dig','dig TXT {{domain}} +short','When TXT/SPF/verification records may expose useful recon clues.','TXT/SPF records, verification hints, and mail-related includes with sensitive strings redacted.','TXT records can contain proof or hints; record them without leaking private answers.'),cmd('dnsenum','dnsenum {{domain}} --threads {{threads}}','After baseline DNS records are captured and brute-force subdomain discovery is allowed.','Subdomain list, resolved hosts, failed zone transfer attempts, and output artifact.','Dictionary discovery should recurse into resolution and web probing.'),cmd('ffuf','ffuf -u {{base_url}}/FUZZ -w {{wordlist}} -mc all -fs {{baseline_size}} -of json -o {{output_file}}','After HTTP is confirmed and a baseline false-positive body size is known.','Status, size, word count, redirect, and matched path deltas.','Baseline filtering should keep why the result mattered.'),cmd('gobuster','gobuster dir -u {{base_url}} -w {{wordlist}} -x php,txt,bak,zip,aspx,jsp -k -o {{output_file}}','When extension guessing is useful for the observed stack.','Discovered routes, extension hits, redirects, and errors.','Extensions should come from stack evidence, not random guessing.'),cmd('curl','curl -i -s {{url}} | tee {{headers_output}}','Before claiming framework, server, auth boundary, or cache behavior.','HTTP status, headers, cookies, redirects, and server hints.','Headers are clues; compare them with source and route behavior.'),cmd('curl','curl -s {{url}} | tee {{source_output}} && grep -Ei "version|powered|generator|copyright|href|script|framework" {{source_output}}','When source-visible strings may identify application or version.','Footer/source/version strings and the file that preserved them.','Source strings need confidence and location before exploit matching.'),cmd('whatweb','whatweb -a 3 {{base_url}}','When automated fingerprinting is in scope and should be compared against manual evidence.','Technology guesses, headers, plugins, and confidence hints.','Automated fingerprints require manual confirmation before path decisions.'),cmd('searchsploit','searchsploit "{{application}} {{version}}"','After a technology/version fingerprint has an evidence source.','Candidate exploit titles, platforms, and IDs.','A candidate is not exploitability until prerequisites and code are reviewed.'),cmd('searchsploit','searchsploit -x {{exploit_id}}','Before adapting a public exploit.','Reviewed code path, parameters, auth requirement, payload behavior, and safety concerns.','Read public exploit code before execution and document assumptions.'),cmd('curl','curl {{webshell_url}} --data-urlencode "cmd={{safe_probe_command}}"','Only after an authorized upload or exploit path produced a command endpoint.','Command output, execution identity, and encoded request proof.','Command proof is separate from interactive shell proof and must be scoped.')];const expected=list([].concat(base.expected||[],base.expectedEvidence||[]),['Authoritative DNS, resolver, reverse DNS, and ownership clues recorded separately','Subdomain and content discovery results include baseline/delta evidence','Extension guesses are tied to stack evidence','Technology/version fingerprints cite source, header, footer, or tool evidence','Exploit candidates are reviewed for prerequisites before use','Authenticated exploit, upload, command execution, and shell evidence stay separate','Dashboard next-notes batch advances after this cluster and matches README projection']);return upsert(frozen(Object.assign({},base,{operatorGoal:base.operatorGoal||'Turn domain, DNS, HTTP, content, and technology clues into evidence-backed web branches without overstating scope or exploitability.',commands:rows(base.commands,commands),expected,expectedEvidence:expected,failureModes:list(base.failureModes,['DNS/cloud ownership clues are not whole-environment scope proof.','Directory hits are noise until status, size, redirect, and body-shape deltas explain them.','Version strings are not exploitability without prerequisite and code review.','Resolver mistakes can invalidate every downstream recon result.']),nextSteps:list(base.nextSteps,['Move to upload/inclusion proof if discovery exposes upload, include, traversal, or served-file behavior.','Move to authz/IDOR testing if discovered routes expose object or role boundaries.','Move to exploit-candidate validation only after technology/version evidence is sourced.','Move to report drafting only when the request/result proof chain is reproducible.']),produces:list(base.produces,['web.discovery.dns_domain_recon_reviewed','web.discovery.resolver_boundary_reviewed','web.discovery.content_enum_reviewed','web.discovery.tech_fingerprint_reviewed','web.discovery.exploit_candidate_reviewed','web.discovery.authenticated_execution_boundary_reviewed','web.discovery.dashboard_queue_verified']),tools:list(base.tools,['host','dig','whois','dnsenum','ffuf','gobuster','curl','whatweb','searchsploit','ExploitDB','exiftool','cewl']),fieldNoteIds:list(base.fieldNoteIds,PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(CARD)).map(n=>n.id)),sourceWebContentDiscoveryFingerprinting83:frozen({wave:W,integratedInto:'new-card-distinct-action-spine',clusterId:CLUSTER,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:NOTE_IDS.length})})),'web-discovery','Web');}
function enrichExisting(id,fields){const base=findCard(id)||stableSeed(id);if(!base)return false;const next=frozen(Object.assign({},base,{expected:list([].concat(base.expected||[],base.expectedEvidence||[]),fields.expected||[]),expectedEvidence:list(base.expectedEvidence||base.expected||[],fields.expected||[]),produces:list(base.produces,fields.produces||[]),fieldNoteIds:list(base.fieldNoteIds,fields.notes||[]),sourceWebContentDiscoveryFingerprinting83:frozen({wave:W,integratedInto:findCard(id)?'existing-card':'stable-card-materialized',clusterId:CLUSTER})}));return upsert(next,next.lane||'web','Web');}
function analyze(text){const redacted=redact(text);const s=redacted.toLowerCase();const facts=[];const warnings=[];function add(cond,fact){if(cond)facts.push(fact);}add(/host -t ns|name server|whois|orgname|registrant organization|reverse dns|ptr|route ?53|aws|azure|cloudflare|ec2|txt/.test(s),'web.discovery.dns_domain_recon_observed');add(/resolv\.conf|nameserver|system dns|explicit dns|resolver/.test(s),'web.discovery.resolver_boundary_observed');add(/dnsenum|subdomain|gobuster|ffuf|directory|hidden director|wordlist|extension/.test(s),'web.discovery.content_enum_observed');add(/whatweb|server:|x-powered-by|generator|copyright|version|source code|footer|framework|qdpm/.test(s),'web.discovery.tech_fingerprint_observed');add(/searchsploit|exploit-db|cve|public exploit|verified|remote code execution|rce/.test(s),'web.discovery.exploit_candidate_observed');add(/curl|--data-urlencode|web shell|backdoor|whoami|nc -lvnp|reverse shell|www-data|cmd=/.test(s),'web.discovery.execution_boundary_observed');add(/exiftool|metadata|email|about us|staff|username|cewl/.test(s),'web.discovery.osint_metadata_observed');if(facts.includes('web.discovery.dns_domain_recon_observed'))warnings.push('DNS, WHOIS, reverse DNS, and cloud-provider clues guide scope, but they are not proof that every related asset belongs to the target.');if(facts.includes('web.discovery.content_enum_observed'))warnings.push('Content-discovery hits need status, size, redirect, and body-shape baselines before they become path-unlock evidence.');if(facts.includes('web.discovery.tech_fingerprint_observed'))warnings.push('Technology and version strings need a source and confidence level before exploit matching.');if(facts.includes('web.discovery.exploit_candidate_observed'))warnings.push('Public exploit candidates require prerequisite and source-code review before execution.');if(facts.includes('web.discovery.execution_boundary_observed'))warnings.push('Credential validity, upload behavior, command execution, user context, and callback shell are separate proof states.');if(facts.includes('web.discovery.resolver_boundary_observed'))warnings.push('Resolver changes should be recorded or DNS results may mislead later route discovery.');return frozen({analyzerId:ANALYZER,summary:redacted,outcomeFacts:freeze(uniq(facts)),warnings:freeze(warnings),redacted:true});}
function patchAnalyzer(){root.OBOL_EVIDENCE_ANALYZERS=Array.isArray(root.OBOL_EVIDENCE_ANALYZERS)?root.OBOL_EVIDENCE_ANALYZERS:[];const analyzer=frozen({id:ANALYZER,title:'Web content discovery and fingerprinting evidence analyzer',analyze});root.OBOL_EVIDENCE_ANALYZERS=freeze(root.OBOL_EVIDENCE_ANALYZERS.filter(a=>a&&a.id!==ANALYZER).concat([analyzer]));root.OBOL_WEB_CONTENT_DISCOVERY_FINGERPRINTING_ANALYZER_V983=analyzer;return true;}
function queueItemFromCluster(cluster){return frozen({id:'source-note-cluster-'+cluster.id,label:cluster.title,detail:'Cluster-driven notes gate: '+cluster.pendingCount+' pending source notes in `'+cluster.id+'` must be mined from complete packet text before terminal dispositions resume.',status:'queued',track:'notes-integration',area:'notes/impact-packets',queueMode:'cluster-review',clusterId:cluster.id,targetCount:cluster.pendingCount,count:cluster.pendingCount,sourceRoute:ROUTE,sourceSelector:'Select cluster `'+cluster.id+'` from `data/product-hardening/source-note-clusters-current.js`, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',selector:'Select cluster `'+cluster.id+'` from `data/product-hardening/source-note-clusters-current.js`, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',acceptance:'Ship public-safe product mechanics from the whole cluster, then disposition each note with card/analyzer/field-note/report/queue/private rationale.'});}
function patchClusters(){const clusters=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!clusters||!Array.isArray(clusters.pendingClusters)||!clusters.status)return false;const pending=clusters.pendingClusters.filter(c=>c&&c.id!==CLUSTER);const reviewQueue=pending.map(queueItemFromCluster);root.OBOL_SOURCE_NOTE_CLUSTERS=Object.assign({},clusters,{status:Object.assign({},clusters.status,{latestCompletedClusterQueue:ACTIVE,latestCompletedClusterId:CLUSTER,latestCompletedClusterReviewTextChars:REVIEW_TEXT_CHARS,latestCompletedClusterOwnerCards:freeze([CARD,UPLOAD,BURP]),reviewedSourceNotes:333,pendingSourceNotes:223,clusteredPendingNotes:223,unclusteredPendingNotes:0,clusterCount:12,nextClusterReviewQueue:NEXT}),pendingClusters:freeze(pending),reviewQueue:freeze(reviewQueue),validate:function(){const total=pending.reduce((sum,c)=>sum+Number(c.pendingCount||0),0);const failures=[];if(total!==223)failures.push('pending cluster counts must equal 223 after v9.83');if(reviewQueue.length!==12)failures.push('review queue should contain 12 pending clusters after v9.83');if(!reviewQueue[0]||reviewQueue[0].id!==NEXT)failures.push('next queue should be '+NEXT);return failures;}});return true;}
function patchQueue(){const q=root.OBOL_PRODUCT_HARDENING;if(!q)return false;const oldTotals=typeof q.totals==='function'?q.totals.bind(q):null;const oldNext=typeof q.concreteBuildNext==='function'?q.concreteBuildNext.bind(q):(typeof q.buildNext==='function'?q.buildNext.bind(q):null);const clusters=root.OBOL_SOURCE_NOTE_CLUSTERS;const nextBatch=(clusters&&Array.isArray(clusters.reviewQueue)&&clusters.reviewQueue[0])||frozen({id:NEXT,label:'Windows credential dumping, LSASS, registry hives, and secret handling',detail:'Cluster-driven notes gate: mine the credential-dumping and Windows secrets source cluster from complete packet text.',status:'queued',track:'notes-integration',area:'notes/impact-packets',queueMode:'cluster-review',clusterId:'credential-dumping-lsass-and-windows-secrets',targetCount:15,count:15,sourceRoute:ROUTE,sourceSelector:'Select cluster `credential-dumping-lsass-and-windows-secrets` from `data/product-hardening/source-note-clusters-current.js`, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',selector:'Select cluster `credential-dumping-lsass-and-windows-secrets` from `data/product-hardening/source-note-clusters-current.js`, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',acceptance:'Ship public-safe product mechanics from the whole cluster, then disposition each note with card/analyzer/field-note/report/queue/private rationale.'});function totals(){const base=oldTotals?oldTotals():{};return Object.assign({},base,{complete:227,total:Number(base.total||657),pct:35,queued:Number(base.queued||6),modeled:Number(base.modeled||9),notes:Number(base.notes||556),resources:Number(base.resources||1326)});}function concreteBuildNext(limit){const existing=oldNext?oldNext(10):[];const filtered=existing.filter(item=>item&&item.id!==ACTIVE&&item.id!==nextBatch.id);return [nextBatch].concat(filtered).slice(0,limit||5);}root.OBOL_PRODUCT_HARDENING=Object.assign({},q,{nextNotesBatch:nextBatch,totals,concreteBuildNext,buildNext:concreteBuildNext,__webContentDiscoveryFingerprintingV983:true});root.OBOL_PRODUCT_HARDENING_QUEUE_HYGIENE=Object.assign({},root.OBOL_PRODUCT_HARDENING_QUEUE_HYGIENE||{},{nextNotesBatch:nextBatch,validatedBy:W});return true;}
function run(){const failures=[];const notesIntegrated=patchNotes();if(!notesIntegrated)failures.push('note integration unavailable');const discoveryCardIntegrated=makeDiscoveryCard();const uploadIntegrated=enrichExisting(UPLOAD,{expected:['Discovered upload or served-file routes are handed to upload/inclusion proof with request/result evidence.'],produces:['web.discovery.upload_route_handoff_reviewed'],notes:PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(UPLOAD)).map(n=>n.id)});const burpIntegrated=enrichExisting(BURP,{expected:['Content-discovery fuzzing results carry baseline, status, size, and body-shape deltas.'],produces:['web.discovery.fuzz_delta_reviewed'],notes:PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(BURP)).map(n=>n.id)});const analyzerIntegrated=patchAnalyzer();const clusterCompleted=patchClusters();if(!clusterCompleted)failures.push('source cluster ledger unavailable');const queuePatched=patchQueue();if(!queuePatched)failures.push('product queue unavailable');const status=frozen({status:failures.length?'partial':'live-integrated',wave:W,activeQueueId:ACTIVE,clusterId:CLUSTER,nextQueueId:NEXT,primaryCardIds:freeze([CARD]),enrichedCardIds:freeze([UPLOAD,BURP]),reviewTextChars:REVIEW_TEXT_CHARS,noteCount:NOTE_IDS.length,publicNoteIds:freeze(PUBLIC_NOTES.map(n=>n.id)),notesIntegrated,discoveryCardIntegrated,uploadIntegrated,burpIntegrated,analyzerIntegrated,clusterCompleted,queuePatched,dashboardPatched:queuePatched&&clusterCompleted,failures:freeze(failures),analyze});root.OBOL_WEB_CONTENT_DISCOVERY_FINGERPRINTING_CLUSTER_V983=status;return status;}
run();
})(typeof window!=='undefined'?window:globalThis);
