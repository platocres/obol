'use strict';
(function(root){
const W='v9.85-pass-the-hash-remote-exec-cluster';
const ACTIVE='source-note-cluster-pass-the-hash-and-remote-exec-artifacts';
const CLUSTER='pass-the-hash-and-remote-exec-artifacts';
const NEXT='source-note-cluster-shells-payloads-and-file-transfer-stabilization';
const ROUTE='platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json';
const PTH='pass-the-hash-proof-chain';
const REMOTE='pth-remote-exec-artifacts';
const TOKEN='pth-token-filtering-check';
const ANALYZER='pass-the-hash-remote-exec-evidence-analyzer-v985';
const REVIEW_TEXT_CHARS=476854;
const NOTE_IDS=Object.freeze(['htb-penetration-tester-29b80edb4523461f','htb-penetration-tester-pth-material-source-window','htb-penetration-tester-pth-mimikatz-process-window','htb-penetration-tester-pth-invoke-smbexec-window','htb-penetration-tester-pth-invoke-wmiexec-window','htb-penetration-tester-pth-impacket-psexec-window','htb-penetration-tester-pth-impacket-wmiexec-window','htb-penetration-tester-pth-evilwinrm-rdp-window','htb-penetration-tester-pth-token-filtering-window','offsec-pen-200-b38664f1cc9b2105','offsec-pen-200-9bbd02153149e94e','offsec-pen-200-b1db6481c5b90a95','offsec-pen-200-0cc4526b3d509ced','offsec-pen-200-pth-cleanup-artifact-window']);
const OWNER_CARDS=Object.freeze([PTH,REMOTE,TOKEN]);
function freeze(list){return Object.freeze((list||[]).slice());}
function frozen(obj){return Object.freeze(obj||{});}
function uniq(list){return Array.from(new Set((list||[]).filter(Boolean)));}
function list(base,extra){return freeze(uniq([].concat(base||[],extra||[])));}
function lanes(){return Array.isArray(root.OBOL_LANES)?root.OBOL_LANES:Array.isArray(root.LANES)?root.LANES:[];}
function ensureLane(id,title){let lane=lanes().find(l=>l&&(l.id===id||l.lane===id));if(!lane){lane={id,lane:id,title:title||id,group:id,cards:[]};if(Array.isArray(root.OBOL_LANES))root.OBOL_LANES.push(lane);else if(Array.isArray(root.LANES))root.LANES.push(lane);else root.OBOL_LANES=[lane];}if(!Array.isArray(lane.cards))lane.cards=[];return lane;}
function findCard(id){if(root.CARDS&&root.CARDS[id])return root.CARDS[id];for(const lane of lanes())for(const card of lane.cards||[])if(card&&card.id===id)return card;return null;}
function upsert(card,laneId,title){const lane=ensureLane(laneId||card.lane||'credentials',title||'Credentials');root.CARDS=root.CARDS&&typeof root.CARDS==='object'?root.CARDS:{};let replaced=false;for(const l of lanes()){if(!Array.isArray(l.cards))continue;const idx=l.cards.findIndex(c=>c&&c.id===card.id);if(idx>=0){l.cards.splice(idx,1,card);replaced=true;}}if(!replaced)lane.cards.push(card);root.CARDS[card.id]=card;return true;}
function cmd(tool,run,when,evidence,note){return frozen({tool,run,when,evidence,useWhen:when,expected:evidence,note:note||evidence});}
function rows(base,extra){const m=new Map();(base||[]).concat(extra||[]).forEach(row=>{if(!row)return;const run=String(row.run||row.value||row.command||'');if(!run)return;const normalized=Object.assign({},row,{run:row.run||run,tool:row.tool||'operator',when:row.when||row.useWhen||'When this proof branch is selected.',evidence:row.evidence||row.expected||row.note||'Record the command result and decision boundary.',useWhen:row.useWhen||row.when||'When this proof branch is selected.',expected:row.expected||row.evidence||row.note||'Record the command result and decision boundary.',note:row.note||row.evidence||row.expected||'Record the command result and decision boundary.'});m.set(run,frozen(normalized));});return freeze(Array.from(m.values()));}
function redact(value){return String(value||'').replace(/\b\d{1,3}(?:\.\d{1,3}){3}(?::\d+)?\b/g,'[host]').replace(/OS\{[^}]+\}|HTB\{[^}]+\}|flag\{[^}]+\}/gi,'[flag-redacted]').replace(/Answer:\s*[^\n\r]+/gi,'Answer: [redacted]').replace(/\b[A-Fa-f0-9]{32}:\S+/g,'[ntlm-hash]:[redacted-secret]').replace(/((?:-H|--hash|\/rc4:|\/NTLM:|\/pth:|\-hashes\s+:)\s*)[A-Fa-f0-9]{32,128}\b/gi,'$1[redacted-hash]').replace(/\b[A-Fa-f0-9]{32,256}\b/g,'[secret-material]').replace(/(-e\s+|EncodedCommand\s+|powershell\s+-e\s+)[A-Za-z0-9+/=]{40,}/gi,'$1[base64-redacted]').replace(/((?:password|passwd|pwd|secret|token|cookie|hash|key|credential|ntlm|nthash)\s*[:=]>?\s*)([^;\s&<]+)/gi,'$1[redacted]').replace(/(net\s+user\s+)([^\s]+)\s+([^\s]+)(\s+\/add)/gi,'$1[local-user] [redacted-password]$4').replace(/\b(?:INLANEFREIGHT|FREIGHTLOGISTICS|Academy_student|HTB_@cademy|offseclab|julio|david|mark|G3t_4CCE\$\$_V1@_PTH|D3V1d_Fl5g_is_Her3|JuL1\(\)_SH@re_fl@g|JuL1\(\)_N3w_fl@g)\b/gi,'[lab-redacted]').slice(0,1800);}
const noteDefs=[
 ['note-pth-material-source-scope-v985','Hash reuse starts with material class and source scope','Before treating a hash as reusable authentication material, record where it came from, whether it is NTLM-compatible material, the identity it belongs to, and whether the scope is local, domain, or unknown. A hash-shaped value is not a working login by itself.','path-guidance',[PTH],['nxc','impacket','mimikatz'],['windows','pth','ntlm','material-scope']],
 ['note-pth-auth-success-scope-v985','PtH success is scoped to account, host, service, and protocol','A successful SMB, WinRM, WMI, RDP, or service-control result proves only that observed channel. Keep the tested account, target host, protocol, privilege level, and result separate before moving the path.','evidence',[PTH],['nxc','evil-winrm','impacket-wmiexec','xfreerdp'],['windows','pth','validation','scope']],
 ['note-pth-process-context-v985','Windows-side PtH launches process context, not universal access','A Windows-side PtH process proves a local process token or logon context. It still needs remote service validation before calling it lateral movement.','evidence',[PTH,REMOTE],['mimikatz','Invoke-TheHash'],['windows','mimikatz','process-context']],
 ['note-pth-smb-admin-share-proof-v985','SMB login, admin share write, and SCM control are different facts','SMB authentication, ADMIN$ write access, Service Control Manager write access, and an interactive shell are separate evidence states. Do not collapse them into one generic owned-host claim.','evidence',[PTH,REMOTE],['nxc','impacket-psexec','impacket-smbexec'],['windows','smb','remote-admin']],
 ['note-pth-remote-exec-artifacts-v985','Remote execution by hash leaves artifacts to review','PsExec, SMBExec, WMIExec, ATExec, Invoke-TheHash, and similar paths may create services, upload binaries, start processes, trigger callbacks, or modify users. Track those artifacts and cleanup separately from credential validation.','cleanup',[REMOTE,PTH],['impacket-psexec','impacket-smbexec','impacket-wmiexec','impacket-atexec','Invoke-TheHash'],['windows','remote-exec','artifacts','cleanup']],
 ['note-pth-winrm-rdp-boundaries-v985','WinRM and RDP hash paths have their own policy boundaries','WinRM hash authentication and RDP restricted-admin style access are different proof paths. Record reachability, policy support, credential scope, and shell/session proof independently.','troubleshooting',[PTH,TOKEN],['evil-winrm','xfreerdp','reg'],['windows','winrm','rdp','restricted-admin']],
 ['note-pth-token-filtering-failure-v985','Token filtering can make a valid local hash look broken','Local administrator hash reuse can fail or lose privilege because of remote UAC, LocalAccountTokenFilterPolicy, FilterAdministratorToken, Restricted Admin settings, service reachability, or account policy. Classify the failure before discarding the material.','troubleshooting',[TOKEN,PTH],['reg','nxc','evil-winrm','xfreerdp'],['windows','uac','token-filtering']],
 ['note-pth-failure-disposition-v985','Failed PtH attempts need a reasoned disposition','A failed attempt should be tagged by likely cause: wrong material, wrong identity, protocol mismatch, no admin rights, service closed, token filtering, restricted-admin policy, or lockout. The next test should narrow only one variable.','troubleshooting',[PTH,TOKEN],['nxc','impacket','evil-winrm'],['windows','failure-mode','lockout']],
 ['note-pth-report-redaction-cleanup-v985','PtH report evidence should keep raw material out','Reports should describe material class, source, tested channel, result, impact, artifacts, and cleanup without printing hashes, recovered passwords, payloads, or target-specific answer strings.','report-guidance',[PTH,REMOTE,TOKEN],['nxc','impacket','evil-winrm'],['windows','reporting','redaction']],
 ['note-pth-dashboard-handoff-v985','After PtH, the next product gap is shell and payload stabilization','Once hash validation and remote execution artifacts are modeled, the dashboard should move to shell receipt, listener, payload, file-transfer, and session-stabilization mechanics.','product-guidance',[PTH,REMOTE,TOKEN],['nc','msfvenom','python3'],['dashboard','queue','handoff']]
];
const PUBLIC_NOTES=freeze(noteDefs.map(d=>frozen({id:d[0],title:d[1],body:d[2],kind:d[3],cardIds:freeze(d[4]),pathIds:freeze(d[4]),toolIds:freeze(d[5]),tags:freeze(d[6]),sourceRefs:NOTE_IDS,reviewWave:W})));
function patchNotes(){const prev=root.OBOL_NOTE_INTEGRATION;if(!prev||!Array.isArray(prev.publicFieldNotes))return false;const map=new Map(prev.publicFieldNotes.map(n=>[n&&n.id,n]).filter(p=>p[0]));PUBLIC_NOTES.forEach(n=>map.set(n.id,n));root.OBOL_NOTE_INTEGRATION=Object.assign({},prev,{publicFieldNotes:freeze(Array.from(map.values())),__passTheHashRemoteExecV985:true});return true;}
function enrichPth(){const base=Object.assign({},findCard(PTH)||{id:PTH,title:'Pass-the-Hash Proof Chain',phase:'credentials',lane:'credentials',group:'credentials',commands:[],expected:[],produces:[],tools:[],fieldNoteIds:[]});const commands=[cmd('nxc','nxc smb {{target_or_cidr}} -u {{user}} -H {{nt_hash}} --local-auth','When validating a local-account NT hash against SMB.','Authentication result, host, account scope, and whether admin rights are indicated.','Remove --local-auth only after domain scope is proven.'),cmd('nxc','nxc smb {{target_or_cidr}} -d {{domain}} -u {{user}} -H {{nt_hash}} --shares','When validating a domain-scoped NT hash and share rights.','SMB auth result, share access, and admin-share evidence without overclaiming shell access.','Share read/write is not the same as remote command execution.'),cmd('impacket-psexec','impacket-psexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}}','When SMB admin share and service-control execution are in scope.','Writable share, service creation/start, shell prompt, and cleanup artifacts.','Track service and uploaded binary artifacts explicitly.'),cmd('impacket-wmiexec','impacket-wmiexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}}','When WMI command execution is a better fit than service-binary upload.','WMI auth, process execution result, command output, and cleanup needs.','WMI success proves a different channel than PsExec.'),cmd('impacket-smbexec','impacket-smbexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}}','When testing SMBExec-style service execution.','Service creation behavior, command execution proof, and cleanup state.','Do not hide service artifacts behind credential wording.'),cmd('impacket-atexec','impacket-atexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}} {{command}}','When scheduled-task execution is selected.','Task execution result, output retrieval, and task cleanup.','ATExec is remote execution, not just authentication.'),cmd('evil-winrm','evil-winrm -i {{target}} -u {{user}} -H {{nt_hash}}','When WinRM is reachable and hash auth is supported.','WinRM auth result, prompt/session proof, user context, and failure reason.','WinRM failure does not invalidate SMB/RDP by itself.'),cmd('xfreerdp','xfreerdp /v:{{target}} /u:{{user}} /pth:{{nt_hash}} /restricted-admin +clipboard /cert:ignore','When RDP restricted-admin style access is in scope.','RDP policy support, session result, and account scope.','RDP requires its own policy and service conditions.'),cmd('mimikatz','mimikatz "privilege::debug" "sekurlsa::pth /user:{{user}} /domain:{{domain_or_dot}} /rc4:{{nt_hash}} /run:cmd.exe" exit','When Windows-side process context by hash is authorized.','Process/token context, LUID, spawned program, and follow-on validation target.','Local process context is not remote execution until a remote channel is tested.'),cmd('reg','reg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Policies\\System /v LocalAccountTokenFilterPolicy && reg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Policies\\System /v FilterAdministratorToken','When local-account PtH fails or admin rights look filtered.','Remote UAC/token-filtering policy values or explicit absence.','Classify policy effects before judging the hash bad.'),cmd('sc','sc.exe \\{{target}} query','When SMB auth worked and SCM reachability must be separated.','Service Control Manager access result.','SCM access helps explain PsExec/SMBExec viability.'),cmd('operator-cleanup','Record and remove temporary service names, uploaded binaries, scheduled tasks, created local users, and listener artifacts','After any hash-based remote execution attempt.','Artifact inventory, cleanup result, and report-safe summary.','Cleanup is a proof state, not a footnote.')];const expected=list([].concat(base.expected||[],base.expectedEvidence||[]),['NT hash material class identity and source recorded before reuse','Local account domain account and unknown scope separated','SMB authentication admin share write SCM control and shell proof separated','WinRM RDP SMB WMI service and scheduled-task channels tested independently','Token filtering restricted-admin and remote-UAC policy considered before failure disposition','Remote execution artifacts and cleanup recorded','Raw hashes passwords payloads and flags redacted from report output']);return upsert(frozen(Object.assign({},base,{operatorGoal:base.operatorGoal||base.hypothesis||'Validate NTLM hash material through scoped service proof while separating authentication, privilege, remote execution artifacts, token filtering, and cleanup.',hypothesis:base.hypothesis||'Treat pass-the-hash as protocol-scoped authentication material, not universal access.',commands:rows(base.commands,commands),expected,expectedEvidence:expected,failureModes:list(base.failureModes,['Hash-shaped material is not NTLM-compatible or belongs to the wrong identity.','The tested account is valid but lacks remote administrative rights on the target.','SMB, WinRM, RDP, WMI, or SCM is blocked or disabled for the selected path.','Remote UAC, token filtering, or Restricted Admin policy changes the result.','The attempt created service, task, upload, process, or listener artifacts that still need cleanup.']),nextSteps:list(base.nextSteps,['If SMB auth succeeds, separately test admin share, SCM, WinRM, WMI, or RDP as appropriate.','If remote execution succeeds, inventory artifacts and complete cleanup/report notes.','If local-account PtH fails, check token filtering and service reachability before discarding the material.','After this cluster, move to shell receipt, payload, file-transfer, listener, and stabilization work.']),produces:list(base.produces,['auth.nt_hash_scope_reviewed','auth.pth_protocol_validation_reviewed','auth.remote_exec_artifact_reviewed','auth.token_filtering_reviewed','auth.pth_failure_disposition_reviewed','auth.pth_cleanup_reviewed','evidence.pth_report_boundary_reviewed']),tools:list(base.tools,['nxc','crackmapexec','impacket-psexec','impacket-wmiexec','impacket-smbexec','impacket-atexec','evil-winrm','xfreerdp','mimikatz','reg','sc.exe']),fieldNoteIds:list(base.fieldNoteIds,PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(PTH)).map(n=>n.id)),passTheHashRemoteExec85:frozen({wave:W,integratedInto:'existing-card',clusterId:CLUSTER,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:NOTE_IDS.length})})),'credentials','Credentials');}
function enrichRemote(){const base=Object.assign({},findCard(REMOTE)||{id:REMOTE,title:'Review PtH Remote Execution Artifacts',phase:'credentials',lane:'credentials',group:'credentials',commands:[],expected:[],produces:[],tools:[],fieldNoteIds:[]});const commands=[cmd('impacket-psexec','impacket-psexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}}','When service-based execution is selected after scoped hash validation.','Share write, service name, uploaded binary, shell proof, and deletion result.','Record artifacts before reporting impact.'),cmd('impacket-wmiexec','impacket-wmiexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}} {{command}}','When WMI command execution is selected.','Process execution evidence and output retrieval.','WMI may avoid the same artifacts as service upload but still creates process evidence.'),cmd('impacket-smbexec','impacket-smbexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}}','When SMBExec-style service execution is selected.','Temporary service behavior and cleanup status.','Keep the service lifecycle visible.'),cmd('impacket-atexec','impacket-atexec {{domain_or_host}}/{{user}}@{{target}} -hashes :{{nt_hash}} {{command}}','When scheduled-task execution is selected.','Task execution, output, and task cleanup state.','Scheduled task artifacts are part of the finding.'),cmd('PowerShell','Get-WinEvent -LogName Security -MaxEvents 20 | Select-Object TimeCreated,Id,ProviderName,Message','When a Windows-side session can inspect local evidence after remote exec.','Recent event IDs/provider clues relevant to logon, process, or service activity.','Use only where authorized and keep output redacted.'),cmd('operator-cleanup','Confirm uploaded binaries, temporary services, scheduled tasks, created accounts, and payload files are removed or documented','After any remote execution path.','Cleanup evidence and remaining artifact rationale.','No cleanup means the report needs an explicit residue note.')];const expected=list([].concat(base.expected||[],base.expectedEvidence||[]),['Temporary service process task upload shell and account artifacts inventoried','Execution channel recorded separately from credential validation','Cleanup outcome recorded for every artifact class','Report wording separates authentication from command execution']);return upsert(frozen(Object.assign({},base,{operatorGoal:base.operatorGoal||base.hypothesis||'Review hash-based remote execution as an artifact-producing action separate from credential validation.',commands:rows(base.commands,commands),expected,expectedEvidence:expected,failureModes:list(base.failureModes,['Authentication succeeds but execution channel is blocked.','Remote execution succeeds but shell receipt is missing.','Temporary service, task, upload, or account artifact remains after the attempt.']),nextSteps:list(base.nextSteps,['Inventory artifacts immediately after remote execution.','Return to PtH scope review if auth works but execution fails.','Move to shell/session stabilization once a shell is received.']),produces:list(base.produces,['auth.remote_exec_artifact_reviewed','cleanup.remote_exec_recorded','evidence.remote_exec_report_boundary_reviewed']),tools:list(base.tools,['impacket-psexec','impacket-wmiexec','impacket-smbexec','impacket-atexec','Invoke-TheHash','Get-WinEvent']),fieldNoteIds:list(base.fieldNoteIds,PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(REMOTE)).map(n=>n.id)),passTheHashRemoteExec85:frozen({wave:W,integratedInto:'existing-card',clusterId:CLUSTER})})),'credentials','Credentials');}
function enrichToken(){const base=Object.assign({},findCard(TOKEN)||{id:TOKEN,title:'Check PtH Token Filtering and Account Scope',phase:'credentials',lane:'credentials',group:'credentials',commands:[],expected:[],produces:[],tools:[],fieldNoteIds:[]});const commands=[cmd('reg','reg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Policies\\System /v LocalAccountTokenFilterPolicy','When local administrator hash reuse authenticates but lacks remote admin power.','LocalAccountTokenFilterPolicy value or absence.','Absent value may still mean default filtering behavior.'),cmd('reg','reg query HKLM\\SOFTWARE\\Microsoft\\Windows\\CurrentVersion\\Policies\\System /v FilterAdministratorToken','When built-in Administrator behavior may differ from other local admins.','FilterAdministratorToken value or absence.','Built-in and non-built-in local admin accounts can differ.'),cmd('reg','reg query HKLM\\System\\CurrentControlSet\\Control\\Lsa /v DisableRestrictedAdmin','When testing RDP hash access.','Restricted Admin policy value or absence.','RDP hash behavior depends on this policy and service availability.'),cmd('nxc','nxc smb {{target}} -u {{local_user}} -H {{nt_hash}} --local-auth','When narrowing local-account scope.','SMB authentication result and admin marker for one host.','Do not spray broadly while troubleshooting policy.'),cmd('evil-winrm','evil-winrm -i {{target}} -u {{user}} -H {{nt_hash}}','When checking whether WinRM differs from SMB.','WinRM authentication/shell result or explicit failure.','A WinRM failure does not prove SMB failure.'),cmd('xfreerdp','xfreerdp /v:{{target}} /u:{{user}} /pth:{{nt_hash}} /restricted-admin /cert:ignore','When checking RDP restricted-admin support.','RDP connection result and policy clue.','Keep RDP separate from command-exec channels.')];const expected=list([].concat(base.expected||[],base.expectedEvidence||[]),['Account scope local domain or unknown captured','Remote UAC/token filtering policy checked when local-account behavior is odd','Restricted Admin policy considered for RDP hash use','Failure reason narrowed to protocol policy privilege reachability or material']);return upsert(frozen(Object.assign({},base,{operatorGoal:base.operatorGoal||base.hypothesis||'Explain PtH failures by account scope, service reachability, token filtering, and Restricted Admin policy before changing credential material.',commands:rows(base.commands,commands),expected,expectedEvidence:expected,failureModes:list(base.failureModes,['Remote UAC filters a valid local administrator token.','Restricted Admin is disabled or unavailable for RDP.','WinRM, SMB, or RDP reachability differs by host.','Account lockout or policy makes additional tests risky.']),nextSteps:list(base.nextSteps,['If policy explains failure, document the boundary and try a different authorized channel only when scoped.','If service reachability explains failure, move to port/service validation.','If material is wrong, return to credential classification rather than broad spraying.']),produces:list(base.produces,['auth.token_filtering_reviewed','auth.restricted_admin_reviewed','auth.local_account_scope_reviewed','auth.pth_failure_disposition_reviewed']),tools:list(base.tools,['reg','nxc','crackmapexec','evil-winrm','xfreerdp']),fieldNoteIds:list(base.fieldNoteIds,PUBLIC_NOTES.filter(n=>(n.cardIds||[]).includes(TOKEN)).map(n=>n.id)),passTheHashRemoteExec85:frozen({wave:W,integratedInto:'existing-card',clusterId:CLUSTER})})),'credentials','Credentials');}
function addAnalyzer(){function analyze(text){const raw=String(text||'');const safe=redact(raw);const facts=[];function fact(id){if(!facts.includes(id))facts.push(id);}const warnings=[];if(/\b(pass[- ]the[- ]hash|sekurlsa::pth|\/pth\b|Invoke-(?:SMBExec|WMIExec)|impacket-(?:psexec|wmiexec|smbexec|atexec)|crackmapexec\s+smb|nxc\s+smb|evil-winrm[\s\S]*\s-H\s+|xfreerdp[\s\S]*\/pth:)/i.test(raw))fact('auth.pass_the_hash_attempt_observed');if(/(?:\bNTLM\b|\bNTHASH\b|\bNT hash\b|\/rc4:|\/NTLM:|\/pth:|\s-H\s+|-hashes\s+:)[\s:=]*[A-Fa-f0-9]{32}\b/i.test(raw))fact('auth.nt_hash_material_observed');if(/\b(?:successfully authenticated|authentication succeeded|Pwn3d!|\[\+\].*SMB|Valid credentials|STATUS_SUCCESS)\b/i.test(raw))fact('auth.remote_smb_auth_observed');if(/\b(?:ADMIN\$|Found writable share|Service Control Manager write|Opening SVCManager|Creating service|local admin|Pwn3d!)\b/i.test(raw))fact('auth.remote_admin_rights_observed');if(/\b(?:Command executed|process id|Microsoft Windows \[Version|C:\\Windows\\system32>|PS C:\\|shell connection|reverse shell connection|uploaded file|Starting service|Service [A-Za-z0-9_-]+ (?:created|deleted)|scheduled task|atexec)\b/i.test(raw))fact('auth.remote_execution_artifact_observed');if(/\b(?:evil-winrm|WinRM|xfreerdp|restricted-admin|\/restricted-admin|RDP|wmiexec|psexec|smbexec|atexec|Invoke-WMIExec|Invoke-SMBExec)\b/i.test(raw))fact('auth.remote_exec_channel_observed');if(/\b(?:LocalAccountTokenFilterPolicy|FilterAdministratorToken|remote UAC|token filtering|Restricted Admin|DisableRestrictedAdmin|EnableLUA)\b/i.test(raw))fact('auth.token_filtering_or_restricted_admin_observed');if(/\b(?:--local-auth|domain:\s*\.|local account|workgroup|\.\\[A-Za-z0-9._-]+)\b/i.test(raw))fact('auth.local_account_scope_observed');if(/\b(?:STATUS_LOGON_FAILURE|STATUS_ACCESS_DENIED|ACCESS_DENIED|authentication failed|login failed|account locked|locked out|NT_STATUS_LOGON_FAILURE|NT_STATUS_ACCESS_DENIED)\b/i.test(raw))fact('auth.failure_or_lockout_signal_observed');if(/\b(?:deleted|cleanup|removed|Remove-Item|del\s+|sc\.exe.*delete|service .* deleted|task deleted|uploaded binary removed)\b/i.test(raw))fact('auth.cleanup_artifact_observed');if(facts.includes('auth.nt_hash_material_observed')&&!facts.includes('auth.remote_smb_auth_observed'))warnings.push('Hash material still needs scoped service validation before path movement.');if(facts.includes('auth.remote_smb_auth_observed'))warnings.push('Authentication success is scoped to the observed account, host, service, and protocol.');if(facts.includes('auth.remote_admin_rights_observed'))warnings.push('Admin-share or SCM evidence is stronger than login only, but shell and cleanup are separate proof states.');if(facts.includes('auth.remote_execution_artifact_observed'))warnings.push('Remote execution leaves artifacts; record service, task, upload, process, shell, and cleanup evidence separately.');if(facts.includes('auth.token_filtering_or_restricted_admin_observed'))warnings.push('Token filtering and Restricted Admin settings can explain PtH failures without proving the hash is bad.');if(facts.includes('auth.failure_or_lockout_signal_observed'))warnings.push('Failure needs disposition by material, identity, protocol, privilege, reachability, policy, or lockout risk.');if(/\b(?:OS\{|HTB\{|Answer:|[A-Fa-f0-9]{32}|password\s*[:=]|-hashes\s+:|\s-H\s+[A-Fa-f0-9]{32})/i.test(raw))warnings.push('Raw hashes, passwords, payloads, flags, and answer strings must stay out of public notes and reports.');return frozen({id:ANALYZER,label:'Pass-the-hash remote execution evidence analyzer',matchCount:facts.length,outcomeFacts:freeze(facts),warnings:freeze(warnings),snippet:safe});}const registry=Array.isArray(root.OBOL_EVIDENCE_ANALYZERS)?root.OBOL_EVIDENCE_ANALYZERS.slice():[];const filtered=registry.filter(a=>a&&a.id!==ANALYZER);filtered.push(frozen({id:ANALYZER,label:'Pass-the-hash remote execution evidence analyzer',analyze}));root.OBOL_EVIDENCE_ANALYZERS=freeze(filtered);root.OBOL_PTH_REMOTE_EXEC_ANALYZER_V985=frozen({id:ANALYZER,analyze});const T=root.OBOL_INTAKE_V21;if(T&&typeof T.analyzeTerminal==='function'&&!T.analyzeTerminal.__pthRemoteExecV985){const original=T.analyzeTerminal;T.analyzeTerminal=function(text){const result=original.apply(this,arguments)||{};const analysis=analyze(text);if(analysis.matchCount){const activities=Array.isArray(result.activities)?result.activities.slice():[];activities.push(frozen({id:'evidence-pth-remote-exec-v985',cardId:PTH,title:'PtH remote execution evidence reviewed',result:analysis.outcomeFacts.includes('auth.remote_execution_artifact_observed')?'interesting':'triage',summary:analysis.warnings[0]||'PtH evidence needs scoped validation.',facts:analysis.outcomeFacts}));result.activities=activities;result.passTheHashRemoteExec85=analysis;}return result;};T.analyzeTerminal.__pthRemoteExecV985=true;}return true;}
function patchClusters(){const prev=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!prev||!Array.isArray(prev.pendingClusters)||!Array.isArray(prev.reviewQueue)||!prev.status)return false;const pending=prev.pendingClusters.filter(c=>c&&c.id!==CLUSTER);const total=pending.reduce((sum,c)=>sum+Number(c.pendingCount||0),0);const reviewQueue=pending.map((entry,index)=>frozen({id:index===0?NEXT:'source-note-cluster-'+entry.id,clusterId:entry.id,label:entry.title,status:index===0?'queued':'queued-after-prior-clusters',priority:90-index,pendingCount:entry.pendingCount,readiness:entry.readiness,selector:'Read complete packet text for cluster '+entry.id+' from '+ROUTE+'; mine the whole cluster before terminal dispositions.',acceptance:'Ship public-safe product mechanics from the whole cluster, then disposition each note with card/analyzer/field-note/report/queue/private rationale.'}));const status=frozen(Object.assign({},prev.status,{reviewedSourceNotes:362,pendingSourceNotes:total,clusteredPendingNotes:total,unclusteredPendingNotes:0,clusterCount:pending.length,latestCompletedClusterQueue:ACTIVE,latestCompletedClusterId:CLUSTER,latestCompletedClusterTitle:'Pass-the-hash, remote execution, and token/filtering artifacts',latestCompletedClusterOwnerCards:OWNER_CARDS,latestCompletedClusterReviewTextChars:REVIEW_TEXT_CHARS,latestCompletedClusterNoteCount:NOTE_IDS.length,nextClusterReviewQueue:NEXT}));const api=frozen(Object.assign({},prev,{status,pendingClusters:freeze(pending),reviewQueue:freeze(reviewQueue),validate:function(){const failures=[];const pendingTotal=pending.reduce((sum,c)=>sum+Number(c.pendingCount||0),0);if(pendingTotal!==194)failures.push('pending cluster coverage must equal 194, got '+pendingTotal);if(pending.length!==10)failures.push('expected 10 pending source-note clusters');if(reviewQueue[0]&&reviewQueue[0].id!==NEXT)failures.push('next review queue did not advance to shell/payload stabilization');if(JSON.stringify({status,pending,reviewQueue}).match(/HTB\{|OS\{|flag\.txt|Password123|94\.237|83\.136|Answer:|BEGIN RSA PRIVATE KEY|AKIA[0-9A-Z]{16}/i))failures.push('cluster ledger leaked private/source-specific material');if(typeof prev.validate==='function')failures.push.apply(failures,(prev.validate()||[]).filter(msg=>!/pending cluster coverage|expected \d+ pending source-note clusters|first cluster queue item drifted/i.test(String(msg))));return freeze(failures);}}));root.OBOL_SOURCE_NOTE_CLUSTERS=api;return total===194&&pending.length===10&&reviewQueue[0]&&reviewQueue[0].id===NEXT;}
function patchQueue(){const q=root.OBOL_PRODUCT_HARDENING;if(!q)return false;const label='Shells, payloads, file transfer, listener, and session stabilization';q.nextNotesBatch={id:NEXT,label,sourceRoute:ROUTE,count:24,clusterId:'shells-payloads-and-file-transfer-stabilization',selector:'Select cluster shells-payloads-and-file-transfer-stabilization from data/product-hardening/source-note-clusters-current.js, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',queueMode:'cluster-review'};if(Array.isArray(q.items)){let done=q.items.find(item=>item&&item.id===ACTIVE);if(!done){done={id:ACTIVE,track:'notes-integration'};q.items.push(done);}Object.assign(done,{status:'complete',priority:89.7,label:'Pass-the-hash, remote execution, and token/filtering artifacts',detail:'v9.85 mined the full cluster into stable PtH, remote-execution artifact, token-filtering, analyzer, field-note, and queue handoff mechanics.'});let next=q.items.find(item=>item&&item.id===NEXT);if(!next){next={id:NEXT,track:'notes-integration'};q.items.push(next);}Object.assign(next,{status:'queued',priority:89.6,label,detail:'Mine shell receipt, payload selection, file transfer, listener, and session stabilization from the next complete source cluster.'});}if(typeof q.concreteBuildNext==='function'&&!q.concreteBuildNext.__pthRemoteExecV985){const original=q.concreteBuildNext.bind(q);q.concreteBuildNext=function(count){const first=q.nextNotesBatch?Object.assign({track:'notes-integration',status:'queued',priority:89.6},q.nextNotesBatch):null;const rest=original(count||5).filter(item=>item&&item.id!==ACTIVE&&item.id!==NEXT);return (first?[first]:[]).concat(rest).slice(0,count||5);};q.concreteBuildNext.__pthRemoteExecV985=true;}return q.nextNotesBatch&&q.nextNotesBatch.id===NEXT;}
function patchProgress(){const p=root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS;if(!p)return false;root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS=frozen(Object.assign({},p,{reviewed:362,pending:194,nextSelectorBatch:NEXT,nextNotesBatch:NEXT,clusterMode:'cluster-review-queue-active',clusteredPendingNotes:194,unclusteredPendingNotes:0,latestCompletedClusterQueue:ACTIVE,latestCompletedClusterId:CLUSTER}));return true;}
function run(){const notesIntegrated=patchNotes();const pthIntegrated=enrichPth();const remoteIntegrated=enrichRemote();const tokenIntegrated=enrichToken();const analyzerIntegrated=addAnalyzer();const clusterCompleted=patchClusters();const queuePatched=patchQueue();const progressPatched=patchProgress();const failures=[];if(!notesIntegrated)failures.push('field notes did not integrate');if(!pthIntegrated)failures.push('pass-the-hash proof chain did not integrate');if(!remoteIntegrated)failures.push('remote execution artifact card did not integrate');if(!tokenIntegrated)failures.push('token filtering card did not integrate');if(!analyzerIntegrated)failures.push('analyzer did not integrate');if(!clusterCompleted)failures.push('source cluster ledger did not advance');if(!queuePatched)failures.push('product queue did not advance');root.OBOL_PASS_THE_HASH_REMOTE_EXEC_CLUSTER_V985=frozen({wave:W,status:failures.length?'partial':'live-integrated',activeQueueId:ACTIVE,clusterId:CLUSTER,nextQueueId:NEXT,primaryCardId:PTH,enrichedCardIds:OWNER_CARDS,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:NOTE_IDS.length,publicNoteIds:freeze(PUBLIC_NOTES.map(n=>n.id)),notesIntegrated,pthIntegrated,remoteIntegrated,tokenIntegrated,analyzerIntegrated,clusterCompleted,queuePatched,progressPatched,failures});return root.OBOL_PASS_THE_HASH_REMOTE_EXEC_CLUSTER_V985;}
const packet=frozen({wave:W,activeQueueId:ACTIVE,clusterId:CLUSTER,nextQueueId:NEXT,ownerCards:OWNER_CARDS,sourceRoute:ROUTE,sourcePackets:freeze(['data/review-packets/htb-penetration-tester-01.json','data/review-packets/offsec-pen-200-05.json']),reviewTextPolicy:'complete_cleaned_text',reviewTextChars:REVIEW_TEXT_CHARS,noteIds:NOTE_IDS,publicNotes:PUBLIC_NOTES,analyzerId:ANALYZER,analyze:function(text){const fn=root.OBOL_PTH_REMOTE_EXEC_ANALYZER_V985&&root.OBOL_PTH_REMOTE_EXEC_ANALYZER_V985.analyze;return fn?fn(text):frozen({id:ANALYZER,matchCount:0,outcomeFacts:freeze([]),warnings:freeze([]),snippet:redact(text)});},run});root.OBOL_PTH_REMOTE_EXEC_PACKET_V985=packet;const first=run();if(typeof window!=='undefined'){let tries=0;const schedule=typeof window.setTimeout==='function'?window.setTimeout.bind(window):null;const attempt=function(){const result=run();tries+=1;if(result.status!=='live-integrated'&&tries<160&&schedule)schedule(attempt,50);};if(first.status!=='live-integrated'&&schedule)schedule(attempt,0);if(typeof window.addEventListener==='function'){window.addEventListener('hashchange',attempt);window.addEventListener('focus',attempt);}}
if(typeof module!=='undefined'&&module.exports)module.exports=packet;
})(typeof window!=='undefined'?window:globalThis);
