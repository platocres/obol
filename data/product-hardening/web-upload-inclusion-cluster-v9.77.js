'use strict';
(function(root){
const WAVE='v9.77-web-upload-file-inclusion-cluster';
const ACTIVE_QUEUE_ID='source-note-cluster-web-upload-file-inclusion-001';
const ACTIVE_CLUSTER_ID='web-upload-file-inclusion-expansion';
const CARD_ID='web-upload-inclusion-proof-chain';
const ANALYZER_ID='upload-inclusion-evidence-analyzer-v977';
const SOURCE_ROUTE='platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json';
const SOURCE_PACKETS=Object.freeze(['data/review-packets/htb-penetration-tester-03.json','data/review-packets/htb-penetration-tester-04.json']);
const REVIEW_TEXT_CHARS=441925,TOTAL_NOTES=40,UPLOAD_INCLUSION_NOTES=12,NEIGHBOR_NOTES=28;
const PUBLIC_NOTE_IDS=Object.freeze(['note-file-handling-impact-ladder-v977','note-file-read-vs-execution-boundary-v977','note-file-handling-negative-controls-v977','note-file-handling-report-evidence-v977','note-cluster-window-split-rationale-v977']);
function fl(v){return Object.freeze((v||[]).slice());}
function fo(v){return Object.freeze(v||{});}
function uniq(v){return Array.from(new Set((v||[]).filter(Boolean)));}
function lanes(){return Array.isArray(root.OBOL_LANES)?root.OBOL_LANES:Array.isArray(root.LANES)?root.LANES:[];}
function liveCard(id){if(!id)return null;if(typeof root.liveCardById==='function'){try{const c=root.liveCardById(id);if(c)return c;}catch(_){}}if(root.CARDS&&root.CARDS[id])return root.CARDS[id];for(const lane of lanes())for(const card of lane.cards||[])if(card&&card.id===id)return card;return null;}
function ensureLane(){if(!Array.isArray(root.OBOL_LANES)&&!Array.isArray(root.LANES))root.OBOL_LANES=[];let lane=lanes().find(l=>l&&(l.id==='web-file-handling'||l.lane==='web-file-handling'));if(!lane){lane={id:'web-file-handling',lane:'web-file-handling',title:'Web File Handling',group:'Initial Access & Web',cards:[]};if(Array.isArray(root.OBOL_LANES))root.OBOL_LANES.push(lane);else if(Array.isArray(root.LANES))root.LANES.push(lane);}if(!Array.isArray(lane.cards))lane.cards=[];return lane;}
function replaceCard(card){const lane=ensureLane(),i=lane.cards.findIndex(c=>c&&c.id===card.id);if(i>=0)lane.cards.splice(i,1,card);else lane.cards.push(card);root.CARDS=root.CARDS&&typeof root.CARDS==='object'?root.CARDS:{};try{root.CARDS[card.id]=card;}catch(_){}return !!liveCard(card.id);}
const PUBLIC_NOTES=fl([
 fo({id:'note-file-handling-impact-ladder-v977',title:'Use an impact ladder for upload and inclusion evidence',body:'Treat file-handling work as a ladder. Prove request control, acceptance, stored name, retrieval path, server-side interpretation, and only then file read or command effect. Do not jump from accepted upload, readable include, or fuzzer hit to execution.',kind:'path-guidance',cardIds:fl([CARD_ID]),toolIds:fl(['curl','ffuf','burp-intruder']),pathIds:fl(['path',CARD_ID]),tags:fl(['file-upload','file-inclusion','proof-boundary','impact']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-file-read-vs-execution-boundary-v977',title:'Keep file read, source disclosure, and execution separate',body:'A file inclusion finding may prove local file read, transformed source disclosure, remote retrieval, uploaded-file inclusion, or executable interpretation. Record which one happened. Source disclosure or included bytes are not remote code execution unless separate command-result evidence proves interpreted execution.',kind:'evidence',cardIds:fl([CARD_ID]),toolIds:fl(['curl','ffuf']),pathIds:fl([CARD_ID]),tags:fl(['lfi','rfi','wrapper','execution-proof']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-file-handling-negative-controls-v977',title:'Use canaries and negative controls before widening',body:'When testing upload or inclusion behavior, keep one harmless canary file or marker, one known-bad request, and one replayed known-good request. That contrast separates routing mistakes, cached responses, filter behavior, and real downstream handling.',kind:'tool-guidance',cardIds:fl([CARD_ID]),toolIds:fl(['curl','ffuf','burp-intruder']),pathIds:fl([CARD_ID]),tags:fl(['negative-control','canary','fuzzing','replay']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-file-handling-report-evidence-v977',title:'Report the failed boundary, not the payload shape',body:'For reporting, name the failed boundary and evidence for it: missing server-side validation, unsafe storage, unsafe retrieval, unsafe include source, unsafe wrapper handling, executable interpretation, or missing cleanup. Keep lab-specific payload strings and target values out of public guidance.',kind:'report',cardIds:fl([CARD_ID]),toolIds:fl(['curl','ffuf']),pathIds:fl([CARD_ID]),tags:fl(['reporting','remediation','file-upload','file-inclusion']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE}),
 fo({id:'note-cluster-window-split-rationale-v977',title:'Do not force neighbor notes into the wrong product card',body:'The first cluster window also contained adjacent XSS, credential, cracking, and SQLMap notes. v9.77 records queue/private rationale for those notes instead of stuffing them into the upload/inclusion card. Future cluster mining should prefer semantic note membership over raw packet-window convenience.',kind:'lesson',cardIds:fl([CARD_ID]),toolIds:fl([]),pathIds:fl(['dashboard']),tags:fl(['source-mining','cluster-review','queue-hygiene']),sourceRefs:fl([ACTIVE_CLUSTER_ID]),reviewWave:WAVE})
]);
const PRODUCT_CHANGES=fl(PUBLIC_NOTE_IDS.map(id=>'field-note:'+id).concat(['live-card:'+CARD_ID,'evidence-parser-change:'+ANALYZER_ID,'cluster-ledger-completion:'+ACTIVE_QUEUE_ID]));
function redact(v){return String(v||'').replace(/HTB\{[^}]+\}|flag\{[^}]+\}/gi,'[flag-redacted]').replace(/Answer:\s*[^\n\r]+/gi,'Answer: [redacted]').replace(/\b\d{1,3}(?:\.\d{1,3}){3}(?::\d+)?\b/g,'[host]').replace(/\b[A-Fa-f0-9]{24,128}\b/g,'[encoded-or-secret-material]').slice(0,1200);}
function hash(v){let h=0,s=String(v||'');for(let i=0;i<s.length;i++)h=((h<<5)-h+s.charCodeAt(i))|0;return String(Math.abs(h));}
function analyze(text){const raw=String(text||''),matches=[];function add(id,label,fact){matches.push(fo({id,label,fact}));}if(/upload(?:ed)? successfully|multipart\/form-data|stored as|download link|accepted file/i.test(raw))add('upload-accepted','Upload acceptance or storage clue observed','web.upload.acceptance_observed');if(/Content-Type:|Location:|Content-Disposition|source code|download/i.test(raw))add('serving-behavior','Serving, download, or handler clue observed','web.upload.serving_behavior_observed');if(/\b(?:LFI|RFI|file inclusion|include path|open_basedir|wrapper|php:\/\/|data:\/\/|expect:\/\/|\.\.\/|%2e%2e)\b/i.test(raw))add('inclusion-source','Inclusion source or transform clue observed','web.inclusion.transform_observed');if(/\b(?:ffuf|wfuzz|gobuster|Status:|Size:|Words:|Lines:|FUZZ|baseline|filter size|calibration)\b/i.test(raw))add('fuzzing-delta','Fuzzing or response-delta clue observed','web.file_handling.fuzzing_signal_observed');if(/\b(?:canary|marker|known-good|known bad|negative control|replay)\b/i.test(raw))add('control-pair','Canary or negative-control clue observed','web.file_handling.control_pair_observed');if(/\b(?:web shell|reverse shell|command output|uid=|whoami|hostname|system\(|exec\(|shell_exec\(|interpreted|executed)\b/i.test(raw))add('execution-effect','Execution claim or command-effect clue observed','web.upload.execution_claim_observed');const outcomeFacts=fl(uniq(matches.map(m=>m.fact))),warnings=[];if(outcomeFacts.includes('web.upload.acceptance_observed'))warnings.push('Accepted upload is not impact proof. Record stored name, retrieval path, serving behavior, and downstream handling separately.');if(outcomeFacts.includes('web.inclusion.transform_observed'))warnings.push('Separate file read, wrapper transform, remote retrieval, and executable interpretation before reporting impact.');if(outcomeFacts.includes('web.file_handling.fuzzing_signal_observed'))warnings.push('Fuzzer hits are triage. Replay manually and compare against a baseline or negative control.');if(outcomeFacts.includes('web.upload.execution_claim_observed'))warnings.push('Execution requires command-result evidence and cleanup context, not just a reachable file or include response.');return fo({id:ANALYZER_ID,matchCount:matches.length,matches:fl(matches),outcomeFacts,warnings:fl(warnings),snippetHash:hash(redact(raw)),snippet:redact(raw)});}
function upsertNotes(){const prev=root.OBOL_NOTE_INTEGRATION;if(!prev||!Array.isArray(prev.publicFieldNotes))return false;const byId=new Map(prev.publicFieldNotes.map(n=>[n&&n.id,n]).filter(p=>p[0]));for(const n of PUBLIC_NOTES)byId.set(n.id,n);root.OBOL_NOTE_INTEGRATION=fo({...prev,publicFieldNotes:fl(Array.from(byId.values())),__webUploadInclusionClusterV977:true});return true;}
function installCard(){const prev=liveCard(CARD_ID)||{},prior=Array.isArray(prev.commands)?prev.commands:[];function cmd(tool,run,note){return fo({tool,run,note});}const add=[cmd('curl','curl -i -s -k -F "file=@{{benign_canary_file}}" "{{upload_url}}"','Use a harmless canary first. This proves only acceptance and response handling.'),cmd('curl','curl -i -s -k "{{stored_or_include_url}}"','Replay the stored or included resource and capture status, headers, body behavior, and handler clues.'),cmd('ffuf','ffuf -u "{{url}}?{{parameter}}=FUZZ" -w {{wordlist}} -mc all -fs {{baseline_size}}','Use after a parameter or include-source hypothesis exists. Treat hits as triage until manually replayed.')];const byRun=new Map();prior.concat(add).forEach(c=>{if(c&&c.run)byRun.set(c.run,c);});return replaceCard(fo({...prev,id:CARD_ID,lane:prev.lane||'web-file-handling',title:prev.title||'Upload and Inclusion Proof Chain',hypothesis:'Use this when upload, file-read, wrapper, remote-include, or web-shell evidence appears. Prove control, acceptance, storage, retrieval, interpretation, downstream effect, and cleanup as separate facts.',commands:fl(Array.from(byRun.values())),expected:fl(uniq([].concat(prev.expected||[],['benign canary or baseline recorded','stored name or include source captured','response headers and body behavior reviewed','file read or execution boundary selected','negative control compared','cleanup state recorded']))),produces:fl(uniq([].concat(prev.produces||[],['web.file_handling.impact_ladder_reviewed','web.file_handling.control_pair_reviewed','web.file_handling.report_boundary_selected']))),tools:fl(uniq([].concat(prev.tools||[],['curl','ffuf','Burp Intruder']))),sourceMined77:fo({wave:WAVE,proof:'data/product-hardening/web-upload-inclusion-cluster-v9.77.js',sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:TOTAL_NOTES,uploadInclusionNotes:UPLOAD_INCLUSION_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,publicNotes:PUBLIC_NOTE_IDS})}));}
function patchEvidence(){const T=root.OBOL_INTAKE_V21;if(!T||typeof T.analyzeTerminal!=='function')return false;if(T.analyzeTerminal.__webUploadInclusionClusterV977)return true;const original=T.analyzeTerminal;try{T.analyzeTerminal=function(text){const result=original.apply(this,arguments)||{},a=analyze(text);if(a.matchCount){const acts=Array.isArray(result.activities)?result.activities.slice():[];acts.push(fo({id:'evidence-file-handling-'+a.snippetHash,cardId:CARD_ID,title:'File-handling evidence staged for proof review',result:a.outcomeFacts.includes('web.upload.execution_claim_observed')?'interesting':'triage',summary:a.warnings[0]||'File-handling evidence needs staged proof.',facts:a.outcomeFacts}));result.activities=acts;result.uploadInclusionEvidence77=a;}return result;};T.analyzeTerminal.__webUploadInclusionClusterV977=true;return true;}catch(_){return false;}}
function completeCluster(){const prev=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!prev||!prev.status||!Array.isArray(prev.pendingClusters)||!Array.isArray(prev.reviewQueue))return false;if(prev.status.latestCompletedClusterQueue===ACTIVE_QUEUE_ID)return true;const pending=fl(prev.pendingClusters.filter(c=>c&&c.id!==ACTIVE_CLUSTER_ID)),queue=fl(prev.reviewQueue.filter(i=>i&&i.id!==ACTIVE_QUEUE_ID)),remaining=pending.reduce((s,c)=>s+Number(c.pendingCount||0),0),counts=pending.reduce((a,c)=>{a[c.readiness]=(a[c.readiness]||0)+1;return a;},{});const done=fo({id:ACTIVE_CLUSTER_ID,queueId:ACTIVE_QUEUE_ID,completedBy:'v9.77',sourceRoute:SOURCE_ROUTE,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:TOTAL_NOTES,productOutputs:PRODUCT_CHANGES,trueUploadInclusionNotes:UPLOAD_INCLUSION_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,rationale:'Whole-cluster packet windows were re-read. Upload and inclusion mechanics landed in the proof-chain card, analyzer, field notes, and report guidance; adjacent XSS, credential, cracking, and SQLMap notes received queue/private rationale instead of being forced into the wrong card.'});const status=fo({...prev.status,schemaVersion:'2.1.0',reviewedSourceNotes:215,pendingSourceNotes:remaining,clusteredPendingNotes:remaining,unclusteredPendingNotes:0,clusterCount:pending.length,readyClusterCount:counts['ready-to-mine']||0,privateHeavyClusterCount:counts['private-heavy']||0,needsSplitClusterCount:counts['needs-split']||0,latestCompletedClusterQueue:ACTIVE_QUEUE_ID,latestCompletedClusterId:ACTIVE_CLUSTER_ID,latestCompletedClusterReviewTextChars:REVIEW_TEXT_CHARS,latestClusterReviewWave:WAVE,nextClusterReviewQueue:queue[0]&&queue[0].id||null});function validate(){const f=[],sum=pending.reduce((s,c)=>s+Number(c.pendingCount||0),0);if(sum!==status.pendingSourceNotes)f.push('pending cluster counts do not match pending source notes');if(queue.length!==pending.length)f.push('review queue length must match pending clusters');if(queue.some(i=>i&&i.id===ACTIVE_QUEUE_ID))f.push('completed active cluster still appears in review queue');if(pending.some(c=>c&&c.id===ACTIVE_CLUSTER_ID))f.push('completed active cluster still appears pending');if(/HTB\{|flag\.txt|Password123|94\.237|83\.136|Answer:|BEGIN RSA PRIVATE KEY|AKIA[0-9A-Z]{16}/i.test(JSON.stringify({pending,queue,done,status})))f.push('cluster completion metadata leaked private/source-specific material');return fl(f);}root.OBOL_SOURCE_NOTE_CLUSTERS=fo({...prev,status,pendingClusters:pending,reviewQueue:queue,completedClusters:fl([].concat(prev.completedClusters||[],[done])),latestCompletedCluster:done,validate});return true;}
function updateProgress(){const p=root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS;if(!p||typeof p!=='object')return false;root.OBOL_PRODUCT_HARDENING_NOTE_PROGRESS=fo({...p,reviewed:215,pending:341,latestWave:WAVE,latestClusterReview:fo({id:ACTIVE_QUEUE_ID,clusterId:ACTIVE_CLUSTER_ID,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,notesReviewed:TOTAL_NOTES,publicNotes:PUBLIC_NOTE_IDS,productChanges:PRODUCT_CHANGES,neighborNotesDispositioned:NEIGHBOR_NOTES})});return true;}
function updateQueueItem(){const q=root.OBOL_PRODUCT_HARDENING;if(!q||!Array.isArray(q.items))return false;const item=q.items.find(e=>e&&e.id===ACTIVE_QUEUE_ID);if(item){item.status='complete';item.completedBy='v9.77';item.detail='Complete: v9.77 mined the upload/inclusion mechanics from the active 40-note cluster window, recorded neighbor-note disposition rationale, and advanced the cluster queue.';}return true;}
function validate(){const f=[],notes=root.OBOL_NOTE_INTEGRATION&&Array.isArray(root.OBOL_NOTE_INTEGRATION.publicFieldNotes)?root.OBOL_NOTE_INTEGRATION.publicFieldNotes:[],ids=new Set(notes.map(n=>n&&n.id).filter(Boolean)),c=root.OBOL_SOURCE_NOTE_CLUSTERS;PUBLIC_NOTE_IDS.forEach(id=>{if(!ids.has(id))f.push('missing public field note '+id);});if(!c||!c.status||c.status.latestCompletedClusterQueue!==ACTIVE_QUEUE_ID)f.push('source cluster ledger did not record v9.77 completion');if(c&&c.status&&Number(c.status.pendingSourceNotes)!==341)f.push('expected 341 pending notes after completing 40-note cluster');if(c&&Array.isArray(c.reviewQueue)&&c.reviewQueue.some(i=>i&&i.id===ACTIVE_QUEUE_ID))f.push('completed cluster queue item still pending');if(!liveCard(CARD_ID))f.push('missing upload/inclusion proof-chain card');return fl(f);}
function integrate(){const notesIntegrated=upsertNotes(),cardInstalled=installCard(),evidenceInstalled=patchEvidence(),clusterCompleted=completeCluster(),progressUpdated=updateProgress(),queueUpdated=updateQueueItem(),failures=validate();const api=fo({wave:WAVE,status:failures.length?'partial':'live-integrated',activeQueueId:ACTIVE_QUEUE_ID,activeClusterId:ACTIVE_CLUSTER_ID,sourceRoute:SOURCE_ROUTE,sourcePackets:SOURCE_PACKETS,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:TOTAL_NOTES,uploadInclusionNotes:UPLOAD_INCLUSION_NOTES,neighborNotesDispositioned:NEIGHBOR_NOTES,publicNoteIds:PUBLIC_NOTE_IDS,productChanges:PRODUCT_CHANGES,analyzerId:ANALYZER_ID,cardId:CARD_ID,notesIntegrated,cardInstalled,evidenceInstalled,clusterCompleted,progressUpdated,queueUpdated,failures,analyze,integrate,validate});root.OBOL_WEB_UPLOAD_INCLUSION_CLUSTER_V977=api;return api;}
const first=integrate();if(typeof root.setTimeout==='function'){let tries=0;const attempt=()=>{const r=integrate();tries+=1;if(r.status!=='live-integrated'&&tries<180)root.setTimeout(attempt,50);};if(first.status!=='live-integrated')root.setTimeout(attempt,0);if(typeof root.addEventListener==='function'){root.addEventListener('hashchange',attempt);root.addEventListener('focus',attempt);}}
if(typeof module!=='undefined'&&module.exports)module.exports=root.OBOL_WEB_UPLOAD_INCLUSION_CLUSTER_V977;
})(typeof window!=='undefined'?window:globalThis);
