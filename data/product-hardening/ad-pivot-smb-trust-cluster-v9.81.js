'use strict';
(function(root){
const W='v9.81-ad-pivot-smb-trust-kerberos-cluster';
const ACTIVE='source-note-cluster-browser-client-cookie-transform-workflows';
const ORIGINAL='browser-client-cookie-transform-workflows';
const FIXED='ad-pivot-smb-trust-kerberos-workflows';
const NEXT='source-note-cluster-web-proxy-fuzzing-and-transform-workflows';
const ROUTE='platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json';
const PACK=Object.freeze(['data/review-packets/htb-penetration-tester-08.json']);
const REVIEW_TEXT_CHARS=590379;
const NOTE_IDS=Object.freeze(['htb-penetration-tester-f5830bc01776efd5','htb-penetration-tester-f6638e21595b7f37','htb-penetration-tester-2ef92519d73f3cdb','htb-penetration-tester-ca2f9dbdf6e54247','htb-penetration-tester-b9f62798a6a48d34','htb-penetration-tester-c3600c3535fafe70','htb-penetration-tester-6c769e636c399625','htb-penetration-tester-f39ca95dd46ffb6c']);
const AD='ad-enumeration-bloodhound-collection';
const MSF='metasploit-resource-pivot-workflow';
const ANALYZER='ad-pivot-smb-trust-evidence-analyzer-v981';
const UI_SLOP=/fills an unresolved methodology gap|methodology gap|UNKNOWN|source-mining|source re-mining|release cleanup|patch panel|stabilizer/i;
const PUBLIC_NOTES=Object.freeze([
 ['note-ad-misc-misconfig-lead-triage-v981','Triage AD misconfigurations as leads before attack selection','Exchange groups, printer coercion exposure, weak directory settings, DNS records, GPP residue, roastable users, and ACL edges are separate lead classes. Record identity, object, permission, protocol, and tested boundary before turning a finding into lateral movement or domain-impact language.','path-guidance',[AD],['active-directory','misconfiguration','lead-triage'],['PowerView','BloodHound','ldapsearch','adidnsdump']],
 ['note-ad-smb-share-proof-boundary-v981','SMB enumeration needs share, permission, and content proof','SMB evidence should keep dialect/signing, null or credentialed session state, share list, per-share permission, recursive listing, download/upload proof, and RPC findings separate. A visible share is a lead; readable or writable content is the path-moving evidence.','evidence',[AD],['smb','shares','rpc','evidence'],['smbclient','smbmap','rpcclient','enum4linux-ng']],
 ['note-ad-pivot-route-proof-boundary-v981','Pivots need listener, route, and internal-service proof','A tunnel is not useful until the listener exists, the route or proxy configuration points through the compromised host, and a narrow internal probe proves reachability. Keep SSH forwards, dynamic SOCKS, proxychains scans, Metasploit routes, and remote GUI/shell channels as separate proof states.','tool-guidance',[MSF],['pivoting','socks','proxychains','route-proof'],['ssh','proxychains','nmap','msfconsole']],
 ['note-ad-trust-kerberos-scope-v981','Trust and Kerberos findings must preserve domain scope','Cross-domain Kerberos work has to keep trust direction, target domain, SPN ownership, ticket material, cracking result, and cross-domain access validation separate. A roastable account in another domain is not partner-forest compromise until validated against an in-scope service.','evidence',[AD],['active-directory','trusts','kerberoasting','kerberos'],['PowerView','Rubeus','hashcat']],
 ['note-ad-dcsync-replication-proof-v981','DCSync needs replication-rights proof before credential-dump impact','Domain replication output is a high-impact credential path only after the controlled account and required replication rights are proven. Keep rights enumeration, tool invocation, extracted material class, password-history or reversible-encryption evidence, and cleanup/reporting boundaries separate.','evidence',[AD],['active-directory','dcsync','replication','credential-boundary'],['PowerView','secretsdump','mimikatz']],
 ['note-ad-privileged-access-channel-v981','Remote access edges need channel-specific validation','BloodHound CanRDP, CanPSRemote, SQLAdmin, local-admin, and group-derived execution edges are planning evidence. Validate the actual channel with the correct tool and record whether access is GUI, shell, database execution, share access, or only a denied attempt.','path-guidance',[AD,MSF],['active-directory','rdp','winrm','mssql','access-validation'],['xfreerdp','evil-winrm','mssqlclient.py','PowerView']],
 ['note-ad-cluster-reconciliation-v981','Correct bad cluster labels instead of building the wrong product surface','When a generated cluster title does not match complete packet evidence, fix the queue metadata and mine the real operator spine. This assigned window is AD, SMB, pivoting, trusts, DCSync, privileged access, and Kerberos workflow material, not browser cookie transform work.','queue',[AD,MSF],['cluster-reconciliation','notes','queue'],[]]
]);
function fl(v){return Object.freeze((v||[]).slice());}
function fo(v){return Object.freeze(v||{});}
function uniq(v){return Array.from(new Set((v||[]).filter(Boolean)));}
function lanes(){return Array.isArray(root.OBOL_LANES)?root.OBOL_LANES:(Array.isArray(root.LANES)?root.LANES:[]);}
function findCard(id){if(root.CARDS&&root.CARDS[id])return root.CARDS[id];for(const lane of lanes())for(const card of lane.cards||[])if(card&&card.id===id)return card;return null;}
function ensureLane(id,title){let lane=lanes().find(l=>l&&(l.id===id||l.lane===id));if(!lane){lane={id,lane:id,title:title||id,group:id,cards:[]};if(Array.isArray(root.OBOL_LANES))root.OBOL_LANES.push(lane);else if(Array.isArray(root.LANES))root.LANES.push(lane);else root.OBOL_LANES=[lane];}if(!Array.isArray(lane.cards))lane.cards=[];return lane;}
function cleanUiValue(value){
 if(typeof value==='string')return UI_SLOP.test(value)?undefined:value;
 if(Array.isArray(value))return value.map(cleanUiValue).filter(v=>v!==undefined);
 if(value&&typeof value==='object'){
  const out={};
  for(const key of Object.keys(value)){
   if(UI_SLOP.test(key))continue;
   const next=cleanUiValue(value[key]);
   if(next!==undefined)out[key]=next;
  }
  return out;
 }
 return value;
}
function upsert(card,laneId,title){const lane=ensureLane(laneId||card.lane||'ad-enumeration',title||'Active Directory');let seen=false;if(root.CARDS&&root.CARDS[card.id]){root.CARDS[card.id]=card;seen=true;}for(const l of lanes()){if(!Array.isArray(l.cards))continue;const i=l.cards.findIndex(c=>c&&c.id===card.id);if(i>=0){l.cards.splice(i,1,card);seen=true;}}if(!seen)lane.cards.push(card);root.CARDS=root.CARDS&&typeof root.CARDS==='object'?root.CARDS:{};try{root.CARDS[card.id]=card;}catch(_err){}return true;}
function rows(a,b,key){const m=new Map();(a||[]).concat(b||[]).forEach(x=>{const k=x&&String(x[key]||x.run||x.value||x.command||'');if(k)m.set(k,x);});return Array.from(m.values());}
function cmd(tool,run,when,evidence,note){return fo({tool,run,when,evidence,useWhen:when,expected:evidence,note:note||evidence});}
function normCmds(list){return fl((list||[]).map((entry)=>{const run=entry&&String(entry.run||entry.value||entry.command||entry.text||'');if(!run)return null;const tool=String(entry.tool||entry.label||'operator');const when=String(entry.when||entry.useWhen||entry.note||'Run only after the relevant scope, identity, and target are selected.');const evidence=String(entry.evidence||entry.expected||entry.note||'Paste the command output, exact denial or success boundary, and cleanup state.');return fo(Object.assign({},entry,{tool,run,when,evidence,useWhen:when,expected:evidence}));}).filter(Boolean));}
function fallbackList(base,extra){return fl(uniq([].concat(base||[],extra||[])));}
function redact(value){return String(value||'').replace(/HTB\{[^}]+}|flag\{[^}]+}/gi,'[flag-redacted]').replace(/Answer:\s*[^\n\r]+/gi,'Answer: [redacted]').replace(/\b\d{1,3}(?:\.\d{1,3}){3}(?::\d+)?\b/g,'[host]').replace(/\b[A-Fa-f0-9]{24,128}\b/g,'[encoded-or-secret-material]').replace(/((?:password|passwd|pwd|secret|token|cookie|hash|key)\s*[:=]\s*)([^;\s&]+)/gi,'$1[redacted]').replace(/\bINLANEFREIGHT\b|\bFREIGHTLOGISTICS\b|Academy_student|HTB_@cademy/gi,'[lab-redacted]').slice(0,1600);}
function note(row){return fo({id:row[0],title:row[1],body:row[2],kind:row[3],cardIds:fl(row[4]),pathIds:fl(row[4]),toolIds:fl(row[6]),tags:fl(row[5]),sourceRefs:NOTE_IDS,reviewWave:W});}
const NOTES=fl(PUBLIC_NOTES.map(note));
function patchNotes(){const prev=root.OBOL_NOTE_INTEGRATION;if(!prev||!Array.isArray(prev.publicFieldNotes))return false;const map=new Map(prev.publicFieldNotes.map(n=>[n&&n.id,n]).filter(p=>p[0]));NOTES.forEach(n=>map.set(n.id,n));root.OBOL_NOTE_INTEGRATION=fo(Object.assign({},prev,{publicFieldNotes:fl(Array.from(map.values())),__adPivotSmbTrustClusterV981:true}));return true;}
function patchAdCard(){const base=Object.assign({},cleanUiValue(findCard(AD)||{id:AD,title:'AD Enumeration Collection Spine',phase:'ad',lane:'ad-enumeration',group:'active-directory',commands:[],expected:[],expectedEvidence:[],produces:[],tools:[],fieldNoteIds:[]}));const commands=[
 cmd('smbclient','smbclient -N -L //{{target}}','Check whether SMB allows null-session share listing before trying credentialed access.','Share list, authentication mode, dialect or signing clue, and exact error if denied.','Null session is a lead, not access proof.'),
 cmd('smbmap','smbmap -H {{target}} -u {{user_or_blank}} -p {{password_or_blank}}','Classify readable and writable shares after share discovery.','Per-share READ/WRITE/NO ACCESS rows tied to the identity used.','Do not treat a share name as content access until permissions are visible.'),
 cmd('rpcclient','rpcclient -U "{{domain_or_workgroup}}/{{user}}%{{password}}" {{target}} -c "enumdomusers; enumdomgroups"','Use RPC to enumerate users and groups when SMB/RPC access is permitted.','User/group rows or a denial that proves the boundary.','RPC enumeration should be recorded with the credential context.'),
 cmd('PowerView','Get-DomainTrust; Get-DomainForeignGroupMember -Domain {{trusted_domain}}','Map trust direction and foreign group membership before any cross-domain attack path.','Trust relationship rows, direction, partner domain, and foreign-principal mapping.','Trust evidence defines scope and does not prove compromise by itself.'),
 cmd('PowerView','Get-DomainUser -SPN -Domain {{domain}} | Select-Object samaccountname,serviceprincipalname','Find Kerberoast candidates in the current or trusted domain.','SPN-bearing users and service names plus target domain context.','Discovery does not prove a crackable ticket or service access.'),
 cmd('PowerView','Get-ObjectAcl "{{domain_dn}}" -ResolveGUIDs | ? {$_.ObjectAceType -match "Replication-Get"} | Select-Object AceQualifier,ObjectDN,ActiveDirectoryRights,SecurityIdentifier,ObjectAceType','Prove whether the controlled identity has replication rights before DCSync wording.','Rights rows that show DS-Replication permissions for the controlled principal.','Rights evidence comes before any credential-dump impact claim.'),
 cmd('PowerView','Get-NetLocalGroupMember -ComputerName {{host}} -GroupName "Remote Management Users"; Get-NetLocalGroupMember -ComputerName {{host}} -GroupName "Remote Desktop Users"','Validate whether an AD principal has WinRM or RDP access to a host.','Local group membership output tied to host, group, user, and domain context.','Remote access edges still need channel-specific login proof.'),
 cmd('secretsdump.py','secretsdump.py -just-dc-user {{target_user}} {{domain}}/{{user}}@{{dc_host}}','After replication rights are proven, request one bounded account to prove DCSync capability.','Tool output showing requested user scope and extracted material class with secrets redacted.','Prefer narrow proof before broad domain credential extraction.')
];const expected=fallbackList([].concat(base.expected||[],base.expectedEvidence||[]),['SMB discovery, permission, RPC, and content-access states separated','Trust direction and target-domain scope captured before cross-domain Kerberos wording','Replication rights proven before DCSync impact wording','Remote access channel validation separated for RDP, WinRM, MSSQL, and SMB','ACL edge, object, principal, and cleanup requirements captured']);return upsert(fo(Object.assign({},base,{operatorGoal:base.operatorGoal||'Run a bounded AD enumeration and privilege-edge command spine, paste the evidence, and decide whether SMB, trust, Kerberos, DCSync, or remote-access validation is the next safe move.',commands:normCmds(rows(base.commands,commands,'run')),expected,expectedEvidence:expected,failureModes:fallbackList(base.failureModes,['SMB share discovery without READ/WRITE proof remains a lead.','Trust and Kerberos candidates require scoped validation before cross-domain impact wording.','Replication or ACL abuse evidence must name the controlled principal, exact right, and cleanup boundary.']),nextSteps:fallbackList(base.nextSteps,['Move to share/content review after readable SMB evidence.','Move to Kerberos roasting or trust validation only after SPN/trust scope is proven.','Move to DCSync proof only after replication rights are observed.']),produces:fallbackList(base.produces,['service.smb_enumeration_reviewed','ad.trust_boundary_reviewed','ad.kerberoast_scope_reviewed','ad.dcsync_rights_reviewed','ad.remote_access_channel_reviewed','ad.acl_abuse_boundary_reviewed']),tools:fallbackList(base.tools,['smbclient','smbmap','rpcclient','enum4linux-ng','PowerView','Rubeus','hashcat','secretsdump.py','evil-winrm','xfreerdp','mssqlclient.py']),fieldNoteIds:fallbackList(base.fieldNoteIds,NOTES.filter(n=>(n.cardIds||[]).includes(AD)).map(n=>n.id)),sourceAdPivot81:fo({wave:W,integratedInto:'existing-card',correctedFrom:ORIGINAL,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:NOTE_IDS.length})})),'ad-enumeration','Active Directory');}
function patchPivotCard(){const base=Object.assign({},cleanUiValue(findCard(MSF)||{id:MSF,title:'Metasploit Resource Pivot Spine',phase:'pivoting',lane:'post-exploitation',group:'metasploit',commands:[],expected:[],expectedEvidence:[],produces:[],tools:[],fieldNoteIds:[]}));const commands=[
 cmd('ssh','ssh -N -L {{local_port}}:{{remote_host}}:{{remote_port}} {{user}}@{{pivot_host}}','Use local forwarding when a specific remote service is known behind the pivot.','Local listener proof plus service banner or request through the forwarded port.','A listener alone does not prove the remote service is reachable.'),
 cmd('ssh','ssh -N -D {{socks_port}} {{user}}@{{pivot_host}}','Use dynamic SOCKS when internal targets or ports are not yet identified.','SOCKS listener proof and proxy configuration used for the next probe.','Keep the proxy route separate from internal scan proof.'),
 cmd('proxychains','proxychains -q nmap -sT -Pn -n -p {{ports}} {{internal_target_or_cidr}}','Run a bounded TCP connect scan through the proven SOCKS proxy.','Open/closed/filtered rows tied to the proxied target and selected port set.','Proxychains scan output proves reachability, not exploitation.'),
 cmd('msfconsole','msfconsole -q -x "sessions -l; route print; route add {{subnet}} {{netmask}} {{session_id}}; route print"','When using Meterpreter, bind route creation to the actual session and interface evidence.','Session list and route table before and after the change.','Wrong-session routes invalidate downstream scan results.')
];const expected=fallbackList([].concat(base.expected||[],base.expectedEvidence||[]),['Local forward, dynamic SOCKS, proxychains scan, and Metasploit route states separated','Internal service reachability proven with a bounded probe after proxy or route setup','RDP/WinRM/MSSQL use through a pivot tied to channel-specific evidence']);return upsert(fo(Object.assign({},base,{operatorGoal:base.operatorGoal||'Build a visible pivot command spine, prove the listener or route, run one bounded internal probe, and paste the evidence before treating the pivot as usable.',commands:normCmds(rows(base.commands,commands,'run')),expected,expectedEvidence:expected,failureModes:fallbackList(base.failureModes,['A listener without a successful internal probe is not route proof.','A route added to the wrong session invalidates downstream results.','Proxychains scan output proves reachability only, not exploitation.']),nextSteps:fallbackList(base.nextSteps,['Move to internal service enumeration after route and bounded scan proof.','Move to channel-specific login validation after reachable RDP, WinRM, SMB, or MSSQL is observed.','Move to cleanup/reporting after jobs, listeners, routes, and temporary artifacts are accounted for.']),produces:fallbackList(base.produces,['pivot.local_forward_reviewed','pivot.dynamic_socks_reviewed','pivot.proxychains_probe_reviewed','pivot.msf_route_reviewed']),tools:fallbackList(base.tools,['ssh','proxychains','nmap','msfconsole','meterpreter','xfreerdp','evil-winrm','mssqlclient.py']),fieldNoteIds:fallbackList(base.fieldNoteIds,NOTES.filter(n=>(n.cardIds||[]).includes(MSF)).map(n=>n.id)),sourceAdPivot81:fo({wave:W,integratedInto:'existing-card',correctedFrom:ORIGINAL,reviewTextChars:REVIEW_TEXT_CHARS,totalNotes:NOTE_IDS.length})})),'post-exploitation','Post Exploitation');}
function analyze(text){const raw=String(text||''),lower=raw.toLowerCase(),matches=[];const add=(id,label,fact)=>matches.push(fo({id,label,fact}));
 if(/smbclient|smbmap|enum4linux|rpcclient|share|ipc\$|signing|smb/i.test(raw))add('smb','SMB enumeration or share-boundary evidence observed','service.smb_enumeration_observed');
 if(/ssh\s+-n\s+-[ld]|proxychains|socks|autoroute|route\s+add|meterpreter|portscan|pivot/i.test(raw))add('pivot','Pivot route, proxy, or internal probe evidence observed','pivot.route_or_proxy_observed');
 if(/forest|trust|foreigngroupmember|foreign security principal|sid history|extra sid|cross-domain|cross-forest/i.test(raw))add('trust','Trust, forest, or cross-domain boundary evidence observed','ad.trust_or_forest_boundary_observed');
 if(/dcsync|replication-get|replicating directory changes|secretsdump|lsadump::dcsync|ntds/i.test(raw))add('dcsync','DCSync rights or replication evidence observed','ad.dcsync_rights_or_replication_observed');
 if(/canrdp|canpsremote|sqladmin|evil-winrm|enter-pssession|xfreerdp|mssqlclient|remote management users|remote desktop users/i.test(raw))add('remote-access','Remote access channel evidence observed','ad.remote_access_channel_observed');
 if(/kerberoast|asreproast|rubeus|\$krb5tgs\$|spn|serviceprincipalname|hashcat/i.test(raw))add('kerberos','Kerberos roast or SPN evidence observed','ad.kerberoast_candidate_observed');
 if(/genericall|genericwrite|writeowner|writedacl|forcechangepassword|addself|acl|ace|powerview/i.test(raw))add('acl','ACL or ACE rights evidence observed','ad.acl_rights_observed');
 const facts=fl(uniq(matches.map(m=>m.fact))),warnings=[];
 if(facts.includes('service.smb_enumeration_observed'))warnings.push('SMB enumeration must separate share discovery, permissions, content access, and upload/write proof.');
 if(facts.includes('pivot.route_or_proxy_observed'))warnings.push('Pivot output proves transport only after listener, route/proxy config, and a bounded internal probe are captured.');
 if(facts.includes('ad.trust_or_forest_boundary_observed'))warnings.push('Trust evidence defines possible scope; it does not prove cross-domain access until an in-scope service validates it.');
 if(facts.includes('ad.dcsync_rights_or_replication_observed'))warnings.push('DCSync impact requires controlled identity plus replication-rights proof; raw credential material must remain redacted.');
 if(facts.includes('ad.remote_access_channel_observed'))warnings.push('RDP, WinRM, MSSQL, and SMB are different access channels and need channel-specific validation.');
 if(facts.includes('ad.kerberoast_candidate_observed'))warnings.push('SPN discovery or ticket capture is not account compromise until cracking and scoped authentication are validated.');
 if(facts.includes('ad.acl_rights_observed'))warnings.push('ACL edges can be destructive; record approval, exact object/right, action taken, and cleanup state.');
 return fo({id:ANALYZER,matchCount:matches.length,matches:fl(matches),outcomeFacts:facts,warnings:fl(warnings),snippet:redact(lower)});
}
function registerAnalyzer(){const existing=Array.isArray(root.OBOL_EVIDENCE_ANALYZERS)?root.OBOL_EVIDENCE_ANALYZERS:[];root.OBOL_EVIDENCE_ANALYZERS=fl(existing.filter(a=>!(a&&a.id===ANALYZER)).concat([fo({id:ANALYZER,wave:W,analyze})]));root.OBOL_AD_PIVOT_SMB_TRUST_ANALYZER_V981=analyze;return true;}
function preserveLegacyIntake(){const intake=root.OBOL_INTAKE_V21;if(!intake||typeof intake.analyzeTerminal!=='function')return true;const previous=intake.analyzeTerminal;if(previous.__obolAdPivot81Preserved)return true;intake.analyzeTerminal=function(text){const result=previous.call(this,text)||{};if(!Array.isArray(result.activities))result.activities=[];const raw=String(text||'');function push(id,label){if(!result.activities.some(a=>a&&a.analyzerId===id))result.activities.push({analyzerId:id,label,source:'v9.81 stable owner preservation'});}if(/BloodHound|Find-DomainShare|serviceprincipalname|GenericAll|PowerView|DCSync|smbclient|smbmap/i.test(raw))push('ad-enumeration-evidence-analyzer','AD enumeration evidence observed');if(/msfconsole|Meterpreter|route add|route print|SOCKS|proxychains|TCP OPEN/i.test(raw))push('metasploit-workflow-evidence-analyzer','Metasploit pivot evidence observed');return result;};intake.analyzeTerminal.__obolAdPivot81Preserved=true;return true;}
function reviewQueueFrom(clusters){return fl((clusters||[]).map(c=>fo(Object.assign({},c,{id:'source-note-cluster-'+c.id,clusterId:c.id,label:c.title,targetCount:c.pendingCount,queueMode:'cluster-review',sourceRoute:ROUTE,acceptance:'Ship public-safe product mechanics from the whole cluster, then disposition each note with card/analyzer/field-note/report/queue/private rationale.'}))));}
function patchClusters(){const prev=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!prev||!prev.status||!Array.isArray(prev.pendingClusters))return false;const pending=prev.pendingClusters.filter(c=>c&&c.id!==ORIGINAL);const queue=reviewQueueFrom(pending);const total=pending.reduce((sum,c)=>sum+Number(c.pendingCount||0),0);const status=fo(Object.assign({},prev.status,{reviewedSourceNotes:307,pendingSourceNotes:total,clusteredPendingNotes:total,unclusteredPendingNotes:0,clusterCount:pending.length,latestCompletedClusterQueue:ACTIVE,latestCompletedClusterId:FIXED,latestCompletedOriginalClusterId:ORIGINAL,latestCompletedClusterTitle:'AD pivot, SMB, trust, DCSync, privileged access, and Kerberos workflows',latestCompletedClusterOwnerCards:fl([AD,MSF]),latestCompletedClusterSourcePackets:PACK,latestCompletedClusterReviewTextChars:REVIEW_TEXT_CHARS,latestClusterReviewWave:W,latestClusterReconciliation:'v9.81 corrected a generated browser/cookie transform label after complete-packet review showed the assigned source window was AD, SMB, pivoting, trusts, DCSync, privileged access, and Kerberos workflow material.',nextClusterReviewQueue:queue[0]?queue[0].id:NEXT}));root.OBOL_SOURCE_NOTE_CLUSTERS=fo(Object.assign({},prev,{status,pendingClusters:fl(pending),reviewQueue:queue,validate:function(){const failures=[];const pendingTotal=(this.pendingClusters||[]).reduce((sum,c)=>sum+Number(c.pendingCount||0),0);if(pendingTotal!==this.status.pendingSourceNotes)failures.push('pending cluster total mismatch');if((this.pendingClusters||[]).length!==this.status.clusterCount)failures.push('cluster count mismatch');if((this.reviewQueue||[]).length!==(this.pendingClusters||[]).length)failures.push('review queue mismatch');if((this.pendingClusters||[]).some(c=>c&&c.id===ORIGINAL))failures.push('mislabeled generated cluster still pending');return failures;}}));return true;}
function patchQueue(){const q=root.OBOL_PRODUCT_HARDENING,clusters=root.OBOL_SOURCE_NOTE_CLUSTERS;if(!q||!clusters||!Array.isArray(clusters.reviewQueue)||!clusters.reviewQueue[0])return false;const next=clusters.reviewQueue[0];q.nextNotesBatch=Object.assign({},next,{id:next.id,label:next.label||next.title,clusterId:next.clusterId,queueMode:'cluster-review',targetCount:next.targetCount||next.pendingCount,sourceSelector:'Select cluster `'+next.clusterId+'` from `data/product-hardening/source-note-clusters-current.js`, re-read every assigned source-window note from complete packet text, and mine the whole cluster before terminal dispositions.',acceptance:'Ship public-safe product mechanics from the whole cluster, then disposition each note with card/analyzer/field-note/report/queue/private rationale.'});q.__adPivotSmbTrustClusterV981=true;return true;}
function run(){const values={notesIntegrated:patchNotes(),adCardIntegrated:patchAdCard(),pivotCardIntegrated:patchPivotCard(),analyzerIntegrated:registerAnalyzer(),legacyIntakePreserved:preserveLegacyIntake(),clusterCompleted:patchClusters(),queuePatched:patchQueue()};const failures=Object.keys(values).filter(k=>!values[k]);root.OBOL_AD_PIVOT_SMB_TRUST_CLUSTER_V981=fo(Object.assign({status:failures.length?'partial':'live-integrated',activeQueueId:ACTIVE,originalClusterId:ORIGINAL,correctedClusterId:FIXED,primaryCardIds:fl([AD,MSF]),analyzerId:ANALYZER,sourceRoute:ROUTE,sourcePackets:PACK,reviewTextChars:REVIEW_TEXT_CHARS,noteCount:NOTE_IDS.length,selectedNoteIds:NOTE_IDS,publicNoteIds:fl(PUBLIC_NOTES.map(n=>n[0])),nextClusterQueue:NEXT,productChanges:fl(PUBLIC_NOTES.map(n=>'field-note:'+n[0]).concat(['card-enrichment:'+AD,'card-enrichment:'+MSF,'evidence-analyzer:'+ANALYZER,'cluster-ledger-correction:'+ACTIVE,'cluster-ledger-completion:'+FIXED])),failures,analyze},values));}
run();
})(typeof window!=='undefined'?window:globalThis);
