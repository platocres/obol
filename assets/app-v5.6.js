// Obol v5.6 UI overlay — surface mapped-delivery cleanup and canonical progress.
'use strict';
(function(){
function page56(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model56(){try{return C.northStarDashboard56(state,LANES,ctx());}catch(e){return null;}}
function stat56(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render56(){if(page56()!=='dashboard')return;const v=document.querySelector('#view'),d=model56();if(!v||!d||!d.canonicalAdvance56||v.querySelector('.advance56'))return;const a=d.canonicalAdvance56,r=d.deliveryRepair56||{},q=d.buildNext56||{};const html='<section class="dash-panel50 advance56"><div class="dash-head50"><div><span>v5.6 delivery + methodology advance</span><h3>Mapped delivery debt cleared, then five mature branches completed</h3></div><span>'+esc(String(a.coveragePct))+'% fully implemented</span></div><p class="advance-copy56">The synchronized v5.5 Build Next queue drove this release. Remaining mapped workflows now carry explicit Evidence contracts, the seven execution-metadata gaps are audited, and five mature partial branches moved to implemented without turning discovery, ticket creation, relay startup, or credential material into unproven access.</p><div class="dash-mini50">'+stat56('Fully implemented',a.toImplemented+'/127','up '+a.delta+' canonical sections in v5.6')+stat56('Delivery repairs',r.ready+'/'+r.targeted,'v5.6 repaired workflow contracts ready')+stat56('Mapped debt left',r.mappedDeliveryRemaining||0,'mapped workflows with delivery debt')+stat56('Build Next',q.total||0,(q.canonicalGaps||0)+' canonical gaps remain')+'</div><div class="readme-sync54"><b>README queue remains synchronized</b><span>North Star Build Next → generated README snapshot → CI --check</span><code>node tools/sync-readme-build-next.js --check</code></div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync56(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v5.6';document.title='Obol v5.6 — Offensive Box Operations Ledger';render56();}
window.addEventListener('hashchange',()=>setTimeout(sync56,140));setTimeout(sync56,640);setTimeout(sync56,1040);setTimeout(sync56,1440);
})();
