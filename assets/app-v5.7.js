// Obol v5.7 UI overlay — surface the canonical gap completion wave.
'use strict';
(function(){
function page57(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model57(){try{return C.northStarDashboard57(state,LANES,ctx());}catch(e){return null;}}
function stat57(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render57(){if(page57()!=='dashboard')return;const v=document.querySelector('#view'),d=model57();if(!v||!d||!d.canonicalAdvance57||v.querySelector('.advance57'))return;const a=d.canonicalAdvance57,w=d.gapWave57||{},q=d.buildNext57||{};const html='<section class="dash-panel50 advance57"><div class="dash-head50"><div><span>v5.7 canonical gap wave</span><h3>Highest-priority Build Next gaps now have delivery-ready owners</h3></div><span>'+esc(String(a.coveragePct))+'% fully implemented</span></div><p class="advance-copy57">v5.7 follows the generated v5.6 Build Next ordering: DNSAdmins abuse, Entra/AD Connect discovery, Certifried, MS14-068, and noPac now have explicit command, execution-side, Evidence, decision-path, cleanup/reporting, and conservative proof contracts.</p><div class="dash-mini50">'+stat57('Fully implemented',a.toImplemented+'/127','up '+a.delta+' canonical sections in v5.7')+stat57('Canonical gaps',w.remainingGaps||0,'down '+a.gapDelta+' in this release')+stat57('v5.7 delivery ready',a.deliveryReady+'/'+a.total,'new canonical owners')+stat57('Build Next',q.total||0,(q.canonicalGaps||0)+' canonical gaps remain')+'</div><div class="readme-sync54"><b>README queue remains synchronized</b><span>North Star Build Next → generated README snapshot → CI --check</span><code>node tools/sync-readme-build-next.js --check</code></div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync57(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v5.7';document.title='Obol v5.7 — Offensive Box Operations Ledger';render57();}
window.addEventListener('hashchange',()=>setTimeout(sync57,140));setTimeout(sync57,640);setTimeout(sync57,1040);setTimeout(sync57,1440);
})();
