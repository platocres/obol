// Obol v6.5 Intake overlay — conservative copy/paste Evidence for the first five atomic AD CS fidelity units.
(function(root){
'use strict';
const C=root.OBOL_CORE_V2,T=root.OBOL_INTAKE_V21,M=root.OBOL_METHODOLOGY_V65;
if(!C||!T||!T.analyzeTerminal||!M)throw new Error('Obol core, terminal intake, and v6.5 methodology are required before intake-v6.5.js');
const oldAnalyze=T.analyzeTerminal,normalize=root.OBOL_INTAKE_V25&&root.OBOL_INTAKE_V25.normalizeText||((x)=>String(x||''));
function norm(s){return String(s||'').toLowerCase().replace(/\s+/g,' ').trim();}
function cardMap(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function intent65(command){const c=norm(command);if(/certipy\s+find\b|certify(?:\.exe)?\s+find\b|certutil\s+-v\s+-dstemplate\b|ldeep\s+ldap\b.*\btemplates\b/.test(c))return'adcs-enumeration55';if(/certipy\s+relay\b|ntlmrelayx(?:\.py)?\b.*--adcs\b/.test(c))return'adcs-esc8-65';if(/certipy\s+req\b.*(?:-on-behalf-of\b|--on-behalf-of\b)|certify(?:\.exe)?\s+request\b.*\/onbehalfof:/.test(c))return'adcs-esc3-65';if(/certipy\s+req\b.*(?:-upn\b|--upn\b)|certify(?:\.exe)?\s+request\b.*\/altname:/.test(c))return'adcs-esc1-65';if(/certipy\s+req\b|certify(?:\.exe)?\s+request\b/.test(c))return'adcs-esc2-65';return'';}
function proof65(cardId,command,output){const t=String(output||''),facts=[];let success=false,why='';const add=x=>{if(!facts.includes(x))facts.push(x);};if(cardId==='adcs-enumeration55'){if(/certificate authorities|certificate templates|enterprise ca|enabled certificate templates/i.test(t)){success=true;why='AD CS enumeration explicitly returned CA/template inventory.';add('adcs.enumerated');}if(/web enrollment[^\n]*(?:enabled|http|https)|http web enrollment/i.test(t)){success=true;why='AD CS enumeration explicitly identified Web Enrollment.';add('adcs.web_enrollment');}if(/\besc1\b/i.test(t)){success=true;add('adcs.esc1');}if(/\besc2\b/i.test(t)){success=true;add('adcs.esc2');}if(/\besc3\b/i.test(t)){success=true;add('adcs.esc3');}}else if(['adcs-esc8-65','adcs-esc1-65','adcs-esc2-65','adcs-esc3-65'].includes(cardId)){if(/got certificate|saved certificate|certificate and private key|saved.*\.pfx|writing.*\.pfx/i.test(t)){success=true;why='The reviewed AD CS request explicitly returned certificate material.';add('credential.certificate');}}
return{success,facts,why:why||(success?'Explicit v6.5 AD CS proof boundary met.':'Recognized v6.5 AD CS command; explicit proof boundary was not present.')};}
function commandFromLine(line){const s=String(line||''),patterns=[/certipy\s+(?:find|relay|req)\b/i,/certify(?:\.exe)?\s+(?:find|request)\b/i,/ntlmrelayx(?:\.py)?\b/i,/certutil\s+-v\s+-dstemplate\b/i,/ldeep\s+ldap\b/i];let best=null;for(const re of patterns){const m=re.exec(s);if(m&&(!best||m.index<best.index))best=m;}return best?s.slice(Math.max(0,best.index-48)).trim():'';}
function segments65(text){const lines=String(text||'').split(/\r?\n/),out=[];let cur=null;const push=()=>{if(!cur)return;cur.output=cur.output.join('\n').trim();out.push(cur);cur=null;};for(const line of lines){const cmd=commandFromLine(line);if(cmd&&intent65(cmd)){push();cur={command:cmd,rawPrompt:line,output:[]};continue;}if(cur)cur.output.push(line);}push();return out;}
function proposal65(seg,lanes){const id=intent65(seg&&seg.command),card=id&&cardMap(lanes)[id];if(!card)return null;const evidence=String(seg&&seg.output||'').trim(),p=proof65(id,seg.command,evidence);return{cardId:id,title:card.title,command:String(seg.command||''),evidence:evidence.slice(0,3000),outputSnippet:evidence.slice(0,1200),result:p.success?'success':'tried',assessment:p.success?'supported':'attempted',confidence:p.success?'high':'medium',reason:p.why,outcomeFacts:p.success?p.facts:[],fingerprint:'terminal:'+(C.simpleHash?C.simpleHash(id+'|'+norm(seg.command)+'|'+evidence.slice(0,800)):id),credentialId:'',service:''};}
function repair65(a,lanes){if(!a)return a;const inferred=intent65(a.command),id=inferred||a.cardId;if(!(M.cardIds||[]).includes(id))return a;const card=cardMap(lanes)[id];if(!card)return a;const p=proof65(id,a.command,a.evidence||a.outputSnippet||'');a.cardId=id;a.title=card.title;if(p.success){a.result='success';a.assessment='supported';a.confidence='high';a.reason=p.why;a.outcomeFacts=[...new Set(p.facts||[])];}else{a.result=a.result==='failed'?'failed':'tried';a.assessment=a.assessment==='refuted'?'refuted':'attempted';a.confidence='medium';a.reason=p.why;a.outcomeFacts=[];}a.outcomeFacts=(a.outcomeFacts||[]).filter(x=>!['access.admin','access.system','credential.plaintext','foothold.windows','foothold.linux'].includes(x));a.fingerprint='terminal:'+(C.simpleHash?C.simpleHash(id+'|'+norm(a.command)+'|'+String(a.evidence||a.outputSnippet||'').slice(0,800)):id);return a;}
T.analyzeTerminal=function(text,lanes,state,ctx){const clean=normalize(text),r=oldAnalyze(clean,lanes,state,ctx);r.activities=Array.isArray(r.activities)?r.activities:[];const merged=[],seen=new Set();for(const seg of [...(r.segments||[]),...segments65(clean)]){const k=norm(seg&&seg.command);if(!k||seen.has(k))continue;seen.add(k);merged.push(seg);}r.segments=merged;const existing=new Set(r.activities.map(a=>norm(a.command)));for(const seg of merged){const k=norm(seg.command);if(existing.has(k))continue;const p=proposal65(seg,lanes);if(p){r.activities.push(p);existing.add(k);}}r.activities=r.activities.map(a=>repair65(a,lanes));const uniq=new Set();r.activities=r.activities.filter(a=>{const k=a.fingerprint||[a.cardId,norm(a.command),String(a.evidence||'').slice(0,300)].join('|');if(uniq.has(k))return false;uniq.add(k);return true;});r.atomicAdcs65=[...new Set(r.activities.filter(a=>(M.cardIds||[]).includes(a.cardId)).map(a=>a.cardId))];return r;};
root.OBOL_INTAKE_V65={version:'6.5.0',intent65,proof65,segments65,proposal65,repairActivity65:repair65};
})(typeof window!=='undefined'?window:globalThis);
