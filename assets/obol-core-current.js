/*
 * Generated by tools/sync-core-current.js from data/runtime-manifest.js.
 * Do not edit manually.
 *
 * Ownership area: core (startup) - Core state and derivation.
 * Browser-local state, migrations, proof boundaries, applicability, ranking, progress, and report readiness.
 *
 * This file is the single current runtime owner for 69 historical
 * core fragments. It replays their surviving release deltas as isolated lexical
 * blocks rather than as exact historical-file concatenation, preserving the
 * migration and ranking closures while removing the versioned fragment chain
 * from current browser and Node execution.
 *
 * Historical fragment order sha256: a76a0ee9d6c30932e17454924d6c6b487a21554076b6f4c2e04e06c6142c3e7c
 * Generated body sha256: 6e64f7af93465cb130bd1731a4c6f145102291cff8b3b449cded56356f41e076
 * First fragment: assets/core-v2-base.js
 * Last fragment:  assets/core-v8.8.js
 */

(function(root){
'use strict';
/* obol-core-base-scope: assets/core-v2-base.js + assets/core-v2.js */
{
// Obol v2 core — shared classic-script lexical scope.
const root = typeof window!=='undefined'?window:globalThis;
'use strict';

const VERSION = '2.0.0';
const SCHEMA_VERSION = 2;

const FACT_LABELS = {
  'scope.defined':'Scope defined','host.alive':'Host alive','scan.initial':'Initial scan complete','scan.full':'Full scan complete',
  'ad.domain_known':'Domain known','ad.dc_candidate':'Domain controller candidate','ad.user_list':'Validated user list','ad.graph.collected':'BloodHound graph collected','ad.attack_paths':'AD attack paths available',
  'credential.available':'Validated credential available','credential.candidate':'Credential candidate available','credential.ntlm_hash':'NTLM hash available',
  'foothold.linux':'Linux foothold','foothold.windows':'Windows foothold','foothold.webshell':'Web shell foothold',
  'access.admin':'Local administrator access','access.root':'Root access','access.system':'SYSTEM access',
  'smb.signing_required':'SMB signing required','smb.signing_disabled':'SMB signing not required','kerberos.clock_skew':'Kerberos clock skew detected',
  'os.windows':'Windows target','os.linux':'Linux target','nfs.reachable':'NFS reachable','nfs.exports':'NFS exports confirmed'
};

const HOST_PREFIXES = ['port:','svc.','smb.','ldap.','kerberos.','web.','ssh.','winrm.','ftp.','mssql.','mysql.','postgresql.','rdp.','nfs.','ike.','finger.','k8s.','docker.','redis.','elastic.','oracle.','vnc.','smtp.','os.','foothold.','access.','shell.','exploit.'];
const DOMAIN_PREFIXES = ['ad.','dns.','hash.asrep','hash.tgs'];
const GLOBAL_FACTS = new Set(['scope.defined']);

function now(){ return new Date().toISOString(); }
function uid(prefix){ return (prefix || 'id') + '-' + Date.now().toString(36) + '-' + Math.random().toString(36).slice(2,8); }
function clean(s){ return String(s == null ? '' : s).trim(); }
function slug(s){ return clean(s).toLowerCase().replace(/[^a-z0-9]+/g,'-').replace(/^-|-$/g,'').slice(0,72) || 'item'; }
function simpleHash(s){
  s = String(s || ''); let h = 2166136261;
  for (let i=0;i<s.length;i++){ h ^= s.charCodeAt(i); h = Math.imul(h, 16777619); }
  return (h >>> 0).toString(36);
}
function commandId(cmd, index){ return cmd.id || ('cmd-' + slug(cmd.tool || 'tool') + '-' + simpleHash(cmd.run || index)); }
function optionId(opt, index){
  if (opt.id) return opt.id;
  if (opt.flag) return 'flag:' + opt.flag;
  if (opt.script) return 'script:' + opt.script;
  if (opt.radio) return 'radio:' + opt.radio + ':' + opt.value;
  if (opt.arg) return 'arg:' + opt.arg;
  return 'opt:' + index;
}
function labelFact(f){
  if (FACT_LABELS[f]) return FACT_LABELS[f];
  if (f.startsWith('port:')) return 'Port ' + f.slice(5) + ' reachable';
  return f.replace(/[._:]+/g,' ').replace(/\b\w/g,m=>m.toUpperCase());
}

function newState(){
  return {
    schemaVersion: SCHEMA_VERSION,
    obolVersion: VERSION,
    createdAt: now(), updatedAt: now(),
    params: {}, hosts: [], domains: [],
    activeContext: { type:'global', id:'global' },
    facts: [{ id:'scope.defined', scope:'global', subject:'global', source:'system', evidence:'Workspace initialized', confidence:'high', observedAt:now() }],
    artifacts: { users:[], hashes:[], creds:[] },
    identities: [], credentials: [], activities: [], drafts: {},
    ui: { variants:{}, opts:{}, bannerDismissed:false, reportMode:'standard', pathShowAll:false, lastEvidenceUpdate:null, lastCopied:{} },
    bh: null, timerEnd:null, timerStart:null
  };
}

function normalizeHost(h){
  const ip = clean(h.ip); const id = h.id || (ip ? 'host:' + ip : uid('host'));
  return { id, name:clean(h.name), ip, hostname:clean(h.hostname), domain:clean(h.domain).toLowerCase(), os:clean(h.os), notes:clean(h.notes), pwned:clean(h.pwned), ports:Array.isArray(h.ports)?h.ports:[], flags:Array.isArray(h.flags)?h.flags:[], creds:Array.isArray(h.creds)?h.creds:[] };
}
function normalizeDomain(d){
  const name = clean(d.name || d.domain).toLowerCase();
  return { id:d.id || ('domain:' + (name || uid('domain'))), name, base_dn:clean(d.base_dn), netbios:clean(d.netbios) };
}
function normalizeContext(state, ctx){
  if (!ctx) ctx = state.activeContext;
  if (!ctx || !ctx.type) return {type:'global',id:'global'};
  if (ctx.type === 'host' && state.hosts.some(h=>h.id===ctx.id)) return {type:'host',id:ctx.id};
  if (ctx.type === 'domain' && state.domains.some(d=>d.id===ctx.id)) return {type:'domain',id:ctx.id};
  return {type:'global',id:'global'};
}
function contextKey(ctx){ ctx = ctx || {type:'global',id:'global'}; return ctx.type + ':' + ctx.id; }
function contextLabel(state, ctx){
  ctx = normalizeContext(state, ctx);
  if (ctx.type === 'host'){
    const h = state.hosts.find(x=>x.id===ctx.id); return h ? (h.name || h.hostname || h.ip || h.id) : ctx.id;
  }
  if (ctx.type === 'domain'){
    const d = state.domains.find(x=>x.id===ctx.id); return d ? d.name : ctx.id;
  }
  return 'Engagement';
}
function hostForContext(state, ctx){ ctx=normalizeContext(state,ctx); return ctx.type==='host' ? state.hosts.find(h=>h.id===ctx.id) || null : null; }
function domainForContext(state, ctx){
  ctx=normalizeContext(state,ctx);
  if (ctx.type==='domain') return state.domains.find(d=>d.id===ctx.id) || null;
  if (ctx.type==='host'){
    const h=hostForContext(state,ctx); if (!h || !h.domain) return null;
    return state.domains.find(d=>d.name===h.domain) || null;
  }
  return null;
}
function ensureDomain(state, name, extras){
  name = clean(name).toLowerCase(); if (!name) return null;
  let d = state.domains.find(x=>x.name===name);
  if (!d){ d = normalizeDomain({name}); state.domains.push(d); }
  if (extras){ if (extras.base_dn && !d.base_dn) d.base_dn=extras.base_dn; if (extras.netbios && !d.netbios) d.netbios=extras.netbios; }
  return d;
}
function mergeHost(state, incoming){
  const n=normalizeHost(incoming); let h = state.hosts.find(x => (n.ip && x.ip===n.ip) || x.id===n.id);
  if (!h){ state.hosts.push(n); h=n; }
  else {
    for (const k of ['name','hostname','domain','os','notes','pwned']) if (n[k]) h[k]=n[k];
    if (n.ports && n.ports.length) h.ports=n.ports;
    if (n.flags && n.flags.length) h.flags=n.flags;
    if (n.creds && n.creds.length) h.creds=n.creds;
  }
  if (h.domain) ensureDomain(state,h.domain);
  return h;
}

function factScope(state, factId, ctx){
  ctx = normalizeContext(state,ctx);
  if (GLOBAL_FACTS.has(factId)) return {scope:'global',subject:'global'};
  if (HOST_PREFIXES.some(p=>factId.startsWith(p)) || /^port:\d+$/.test(factId)){
    if (ctx.type==='host') return {scope:'host',subject:ctx.id};
    return {scope:'global',subject:'global'};
  }
  if (DOMAIN_PREFIXES.some(p=>factId.startsWith(p))){
    const d = domainForContext(state,ctx);
    if (d) return {scope:'domain',subject:d.id};
    if (ctx.type==='domain') return {scope:'domain',subject:ctx.id};
    return {scope:'global',subject:'global'};
  }
  if (factId.startsWith('credential.')){
    const d = domainForContext(state,ctx);
    return d ? {scope:'domain',subject:d.id} : {scope:'global',subject:'global'};
  }
  return ctx.type==='host' ? {scope:'host',subject:ctx.id} : ctx.type==='domain' ? {scope:'domain',subject:ctx.id} : {scope:'global',subject:'global'};
}
function factVisibleInContext(state, rec, ctx){
  ctx=normalizeContext(state,ctx);
  if (rec.scope==='global' || rec.subject==='global') return true;
  if (ctx.type==='host'){
    if (rec.scope==='host' && rec.subject===ctx.id) return true;
    const d=domainForContext(state,ctx); if (d && rec.scope==='domain' && rec.subject===d.id) return true;
  }
  if (ctx.type==='domain' && rec.scope==='domain' && rec.subject===ctx.id) return true;
  return false;
}
function effectiveFactRecords(state, ctx){ return state.facts.filter(r=>factVisibleInContext(state,r,ctx)); }
function effectiveFacts(state, ctx){ return new Set(effectiveFactRecords(state,ctx).map(r=>r.id)); }
function hasFact(state,id,ctx){ return effectiveFacts(state,ctx).has(id); }
function addFact(state, id, opts){
  opts=opts||{}; id=clean(id); if (!id) return {added:false,record:null};
  const ctx=normalizeContext(state,opts.context);
  const sc=opts.scope && opts.subject ? {scope:opts.scope,subject:opts.subject} : factScope(state,id,ctx);
  const existing=state.facts.find(r=>r.id===id && r.scope===sc.scope && r.subject===sc.subject);
  if (existing){
    if (opts.evidence && !existing.evidence) existing.evidence=clean(opts.evidence).slice(0,500);
    if (opts.confidence) existing.confidence=opts.confidence;
    return {added:false,record:existing};
  }
  const rec={ id, scope:sc.scope, subject:sc.subject, source:opts.source||'manual', evidence:clean(opts.evidence).slice(0,500), confidence:opts.confidence||'high', observedAt:opts.observedAt||now() };
  state.facts.push(rec); return {added:true,record:rec};
}
function removeFact(state,id,ctx){
  ctx=normalizeContext(state,ctx);
  const before=state.facts.length;
  state.facts=state.facts.filter(r=>!(r.id===id && factVisibleInContext(state,r,ctx) && r.id!=='scope.defined'));
  return state.facts.length!==before;
}
function factReasons(state,id,ctx){ return effectiveFactRecords(state,ctx).filter(r=>r.id===id); }
function applicable(card, factSet){
  factSet=factSet||new Set(); const p=card.prereq||{};
  const all=p.all||[], any=p.any||[], none=(p.none||[]).concat(card.blocks||[]);
  if (!all.every(f=>factSet.has(f))) return false;
  if (any.length && !any.some(f=>factSet.has(f))) return false;
  if (none.some(f=>factSet.has(f))) return false;
  return true;
}
function grounded(card, factSet){
  const p=card.prereq||{}; const need=(p.all||[]).concat(p.any||[]);
  return need.some(f=>f!=='scope.defined' && factSet.has(f));
}
function latestActivity(state,cardId,ctx){
  const key=contextKey(normalizeContext(state,ctx));
  for (let i=state.activities.length-1;i>=0;i--){ const a=state.activities[i]; if (a.cardId===cardId && a.contextKey===key) return a; }
  return null;
}
function statusFor(state,cardId,ctx){ const a=latestActivity(state,cardId,ctx); return a ? (a.result==='success'?'done':a.result==='tried'?'tried':'new') : 'new'; }
function phaseLevel(facts){
  if (facts.has('access.root')||facts.has('access.system')||facts.has('access.admin')) return 5;
  if (facts.has('foothold.linux')||facts.has('foothold.windows')||facts.has('foothold.webshell')) return 4;
  if (facts.has('credential.available')||facts.has('credential.ntlm_hash')) return 3;
  if (facts.has('ad.user_list')||facts.has('ad.domain_known')) return 2;
  if (facts.has('scan.initial')) return 1;
  return 0;
}
function cardPhaseWeight(card){
  const lane=(card.lane||'').toLowerCase();
  if (/recon/.test(lane)) return 1;
  if (/web|poison|credential|crack/.test(lane)) return 2;
  if (/ad|active/.test(lane)) return 3;
  if (/privesc/.test(lane)) return 4;
  if (/pivot|lateral/.test(lane)) return 5;
  return 3;
}
function whyNow(card,facts,meta){
  const p=card.prereq||{}; const matched=(p.all||[]).concat((p.any||[]).filter(f=>facts.has(f))).filter(f=>f!=='scope.defined'&&facts.has(f));
  const bits=[];
  if (meta&&meta.newly) bits.push('newly unlocked by your latest evidence');
  if (matched.length) bits.push('matches ' + matched.slice(0,3).map(labelFact).join(', '));
  if (!(p.all||[]).some(f=>f.startsWith('credential.')) && !(p.any||[]).some(f=>f.startsWith('credential.'))) bits.push('does not require a credential prerequisite');
  return bits.length ? bits.join('; ') : 'applicable from the current evidence state';
}
function rankCard(state,card,ctx,meta){
  const facts=effectiveFacts(state,ctx); const p=card.prereq||{}; let score=0;
  if (meta&&meta.newly) score += 120;
  const matchedAll=(p.all||[]).filter(f=>facts.has(f)&&f!=='scope.defined').length;
  const matchedAny=(p.any||[]).filter(f=>facts.has(f)&&f!=='scope.defined').length;
  score += matchedAll*20 + matchedAny*14;
  const serviceMatch=(p.all||[]).concat(p.any||[]).some(f=>/^(port:|smb\.|ldap\.|kerberos\.|web\.|ssh\.|winrm\.|ftp\.|mssql\.|mysql\.|postgresql\.|rdp\.|nfs\.)/.test(f)&&facts.has(f));
  if (serviceMatch) score += 20;
  const level=phaseLevel(facts), cp=cardPhaseWeight(card); score += Math.max(0,18-Math.abs(level-cp)*6);
  const st=statusFor(state,card.id,ctx); if (st==='tried') score -= 25; if (st==='done') score -= 200;
  const sev=((card.report||{}).severity||'').toLowerCase(); score += ({critical:5,high:4,medium:3,low:2,informational:1,info:1}[sev]||0);
  return score;
}
function rankedApplicable(state,lanes,ctx,opts){
  opts=opts||{}; const facts=effectiveFacts(state,ctx); const newest=new Set(((state.ui.lastEvidenceUpdate||{}).newly)||[]);
  let cards=lanes.flatMap(l=>l.cards).filter(c=>applicable(c,facts)&&statusFor(state,c.id,ctx)!=='done');
  if (!opts.showAll && effectiveFactRecords(state,ctx).some(r=>r.id!=='scope.defined')) cards=cards.filter(c=>grounded(c,facts));
  return cards.map(c=>({card:c,score:rankCard(state,c,ctx,{newly:newest.has(c.id)}),why:whyNow(c,facts,{newly:newest.has(c.id)}),newly:newest.has(c.id)})).sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
}
function snapshotApplicable(state,lanes,ctx){ return new Set(rankedApplicable(state,lanes,ctx,{showAll:true}).map(x=>x.card.id)); }

function normalizeArtifact(kind,v){ return clean(v); }
function addArtifact(state,kind,v){
  if (!state.artifacts[kind]) state.artifacts[kind]=[]; v=normalizeArtifact(kind,v); if (!v) return false;
  if (!state.artifacts[kind].includes(v)){ state.artifacts[kind].push(v); return true; }
  return false;
}
function ensureIdentity(state,name,opts){
  opts=opts||{}; name=clean(name).replace(/^.*\\/,''); if (!name) return null;
  const domain=clean(opts.domain).toLowerCase(); let i=state.identities.find(x=>x.name.toLowerCase()===name.toLowerCase() && (x.domain||'')===domain);
  if (!i){ i={id:uid('identity'),name,domain,source:opts.source||'intake',firstSeen:now()}; state.identities.push(i); }
  return i;
}
function detectSecretType(secret){ secret=clean(secret); if (/^[0-9a-f]{32}$/i.test(secret)) return 'ntlm'; if (/\$krb5/i.test(secret)) return 'kerberos'; if (/^[A-Za-z0-9+/=]{40,}$/.test(secret)) return 'token'; return 'password'; }
function addCredential(state,cred,opts){
  opts=opts||{}; const username=clean(cred.username||cred.user); const secret=clean(cred.secret); if (!username||!secret) return null;
  const domain=clean(cred.domain||opts.domain).toLowerCase(); const type=cred.secretType||detectSecretType(secret);
  let c=state.credentials.find(x=>x.username.toLowerCase()===username.toLowerCase()&&(x.domain||'')===domain&&x.secret===secret&&x.secretType===type);
  if (!c){
    c={ id:uid('cred'), username, domain, secretType:type, secret, source:cred.source||opts.source||'intake', sourceContext:opts.context?contextKey(opts.context):'global:global', obtainedFrom:clean(cred.obtainedFrom), validatedAgainst:Array.isArray(cred.validatedAgainst)?cred.validatedAgainst:[], privilege:clean(cred.privilege), firstSeen:now(), confidence:cred.confidence||'medium' };
    state.credentials.push(c); ensureIdentity(state,username,{domain,source:c.source});
  }
  return c;
}

function applyEvidenceUpdate(state,lanes,update){
  update=update||{}; const ctx=normalizeContext(state,update.context||state.activeContext); const before=snapshotApplicable(state,lanes,ctx);
  const gained=[], addedHosts=[], addedArtifacts={users:0,hashes:0,creds:0};
  for (const h of update.hosts||[]){ const mh=mergeHost(state,h); addedHosts.push(mh.id); }
  if (update.domain){ const d=ensureDomain(state,update.domain,{base_dn:update.base_dn,netbios:update.netbios}); if (d && ctx.type==='host'){ const h=hostForContext(state,ctx); if (h&&!h.domain) h.domain=d.name; } }
  for (const [k,v] of Object.entries(update.params||{})) if (v && !state.params[k]) state.params[k]=v;
  for (const f of update.facts||[]){
    const spec=typeof f==='string'?{id:f}:f; const r=addFact(state,spec.id,{context:spec.context||ctx,scope:spec.scope,subject:spec.subject,source:spec.source||update.source||'intake',evidence:spec.evidence||update.evidence||'',confidence:spec.confidence||update.confidence||'high'}); if (r.added) gained.push(r.record.id);
  }
  const arts=update.artifacts||{};
  for (const k of ['users','hashes','creds']) for (const v of arts[k]||[]) if (addArtifact(state,k,v)) addedArtifacts[k]++;
  const dom=(domainForContext(state,ctx)||{}).name||state.params.domain||'';
  for (const u of arts.users||[]) ensureIdentity(state,u,{domain:dom,source:update.source});
  for (const raw of arts.creds||[]){
    let m=String(raw).match(/^([^:\s]+):(.+)$/); if (m) addCredential(state,{username:m[1],secret:m[2],source:update.source},{domain:dom,context:ctx});
    else { m=String(raw).match(/^([^\s]+)\s+\((?:NT:\s*)?([0-9a-f]{32})\)$/i); if (m) addCredential(state,{username:m[1],secret:m[2],secretType:'ntlm',source:update.source},{domain:dom,context:ctx}); }
  }
  const after=snapshotApplicable(state,lanes,ctx); const newly=[...after].filter(id=>!before.has(id));
  state.ui.lastEvidenceUpdate={ at:now(), source:update.source||'intake', contextKey:contextKey(ctx), facts:[...new Set(gained)], newly, artifactCounts:addedArtifacts, hosts:addedHosts };
  state.updatedAt=now(); return state.ui.lastEvidenceUpdate;
}

function recordActivity(state,activity){
  const ctx=normalizeContext(state,activity.context||state.activeContext); const a={
    id:uid('activity'), cardId:activity.cardId, contextKey:contextKey(ctx), context:{...ctx}, contextLabel:contextLabel(state,ctx),
    result:activity.result||'tried', outcomeFacts:Array.isArray(activity.outcomeFacts)?activity.outcomeFacts:[], command:clean(activity.command), evidence:clean(activity.evidence), at:activity.at||now(), source:activity.source||'manual'
  };
  state.activities.push(a);
  if (a.result==='success') for (const f of a.outcomeFacts) addFact(state,f,{context:ctx,source:'card:'+a.cardId,evidence:a.evidence,confidence:'high',observedAt:a.at});
  state.updatedAt=now(); return a;
}
function resetCard(state,cardId,ctx){ const key=contextKey(normalizeContext(state,ctx)); state.activities=state.activities.filter(a=>!(a.cardId===cardId&&a.contextKey===key)); }

function migrateV1(v1){
  const s=newState(); if (!v1||typeof v1!=='object') return s;
  s.params={...(v1.params||{})};
  for (const b of v1.boxes||[]) mergeHost(s,b);
  if (s.hosts.length) s.activeContext={type:'host',id:s.hosts[0].id};
  const ctx=s.activeContext;
  for (const f of v1.facts||[]) addFact(s,f,{context:ctx,source:'migration:v1',evidence:'Imported from obol-state-v1',confidence:'medium'});
  s.artifacts={users:[...((v1.artifacts||{}).users||[])],hashes:[...((v1.artifacts||{}).hashes||[])],creds:[...((v1.artifacts||{}).creds||[])]};
  const dom=s.params.domain||((s.hosts.find(h=>h.domain)||{}).domain)||''; if (dom) ensureDomain(s,dom,{base_dn:s.params.base_dn,netbios:s.params.dc_netbios});
  for (const u of s.artifacts.users) ensureIdentity(s,u,{domain:dom,source:'migration:v1'});
  for (const c of s.artifacts.creds){ const m=String(c).match(/^([^:\s]+):(.+)$/); if (m) addCredential(s,{username:m[1],secret:m[2],source:'migration:v1'},{domain:dom,context:ctx}); }
  for (const [cardId,p] of Object.entries(v1.progress||{})){
    if (!p||(!p.status&&!p.evidence)) continue;
    recordActivity(s,{cardId,context:ctx,result:p.status==='done'?'success':'tried',outcomeFacts:[],evidence:p.evidence||'',at:p.at||now(),source:'migration:v1'});
  }
  s.ui={...s.ui,...(v1.ui||{}),variants:{...((v1.ui||{}).variants||{})},opts:{},lastEvidenceUpdate:(v1.ui||{}).lastIntake||null};
  s.bh=v1.bh||null; s.timerEnd=v1.timerEnd||null; s.timerStart=v1.timerStart||null;
  s.migratedFrom='obol-state-v1'; return s;
}
function coerceState(raw){
  if (!raw||typeof raw!=='object') return newState();
  if (raw.schemaVersion===SCHEMA_VERSION && Array.isArray(raw.hosts) && Array.isArray(raw.facts)){
    const s={...newState(),...raw}; s.ui={...newState().ui,...(raw.ui||{})}; s.artifacts={...newState().artifacts,...(raw.artifacts||{})};
    s.hosts=(raw.hosts||[]).map(normalizeHost); s.domains=(raw.domains||[]).map(normalizeDomain); s.activeContext=normalizeContext(s,raw.activeContext); return s;
  }
  if (raw.boxes||Array.isArray(raw.facts)) return migrateV1(raw);
  throw new Error('Unsupported Obol workspace schema');
}
function sanitizedCopy(state){
  const s=JSON.parse(JSON.stringify(state));
  s.credentials=(s.credentials||[]).map(c=>({...c,secret:'[REDACTED]'}));
  s.artifacts.creds=(s.artifacts.creds||[]).map(()=> '[REDACTED]');
  s.artifacts.hashes=(s.artifacts.hashes||[]).map(()=> '[REDACTED HASH]');
  for (const h of s.hosts||[]) h.creds=(h.creds||[]).map(c=>({...c,secret:'[REDACTED]'}));
  return s;
}

root.OBOL_CORE_V2={ VERSION,SCHEMA_VERSION,newState,migrateV1,coerceState,sanitizedCopy,uid,slug,simpleHash,commandId,optionId,labelFact,contextKey,contextLabel,normalizeContext,hostForContext,domainForContext,ensureDomain,mergeHost,factScope,effectiveFactRecords,effectiveFacts,hasFact,addFact,removeFact,factReasons,applicable,grounded,statusFor,latestActivity,whyNow,rankCard,rankedApplicable,snapshotApplicable,addArtifact,ensureIdentity,addCredential,applyEvidenceUpdate,recordActivity,resetCard,now };
Object.assign(root,{addArtifact:addArtifact,addCredential:addCredential,addFact:addFact,applicable:applicable,applyEvidenceUpdate:applyEvidenceUpdate,cardPhaseWeight:cardPhaseWeight,clean:clean,coerceState:coerceState,commandId:commandId,contextKey:contextKey,contextLabel:contextLabel,detectSecretType:detectSecretType,domainForContext:domainForContext,effectiveFactRecords:effectiveFactRecords,effectiveFacts:effectiveFacts,ensureDomain:ensureDomain,ensureIdentity:ensureIdentity,factReasons:factReasons,factScope:factScope,factVisibleInContext:factVisibleInContext,grounded:grounded,hasFact:hasFact,hostForContext:hostForContext,labelFact:labelFact,latestActivity:latestActivity,mergeHost:mergeHost,migrateV1:migrateV1,newState:newState,normalizeArtifact:normalizeArtifact,normalizeContext:normalizeContext,normalizeDomain:normalizeDomain,normalizeHost:normalizeHost,now:now,optionId:optionId,phaseLevel:phaseLevel,rankCard:rankCard,rankedApplicable:rankedApplicable,recordActivity:recordActivity,removeFact:removeFact,resetCard:resetCard,sanitizedCopy:sanitizedCopy,simpleHash:simpleHash,slug:slug,snapshotApplicable:snapshotApplicable,statusFor:statusFor,uid:uid,whyNow:whyNow});
}

/* obol-core-delta: assets/core-v2.1.js */
{
// Obol v2.1 intelligence layer — methodology graph, knowledge state, coverage, hypotheses and stuck analysis.
const C=root.OBOL_CORE_V2;
if(!C) throw new Error('Obol v2 core must load before core-v2.1.js');
const VERSION='2.1.0';
const oldNew=C.newState, oldCoerce=C.coerceState, oldMigrate=C.migrateV1, oldRecord=C.recordActivity, oldSanitize=C.sanitizedCopy;

function ensure21(s){
  s=s||{}; s.obolVersion=VERSION;
  s.knowledge=Array.isArray(s.knowledge)?s.knowledge:[];
  s.hypothesisNotes=s.hypothesisNotes&&typeof s.hypothesisNotes==='object'?s.hypothesisNotes:{};
  s.ui=s.ui||{}; s.ui.lastIntakeActivities=Array.isArray(s.ui.lastIntakeActivities)?s.ui.lastIntakeActivities:[];
  s.ui.coverageOpen=!!s.ui.coverageOpen;
  return s;
}
C.VERSION=VERSION;
C.newState=function(){ return ensure21(oldNew()); };
C.coerceState=function(raw){ return ensure21(oldCoerce(raw)); };
C.migrateV1=function(raw){ return ensure21(oldMigrate(raw)); };

function scopeFor(state,id,ctx){
  const c=C.normalizeContext(state,ctx||state.activeContext);
  try{return C.factScope(state,id,c);}catch(e){return c.type==='host'?{scope:'host',subject:c.id}:c.type==='domain'?{scope:'domain',subject:c.id}:{scope:'global',subject:'global'};}
}
function recordKnowledge(state,id,status,opts){
  opts=opts||{}; id=String(id||'').trim(); status=String(status||'unknown').toLowerCase();
  if(!id) return null;
  if(!['supported','refuted','inconclusive','unknown'].includes(status)) status='unknown';
  ensure21(state); const sc=opts.scope&&opts.subject?{scope:opts.scope,subject:opts.subject}:scopeFor(state,id,opts.context);
  const r={id,scope:sc.scope,subject:sc.subject,status,source:opts.source||'manual',evidence:String(opts.evidence||'').trim().slice(0,1000),confidence:opts.confidence||'medium',observedAt:opts.observedAt||C.now()};
  state.knowledge.push(r); state.updatedAt=C.now(); return r;
}
function knowledgeVisible(state,r,ctx){
  const c=C.normalizeContext(state,ctx||state.activeContext);
  if(r.scope==='global'||r.subject==='global')return true;
  if(c.type==='host'){
    if(r.scope==='host'&&r.subject===c.id)return true;
    const d=C.domainForContext(state,c); if(d&&r.scope==='domain'&&r.subject===d.id)return true;
  }
  return c.type==='domain'&&r.scope==='domain'&&r.subject===c.id;
}
function knowledgeFor(state,id,ctx){return (state.knowledge||[]).filter(r=>(!id||r.id===id)&&knowledgeVisible(state,r,ctx)).sort((a,b)=>String(a.observedAt).localeCompare(String(b.observedAt)));}
function knowledgeStatus(state,id,ctx){const rows=knowledgeFor(state,id,ctx);return rows.length?rows[rows.length-1].status:'unknown';}
C.recordKnowledge=recordKnowledge; C.knowledgeFor=knowledgeFor; C.knowledgeStatus=knowledgeStatus;

C.recordActivity=function(state,a){
  a=a||{}; ensure21(state);
  const rec=oldRecord(state,a);
  rec.assessment=a.assessment||((a.result==='success')?'supported':'attempted');
  rec.inferred=!!a.inferred; rec.confidence=a.confidence|| (a.inferred?'medium':'high');
  rec.reason=String(a.reason||'').slice(0,500); rec.importFingerprint=a.importFingerprint||'';
  rec.outputSnippet=String(a.outputSnippet||a.evidence||'').slice(0,1500);
  if(rec.result==='success') for(const f of rec.outcomeFacts||[]) recordKnowledge(state,f,'supported',{context:rec.context,source:'activity:'+rec.cardId,evidence:rec.evidence,confidence:rec.confidence,observedAt:rec.at});
  for(const f of a.refutedFacts||[]) recordKnowledge(state,f,'refuted',{context:rec.context,source:'activity:'+rec.cardId,evidence:rec.evidence,confidence:rec.confidence,observedAt:rec.at});
  return rec;
};
function activityExists(state,fingerprint,ctx){
  if(!fingerprint)return false; const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));
  return (state.activities||[]).some(a=>a.contextKey===key&&a.importFingerprint===fingerprint);
}
C.activityExists=activityExists;

function allCards(lanes){return (lanes||[]).flatMap(l=>(l.cards||[]).map(c=>({card:c,lane:l})));}
function nonScopePrereqs(card){const p=card.prereq||{};return (p.all||[]).concat(p.any||[]).filter(f=>f!=='scope.defined');}
function graph(lanes){
  const rows=allCards(lanes), consumers={};
  for(const {card} of rows) for(const f of nonScopePrereqs(card)){(consumers[f]=consumers[f]||[]).push(card.id);}
  const nodes={}; for(const {card,lane} of rows)nodes[card.id]={id:card.id,title:card.title,lane:card.lane||lane.lane,produces:[...(card.produces||[])],requires:nonScopePrereqs(card),unlocks:[...new Set((card.produces||[]).flatMap(f=>consumers[f]||[]))]};
  return {nodes,consumers};
}
C.methodologyGraph=graph;
function unlockPotential(card,lanes,facts){
  facts=facts||new Set(); const before=new Set(allCards(lanes).filter(x=>C.applicable(x.card,facts)).map(x=>x.card.id)), sim=new Set(facts);
  for(const f of card.produces||[])sim.add(f);
  const unlocked=allCards(lanes).filter(x=>!before.has(x.card.id)&&C.applicable(x.card,sim)).map(x=>x.card.id);
  return {count:unlocked.length,cards:unlocked};
}
C.unlockPotential=unlockPotential;

function relevantCards(state,lanes,ctx){
  const fs=C.effectiveFacts(state,ctx), level=(fs.has('access.root')||fs.has('access.system')||fs.has('access.admin'))?5:(fs.has('foothold.linux')||fs.has('foothold.windows')||fs.has('foothold.webshell'))?4:(fs.has('credential.available')||fs.has('credential.ntlm_hash'))?3:(fs.has('ad.domain_known')||fs.has('ad.user_list'))?2:fs.has('scan.initial')?1:0;
  return allCards(lanes).filter(({card})=>{
    if(C.grounded(card,fs))return true;
    const req=nonScopePrereqs(card); if(!req.length)return level===0;
    if(level>=4&&/privesc/.test(card.lane||''))return true;
    return false;
  });
}
function coverageSummary(state,lanes,ctx){
  const rel=relevantCards(state,lanes,ctx), by={};
  for(const {card,lane} of rel){const k=card.lane||lane.lane;by[k]=by[k]||{lane:k,title:lane.title||k,relevant:0,tried:0,succeeded:0,remaining:0};const b=by[k];b.relevant++;const st=C.statusFor(state,card.id,ctx);if(st==='done')b.succeeded++;else if(st==='tried')b.tried++;else b.remaining++;}
  const lanesOut=Object.values(by).map(x=>({...x,coverage:x.relevant?Math.round(((x.tried+x.succeeded)/x.relevant)*100):100,successCoverage:x.relevant?Math.round((x.succeeded/x.relevant)*100):100})).sort((a,b)=>a.coverage-b.coverage||b.relevant-a.relevant);
  const total=lanesOut.reduce((a,x)=>a+x.relevant,0), touched=lanesOut.reduce((a,x)=>a+x.tried+x.succeeded,0), done=lanesOut.reduce((a,x)=>a+x.succeeded,0);
  return {total,touched,done,remaining:Math.max(0,total-touched),coverage:total?Math.round(touched/total*100):0,successCoverage:total?Math.round(done/total*100):0,lanes:lanesOut};
}
C.coverageSummary=coverageSummary;

function hypothesisRows(state,lanes,ctx){
  const fs=C.effectiveFacts(state,ctx), rows=[];
  for(const {card,lane} of relevantCards(state,lanes,ctx)){
    const latest=C.latestActivity(state,card.id,ctx), p=card.prereq||{}, supporting=(p.all||[]).concat(p.any||[]).filter(f=>fs.has(f)&&f!=='scope.defined'), contradicting=(p.none||[]).concat(card.blocks||[]).filter(f=>fs.has(f));
    let status='unresolved';
    if(latest&&latest.result==='success')status='confirmed'; else if(latest&&latest.assessment==='refuted')status='weakened'; else if(contradicting.length)status='blocked'; else if(C.applicable(card,fs))status=latest?'tested':'testable';
    rows.push({cardId:card.id,title:card.title,lane:card.lane||lane.lane,hypothesis:String(card.hypothesis||''),status,supporting,contradicting,latest});
  }
  return rows;
}
C.hypothesesForContext=hypothesisRows;

const oldRank=C.rankCard;
function laneGapBonus(state,lanes,ctx,card){const cov=coverageSummary(state,lanes,ctx).lanes.find(x=>x.lane===card.lane);return cov&&cov.remaining?Math.min(24,Math.round((100-cov.coverage)/5)):0;}
function rank21(state,card,lanes,ctx,meta){
  const fs=C.effectiveFacts(state,ctx); let score=oldRank(state,card,ctx,meta||{}); const unknown=(card.produces||[]).filter(f=>!fs.has(f)&&knowledgeStatus(state,f,ctx)!=='refuted');
  const up=unlockPotential(card,lanes,fs); score+=Math.min(42,up.count*7)+Math.min(30,unknown.length*10)+laneGapBonus(state,lanes,ctx,card);
  const acts=(state.activities||[]).filter(a=>a.cardId===card.id&&a.contextKey===C.contextKey(C.normalizeContext(state,ctx)));
  if(!acts.length)score+=12; else if(acts.some(a=>a.assessment==='refuted'))score-=18; if(acts.length>1&&!acts.some(a=>a.result==='success'))score-=Math.min(24,(acts.length-1)*8);
  return score;
}
function why21(state,card,lanes,ctx,meta){
  const fs=C.effectiveFacts(state,ctx), p=card.prereq||{}, matched=(p.all||[]).concat((p.any||[]).filter(f=>fs.has(f))).filter(f=>f!=='scope.defined'&&fs.has(f)), up=unlockPotential(card,lanes,fs), unknown=(card.produces||[]).filter(f=>!fs.has(f)); const bits=[];
  if(meta&&meta.newly)bits.push('newly unlocked by the latest evidence');
  if(matched.length)bits.push('supported by '+matched.slice(0,3).map(C.labelFact).join(', '));
  if(unknown.length)bits.push('tests '+unknown.slice(0,2).map(C.labelFact).join(', '));
  if(up.count)bits.push('could unlock '+up.count+' downstream '+(up.count===1?'technique':'techniques'));
  const st=C.statusFor(state,card.id,ctx); if(st==='tried')bits.push('previously tried, not yet confirmed');
  return bits.join('; ')||'fills an unresolved methodology gap in this context';
}
C.rankCard=function(state,card,ctx,meta){return rank21(state,card,root.OBOL_LANES||[],ctx,meta);};
C.whyNow=function(card,facts,meta){return (meta&&meta.reason)||'evidence-grounded methodology step';};
C.rankedApplicable=function(state,lanes,ctx,opts){
  opts=opts||{};const fs=C.effectiveFacts(state,ctx), newest=new Set(((state.ui.lastEvidenceUpdate||{}).newly)||[]);let cards=allCards(lanes).map(x=>x.card).filter(c=>C.applicable(c,fs)&&C.statusFor(state,c.id,ctx)!=='done');
  if(!opts.showAll&&C.effectiveFactRecords(state,ctx).some(r=>r.id!=='scope.defined'))cards=cards.filter(c=>C.grounded(c,fs));
  return cards.map(c=>{const newly=newest.has(c.id),score=rank21(state,c,lanes,ctx,{newly}),up=unlockPotential(c,lanes,fs);return {card:c,score,why:why21(state,c,lanes,ctx,{newly}),newly,unlocks:up.cards,infoGain:(c.produces||[]).filter(f=>!fs.has(f)).length};}).sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
};
C.snapshotApplicable=function(state,lanes,ctx){return new Set(C.rankedApplicable(state,lanes,ctx,{showAll:true}).map(x=>x.card.id));};

function serviceFacts(fs){
  const map=[['smb','smb.reachable'],['winrm','winrm.reachable'],['rdp','rdp.reachable'],['ssh','ssh.reachable'],['ldap','ldap.reachable'],['kerberos','kerberos.reachable'],['mssql','mssql.reachable'],['mysql','mysql.reachable'],['postgresql','postgresql.reachable'],['ftp','ftp.reachable'],['web','web.reachable']];
  return map.filter(x=>fs.has(x[1])).map(x=>x[0]);
}
function noteCredentialValidation(state,credId,service,result,opts){
  const c=(state.credentials||[]).find(x=>x.id===credId);if(!c)return null;c.validations=Array.isArray(c.validations)?c.validations:[];const r={service,result:result==='success'?'success':'failed',contextKey:C.contextKey(C.normalizeContext(state,(opts||{}).context||state.activeContext)),at:C.now(),source:(opts||{}).source||'activity',evidence:String((opts||{}).evidence||'').slice(0,300)};c.validations.push(r);if(r.result==='success'&&!c.validatedAgainst.includes(service))c.validatedAgainst.push(service);return r;
}
C.noteCredentialValidation=noteCredentialValidation;
function credentialCampaigns(state,lanes,ctx){
  const fs=C.effectiveFacts(state,ctx), services=serviceFacts(fs), key=C.contextKey(C.normalizeContext(state,ctx));
  return (state.credentials||[]).map(c=>{const vals=(c.validations||[]).filter(v=>v.contextKey===key), status={};for(const s of services){const v=vals.filter(x=>x.service===s).slice(-1)[0];status[s]=v?v.result:'untested';}return {credential:c,services,status,untested:services.filter(s=>status[s]==='untested'),successful:services.filter(s=>status[s]==='success'),failed:services.filter(s=>status[s]==='failed')};}).filter(x=>x.services.length);
}
C.credentialCampaigns=credentialCampaigns;

function contradictions(state,ctx){
  const grouped={};for(const r of knowledgeFor(state,null,ctx)){grouped[r.id]=grouped[r.id]||new Set();grouped[r.id].add(r.status);}return Object.entries(grouped).filter(([,s])=>s.has('supported')&&s.has('refuted')).map(([id])=>id);
}
function stuckAnalysis(state,lanes,ctx){
  const cov=coverageSummary(state,lanes,ctx), ranked=C.rankedApplicable(state,lanes,ctx,{showAll:false}), issues=[];
  for(const l of cov.lanes.filter(x=>x.remaining&&x.coverage<70).slice(0,4))issues.push({type:'coverage',priority:80-l.coverage,title:l.title+' is shallow',detail:l.remaining+' relevant technique'+(l.remaining===1?' remains':'s remain')+' untried ('+l.coverage+'% touched).'});
  const campaigns=credentialCampaigns(state,lanes,ctx);for(const x of campaigns.filter(x=>x.untested.length).slice(0,3))issues.push({type:'credential',priority:95,title:'Credential reuse is incomplete',detail:(x.credential.domain?x.credential.domain+'\\':'')+x.credential.username+' has not been tested against '+x.untested.join(', ')+'.'});
  const cs=contradictions(state,ctx);if(cs.length)issues.push({type:'contradiction',priority:100,title:'Evidence conflicts need resolution',detail:cs.slice(0,5).map(C.labelFact).join(', ')+' have both supporting and refuting observations.'});
  const acts=(state.activities||[]).filter(a=>a.contextKey===C.contextKey(C.normalizeContext(state,ctx)));const abandoned={};for(const a of acts)if(a.result!=='success')abandoned[a.cardId]=(abandoned[a.cardId]||0)+1;for(const [id,n] of Object.entries(abandoned).filter(x=>x[1]>=2).slice(0,3)){const card=allCards(lanes).map(x=>x.card).find(c=>c.id===id);if(card)issues.push({type:'repeat',priority:55,title:'Repeated attempt without new evidence: '+card.title,detail:n+' attempts are recorded. Re-check the underlying assumption or choose a different branch before repeating it.'});}
  if(ranked.length)issues.push({type:'next',priority:90,title:'Highest-information untried step',detail:ranked[0].card.title+' — '+ranked[0].why,cardId:ranked[0].card.id});
  const fs=C.effectiveFacts(state,ctx);if((fs.has('foothold.linux')||fs.has('foothold.windows'))&&!cov.lanes.some(l=>/privesc/.test(l.lane)&&l.touched))issues.push({type:'phase',priority:92,title:'Foothold gained but privilege-escalation baseline is untouched',detail:'Run the local enumeration baseline before spending time on exotic escalation paths.'});
  return {issues:issues.sort((a,b)=>b.priority-a.priority),coverage:cov,campaigns,contradictions:cs,ranked:ranked.slice(0,8),hypotheses:hypothesisRows(state,lanes,ctx).filter(h=>['testable','tested','weakened'].includes(h.status)).slice(0,12)};
}
C.stuckAnalysis=stuckAnalysis;

function redactText(state,text,includeSecrets){
  let out=String(text==null?'':text);if(includeSecrets)return out;
  const secrets=[];for(const c of state.credentials||[])if(c.secret)secrets.push(c.secret);for(const v of ((state.artifacts||{}).creds||[])){const i=String(v).indexOf(':');if(i>=0)secrets.push(String(v).slice(i+1));}for(const s of [...new Set(secrets)].filter(Boolean).sort((a,b)=>b.length-a.length)){out=out.split(s).join('[REDACTED]');}return out;
}
C.redactText=redactText;
C.sanitizedCopy=function(state){const s=oldSanitize(state);for(const a of s.activities||[]){a.command=redactText(state,a.command,false);a.evidence=redactText(state,a.evidence,false);a.outputSnippet=redactText(state,a.outputSnippet,false);}for(const k of Object.keys(s.hypothesisNotes||{}))s.hypothesisNotes[k]=redactText(state,s.hypothesisNotes[k],false);return s;};

root.OBOL_CORE_V21={VERSION,ensure21};
}

/* obol-core-delta: assets/core-v2.2.js */
{
// Obol v2.2 core overlay — tool resolution, maneuver coverage, service depth, transitions and report evidence state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.2.js');
const REG=()=>root.OBOL_TOOLS_V22||{tools:{},norm:x=>String(x||'').toLowerCase()};
const VERSION='2.2.0';
const oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldCoverage=C.coverageSummary;
function ensure22(s){
  s=s||{};s.obolVersion=VERSION;s.toolProfile=s.toolProfile&&typeof s.toolProfile==='object'?s.toolProfile:{};
  s.reportEvidence=s.reportEvidence&&typeof s.reportEvidence==='object'?s.reportEvidence:{};
  s.ui=s.ui||{};s.ui.commandAdvanced=s.ui.commandAdvanced&&typeof s.ui.commandAdvanced==='object'?s.ui.commandAdvanced:{};
  s.ui.toolAlternatives=s.ui.toolAlternatives&&typeof s.ui.toolAlternatives==='object'?s.ui.toolAlternatives:{};
  s.ui.workflowOpen=s.ui.workflowOpen&&typeof s.ui.workflowOpen==='object'?s.ui.workflowOpen:{};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure22(oldNew());};
C.coerceState=function(raw){return ensure22(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure22(oldMigrate(raw));};
C.ensure22=ensure22;

function toolId(name){return REG().norm?REG().norm(name):String(name||'').toLowerCase();}
function toolMeta(name){const id=toolId(name),t=(REG().tools||{})[id];return t?{id,...t}:{id,label:name||id,bin:name||id,kali:'unknown',preference:50,capabilities:[]};}
function toolState(state,name){
  ensure22(state);const m=toolMeta(name),p=state.toolProfile[m.id];if(p&&p.status)return{...m,status:p.status,explicit:true,updatedAt:p.updatedAt||'',note:p.note||''};
  const assumed=(m.kali==='expected'||m.kali==='common');return{...m,status:assumed?'assumed':'unknown',explicit:false};
}
function setToolState(state,name,status,note){ensure22(state);const m=toolMeta(name);if(!['available','missing','unknown','assumed'].includes(status))status='unknown';state.toolProfile[m.id]={status,note:String(note||''),updatedAt:C.now()};state.updatedAt=C.now();return toolState(state,m.id);}
function toolUsable(state,name){return !['missing'].includes(toolState(state,name).status);}
C.toolId=toolId;C.toolMeta=toolMeta;C.toolState=toolState;C.setToolState=setToolState;C.toolUsable=toolUsable;

function maneuverKey(card){return String(card&&card.maneuver||card&&card.maneuverId||card&&card.id||'unknown');}
function commandTool(cmd){return toolId((cmd||{}).tool||'');}
function implementationRank(state,cmd,index){const m=toolMeta(commandTool(cmd)),st=toolState(state,m.id);let score=Number(m.preference||50);if(m.id==='nxc')score+=15;if(cmd&&cmd.preferred)score+=20;if(index===0)score+=2;if(st.status==='available')score+=10;if(st.status==='missing')score-=1000;if(st.status==='unknown')score-=4;return score;}
function implementations(state,card){return (card&&card.commands||[]).map((cmd,index)=>({cmd,index,tool:toolMeta(commandTool(cmd)),availability:toolState(state,commandTool(cmd)),rank:implementationRank(state,cmd,index)})).sort((a,b)=>b.rank-a.rank||a.index-b.index);}
function preferredImplementation(state,card){const rows=implementations(state,card);return rows.find(x=>x.availability.status!=='missing')||rows[0]||null;}
C.maneuverKey=maneuverKey;C.commandTool=commandTool;C.implementations=implementations;C.preferredImplementation=preferredImplementation;

const SERVICE_RULES=[
 ['smb',/smb|port:445|port:139|rpcclient|enum4linux|smbclient|smbmap/],['ldap',/ldap|port:389|port:636|3268/],['kerberos',/kerb|port:88|asrep|tgs|roast/],
 ['web',/\bweb\b|http|https|ffuf|ferox|gobuster|whatweb|sqlmap|upload|lfi|xss|sqli/],['ssh',/ssh|port:22/],['ftp',/ftp|port:21/],['nfs',/nfs|port:2049/],
 ['mssql',/mssql|1433/],['mysql',/mysql|3306/],['postgresql',/postgres|5432/],['rdp',/rdp|3389/],['winrm',/winrm|5985|5986/],['snmp',/snmp|161/],
 ['linux-privesc',/linux-privesc|sudo|suid|capabilit|cron|linpeas/],['windows-privesc',/windows-privesc|seimpersonate|service|winpeas|alwaysinstallelevated/],['ad',/active directory|\bad\b|bloodhound|dcsync|acl|domain/],['pivot',/pivot|tunnel|chisel|ligolo|socks|proxychains/]
];
function serviceForCard(card){const text=[card&&card.lane,card&&card.title,card&&card.id,...(card&&card.commands||[]).flatMap(c=>[c.tool,c.run])].join(' ').toLowerCase();for(const [id,re] of SERVICE_RULES)if(re.test(text))return id;return card&&card.lane||'other';}
C.serviceForCard=serviceForCard;
function uniqueManeuvers(rows){const map=new Map();for(const x of rows){const k=maneuverKey(x.card||x);if(!map.has(k))map.set(k,x);}return[...map.values()];}
function serviceDepth(state,lanes,ctx){
  const facts=C.effectiveFacts(state,ctx),rows=[];for(const lane of lanes||[])for(const card of lane.cards||[]){if(!C.grounded(card,facts)&&!C.applicable(card,facts))continue;rows.push({card,lane,service:serviceForCard(card)});}
  const out={};for(const x of uniqueManeuvers(rows)){const s=x.service,b=out[s]||(out[s]={service:s,relevant:0,tried:0,succeeded:0,remaining:0,depth:0});b.relevant++;const st=C.statusFor(state,x.card.id,ctx);if(st==='done')b.succeeded++;else if(st==='tried')b.tried++;else b.remaining++;}
  for(const b of Object.values(out))b.depth=b.relevant?Math.round((b.tried+b.succeeded)/b.relevant*100):0;
  return Object.values(out).sort((a,b)=>a.depth-b.depth||b.relevant-a.relevant);
}
C.serviceDepth=serviceDepth;

if(oldCoverage){C.coverageSummary=function(state,lanes,ctx){
  const base=oldCoverage(state,lanes,ctx),facts=C.effectiveFacts(state,ctx),relevant=[];
  for(const lane of lanes||[])for(const card of lane.cards||[]){if(C.grounded(card,facts)||C.applicable(card,facts))relevant.push({card,lane});}
  const uniq=uniqueManeuvers(relevant),by={};for(const {card,lane} of uniq){const k=card.lane||lane.lane,b=by[k]||(by[k]={lane:k,title:lane.title||k,relevant:0,tried:0,succeeded:0,remaining:0});b.relevant++;const st=C.statusFor(state,card.id,ctx);if(st==='done')b.succeeded++;else if(st==='tried')b.tried++;else b.remaining++;}
  const lanesOut=Object.values(by).map(x=>({...x,coverage:x.relevant?Math.round((x.tried+x.succeeded)/x.relevant*100):100,successCoverage:x.relevant?Math.round(x.succeeded/x.relevant*100):100})).sort((a,b)=>a.coverage-b.coverage||b.relevant-a.relevant);
  const total=lanesOut.reduce((a,x)=>a+x.relevant,0),touched=lanesOut.reduce((a,x)=>a+x.tried+x.succeeded,0),done=lanesOut.reduce((a,x)=>a+x.succeeded,0);
  return{...base,total,touched,done,remaining:Math.max(0,total-touched),coverage:total?Math.round(touched/total*100):0,successCoverage:total?Math.round(done/total*100):0,lanes:lanesOut,services:serviceDepth(state,lanes,ctx)};
};}

const TRANSITION_FACTS={
 'credential.available':'credential','credential.ntlm_hash':'credential','credential.plaintext':'credential',
 'foothold.linux':'foothold','foothold.windows':'foothold','foothold.webshell':'foothold',
 'access.admin':'privilege','access.root':'privilege','access.system':'privilege',
 'ad.domain_known':'domain','ad.attack_paths':'domain','relay.success':'lateral','pivot.established':'network','network.internal':'network'
};
function transitionsFor(outcomes){return[...new Set((outcomes||[]).map(f=>TRANSITION_FACTS[f]).filter(Boolean))];}
const baseRecord=C.recordActivity;
C.recordActivity=function(state,a){ensure22(state);const rec=baseRecord(state,a);rec.maneuverKey=a.maneuverKey||rec.maneuverKey||a.cardId;rec.tool=a.tool||rec.tool||inferToolFromCommand(rec.command);rec.transitions=Array.isArray(a.transitions)?a.transitions:transitionsFor(rec.outcomeFacts);return rec;};
function inferToolFromCommand(command){let s=String(command||'').trim().replace(/^(sudo|proxychains4?|env)\s+/,'').split(/\s+/)[0]||'';s=s.split('/').pop().replace(/\.py$|\.exe$/i,'');return toolId(s);}
C.inferToolFromCommand=inferToolFromCommand;C.transitionsFor=transitionsFor;
function transitionTimeline(state,ctx){const key=ctx?C.contextKey(C.normalizeContext(state,ctx)):null;return(state.activities||[]).filter(a=>(!key||a.contextKey===key)&&(a.transitions||[]).length).map(a=>({at:a.at,contextKey:a.contextKey,contextLabel:a.contextLabel,cardId:a.cardId,maneuverKey:a.maneuverKey,tool:a.tool,transitions:a.transitions,outcomeFacts:a.outcomeFacts||[],command:a.command,evidence:a.evidence}));}
C.transitionTimeline=transitionTimeline;

function evidenceBucket(state,ctx){ensure22(state);const key=typeof ctx==='string'?ctx:C.contextKey(C.normalizeContext(state,ctx||state.activeContext));state.reportEvidence[key]=state.reportEvidence[key]||{};return state.reportEvidence[key];}
function setReportEvidence(state,ctx,id,value,note){const b=evidenceBucket(state,ctx);b[id]={done:!!value,note:String(note||''),updatedAt:C.now()};state.updatedAt=C.now();return b[id];}
function reportEvidenceStatus(state,ctx,id){return evidenceBucket(state,ctx)[id]||{done:false,note:''};}
C.evidenceBucket=evidenceBucket;C.setReportEvidence=setReportEvidence;C.reportEvidenceStatus=reportEvidenceStatus;

function workflowRows(state,lanes,ctx){
  const facts=C.effectiveFacts(state,ctx),groups={};for(const lane of lanes||[])for(const card of lane.cards||[]){if(!C.grounded(card,facts)&&!C.applicable(card,facts))continue;const w=card.workflow||serviceForCard(card),g=groups[w]||(groups[w]={id:w,title:String(w).replace(/[-_]/g,' ').replace(/\b\w/g,m=>m.toUpperCase()),cards:[]});g.cards.push(card);}
  return Object.values(groups).map(g=>{const uniq=uniqueManeuvers(g.cards.map(card=>({card}))).map(x=>x.card),steps=uniq.map(card=>({cardId:card.id,title:card.title,status:C.statusFor(state,card.id,ctx),maneuver:maneuverKey(card)}));const touched=steps.filter(s=>s.status!=='new').length,done=steps.filter(s=>s.status==='done').length;return{...g,steps,total:steps.length,touched,done,remaining:steps.length-touched,depth:steps.length?Math.round(touched/steps.length*100):0};}).filter(g=>g.total).sort((a,b)=>a.depth-b.depth||b.total-a.total);
}
C.workflowRows=workflowRows;
root.OBOL_CORE_V22={VERSION,ensure22};
}

/* obol-core-delta: assets/core-v2.3.js */
{
// Obol v2.3 core overlay — evidence-to-artifact handoffs and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.3.js');
const VERSION='2.3.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldApply=C.applyEvidenceUpdate;
function ensure23(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};s.ui.artifactActions=s.ui.artifactActions&&typeof s.ui.artifactActions==='object'?s.ui.artifactActions:{};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure23(oldNew());};
C.coerceState=function(raw){return ensure23(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure23(oldMigrate(raw));};
C.ensure23=ensure23;
C.applyEvidenceUpdate=function(state,lanes,update){
  ensure23(state);update=update||{};const users=((update.artifacts||{}).users||[]).map(x=>String(x||'').trim()).filter(Boolean);
  if(users.length){update={...update,facts:[...(update.facts||[])]};if(!update.facts.some(f=>(typeof f==='string'?f:f&&f.id)==='ad.user_list'))update.facts.push({id:'ad.user_list',source:update.source||'intake',evidence:'Distilled '+users.length+' user identit'+(users.length===1?'y':'ies')+' from reviewed evidence',confidence:'high'});}
  return oldApply(state,lanes,update);
};
root.OBOL_CORE_V23={VERSION,ensure23};
}

/* obol-core-delta: assets/core-v2.4.js */
{
// Obol v2.4 core overlay — persistent operator work queue and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.4.js');
const VERSION='2.4.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRecord=C.recordActivity;
function ensure24(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  s.workQueue=Array.isArray(s.workQueue)?s.workQueue:[];
  s.workQueue=s.workQueue.map((x,i)=>({id:x.id||('work-'+i),cardId:String(x.cardId||''),contextKey:String(x.contextKey||'global:global'),contextLabel:String(x.contextLabel||''),priority:['high','normal','low'].includes(x.priority)?x.priority:'normal',note:String(x.note||''),status:['planned','done','deferred'].includes(x.status)?x.status:'planned',createdAt:x.createdAt||C.now(),updatedAt:x.updatedAt||x.createdAt||C.now(),completedAt:x.completedAt||null})).filter(x=>x.cardId);
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure24(oldNew());};
C.coerceState=function(raw){return ensure24(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure24(oldMigrate(raw));};
function queueFor(state,ctx,opts){
  ensure24(state);opts=opts||{};const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),order={high:0,normal:1,low:2};
  return state.workQueue.filter(x=>(opts.allContexts||x.contextKey===key)&&(!opts.status||x.status===opts.status)).slice().sort((a,b)=>(order[a.priority]-order[b.priority])||String(a.createdAt).localeCompare(String(b.createdAt)));
}
function queueItem(state,cardId,ctx){ensure24(state);const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c);return state.workQueue.find(x=>x.cardId===cardId&&x.contextKey===key&&x.status==='planned')||null;}
function addToQueue(state,cardId,ctx,opts){
  ensure24(state);opts=opts||{};const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c),existing=queueItem(state,cardId,c);if(existing){if(opts.priority)existing.priority=opts.priority;if(opts.note!=null)existing.note=String(opts.note);existing.updatedAt=C.now();return existing;}
  const x={id:C.uid('work'),cardId:String(cardId),contextKey:key,contextLabel:C.contextLabel(state,c),priority:['high','normal','low'].includes(opts.priority)?opts.priority:'normal',note:String(opts.note||''),status:'planned',createdAt:C.now(),updatedAt:C.now(),completedAt:null};state.workQueue.push(x);return x;
}
function updateQueueItem(state,id,patch){ensure24(state);const x=state.workQueue.find(r=>r.id===id);if(!x)return null;patch=patch||{};if(['high','normal','low'].includes(patch.priority))x.priority=patch.priority;if(patch.note!=null)x.note=String(patch.note);if(['planned','done','deferred'].includes(patch.status)){x.status=patch.status;if(patch.status==='done')x.completedAt=C.now();else if(patch.status!=='done')x.completedAt=null;}x.updatedAt=C.now();return x;}
function removeQueueItem(state,id){ensure24(state);const n=state.workQueue.length;state.workQueue=state.workQueue.filter(x=>x.id!==id);return state.workQueue.length!==n;}
C.recordActivity=function(state,a){const rec=oldRecord(state,a);if(rec&&rec.result==='success'){ensure24(state);for(const x of state.workQueue.filter(q=>q.cardId===rec.cardId&&q.contextKey===rec.contextKey&&q.status==='planned')){x.status='done';x.completedAt=rec.at||C.now();x.updatedAt=rec.at||C.now();}}return rec;};
C.ensure24=ensure24;C.queueFor=queueFor;C.queueItem=queueItem;C.addToQueue=addToQueue;C.updateQueueItem=updateQueueItem;C.removeQueueItem=removeQueueItem;
root.OBOL_CORE_V24={VERSION,ensure24};
}

/* obol-core-delta: assets/core-v2.5.js */
{
// Obol v2.5 core overlay — script-builder state, richer evidence handoffs, and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.5.js');
const VERSION='2.5.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1;
function ensure25(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  s.ui.scriptBuilders=s.ui.scriptBuilders&&typeof s.ui.scriptBuilders==='object'?s.ui.scriptBuilders:{};
  s.ui.methodologyMapOpen=s.ui.methodologyMapOpen!==false;
  s.ui.intakeNormalization=s.ui.intakeNormalization&&typeof s.ui.intakeNormalization==='object'?s.ui.intakeNormalization:{};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure25(oldNew());};
C.coerceState=function(raw){return ensure25(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure25(oldMigrate(raw));};
function scriptBuilderState(state,id,profile){
  ensure25(state);const key=String(id||'script'),p=profile||{},cur=state.ui.scriptBuilders[key]||{};
  const next={selected:{...(cur.selected||{})},radio:{...(cur.radio||{})},args:{...(cur.args||{})}};
  for(const c of p.controls||[]){
    if(c.type==='toggle'&&next.selected[c.id]===undefined)next.selected[c.id]=c.default!==false;
    if(c.type==='radio'&&!next.radio[c.id])next.radio[c.id]=c.default||((c.options||[])[0]||{}).value||'';
    if(c.type==='arg'&&next.args[c.id]===undefined&&c.default!=null)next.args[c.id]=String(c.default);
  }
  state.ui.scriptBuilders[key]=next;return next;
}
function updateScriptBuilder(state,id,patch){
  ensure25(state);const cur=state.ui.scriptBuilders[id]||{selected:{},radio:{},args:{}};patch=patch||{};
  if(patch.selected)cur.selected={...(cur.selected||{}),...patch.selected};
  if(patch.radio)cur.radio={...(cur.radio||{}),...patch.radio};
  if(patch.args)cur.args={...(cur.args||{}),...patch.args};
  state.ui.scriptBuilders[id]=cur;state.updatedAt=C.now();return cur;
}
function referencedParams(text){const out=[];String(text||'').replace(/{{(\w+)}}/g,(m,k)=>{if(!out.includes(k))out.push(k);return m;});return out;}
C.ensure25=ensure25;C.scriptBuilderState=scriptBuilderState;C.updateScriptBuilder=updateScriptBuilder;C.referencedParams=referencedParams;
root.OBOL_CORE_V25={VERSION,ensure25};
}

/* obol-core-delta: assets/core-v2.6.js */
{
// Obol v2.6 core overlay — typed artifacts, negative-evidence semantics, workspace search, and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.6.js');
const VERSION='2.6.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRecord=C.recordActivity,oldSanitize=C.sanitizedCopy,oldRanked=C.rankedApplicable;
const KINDS=['hosts','shares','urls','files','tickets','certificates','subnets','secrets'];
function ensure26(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};s.ui.negativeOutcome=s.ui.negativeOutcome&&typeof s.ui.negativeOutcome==='object'?s.ui.negativeOutcome:{};
  s.typedArtifacts=s.typedArtifacts&&typeof s.typedArtifacts==='object'?s.typedArtifacts:{};
  for(const k of KINDS)s.typedArtifacts[k]=Array.isArray(s.typedArtifacts[k])?s.typedArtifacts[k]:[];
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure26(oldNew());};
C.coerceState=function(raw){return ensure26(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure26(oldMigrate(raw));};
function artifactKey(v){return String(v||'').trim().toLowerCase();}
function addTypedArtifact(state,kind,value,opts){
  ensure26(state);if(!KINDS.includes(kind))return null;value=String(value||'').trim();if(!value)return null;opts=opts||{};
  const key=artifactKey(value),rows=state.typedArtifacts[kind];let row=rows.find(x=>artifactKey(x.value)===key);if(row)return row;
  const ctx=C.normalizeContext(state,opts.context||state.activeContext);row={id:C.uid('artifact'),kind,value,contextKey:C.contextKey(ctx),contextLabel:C.contextLabel(state,ctx),source:opts.source||'intake',confidence:opts.confidence||'medium',observedAt:opts.observedAt||C.now()};rows.push(row);state.updatedAt=C.now();return row;
}
function typedArtifactsFor(state,kind,ctx){ensure26(state);const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));const rows=kind?state.typedArtifacts[kind]||[]:KINDS.flatMap(k=>state.typedArtifacts[k]||[]);return rows.filter(x=>x.contextKey===key||x.contextKey==='global:global');}
function handoffForArtifact(row){if(!row)return null;const v=row.value||'';if(row.kind==='hosts')return{param:'target',value:v};if(row.kind==='urls')return{param:'url',value:v};if(row.kind==='shares')return{param:'share',value:v};if(row.kind==='files'||row.kind==='tickets'||row.kind==='certificates')return{param:'file',value:v};if(row.kind==='subnets')return{param:'subnet',value:v};if(row.kind==='secrets')return{param:/^[0-9a-f]{32}$/i.test(v)?'hash':'password',value:v};return null;}
function applyArtifactHandoff(state,row){const h=handoffForArtifact(row);if(!h)return null;state.params=state.params||{};state.params[h.param]=h.value;state.updatedAt=C.now();return h;}
C.recordActivity=function(state,a){a=a||{};const rec=oldRecord(state,a);if(rec){rec.failureClass=a.failureClass||rec.failureClass||'';if(a.assessment)rec.assessment=a.assessment;rec.reason=String(a.reason||rec.reason||'').slice(0,500);}return rec;};
const PENALTY={tool_failed:3,inconclusive:8,service_rejected:32,refuted:90};
C.rankedApplicable=function(state,lanes,ctx,opts){
  const rows=oldRanked(state,lanes,ctx,opts||{}).map(r=>{const a=C.latestActivity(state,r.card.id,ctx),klass=(a&&a.failureClass)||((a&&a.assessment==='refuted')?'refuted':'');if(klass&&PENALTY[klass]){r={...r,score:r.score-PENALTY[klass],why:(r.why?r.why+'; ':'')+(klass==='tool_failed'?'last attempt failed at the tool/environment layer':klass==='inconclusive'?'last attempt was inconclusive':klass==='service_rejected'?'the service rejected this technique':'the underlying hypothesis was explicitly refuted')};}return r;});
  return rows.filter(r=>{const a=C.latestActivity(state,r.card.id,ctx);return !((a&&((a.failureClass==='refuted')||a.assessment==='refuted'))&&!(opts||{}).showAll);}).sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
};
function searchWorkspace(state,lanes,query,ctx){
  ensure26(state);const q=String(query||'').trim().toLowerCase();if(!q)return[];const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),out=[],push=(kind,title,detail,href,hay)=>{if(String(hay||'').toLowerCase().includes(q))out.push({kind,title,detail,href});};
  for(const f of C.effectiveFactRecords(state,ctx))push('fact',f.id,C.labelFact(f.id),'#/path',[f.id,f.evidence,f.source].join(' '));
  for(const k of KINDS)for(const a of state.typedArtifacts[k]||[])if(a.contextKey===key||a.contextKey==='global:global')push('artifact',k+' · '+a.value,a.contextLabel,'#/intake',[k,a.value,a.source].join(' '));
  for(const a of state.activities||[])if(a.contextKey===key)push('activity',a.cardId,a.result+' · '+(a.failureClass||a.assessment||''),'#/card/'+encodeURIComponent(a.cardId),[a.cardId,a.command,a.evidence,a.reason,a.failureClass,a.assessment].join(' '));
  for(const w of state.workQueue||[])if(w.contextKey===key)push('queue',w.cardId,w.priority+' · '+w.status,'#/queue',[w.cardId,w.note,w.priority,w.status].join(' '));
  for(const l of lanes||[])for(const c of l.cards||[]){push('card',c.title,c.lane||l.title,'#/card/'+encodeURIComponent(c.id),[c.id,c.title,c.hypothesis,c.lane,l.title].join(' '));for(const cmd of c.commands||[])push('command',c.title+' · '+cmd.tool,cmd.run,'#/card/'+encodeURIComponent(c.id),[cmd.tool,cmd.run,cmd.note].join(' '));}
  const seen=new Set();return out.filter(x=>{const k=[x.kind,x.title,x.detail,x.href].join('|');if(seen.has(k))return false;seen.add(k);return true;}).slice(0,100);
}
C.ensure26=ensure26;C.addTypedArtifact=addTypedArtifact;C.typedArtifactsFor=typedArtifactsFor;C.handoffForArtifact=handoffForArtifact;C.applyArtifactHandoff=applyArtifactHandoff;C.searchWorkspace=searchWorkspace;C.TYPED_ARTIFACT_KINDS=KINDS;
C.sanitizedCopy=function(state){const s=ensure26(oldSanitize(state));s.typedArtifacts.secrets=(s.typedArtifacts.secrets||[]).map(x=>({...x,value:'[REDACTED SECRET]'}));return s;};
root.OBOL_CORE_V26={VERSION,ensure26,KINDS};
}

/* obol-core-delta: assets/core-v2.7.js */
{
// Obol v2.7 core overlay — artifact lineage, direct command bindings, internal-network model, filtered search, and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.7.js');
const VERSION='2.7.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldAdd=C.addTypedArtifact,oldHandoff=C.applyArtifactHandoff,oldSanitize=C.sanitizedCopy;
function ensure27(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  s.ui.searchFilters27=s.ui.searchFilters27&&typeof s.ui.searchFilters27==='object'?s.ui.searchFilters27:{kind:'all',artifactKind:'all',source:'',result:'all',time:'all'};
  s.ui.intakeSource27=s.ui.intakeSource27&&typeof s.ui.intakeSource27==='object'?s.ui.intakeSource27:null;
  s.networkModel=s.networkModel&&typeof s.networkModel==='object'?s.networkModel:{};
  s.networkModel.observations=Array.isArray(s.networkModel.observations)?s.networkModel.observations:[];
  for(const k of C.TYPED_ARTIFACT_KINDS||[])for(const a of (s.typedArtifacts&&s.typedArtifacts[k])||[]){a.producedBy=Array.isArray(a.producedBy)?a.producedBy:[];a.consumedBy=Array.isArray(a.consumedBy)?a.consumedBy:[];a.tags=Array.isArray(a.tags)?a.tags:[];}
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure27(oldNew());};
C.coerceState=function(raw){return ensure27(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure27(oldMigrate(raw));};
function tinyHash(s){return C.simpleHash?C.simpleHash(String(s||'')):String(s||'').slice(0,32);}
function producerSpec(state,opts){
  opts=opts||{};const src=opts.producer||((opts.source||'').startsWith('intake')?(state.ui||{}).intakeSource27:null)||{};
  return {id:src.id||('producer-'+tinyHash([src.activityId,src.cardId,src.command,opts.source,src.at].join('|'))),activityId:src.activityId||'',cardId:src.cardId||'',command:String(src.command||'').slice(0,500),source:src.source||opts.source||'intake',contextKey:src.contextKey||C.contextKey(C.normalizeContext(state,opts.context||state.activeContext)),at:src.at||opts.observedAt||C.now()};
}
function addUniqueLineage(list,item){const key=[item.activityId,item.cardId,item.command,item.source,item.contextKey].join('|');if(!list.some(x=>[x.activityId,x.cardId,x.command,x.source,x.contextKey].join('|')===key))list.push(item);}
C.addTypedArtifact=function(state,kind,value,opts){ensure27(state);const row=oldAdd(state,kind,value,opts||{});if(!row)return row;row.producedBy=Array.isArray(row.producedBy)?row.producedBy:[];row.consumedBy=Array.isArray(row.consumedBy)?row.consumedBy:[];row.tags=Array.isArray(row.tags)?row.tags:[];addUniqueLineage(row.producedBy,producerSpec(state,opts));return row;};
function recordArtifactConsumption(state,row,consumer){ensure27(state);if(!row)return null;row.consumedBy=Array.isArray(row.consumedBy)?row.consumedBy:[];consumer=consumer||{};const c={id:consumer.id||C.uid('consume'),cardId:consumer.cardId||'',commandId:consumer.commandId||'',field:consumer.field||'',param:consumer.param||'',source:consumer.source||'operator-handoff',contextKey:consumer.contextKey||C.contextKey(C.normalizeContext(state,consumer.context||state.activeContext)),at:consumer.at||C.now()};addUniqueLineage(row.consumedBy,c);state.updatedAt=C.now();return c;}
C.applyArtifactHandoff=function(state,row){const h=oldHandoff(state,row);if(h)recordArtifactConsumption(state,row,{field:'shared parameter',param:h.param,source:'artifact-handoff'});return h;};
function roleForArtifact(row){if(!row)return[];if(row.kind==='hosts')return['target','host','rhost','destination','server'];if(row.kind==='urls')return['url','uri','endpoint'];if(row.kind==='shares')return['share','unc'];if(row.kind==='subnets')return['subnet','network','cidr','route'];if(row.kind==='tickets')return['ticket','ccache','kirbi','file','path'];if(row.kind==='certificates')return['certificate','cert','pfx','pem','file','path'];if(row.kind==='files')return['file','path','wordlist','userlist','hashfile'];if(row.kind==='secrets')return[/^[0-9a-f]{32}$/i.test(row.value)?'hash':'password','secret','token','credential'];return[];}
function fieldText(o){return [o.id,o.arg,o.label,o.semantic,o.placeholder,o.tip].filter(Boolean).join(' ').toLowerCase();}
function artifactBindingsForCard(state,card,ctx){
  ensure27(state);if(!card||!C.typedArtifactsFor)return[];const arts=C.typedArtifactsFor(state,null,ctx||state.activeContext),out=[];
  for(const [index,cmd] of (card.commands||[]).entries()){
    const cid=C.commandId(cmd,index),refs=[];String(cmd.run||'').replace(/{{(\w+)}}/g,(m,k)=>{if(!refs.includes(k))refs.push(k);return m;});
    for(const row of arts){const roles=roleForArtifact(row);for(const p of refs)if(roles.some(r=>p.toLowerCase().includes(r)||r.includes(p.toLowerCase())))out.push({row,cardId:card.id,commandId:cid,index,tool:cmd.tool||'',fieldType:'param',field:p,label:p});
      for(const [oi,o] of (cmd.opts||[]).entries()){if(!o.arg)continue;const txt=fieldText(o),role=roles.find(r=>txt.includes(r));if(role)out.push({row,cardId:card.id,commandId:cid,index,tool:cmd.tool||'',fieldType:'option',field:C.optionId(o,oi),label:o.semantic||o.label||o.arg});}
    }
  }
  const seen=new Set();return out.filter(x=>{const k=[x.row.id,x.commandId,x.fieldType,x.field].join('|');if(seen.has(k))return false;seen.add(k);return true;});
}
function bindArtifactToCommand(state,card,row,binding){
  ensure27(state);if(!card||!row||!binding)return null;const cmd=(card.commands||[])[binding.index];if(!cmd)return null;const cid=C.commandId(cmd,binding.index);if(binding.fieldType==='param'){state.params=state.params||{};state.params[binding.field]=row.value;recordArtifactConsumption(state,row,{cardId:card.id,commandId:cid,field:binding.label||binding.field,param:binding.field,source:'command-binding'});return{type:'param',field:binding.field,value:row.value};}
  state.ui.opts=state.ui.opts||{};const key=card.id+':'+cid;state.ui.opts[key]=state.ui.opts[key]||{selected:{},args:{},radio:{}};state.ui.opts[key].args=state.ui.opts[key].args||{};state.ui.opts[key].args[binding.field]=row.value;recordArtifactConsumption(state,row,{cardId:card.id,commandId:cid,field:binding.label||binding.field,source:'command-binding'});return{type:'option',field:binding.field,value:row.value};
}
function observationKey(o){return [o.type,o.interface||'',o.address||'',o.network||'',o.gateway||'',o.destination||'',o.contextKey||''].join('|').toLowerCase();}
function recordNetworkObservations(state,rows,opts){ensure27(state);opts=opts||{};const ctx=C.normalizeContext(state,opts.context||state.activeContext),key=C.contextKey(ctx),added=[];for(const raw of rows||[]){if(!raw||!raw.type)continue;const o={id:raw.id||C.uid('netobs'),type:String(raw.type),interface:String(raw.interface||''),address:String(raw.address||''),network:String(raw.network||''),gateway:String(raw.gateway||''),destination:String(raw.destination||''),metric:String(raw.metric||''),source:raw.source||opts.source||'intake',contextKey:raw.contextKey||key,contextLabel:raw.contextLabel||C.contextLabel(state,ctx),observedAt:raw.observedAt||C.now()};const k=observationKey(o);if(!state.networkModel.observations.some(x=>observationKey(x)===k)){state.networkModel.observations.push(o);added.push(o);}}
  state.updatedAt=C.now();return added;
}
function networkSummary(state,ctx){ensure27(state);const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),rows=state.networkModel.observations.filter(x=>x.contextKey===key||x.contextKey==='global:global');return{observations:rows,interfaces:rows.filter(x=>x.type==='interface'),routes:rows.filter(x=>x.type==='route'),hosts:(state.typedArtifacts&&state.typedArtifacts.hosts||[]).filter(x=>x.contextKey===key||x.contextKey==='global:global'),subnets:(state.typedArtifacts&&state.typedArtifacts.subnets||[]).filter(x=>x.contextKey===key||x.contextKey==='global:global')};}
function withinTime(at,time){if(time==='all'||!time)return true;const ts=Date.parse(at||'');if(!ts)return false;const age=Date.now()-ts,day=86400000;if(time==='24h')return age<=day;if(time==='7d')return age<=7*day;if(time==='30d')return age<=30*day;return true;}
function searchWorkspace27(state,lanes,query,ctx,filters){
  ensure27(state);filters={...state.ui.searchFilters27,...(filters||{})};const q=String(query||'').trim().toLowerCase(),key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),out=[];
  const hit=(kind,title,detail,href,hay,meta)=>{meta=meta||{};if(q&&!String(hay||'').toLowerCase().includes(q))return;if(filters.kind!=='all'&&filters.kind!==kind)return;if(filters.artifactKind!=='all'&&kind==='artifact'&&filters.artifactKind!==meta.artifactKind)return;if(filters.source&& !String(meta.source||'').toLowerCase().includes(String(filters.source).toLowerCase()))return;if(filters.result!=='all'&&kind==='activity'&&filters.result!==meta.result)return;if(filters.result!=='all'&&kind!=='activity')return;if(!withinTime(meta.at,filters.time))return;out.push({kind,title,detail,href,...meta});};
  for(const f of C.effectiveFactRecords(state,ctx))hit('fact',f.id,C.labelFact(f.id),'#/path',[f.id,f.evidence,f.source].join(' '),{source:f.source||'',at:f.observedAt||''});
  for(const k of C.TYPED_ARTIFACT_KINDS||[])for(const a of (state.typedArtifacts&&state.typedArtifacts[k])||[])if(a.contextKey===key||a.contextKey==='global:global')hit('artifact',k+' · '+(k==='secrets'?'••••••••':a.value),a.contextLabel,'#/intake',[k,a.value,a.source,(a.tags||[]).join(' ')].join(' '),{artifactKind:k,source:a.source||'',at:a.observedAt||''});
  for(const a of state.activities||[])if(a.contextKey===key)hit('activity',a.cardId,a.result+' · '+(a.failureClass||a.assessment||''),'#/card/'+encodeURIComponent(a.cardId),[a.cardId,a.command,a.evidence,a.reason,a.failureClass,a.assessment].join(' '),{source:a.source||'',result:a.result||'',at:a.at||''});
  for(const w of state.workQueue||[])if(w.contextKey===key)hit('queue',w.cardId,w.priority+' · '+w.status,'#/queue',[w.cardId,w.note,w.priority,w.status].join(' '),{source:'queue',at:w.updatedAt||w.createdAt||''});
  for(const l of lanes||[])for(const c of l.cards||[]){hit('card',c.title,c.lane||l.title,'#/card/'+encodeURIComponent(c.id),[c.id,c.title,c.hypothesis,c.lane,l.title].join(' '),{source:'methodology'});for(const cmd of c.commands||[])hit('command',c.title+' · '+cmd.tool,cmd.run,'#/card/'+encodeURIComponent(c.id),[cmd.tool,cmd.run,cmd.note].join(' '),{source:'methodology'});}
  const seen=new Set();return out.filter(x=>{const k=[x.kind,x.title,x.detail,x.href].join('|');if(seen.has(k))return false;seen.add(k);return true;}).slice(0,200);
}
C.ensure27=ensure27;C.recordArtifactConsumption=recordArtifactConsumption;C.artifactBindingsForCard=artifactBindingsForCard;C.bindArtifactToCommand=bindArtifactToCommand;C.recordNetworkObservations=recordNetworkObservations;C.networkSummary=networkSummary;C.searchWorkspace27=searchWorkspace27;
C.sanitizedCopy=function(state){const s=ensure27(oldSanitize(state));return s;};
root.OBOL_CORE_V27={VERSION,ensure27};
}

/* obol-core-delta: assets/core-v2.8.js */
{
// Obol v2.8 core overlay — explicit reachability paths, lineage timelines, report readiness, and release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.8.js');
const VERSION='2.8.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldNetwork=C.networkSummary,oldSanitize=C.sanitizedCopy;
function ensure28(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  s.networkModel=s.networkModel&&typeof s.networkModel==='object'?s.networkModel:{};
  s.networkModel.observations=Array.isArray(s.networkModel.observations)?s.networkModel.observations:[];
  s.networkModel.paths=Array.isArray(s.networkModel.paths)?s.networkModel.paths:[];
  s.reportEvidence28=s.reportEvidence28&&typeof s.reportEvidence28==='object'?s.reportEvidence28:{};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure28(oldNew());};
C.coerceState=function(raw){return ensure28(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure28(oldMigrate(raw));};
function ipv4Int(ip){const p=String(ip||'').trim().split('.').map(Number);if(p.length!==4||p.some(n=>!Number.isInteger(n)||n<0||n>255))return null;return (((p[0]<<24)>>>0)+(p[1]<<16)+(p[2]<<8)+p[3])>>>0;}
function cidrSpec(cidr){const m=String(cidr||'').trim().match(/^((?:\d{1,3}\.){3}\d{1,3})\/(\d|[12]\d|3[0-2])$/);if(!m)return null;const ip=ipv4Int(m[1]),bits=+m[2];if(ip===null)return null;const mask=bits===0?0:(0xffffffff<<(32-bits))>>>0;return{cidr:m[1]+'/'+bits,network:(ip&mask)>>>0,mask,bits};}
function cidrContains(cidr,ip){const c=cidrSpec(cidr),n=ipv4Int(ip);return !!c&&n!==null&&((n&c.mask)>>>0)===c.network;}
function pathKey(p){return [p.mode,p.kind,p.network,p.viaContextKey,p.endpoint].join('|').toLowerCase();}
function recordNetworkPath(state,spec,opts){
  ensure28(state);spec=spec||{};opts=opts||{};const c=cidrSpec(spec.network);if(!c)return null;
  const ctx=C.normalizeContext(state,opts.context||state.activeContext),mode=spec.mode==='direct'?'direct':'pivot',kind=mode==='direct'?'direct':String(spec.kind||'other'),status=spec.status==='inactive'?'inactive':'active';
  const row={id:spec.id||C.uid('path'),name:String(spec.name||'').trim(),mode,kind,network:c.cidr,endpoint:String(spec.endpoint||'').trim(),viaContextKey:spec.viaContextKey||C.contextKey(ctx),viaContextLabel:spec.viaContextLabel||C.contextLabel(state,ctx),status,source:spec.source||opts.source||'operator',createdAt:spec.createdAt||C.now(),updatedAt:C.now()};
  const same=state.networkModel.paths.find(x=>pathKey(x)===pathKey(row));if(same){same.status=row.status;same.name=row.name||same.name;same.updatedAt=C.now();same.source=row.source||same.source;return same;}
  state.networkModel.paths.push(row);state.updatedAt=C.now();return row;
}
function updateNetworkPath(state,id,patch){ensure28(state);const x=state.networkModel.paths.find(p=>p.id===id);if(!x)return null;patch=patch||{};if(patch.status==='active'||patch.status==='inactive')x.status=patch.status;if(patch.name!=null)x.name=String(patch.name);if(patch.endpoint!=null)x.endpoint=String(patch.endpoint);x.updatedAt=C.now();state.updatedAt=C.now();return x;}
function removeNetworkPath(state,id){ensure28(state);const n=state.networkModel.paths.length;state.networkModel.paths=state.networkModel.paths.filter(x=>x.id!==id);state.updatedAt=C.now();return state.networkModel.paths.length!==n;}
function networkPaths(state,ctx,opts){ensure28(state);opts=opts||{};const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));return state.networkModel.paths.filter(x=>(opts.allContexts||x.viaContextKey===key||x.viaContextKey==='global:global')&&(!opts.activeOnly||x.status==='active'));}
function observedAddress(state,address,ctx){const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),a=String(address||'');if(((state.typedArtifacts||{}).hosts||[]).some(x=>x.value===a&&(x.contextKey===key||x.contextKey==='global:global')))return true;return (state.networkModel.observations||[]).some(x=>x.address===a&&(x.contextKey===key||x.contextKey==='global:global'));}
function reachabilityFor(state,address,ctx){
  ensure28(state);const ip=String(address||'').trim(),host=C.hostForContext(state,ctx||state.activeContext);if(!ip)return{state:'unknown',via:null};if(host&&host.ip===ip)return{state:'direct',via:null};
  const paths=networkPaths(state,ctx,{activeOnly:true}),direct=paths.find(p=>p.mode==='direct'&&cidrContains(p.network,ip));if(direct)return{state:'direct',via:direct};const pivot=paths.find(p=>p.mode==='pivot'&&cidrContains(p.network,ip));if(pivot)return{state:'pivot',via:pivot};if(observedAddress(state,ip,ctx))return{state:'observed',via:null};return{state:'unknown',via:null};
}
function networkSummary28(state,ctx){
  ensure28(state);const base=oldNetwork?oldNetwork(state,ctx):{observations:[],interfaces:[],routes:[],hosts:[],subnets:[]},paths=networkPaths(state,ctx,{}),addresses=[];
  for(const h of base.hosts||[])if(h.value&&!addresses.includes(h.value))addresses.push(h.value);for(const o of base.observations||[])if(o.address&&!addresses.includes(o.address))addresses.push(o.address);
  const visibility=addresses.map(address=>({address,...reachabilityFor(state,address,ctx)})),counts={direct:0,pivot:0,observed:0,unknown:0};for(const x of visibility)counts[x.state]=(counts[x.state]||0)+1;
  return{...base,paths,directPaths:paths.filter(x=>x.mode==='direct'),pivotPaths:paths.filter(x=>x.mode==='pivot'),visibility,reachabilityCounts:counts};
}
C.networkSummary=networkSummary28;
function artifactById(state,id){for(const k of C.TYPED_ARTIFACT_KINDS||[])for(const a of ((state.typedArtifacts||{})[k]||[]))if(a.id===id)return a;return null;}
function lineageTimeline(state,rowOrId){ensure28(state);const row=typeof rowOrId==='string'?artifactById(state,rowOrId):rowOrId;if(!row)return[];const out=[];
  for(const p of row.producedBy||[])out.push({direction:'produced',at:p.at||row.observedAt||'',cardId:p.cardId||'',activityId:p.activityId||'',source:p.source||row.source||'',command:p.command||'',contextKey:p.contextKey||row.contextKey||'',href:p.cardId?'#/card/'+encodeURIComponent(p.cardId):''});
  for(const c of row.consumedBy||[])out.push({direction:'consumed',at:c.at||'',cardId:c.cardId||'',activityId:'',source:c.source||'',commandId:c.commandId||'',field:c.field||c.param||'',contextKey:c.contextKey||row.contextKey||'',href:c.cardId?'#/card/'+encodeURIComponent(c.cardId):''});
  return out.sort((a,b)=>String(a.at).localeCompare(String(b.at))||a.direction.localeCompare(b.direction));
}
function activityKey(a){return a&&a.id?String(a.id):[a&&a.contextKey||'',a&&a.cardId||'',a&&a.at||''].join('|');}
function setReportProof(state,key,patch){ensure28(state);if(!key)return null;const cur=state.reportEvidence28[key]||{screenshot:false,note:''};patch=patch||{};if(typeof patch.screenshot==='boolean')cur.screenshot=patch.screenshot;if(patch.note!=null)cur.note=String(patch.note).slice(0,500);cur.updatedAt=C.now();state.reportEvidence28[key]=cur;state.updatedAt=C.now();return cur;}
function reportReadiness(state,lanes,ctx){
  ensure28(state);const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),acts=(state.activities||[]).filter(a=>a.contextKey===key&&a.result==='success').slice().sort((a,b)=>String(a.at).localeCompare(String(b.at))),arts=(C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[]));
  const rows=acts.map(a=>{const ak=activityKey(a),proof=state.reportEvidence28[ak]||{},links=arts.reduce((n,x)=>n+(x.producedBy||[]).filter(p=>(p.activityId&&p.activityId===a.id)||(!p.activityId&&p.cardId===a.cardId&&p.contextKey===a.contextKey)).length,0),evidence=!!String(a.evidence||'').trim(),command=!!String(a.command||'').trim(),screenshot=!!proof.screenshot;return{key:ak,activity:a,evidence,command,screenshot,artifactLinks:links,ready:evidence&&command&&screenshot,note:proof.note||''};});
  return{rows,total:rows.length,ready:rows.filter(x=>x.ready).length,missingEvidence:rows.filter(x=>!x.evidence).length,missingCommand:rows.filter(x=>!x.command).length,missingScreenshot:rows.filter(x=>!x.screenshot).length};
}
C.ensure28=ensure28;C.cidrContains=cidrContains;C.recordNetworkPath=recordNetworkPath;C.updateNetworkPath=updateNetworkPath;C.removeNetworkPath=removeNetworkPath;C.networkPaths=networkPaths;C.reachabilityFor=reachabilityFor;C.artifactById=artifactById;C.lineageTimeline=lineageTimeline;C.activityKey28=activityKey;C.setReportProof=setReportProof;C.reportReadiness=reportReadiness;
C.sanitizedCopy=function(state){return ensure28(oldSanitize(state));};
root.OBOL_CORE_V28={VERSION,ensure28};
}

/* obol-core-delta: assets/core-v2.9.js */
{
// Obol v2.9 core overlay — reachability-aware Path relevance, pivot lifecycle, dependency lineage, and proof obligations.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v2.9.js');
const VERSION='2.9.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRecordPath=C.recordNetworkPath,oldUpdatePath=C.updateNetworkPath,oldRanked=C.rankedApplicable,oldReadiness=C.reportReadiness,oldSanitize=C.sanitizedCopy;
function ensure29(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  s.networkModel=s.networkModel&&typeof s.networkModel==='object'?s.networkModel:{};
  s.networkModel.paths=Array.isArray(s.networkModel.paths)?s.networkModel.paths:[];
  for(const p of s.networkModel.paths){p.status=['active','inactive','broken'].includes(p.status)?p.status:'inactive';p.sourceHost=String(p.sourceHost||'');p.note=String(p.note||'');p.lastVerifiedAt=String(p.lastVerifiedAt||'');}
  s.reportEvidence29=s.reportEvidence29&&typeof s.reportEvidence29==='object'?s.reportEvidence29:{};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure29(oldNew());};
C.coerceState=function(raw){return ensure29(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure29(oldMigrate(raw));};

C.recordNetworkPath=function(state,spec,opts){
  ensure29(state);spec=spec||{};const row=oldRecordPath(state,spec,opts||{});if(!row)return row;
  if(spec.status==='broken')row.status='broken';
  row.sourceHost=String(spec.sourceHost||row.sourceHost||'').trim();
  row.note=String(spec.note||row.note||'').trim().slice(0,500);
  row.lastVerifiedAt=String(spec.lastVerifiedAt||row.lastVerifiedAt||'');
  row.updatedAt=C.now();state.updatedAt=C.now();return row;
};
C.updateNetworkPath=function(state,id,patch){
  ensure29(state);patch=patch||{};let row=oldUpdatePath(state,id,patch)||((state.networkModel.paths||[]).find(p=>p.id===id));if(!row)return null;
  if(['active','inactive','broken'].includes(patch.status))row.status=patch.status;
  if(patch.sourceHost!=null)row.sourceHost=String(patch.sourceHost).trim();
  if(patch.note!=null)row.note=String(patch.note).trim().slice(0,500);
  if(patch.endpoint!=null)row.endpoint=String(patch.endpoint).trim();
  if(patch.lastVerifiedAt!=null)row.lastVerifiedAt=String(patch.lastVerifiedAt);
  row.updatedAt=C.now();state.updatedAt=C.now();return row;
};
function verifyNetworkPath(state,id,opts){opts=opts||{};const p=C.updateNetworkPath(state,id,{status:opts.status||'active',lastVerifiedAt:opts.at||C.now(),note:opts.note});return p;}
function pathLifecycleSummary(state,ctx){
  ensure29(state);const rows=C.networkPaths?C.networkPaths(state,ctx||state.activeContext,{}):[];
  return{rows,active:rows.filter(x=>x.status==='active'),inactive:rows.filter(x=>x.status==='inactive'),broken:rows.filter(x=>x.status==='broken'),unverified:rows.filter(x=>x.status==='active'&&!x.lastVerifiedAt)};
}
C.verifyNetworkPath=verifyNetworkPath;C.pathLifecycleSummary=pathLifecycleSummary;

const REMOTE_SERVICES=new Set(['smb','ldap','kerberos','web','ssh','ftp','nfs','mssql','mysql','postgresql','rdp','winrm','snmp']);
function destinationService(o){const s=String(o&&o.destination||'').toLowerCase();const m=s.match(/^([a-z0-9_-]+)(?::\d+)?$/);return m?m[1]:'';}
function cardText(card){return [card&&card.id,card&&card.title,card&&card.lane,card&&card.workflow,...(card&&card.commands||[]).flatMap(x=>[x.tool,x.run,x.note])].join(' ').toLowerCase();}
function cardReachabilitySignal(state,card,ctx){
  ensure29(state);const n=C.networkSummary?C.networkSummary(state,ctx||state.activeContext):{observations:[],visibility:[],paths:[]},service=C.serviceForCard?C.serviceForCard(card):'',text=cardText(card),active=(n.paths||[]).filter(p=>p.status==='active'),broken=(n.paths||[]).filter(p=>p.status==='broken');
  const obs=(n.observations||[]).filter(o=>o.address),serviceObs=obs.filter(o=>!service||destinationService(o)===service);
  const classified=serviceObs.map(o=>({o,r:C.reachabilityFor?C.reachabilityFor(state,o.address,ctx||state.activeContext):{state:'unknown'}}));
  const pivotMatches=classified.filter(x=>x.r.state==='pivot').length,directMatches=classified.filter(x=>x.r.state==='direct').length,observedOnly=classified.filter(x=>x.r.state==='observed').length;
  const pivotSpecific=/proxychains|socks|ligolo|chisel|pivot|tunnel|port[- ]?forward/.test(text)||service==='pivot';
  let delta=0,reason='';
  if(REMOTE_SERVICES.has(service)&&pivotMatches){delta+=Math.min(30,18+pivotMatches*4);reason='explicit active pivot reaches observed '+service+' service'+(pivotMatches===1?'':'s');}
  else if(REMOTE_SERVICES.has(service)&&directMatches){delta+=Math.min(18,10+directMatches*2);reason='explicit direct reachability covers observed '+service+' service'+(directMatches===1?'':'s');}
  else if(pivotSpecific&&active.some(p=>p.mode==='pivot')){delta+=8;reason='an explicit pivot is active in this context';}
  if(pivotSpecific&&!active.some(p=>p.mode==='pivot')&&observedOnly){delta-=8;reason='internal targets are only observed; no active pivot is recorded';}
  if(service==='pivot'&&active.some(p=>p.mode==='pivot')){delta-=12;reason='a pivot is already active; verify or use it before creating another';}
  if(service==='pivot'&&broken.length){delta+=22;reason='a recorded pivot path is broken and needs repair or replacement';}
  return{service,delta,reason,pivotMatches,directMatches,observedOnly,activePivotCount:active.filter(p=>p.mode==='pivot').length,brokenPivotCount:broken.filter(p=>p.mode==='pivot').length};
}
C.cardReachabilitySignal=cardReachabilitySignal;
if(oldRanked){C.rankedApplicable=function(state,lanes,ctx,opts){
  const rows=oldRanked(state,lanes,ctx,opts||{}).map(x=>{const sig=cardReachabilitySignal(state,x.card,ctx);return{...x,score:x.score+sig.delta,reachability:sig,why:sig.reason?(x.why?x.why+'; ':'')+sig.reason:x.why};});
  return rows.sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
};}

function artifactsInContext(state,ctx){const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[])).filter(a=>a.contextKey===key||a.contextKey==='global:global');}
function lineageDependencyGraph(state,ctx){
  ensure29(state);const arts=artifactsInContext(state,ctx),nodes=arts.map(a=>({id:a.id,type:'artifact',kind:a.kind,label:a.kind==='secrets'?'••••••••':a.value,contextKey:a.contextKey})),edges=[];
  for(const src of arts)for(const c of src.consumedBy||[]){if(!c.cardId)continue;for(const dst of arts){if(dst.id===src.id)continue;const producers=(dst.producedBy||[]).filter(p=>p.cardId===c.cardId&&(p.contextKey===c.contextKey||!p.contextKey||!c.contextKey));for(const p of producers)edges.push({id:[src.id,dst.id,c.cardId].join('|'),from:src.id,to:dst.id,cardId:c.cardId,at:p.at||c.at||'',source:c.source||p.source||''});}}
  const seen=new Set();return{nodes,edges:edges.filter(e=>{if(seen.has(e.id))return false;seen.add(e.id);return true;}).sort((a,b)=>String(a.at).localeCompare(String(b.at)))};
}
C.lineageDependencyGraph=lineageDependencyGraph;

function proofBucket29(state,key,create){ensure29(state);if(create&&!state.reportEvidence29[key])state.reportEvidence29[key]={};return state.reportEvidence29[key]||{};}
function setReportProof29(state,key,field,value){if(!key||!field)return null;const b=proofBucket29(state,key,true);b[field]=!!value;b.updatedAt=C.now();state.updatedAt=C.now();return b;}
C.setReportProof29=setReportProof29;
function cardMap(lanes){const m={};for(const l of lanes||[])for(const c of l.cards||[])m[c.id]=c;return m;}
function activityRequirements29(state,lanes,row,ctx){
  const a=row.activity,card=cardMap(lanes)[a.cardId]||{},proof=proofBucket29(state,row.key,false),transitions=a.transitions||[],req=[
    {id:'evidence',label:'Evidence snapshot',done:row.evidence,manual:false},
    {id:'command',label:'Command snapshot',done:row.command,manual:false},
    {id:'screenshot',label:'Screenshot captured',done:row.screenshot,manual:true,legacy:true}
  ];
  const add=(id,label,done,manual=true)=>req.push({id,label,done:!!done,manual});
  if(card.report&&card.report.finding)add('finding-context','Finding metadata',!!card.report.finding,false);
  if(transitions.includes('credential'))add('artifact-provenance','Credential/artifact provenance',row.artifactLinks>0,false);
  if(transitions.includes('foothold')||transitions.includes('privilege')){add('target-visible','Target identity visible in proof',proof.targetVisible);add('identity-visible','User/root/SYSTEM identity visible',proof.identityVisible);}
  if(transitions.includes('privilege'))add('proof-visible','Proof/local evidence captured',proof.proofVisible);
  if(transitions.includes('network')){const life=pathLifecycleSummary(state,ctx||a.context||state.activeContext);add('path-record','Explicit active network path recorded',life.active.length>0,false);}
  return{card,finding:card.report&&card.report.finding||'',severity:card.report&&card.report.severity||'',requirements:req,missing:req.filter(x=>!x.done),ready:req.every(x=>x.done)};
}
if(oldReadiness){C.reportReadiness=function(state,lanes,ctx){
  const base=oldReadiness(state,lanes,ctx),rows=base.rows.map(r=>({...r,...activityRequirements29(state,lanes||[],r,ctx)}));return{...base,rows,ready:rows.filter(x=>x.ready).length,missingTarget:rows.filter(x=>x.requirements.some(q=>q.id==='target-visible'&&!q.done)).length,missingIdentity:rows.filter(x=>x.requirements.some(q=>q.id==='identity-visible'&&!q.done)).length,missingProof:rows.filter(x=>x.requirements.some(q=>q.id==='proof-visible'&&!q.done)).length,missingProvenance:rows.filter(x=>x.requirements.some(q=>q.id==='artifact-provenance'&&!q.done)).length};
};}

C.ensure29=ensure29;
C.sanitizedCopy=function(state){return ensure29(oldSanitize(state));};
root.OBOL_CORE_V29={VERSION,ensure29};
}

/* obol-core-delta: assets/core-v3.0.js */
{
// Obol v3.0 core overlay — workspace overview, workflow/navigation model, and UX state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.0.js');
const VERSION='3.0.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
const NAVIGATION={
  primary:[
    {id:'home',label:'Home',href:'#/home',help:'Resume the current engagement and see what needs attention.'},
    {id:'boxes',label:'Targets',href:'#/boxes',help:'Hosts, domains, ports, flags, and target metadata.'},
    {id:'intake',label:'Evidence',href:'#/intake',help:'Paste tool output and review evidence before applying it.'},
    {id:'path',label:'Next Steps',href:'#/path',help:'Evidence-ranked methodology recommendations for the active context.'},
    {id:'report',label:'Report',href:'#/report',help:'Proof readiness and reproducible report output.'}
  ],
  secondary:[
    {id:'map',label:'Engagement Map',href:'#/map'},
    {id:'lanes',label:'Methodology',href:'#/lanes'},
    {id:'tools',label:'Tool Library',href:'#/tools'},
    {id:'queue',label:'Planned Work',href:'#/queue'},
    {id:'search',label:'Workspace Search',href:'#/search'},
    {id:'lineage',label:'Evidence Lineage',href:'#/lineage'},
    {id:'guide',label:'Guide',href:'#/guide'},
    {id:'settings',label:'Workspace Data',href:'#/settings'}
  ]
};
function ensure30(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const old=s.ui.shell30&&typeof s.ui.shell30==='object'?s.ui.shell30:{};
  s.ui.shell30={sidebarCollapsed:!!old.sidebarCollapsed,onboardingDismissed:!!old.onboardingDismissed,mobileInitialized:!!old.mobileInitialized};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure30(oldNew());};
C.coerceState=function(raw){return ensure30(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure30(oldMigrate(raw));};
function artifactsForContext(state,ctx){
  const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));
  return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[])).filter(a=>a.contextKey===key||a.contextKey==='global:global');
}
function workspaceOverview(state,lanes,ctx){
  ensure30(state);lanes=lanes||[];const context=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(context);
  const facts=(C.effectiveFactRecords?C.effectiveFactRecords(state,context):[]).filter(f=>f.id!=='scope.defined'),artifacts=artifactsForContext(state,context),activities=(state.activities||[]).filter(a=>a.contextKey===key).slice().sort((a,b)=>String(b.at||'').localeCompare(String(a.at||''))),queue=(state.workQueue||[]).filter(w=>w.contextKey===key),planned=queue.filter(w=>!['done','deferred'].includes(w.status)),success=activities.filter(a=>a.result==='success'),tried=activities.filter(a=>a.result==='tried');
  let readiness={total:0,ready:0,rows:[]};try{if(C.reportReadiness)readiness=C.reportReadiness(state,lanes,context)||readiness;}catch(e){}
  let ranked=[];try{if(C.rankedApplicable)ranked=C.rankedApplicable(state,lanes,context,{showAll:false})||[];}catch(e){}
  let network={reachabilityCounts:{},paths:[]};try{if(C.networkSummary)network=C.networkSummary(state,context)||network;}catch(e){}
  const host=C.hostForContext?C.hostForContext(state,context):null,domain=C.domainForContext?C.domainForContext(state,context):null;
  let stage='review',next={label:'Review workspace',href:'#/search',detail:'Search recent evidence, activity, and unresolved work.'};
  if(!(state.hosts||[]).length){stage='setup';next={label:'Add your first target',href:'#/boxes',detail:'Create a host so evidence and activity have a clear scope.'};}
  else if(!facts.length&&!artifacts.length&&!activities.length){stage='evidence';next={label:'Ingest evidence',href:'#/intake',detail:'Paste an nmap scan or terminal output, then review what Obol recognized.'};}
  else if(readiness.total&&readiness.ready<readiness.total){stage='document';next={label:'Finish proof readiness',href:'#/report',detail:(readiness.total-readiness.ready)+' successful action(s) still need report proof.'};}
  else if(planned.length){stage='execute';next={label:'Resume planned work',href:'#/queue',detail:planned.length+' queued action(s) are waiting.'};}
  else if(ranked.length){stage='plan';next={label:'Choose the next step',href:'#/path',detail:'Path has '+ranked.length+' evidence-grounded recommendation(s).'};}
  return{
    context,key,contextLabel:C.contextLabel?C.contextLabel(state,context):key,host,domain,stage,next,
    targets:(state.hosts||[]).length,domains:(state.domains||[]).length,facts:facts.length,artifacts:artifacts.length,activities:activities.length,successes:success.length,tried:tried.length,planned:planned.length,readiness,ranked,network,recent:activities.slice(0,6)
  };
}
function navEntry(id){return NAVIGATION.primary.concat(NAVIGATION.secondary).find(x=>x.id===id)||null;}
C.ensure30=ensure30;C.NAVIGATION30=NAVIGATION;C.workspaceOverview30=workspaceOverview;C.navEntry30=navEntry;
C.sanitizedCopy=function(state){return ensure30(oldSanitize(state));};
root.OBOL_CORE_V30={VERSION,ensure30,NAVIGATION};
}

/* obol-core-delta: assets/core-v3.1.js */
{
// Obol v3.1 core overlay — discovery-first workflow, first-class Nmap planning, and scan-aware resume state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.1.js');
const VERSION='3.1.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldOverview=C.workspaceOverview30;
const PROFILES={
  discover:{id:'discover',label:'Discover hosts',detail:'Find live hosts before you know what is there.',timing:'T4',resolveDns:false,openOnly:false,noPing:false,output:'scans/discovery'},
  quick:{id:'quick',label:'Quick TCP',detail:'Fast first pass across the most common TCP ports.',timing:'T4',resolveDns:false,openOnly:true,noPing:true,output:'scans/quick'},
  full:{id:'full',label:'Full TCP',detail:'Sweep all TCP ports before deeper service work.',timing:'T4',resolveDns:false,openOnly:true,noPing:true,minRate:'1000',output:'scans/full-tcp'},
  service:{id:'service',label:'Service + scripts',detail:'Version detection and default scripts on a known host.',timing:'T4',resolveDns:false,openOnly:true,noPing:true,version:true,scripts:true,output:'scans/services'},
  udp:{id:'udp',label:'Top UDP',detail:'Target the most common UDP services as a separate pass.',timing:'T4',resolveDns:false,openOnly:true,noPing:true,output:'scans/udp'}
};
function ensure31(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const d=s.ui.discovery31&&typeof s.ui.discovery31==='object'?s.ui.discovery31:{};
  s.ui.discovery31={profile:d.profile&&PROFILES[d.profile]?d.profile:'discover',target:String(d.target||''),output:String(d.output||''),ports:String(d.ports||''),timing:String(d.timing||''),minRate:String(d.minRate||''),maxRetries:String(d.maxRetries||''),reason:!!d.reason,version:!!d.version,scripts:!!d.scripts,os:!!d.os,resolveDns:!!d.resolveDns,lastScanAt:d.lastScanAt||'',lastHostCount:+d.lastHostCount||0};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure31(oldNew());};
C.coerceState=function(raw){return ensure31(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure31(oldMigrate(raw));};
if(C.NAVIGATION30&&C.NAVIGATION30.primary){
  const boxes=C.NAVIGATION30.primary.find(x=>x.id==='boxes');if(boxes){boxes.label='Discover';boxes.help='Discover live hosts with Nmap, ingest scan results, and manage known targets.';}
}
function normalizeTarget31(v){v=String(v||'').trim();return v&&/^[A-Za-z0-9._:/,-]+$/.test(v)?v:'';}
function cleanPorts31(v){v=String(v||'').trim();return v&&/^[0-9,-]+$/.test(v)?v:'';}
function cleanInt31(v){v=String(v||'').trim();return /^\d+$/.test(v)?v:'';}
function shellQuote31(v){return "'"+String(v||'').replace(/'/g,"'\\''")+"'";}
function profile31(id){return PROFILES[id]||PROFILES.discover;}
function buildNmapCommand31(opts){
  opts=opts||{};const p=profile31(opts.profile),target=normalizeTarget31(opts.target);if(!target)return'';
  const timing=/^T[0-5]$/.test(String(opts.timing||''))?String(opts.timing):p.timing;
  const ports=cleanPorts31(opts.ports),tokens=['nmap'];
  const noPing=opts.noPing==null?p.noPing:!!opts.noPing,resolveDns=opts.resolveDns==null?p.resolveDns:!!opts.resolveDns;
  const version=opts.version==null?!!p.version:!!opts.version,scripts=opts.scripts==null?!!p.scripts:!!opts.scripts,os=!!opts.os,reason=!!opts.reason;
  if(p.id==='udp')tokens.push('-sU');
  if(p.id==='discover')tokens.push('-sn');else if(noPing)tokens.push('-Pn');
  if(ports)tokens.push('-p',ports);else if(p.id==='quick')tokens.push('--top-ports','1000');else if(p.id==='full')tokens.push('-p-');else if(p.id==='udp')tokens.push('--top-ports','100');
  if(scripts&&p.id!=='discover')tokens.push('-sC');if(version&&p.id!=='discover')tokens.push('-sV');if(os&&p.id!=='discover')tokens.push('-O');
  if(!resolveDns)tokens.push('-n');if((opts.openOnly==null?p.openOnly:!!opts.openOnly)&&p.id!=='discover')tokens.push('--open');if(reason)tokens.push('--reason');if(timing)tokens.push('-'+timing);
  const min=cleanInt31(opts.minRate||p.minRate),retry=cleanInt31(opts.maxRetries||p.maxRetries);if(min)tokens.push('--min-rate',min);if(retry)tokens.push('--max-retries',retry);
  const output=String(opts.output||p.output||'').trim();if(output)tokens.push('-oA',shellQuote31(output));tokens.push(target);return tokens.join(' ');
}
function scannedHost31(state,h){return !!(h&&C.hasFact&&C.hasFact(state,'scan.initial',{type:'host',id:h.id}));}
function discoveryStatus31(state){
  ensure31(state);const hosts=state.hosts||[],scanned=hosts.filter(h=>scannedHost31(state,h)),active=C.hostForContext?C.hostForContext(state,state.activeContext):null;
  return{hosts:hosts.length,scanned:scanned.length,unscanned:hosts.length-scanned.length,activeScanned:scannedHost31(state,active),lastScanAt:state.ui.discovery31.lastScanAt||'',lastHostCount:state.ui.discovery31.lastHostCount||0};
}
function workspaceOverview31(state,lanes,ctx){
  ensure31(state);const o=oldOverview?oldOverview(state,lanes,ctx):{},d=discoveryStatus31(state);o.discovery=d;o.scannedTargets=d.scanned;
  if(!d.hosts){o.stage='discover';o.next={label:'Run host discovery',href:'#/boxes',detail:'Start with Nmap against the authorized IP, CIDR, or range to identify live hosts.'};}
  else if(o.context&&o.context.type==='host'&&!d.activeScanned&&!o.activities&&!o.facts&&!o.artifacts){o.stage='scan';o.next={label:'Scan this target',href:'#/boxes',detail:'This host exists in the workspace but has no baseline Nmap evidence yet.'};}
  return o;
}
C.ensure31=ensure31;C.NMAP_PROFILES31=PROFILES;C.nmapProfile31=profile31;C.buildNmapCommand31=buildNmapCommand31;C.normalizeNmapTarget31=normalizeTarget31;C.discoveryStatus31=discoveryStatus31;C.workspaceOverview30=workspaceOverview31;C.workspaceOverview31=workspaceOverview31;
C.sanitizedCopy=function(state){return ensure31(oldSanitize(state));};
root.OBOL_CORE_V31={VERSION,PROFILES,ensure31,buildNmapCommand31,discoveryStatus31};
}

/* obol-core-delta: assets/core-v3.2.js */
{
// Obol v3.2 core overlay — single-owner workflow surfaces, clearer target semantics, and attention-focused Home guidance.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.2.js');
const VERSION='3.2.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldOverview=C.workspaceOverview31||C.workspaceOverview30;
function ensure32(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure32(oldNew());};
C.coerceState=function(raw){return ensure32(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure32(oldMigrate(raw));};
if(C.NAVIGATION30){
  const primary=C.NAVIGATION30.primary||[],boxes=primary.find(x=>x.id==='boxes'),evidence=primary.find(x=>x.id==='intake');
  if(boxes){boxes.label='Targets';boxes.help='Manage target scope, launch discovery or baseline scanning when needed, and switch the active host.';}
  if(evidence)evidence.help='Review general tool output and structured evidence, then deliberately apply only what you trust.';
  const order=['queue','search','lanes','tools','lineage','map','guide','settings'],secondary=C.NAVIGATION30.secondary||[];
  secondary.sort((a,b)=>order.indexOf(a.id)-order.indexOf(b.id));
}
function attention32(state,lanes,ctx,o){
  o=o||oldOverview(state,lanes,ctx)||{};const items=[],d=o.discovery||{},network=o.network||{},readiness=o.readiness||{total:0,ready:0};
  const broken=(network.paths||[]).filter(x=>x.status==='broken').length,proof=Math.max(0,(readiness.total||0)-(readiness.ready||0)),unscanned=Math.max(0,d.unscanned||0),planned=Math.max(0,o.planned||0);
  if(unscanned)items.push({id:'unscanned',label:unscanned+' target'+(unscanned===1?'':'s')+' without baseline scan evidence',detail:'Scan only when it helps establish or refresh service visibility.',href:'#/boxes',tone:'notice'});
  if(broken)items.push({id:'broken-paths',label:broken+' broken network path'+(broken===1?'':'s'),detail:'Repair or retire stale pivot state before relying on reachability.',href:'#/path',tone:'danger'});
  if(proof)items.push({id:'proof',label:proof+' successful action'+(proof===1?'':'s')+' still need report proof',detail:'Finish evidence and screenshot obligations while the context is fresh.',href:'#/report',tone:'notice'});
  if(planned)items.push({id:'planned',label:planned+' planned action'+(planned===1?'':'s')+' waiting',detail:'Resume operator-selected work without hunting through recommendations.',href:'#/queue',tone:'normal'});
  return{items,unscanned,broken,proof,planned};
}
function workspaceOverview32(state,lanes,ctx){
  ensure32(state);const o=oldOverview?oldOverview(state,lanes,ctx):{},d=o.discovery||{};
  if(!d.hosts){o.stage='setup';o.next={label:'Add or discover a target',href:'#/boxes',detail:'Open Targets to scan an authorized range or add a known host manually.'};}
  else if(o.context&&o.context.type==='host'&&!d.activeScanned&&!o.activities&&!o.facts&&!o.artifacts){o.stage='baseline';o.next={label:'Establish baseline evidence',href:'#/boxes',detail:'This target has no baseline scan evidence yet. Open Targets when a scan is the useful next move.'};}
  o.attention=attention32(state,lanes,ctx,o);return o;
}
C.ensure32=ensure32;C.workspaceAttention32=attention32;C.workspaceOverview30=workspaceOverview32;C.workspaceOverview31=workspaceOverview32;C.workspaceOverview32=workspaceOverview32;
C.sanitizedCopy=function(state){return ensure32(oldSanitize(state));};
root.OBOL_CORE_V32={VERSION,ensure32,attention32};
}

/* obol-core-delta: assets/core-v3.3.js */
{
// Obol v3.3 core overlay — audited command-builder state and current release coercion.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.3.js');
const VERSION='3.3.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure33(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};s.ui.builderHints33=s.ui.builderHints33&&typeof s.ui.builderHints33==='object'?s.ui.builderHints33:{};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure33(oldNew());};
C.coerceState=function(raw){return ensure33(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure33(oldMigrate(raw));};
function selectedOptions33(cmd,st){
 st=st||{selected:{},args:{},radio:{}};const rows=[];
 for(const [i,o] of (cmd&&cmd.opts||[]).entries()){
  const oid=C.optionId(o,i);
  if(o.radio&&st.radio&&st.radio[o.radio]===oid)rows.push({id:oid,option:o,value:o.value});
  else if((o.flag||o.script)&&st.selected&&st.selected[oid])rows.push({id:oid,option:o,value:o.flag||o.script});
  else if(o.arg&&st.args&&String(st.args[oid]||'').trim())rows.push({id:oid,option:o,value:String(st.args[oid]).trim()});
 }
 return rows;
}
function commandOutputs33(cmd,st){return[...new Set(selectedOptions33(cmd,st).map(x=>x.option.result).filter(Boolean))];}
function toolAudit33(){return root.OBOL_TOOL_AUDIT_V33||{version:VERSION,registered:[],commandCount:0,optionedCommands:0,commandTools:[]};}
C.ensure33=ensure33;C.selectedOptions33=selectedOptions33;C.commandOutputs33=commandOutputs33;C.toolAudit33=toolAudit33;
C.sanitizedCopy=function(state){return ensure33(oldSanitize(state));};
root.OBOL_CORE_V33={VERSION,ensure33,selectedOptions33,commandOutputs33,toolAudit33};
}

/* obol-core-delta: assets/core-v3.4.js */
{
// Obol v3.4 core overlay — clearer Next Steps planning, context-rich recommendation summaries, and exact activity lineage handoff.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.4.js');
const VERSION='3.4.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure34(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const old=s.ui.nextSteps34&&typeof s.ui.nextSteps34==='object'?s.ui.nextSteps34:{};
  s.ui.nextSteps34={
    lane:String(old.lane||'all'),
    status:['all','untried','tried','planned'].includes(old.status)?old.status:'all',
    visible:Math.max(5,Math.min(50,+old.visible||8)),
    detailsOpen:!!old.detailsOpen
  };
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure34(oldNew());};
C.coerceState=function(raw){return ensure34(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure34(oldMigrate(raw));};
if(C.NAVIGATION30&&C.NAVIGATION30.primary){
  const path=C.NAVIGATION30.primary.find(x=>x.id==='path');
  if(path){path.label='Next Steps';path.help='Prioritized, evidence-grounded work for the active context, with clear reasons and planning controls.';}
}
function laneName34(lanes,id){
  const l=(lanes||[]).find(x=>x.lane===id);return l?(l.title||l.lane):String(id||'Methodology');
}
function plannedSet34(state,ctx){
  try{return new Set((C.queueFor?C.queueFor(state,ctx,{status:'planned'}):[]).map(x=>x.cardId));}catch(e){return new Set();}
}
function contextArtifacts34(state,ctx){
  const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext));
  return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[])).filter(a=>a.contextKey===key||a.contextKey==='global:global');
}
function signals34(state,row,ctx,planned){
  const out=[],status=C.statusFor(state,row.card.id,ctx);
  if(planned&&planned.has(row.card.id))out.push({id:'planned',label:'planned',tone:'planned'});
  if(row.newly)out.push({id:'new',label:'new evidence',tone:'new'});
  if(status==='tried')out.push({id:'tried',label:'tried before',tone:'tried'});
  if((row.infoGain||0)>0)out.push({id:'info',label:'tests '+row.infoGain+' unknown'+(row.infoGain===1?'':'s'),tone:'info'});
  if((row.unlocks||[]).length)out.push({id:'unlock',label:'unlocks '+row.unlocks.length,tone:'unlock'});
  if(row.reachability&&row.reachability.delta){
    out.push({id:'reach',label:'reachability '+(row.reachability.delta>0?'+':'')+row.reachability.delta,tone:row.reachability.delta>0?'reach':'warn'});
  }
  return out;
}
function targetContext34(state,row,ctx){
  const c=C.normalizeContext(state,ctx||state.activeContext),host=C.hostForContext?C.hostForContext(state,c):null,domain=C.domainForContext?C.domainForContext(state,c):null;
  const label=host?(host.name||host.hostname||host.ip||C.contextLabel(state,c)):domain?(domain.name||C.contextLabel(state,c)):C.contextLabel(state,c);
  const address=host&&host.ip||'',service=row&&row.reachability&&row.reachability.service||'';
  return{label,address,service,contextKey:C.contextKey(c)};
}
function nextStepsOverview34(state,lanes,ctx,opts){
  ensure34(state);opts=opts||{};const c=C.normalizeContext(state,ctx||state.activeContext),prefs=state.ui.nextSteps34;
  const showAll=opts.showAll==null?!!state.ui.pathShowAll:!!opts.showAll,planned=plannedSet34(state,c);
  const ranked=C.rankedApplicable?C.rankedApplicable(state,lanes||[],c,{showAll}):[];
  const lanesAvailable=[...new Set(ranked.map(r=>r.card.lane).filter(Boolean))].map(id=>({id,label:laneName34(lanes,id)})).sort((a,b)=>a.label.localeCompare(b.label));
  if(prefs.lane!=='all'&&!lanesAvailable.some(x=>x.id===prefs.lane))prefs.lane='all';
  let rows=ranked.map((r,i)=>({...r,priority:i+1,laneLabel:laneName34(lanes,r.card.lane),signals:signals34(state,r,c,planned),target:targetContext34(state,r,c),planned:planned.has(r.card.id)}));
  if(prefs.lane!=='all')rows=rows.filter(r=>r.card.lane===prefs.lane);
  if(prefs.status==='untried')rows=rows.filter(r=>C.statusFor(state,r.card.id,c)==='new');
  else if(prefs.status==='tried')rows=rows.filter(r=>C.statusFor(state,r.card.id,c)==='tried');
  else if(prefs.status==='planned')rows=rows.filter(r=>r.planned);
  let coverage={total:0,coverage:0,remaining:0,lanes:[]};try{if(C.coverageSummary)coverage=C.coverageSummary(state,lanes||[],c)||coverage;}catch(e){}
  let network={paths:[],reachabilityCounts:{}};try{if(C.networkSummary)network=C.networkSummary(state,c)||network;}catch(e){}
  let hypotheses=[];try{if(C.hypothesesForContext)hypotheses=C.hypothesesForContext(state,lanes||[],c).filter(x=>['testable','tested','weakened'].includes(x.status));}catch(e){}
  let campaigns=[];try{if(C.credentialCampaigns)campaigns=C.credentialCampaigns(state,lanes||[],c)||[];}catch(e){}
  const facts=(C.effectiveFactRecords?C.effectiveFactRecords(state,c):[]).filter(x=>x.id!=='scope.defined'),artifacts=contextArtifacts34(state,c);
  const broken=(network.paths||[]).filter(x=>x.status==='broken').length,unverified=(network.paths||[]).filter(x=>x.status==='active'&&!x.lastVerifiedAt).length;
  const untestedCredentials=campaigns.reduce((n,x)=>n+(x.untested||[]).length,0);
  return{
    context:c,contextLabel:C.contextLabel(state,c),showAll,prefs,ranked,rows,lanesAvailable,plannedCount:planned.size,
    facts:facts.length,artifacts:artifacts.length,coverage,network,hypotheses,openHypotheses:hypotheses.length,
    untestedCredentials,brokenPaths:broken,unverifiedPaths:unverified,
    latestEvidence:(state.ui&&state.ui.lastEvidenceUpdate)||null
  };
}
function lineageSource34(state,cardId,ctx,command){
  ensure34(state);const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c);
  const latest=(state.activities||[]).filter(a=>a.contextKey===key&&a.cardId===cardId).slice().sort((a,b)=>String(a.at||'').localeCompare(String(b.at||''))).slice(-1)[0]||null;
  return{
    activityId:latest&&latest.id||'',cardId:String(cardId||''),command:String(command||(latest&&latest.command)||'').slice(0,500),
    contextKey:key,source:'card-evidence',at:latest&&latest.at||C.now()
  };
}
C.ensure34=ensure34;C.nextStepsOverview34=nextStepsOverview34;C.nextStepSignals34=signals34;C.nextStepTarget34=targetContext34;C.lineageSource34=lineageSource34;
C.sanitizedCopy=function(state){return ensure34(oldSanitize(state));};
root.OBOL_CORE_V34={VERSION,ensure34,nextStepsOverview34,lineageSource34};
}

/* obol-core-delta: assets/core-v3.5.js */
{
// Obol v3.5 core overlay — evidence activity correction, report readiness semantics, lineage repair, and current release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.5.js');
const VERSION='3.5.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensureReport35(s){
  s.ui=s.ui||{};const old=s.ui.report35&&typeof s.ui.report35==='object'?s.ui.report35:{};
  s.ui.report35={proofFilter:old.proofFilter==='all'?'all':'needs',preview:old.preview==='markdown'?'markdown':'rendered'};
}
function allTyped35(state){return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[]));}
function reconcileActivityLineage35(state){
  if(!state||!Array.isArray(state.activities))return 0;let repaired=0;
  for(const row of allTyped35(state))for(const p of row.producedBy||[]){
    if(p.activityId||!/^(?:network|intake):/i.test(String(p.source||row.source||'')))continue;
    const at=Date.parse(p.at||row.observedAt||'');if(!Number.isFinite(at))continue;
    const candidates=state.activities.filter(a=>a&&a.id&&a.contextKey===(p.contextKey||row.contextKey)&&Number.isFinite(Date.parse(a.at||''))&&Math.abs(Date.parse(a.at)-at)<=5000);
    if(candidates.length!==1)continue;const a=candidates[0];p.activityId=a.id;p.cardId=p.cardId||a.cardId||'';p.command=p.command||String(a.command||'').slice(0,500);repaired++;
  }
  return repaired;
}
function ensure35(s){s=s||{};s.obolVersion=VERSION;ensureReport35(s);reconcileActivityLineage35(s);return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure35(oldNew());};
C.coerceState=function(raw){return ensure35(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure35(oldMigrate(raw));};
function cards35(lanes){const m={};for(const l of lanes||[])for(const c of l.cards||[])m[c.id]=c;return m;}
function activityTransitions35(a){
  const out=new Set(a&&a.transitions||[]);for(const f of a&&a.outcomeFacts||[]){
    if(/^credential\./.test(f)||/^hash\./.test(f))out.add('credential');
    if(/^foothold\./.test(f)||/^shell\./.test(f))out.add('foothold');
    if(/^access\./.test(f))out.add('privilege');
    if(/^pivot\.|^route\.|^port\.forwarded|^host\.dual_homed/.test(f))out.add('network');
    if(/^objective\./.test(f))out.add('objective');
  }
  return [...out];
}
function proofBucket35(state,key){return state.reportEvidence29&&state.reportEvidence29[key]||{};}
function screenshotBucket35(state,key){return state.reportEvidence28&&state.reportEvidence28[key]||{};}
function readinessRow35(state,lanes,a){
  const cmap=cards35(lanes),card=cmap[a.cardId]||{},key=C.activityKey28?C.activityKey28(a):(a.id||[a.contextKey,a.cardId,a.at].join('|'));
  const transitions=activityTransitions35(a),manual=proofBucket35(state,key),legacy=screenshotBucket35(state,key),arts=allTyped35(state);
  const artifactLinks=arts.reduce((n,x)=>n+(x.producedBy||[]).filter(p=>p.activityId===a.id).length,0),evidence=!!String(a.evidence||'').trim(),command=!!String(a.command||'').trim();
  const screenshotRequired=transitions.some(x=>['foothold','privilege','objective'].includes(x));
  const requirements=[{id:'evidence',label:'Evidence snapshot',done:evidence,manual:false},{id:'command',label:'Command snapshot',done:command,manual:false}];
  if(card.report&&card.report.finding)requirements.push({id:'finding-context',label:'Finding metadata',done:true,manual:false});
  if(transitions.includes('credential'))requirements.push({id:'artifact-provenance',label:'Credential/artifact provenance',done:artifactLinks>0,manual:false});
  if(transitions.includes('network')){let active=0;try{active=(C.pathLifecycleSummary(state,a.context||state.activeContext).active||[]).length;}catch(e){}requirements.push({id:'path-record',label:'Explicit active network path',done:active>0,manual:false});}
  if(transitions.includes('foothold')||transitions.includes('privilege')){
    requirements.push({id:'target-visible',label:'Target identity visible in external proof',done:!!manual.targetVisible,manual:true});
    requirements.push({id:'identity-visible',label:'Operator identity visible in external proof',done:!!manual.identityVisible,manual:true});
  }
  if(transitions.includes('privilege'))requirements.push({id:'proof-visible',label:'Proof/local evidence visible',done:!!manual.proofVisible,manual:true});
  if(screenshotRequired)requirements.push({id:'screenshot',label:'Proof screenshot recorded externally',done:!!legacy.screenshot,manual:true,external:true});
  return{key,activity:a,card,transitions,artifactLinks,requirements,missing:requirements.filter(x=>!x.done),ready:requirements.every(x=>x.done),screenshotRequired,finding:card.report&&card.report.finding||'',severity:card.report&&card.report.severity||'',contextLabel:a.contextLabel||a.contextKey};
}
function reportReadiness35(state,lanes){
  ensure35(state);const acts=(state.activities||[]).filter(a=>a.result==='success').slice().sort((a,b)=>String(a.at||'').localeCompare(String(b.at||''))),rows=acts.map(a=>readinessRow35(state,lanes,a));
  let findings=[];try{const R=root.OBOL_REPORT_V2;if(R&&R._findingRows)findings=R._findingRows(state,lanes)||[];}catch(e){}
  let quality=[];try{const R=root.OBOL_REPORT_V2;if(R&&R._qualityChecks)quality=R._qualityChecks(state,lanes)||[];}catch(e){}
  const missingBy={evidence:0,command:0,provenance:0,externalProof:0,path:0};
  for(const r of rows)for(const q of r.missing){if(q.id==='evidence')missingBy.evidence++;else if(q.id==='command')missingBy.command++;else if(q.id==='artifact-provenance')missingBy.provenance++;else if(q.id==='path-record')missingBy.path++;else if(q.manual)missingBy.externalProof++;}
  return{rows,readyRows:rows.filter(x=>x.ready),needsAttention:rows.filter(x=>!x.ready),total:rows.length,ready:rows.filter(x=>x.ready).length,findings,quality,targets:(state.hosts||[]).length,missingBy};
}
C.ensure35=ensure35;C.reconcileActivityLineage35=reconcileActivityLineage35;C.reportReadiness35=reportReadiness35;C.reportReadinessRow35=readinessRow35;C.activityTransitions35=activityTransitions35;
C.sanitizedCopy=function(state){return ensure35(oldSanitize(state));};
root.OBOL_CORE_V35={VERSION,ensure35,reconcileActivityLineage35,reportReadiness35};
}

/* obol-core-delta: assets/core-v3.6.js */
{
// Obol v3.6 core overlay — Rubeus workbench state, current release coercion, and exact-command lineage repair.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.6.js');
const VERSION='3.6.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function catalog36(){return root.OBOL_RUBEUS_V36||{actions:{},fields:{},defaults:()=>({}),build:()=>''};}
function ensureRubeus36(s){
 s.ui=s.ui||{};const cat=catalog36(),ids=Object.keys(cat.actions||{}),old=s.ui.rubeus36&&typeof s.ui.rubeus36==='object'?s.ui.rubeus36:{};
 const action=ids.includes(old.action)?old.action:(ids[0]||'asrep'),values=old.values&&typeof old.values==='object'?old.values:{};
 for(const id of ids)values[id]=values[id]&&typeof values[id]==='object'?values[id]:{};
 s.ui.rubeus36={action,values,lastCommand:String(old.lastCommand||'')};return s.ui.rubeus36;
}
function normCommand36(s){return String(s||'').replace(/\s+/g,' ').trim().toLowerCase();}
function allTyped36(state){return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[]));}
function fillProducer36(p,a){p.activityId=a.id;p.cardId=p.cardId||a.cardId||'';p.command=p.command||String(a.command||'').slice(0,500);}
function reconcileActivityLineage36(state){
 if(!state||!Array.isArray(state.activities))return 0;let repaired=0;
 for(const row of allTyped36(state))for(const p of row.producedBy||[]){
  if(p.activityId)continue;const key=p.contextKey||row.contextKey||'',pc=normCommand36(p.command);
  if(pc){const exact=state.activities.filter(a=>a&&a.id&&a.contextKey===key&&normCommand36(a.command)===pc);if(exact.length===1){fillProducer36(p,exact[0]);repaired++;}continue;}
  if(!/^(?:network|intake|card-evidence)/i.test(String(p.source||row.source||'')))continue;
  const at=Date.parse(p.at||row.observedAt||'');if(!Number.isFinite(at))continue;
  const near=state.activities.filter(a=>a&&a.id&&a.contextKey===key&&Number.isFinite(Date.parse(a.at||''))&&Math.abs(Date.parse(a.at)-at)<=5000);
  if(near.length===1){fillProducer36(p,near[0]);repaired++;}
 }
 return repaired;
}
function ensure36(s){s=s||{};s.obolVersion=VERSION;ensureRubeus36(s);reconcileActivityLineage36(s);return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure36(oldNew());};
C.coerceState=function(raw){return ensure36(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure36(oldMigrate(raw));};
function sourceValue36(state,field,actionValues){
 const params=state.params||{},stored=actionValues&&actionValues[field.id];if(stored!==undefined&&stored!==null&&String(stored)!=='')return stored;
 if(field.id==='authType')return field.default||'rc4';
 if(field.id==='material'){const t=actionValues&&actionValues.authType||'rc4';if(t==='password')return params.password||'';if(t==='rc4')return params.hash||'';if(t==='aes256')return params.aes256||'';}
 if(field.source&&params[field.source])return params[field.source];if(field.fallbackSource&&params[field.fallbackSource])return params[field.fallbackSource];return field.default!=null?field.default:'';
}
function rubeusValues36(state,actionId){
 ensureRubeus36(state);const cat=catalog36(),a=cat.actions[actionId]||cat.actions[state.ui.rubeus36.action];if(!a)return{};const stored=state.ui.rubeus36.values[a.id]||{},out={...(cat.defaults?cat.defaults(a.id):{})};
 for(const id of a.fields||[]){const f=cat.fields[id];if(f)out[id]=sourceValue36(state,f,stored);}
 for(const t of a.toggles||[])out[t.id]=stored[t.id]===undefined?!!t.default:!!stored[t.id];
 return out;
}
function updateRubeus36(state,actionId,patch){ensureRubeus36(state);const cat=catalog36();if(!cat.actions[actionId])return null;state.ui.rubeus36.action=actionId;state.ui.rubeus36.values[actionId]={...(state.ui.rubeus36.values[actionId]||{}),...(patch||{})};state.updatedAt=C.now();return rubeusValues36(state,actionId);}
function rubeusCommand36(state,actionId){const cat=catalog36(),v=rubeusValues36(state,actionId);return cat.build?cat.build(actionId,v):'';}
C.ensure36=ensure36;C.ensureRubeus36=ensureRubeus36;C.rubeusValues36=rubeusValues36;C.updateRubeus36=updateRubeus36;C.rubeusCommand36=rubeusCommand36;C.reconcileActivityLineage36=reconcileActivityLineage36;
// v3.5 runtime calls this historical name. Point it at the stricter current reconciler so ambiguous command lineage stays unresolved.
C.reconcileActivityLineage35=reconcileActivityLineage36;
C.sanitizedCopy=function(state){return ensure36(oldSanitize(state));};
root.OBOL_CORE_V36={VERSION,ensure36,ensureRubeus36,rubeusValues36,reconcileActivityLineage36};
}

/* obol-core-delta: assets/core-v3.7.js */
{
// Obol v3.7 core overlay — target-specific reachability, path freshness, consumer lineage repair, and multi-hop compromise chains.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.7.js');
const VERSION='3.7.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRanked=C.rankedApplicable,oldSanitize=C.sanitizedCopy;
const FRESH_MS=30*60*1000,AGING_MS=4*60*60*1000;
function ensure37(s){
 s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
 const old=s.ui.lineage37&&typeof s.ui.lineage37==='object'?s.ui.lineage37:{};
 s.ui.lineage37={maxDepth:Math.max(2,Math.min(10,+old.maxDepth||6)),showObserved:old.showObserved!==false};
 if(s.networkModel&&Array.isArray(s.networkModel.paths))for(const p of s.networkModel.paths){p.lastVerifiedAt=String(p.lastVerifiedAt||'');p.status=['active','inactive','broken'].includes(p.status)?p.status:'inactive';}
 reconcileConsumerLineage37(s);return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure37(oldNew());};
C.coerceState=function(raw){return ensure37(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure37(oldMigrate(raw));};
function nowMs37(v){const n=Date.parse(v||'');return Number.isFinite(n)?n:Date.now();}
function pathVerification37(path,at){
 if(!path)return{state:'current',ageMs:0,label:'current context'};
 if(path.status==='broken')return{state:'broken',ageMs:Infinity,label:'broken'};
 if(path.status!=='active')return{state:'inactive',ageMs:Infinity,label:'inactive'};
 if(!path.lastVerifiedAt)return{state:'unverified',ageMs:Infinity,label:'not verified'};
 const age=Math.max(0,nowMs37(at)-Date.parse(path.lastVerifiedAt));
 if(age<=FRESH_MS)return{state:'fresh',ageMs:age,label:'fresh'};
 if(age<=AGING_MS)return{state:'aging',ageMs:age,label:'aging'};
 return{state:'stale',ageMs:age,label:'stale'};
}
function obsService37(o){
 const explicit=String(o&&o.service||'').toLowerCase();if(explicit)return explicit;
 const d=String(o&&o.destination||'').toLowerCase();const m=d.match(/^([a-z0-9_-]+)(?::\d+)?$/);return m?m[1]:'';
}
function cardText37(card){return [card&&card.id,card&&card.title,card&&card.lane,card&&card.workflow,...(card&&card.commands||[]).flatMap(x=>[x.tool,x.run,x.note])].join(' ').toLowerCase();}
function pathForAddress37(state,address,ctx,status){
 const rows=C.networkPaths?C.networkPaths(state,ctx||state.activeContext,{}):[];
 return rows.find(p=>(!status||p.status===status)&&C.cidrContains&&C.cidrContains(p.network,address))||null;
}
function serviceTargets37(state,service,ctx){
 const c=C.normalizeContext(state,ctx||state.activeContext),n=C.networkSummary?C.networkSummary(state,c):{observations:[]},seen=new Set(),out=[];
 for(const o of n.observations||[]){const address=String(o.address||'').trim();if(!address||seen.has(address))continue;const os=obsService37(o);if(service&&os&&os!==service)continue;if(service&&!os)continue;seen.add(address);
   const r=C.reachabilityFor?C.reachabilityFor(state,address,c):{state:'unknown',via:null},broken=pathForAddress37(state,address,c,'broken'),verification=pathVerification37(r.via);
   out.push({address,service:os||service||'',state:r.state,via:r.via||null,brokenPath:broken,verification});
 }
 return out.sort((a,b)=>{const order={direct:0,pivot:1,observed:2,unknown:3};return (order[a.state]??9)-(order[b.state]??9)||a.address.localeCompare(b.address);});
}
const REMOTE37=new Set(['smb','ldap','kerberos','web','ssh','ftp','nfs','mssql','mysql','postgresql','rdp','winrm','snmp']);
function targetReachabilitySignal37(state,card,ctx){
 const service=C.serviceForCard?C.serviceForCard(card):'',targets=serviceTargets37(state,service,ctx),text=cardText37(card),pivotSpecific=/proxychains|socks|ligolo|chisel|pivot|tunnel|port[- ]?forward/.test(text)||service==='pivot';
 const pivot=targets.filter(x=>x.state==='pivot'),direct=targets.filter(x=>x.state==='direct'),observed=targets.filter(x=>x.state==='observed'),freshPivot=pivot.filter(x=>x.verification.state==='fresh'),agingPivot=pivot.filter(x=>x.verification.state==='aging'),stalePivot=pivot.filter(x=>['stale','unverified'].includes(x.verification.state)),broken=targets.filter(x=>x.brokenPath),activePivots=(C.networkPaths?C.networkPaths(state,ctx||state.activeContext,{activeOnly:true}):[]).filter(x=>x.mode==='pivot');
 let delta=0,reason='';
 if(REMOTE37.has(service)&&freshPivot.length){delta=Math.min(34,20+freshPivot.length*4);reason='fresh pivot reachability covers '+freshPivot.map(x=>x.address).slice(0,3).join(', ')+(freshPivot.length>3?' +'+(freshPivot.length-3)+' more':'');}
 else if(REMOTE37.has(service)&&agingPivot.length){delta=Math.min(20,10+agingPivot.length*3);reason='aging pivot verification covers '+agingPivot.map(x=>x.address).slice(0,3).join(', ')+'; re-verify before relying on it';}
 else if(REMOTE37.has(service)&&direct.length){delta=Math.min(18,10+direct.length*2);reason='explicit direct reachability covers '+direct.map(x=>x.address).slice(0,3).join(', ');}
 else if(REMOTE37.has(service)&&stalePivot.length){delta=0;reason='matching '+service+' target'+(stalePivot.length===1?' is':'s are')+' behind stale or unverified pivot state';}
 else if(REMOTE37.has(service)&&observed.length){delta=0;reason='matching '+service+' target'+(observed.length===1?' is':'s are')+' observed but not explicitly reachable';}
 if(pivotSpecific&&service!=='pivot'&&activePivots.length&&!reason){delta+=6;reason='an explicit pivot is active, but no service-specific target match is grounded';}
 if(service==='pivot'&&broken.length){delta+=22;reason='a path covering observed target'+(broken.length===1?' is':'s are')+' broken and needs repair';}
 else if(service==='pivot'&&activePivots.length){delta-=12;reason='a pivot is already active; verify or use it before creating another';}
 return{service,delta,reason,targets,pivotMatches:pivot.length,directMatches:direct.length,observedOnly:observed.length,freshPivotCount:freshPivot.length,agingPivotCount:agingPivot.length,stalePivotCount:stalePivot.length,brokenPivotCount:broken.length,activePivotCount:activePivots.length};
}
function stripReachReason37(why,oldReason){why=String(why||'');oldReason=String(oldReason||'');if(!oldReason)return why;const suffix='; '+oldReason;if(why.endsWith(suffix))return why.slice(0,-suffix.length);if(why===oldReason)return'';return why;}
if(oldRanked)C.rankedApplicable=function(state,lanes,ctx,opts){
 const rows=oldRanked(state,lanes,ctx,opts||{}).map(r=>{const oldSig=r.reachability||{},sig=targetReachabilitySignal37(state,r.card,ctx),baseScore=r.score-(oldSig.delta||0),baseWhy=stripReachReason37(r.why,oldSig.reason);return{...r,score:baseScore+sig.delta,reachability:sig,why:sig.reason?(baseWhy?baseWhy+'; ':'')+sig.reason:baseWhy};});
 return rows.sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
};
function allTyped37(state){return (C.TYPED_ARTIFACT_KINDS||[]).flatMap(k=>((state.typedArtifacts||{})[k]||[]));}
function reconcileConsumerLineage37(state){
 if(!state||!Array.isArray(state.activities))return 0;let repaired=0;
 for(const row of allTyped37(state))for(const c of row.consumedBy||[]){
   if(c.activityId||!c.cardId)continue;const key=c.contextKey||row.contextKey||'',at=Date.parse(c.at||'');if(!Number.isFinite(at))continue;
   const matches=state.activities.filter(a=>a&&a.id&&a.cardId===c.cardId&&a.contextKey===key&&Number.isFinite(Date.parse(a.at||''))&&Math.abs(Date.parse(a.at)-at)<=5000);
   if(matches.length===1){c.activityId=matches[0].id;repaired++;}
 }
 if(repaired)state.updatedAt=C.now();return repaired;
}
function graph37(state,ctx){return C.lineageDependencyGraph?C.lineageDependencyGraph(state,ctx||state.activeContext):{nodes:[],edges:[]};}
function compromiseChains37(state,ctx,opts){
 opts=opts||{};const g=graph37(state,ctx),maxDepth=Math.max(2,Math.min(10,+opts.maxDepth||((state.ui&&state.ui.lineage37&&state.ui.lineage37.maxDepth)||6))),adj=new Map(),indeg=new Map();
 for(const n of g.nodes){adj.set(n.id,[]);indeg.set(n.id,0);}for(const e of g.edges){if(!adj.has(e.from))adj.set(e.from,[]);adj.get(e.from).push(e);indeg.set(e.to,(indeg.get(e.to)||0)+1);}
 let roots=g.nodes.filter(n=>(indeg.get(n.id)||0)===0).map(n=>n.id);if(!roots.length)roots=g.nodes.map(n=>n.id);const chains=[];
 function walk(nodeId,nodes,edges,seen){const next=(adj.get(nodeId)||[]).filter(e=>!seen.has(e.to));if(!next.length||edges.length>=maxDepth){if(edges.length)chains.push({artifactIds:nodes.slice(),edges:edges.slice(),hops:edges.length});return;}for(const e of next){const s=new Set(seen);s.add(e.to);walk(e.to,nodes.concat(e.to),edges.concat(e),s);}}
 for(const r of roots)walk(r,[r],[],new Set([r]));
 const uniq=new Map();for(const c of chains){const k=c.artifactIds.join('>')+'|'+c.edges.map(e=>e.cardId).join('>');if(!uniq.has(k))uniq.set(k,c);}return [...uniq.values()].sort((a,b)=>b.hops-a.hops||a.artifactIds.join('|').localeCompare(b.artifactIds.join('|'))).slice(0,40);
}
function artifactNeighborhood37(state,id,ctx,depth){
 const g=graph37(state,ctx),limit=Math.max(1,Math.min(6,+depth||3)),forward=new Map(),back=new Map();for(const e of g.edges){if(!forward.has(e.from))forward.set(e.from,[]);forward.get(e.from).push(e);if(!back.has(e.to))back.set(e.to,[]);back.get(e.to).push(e);}
 function crawl(map,start){const q=[{id:start,d:0}],seen=new Set([start]),nodes=[],edges=[];while(q.length){const cur=q.shift();if(cur.d>=limit)continue;for(const e of map.get(cur.id)||[]){const next=map===forward?e.to:e.from;edges.push(e);if(!seen.has(next)){seen.add(next);nodes.push(next);q.push({id:next,d:cur.d+1});}}}return{artifactIds:nodes,edges};}
 return{upstream:crawl(back,id),downstream:crawl(forward,id)};
}
C.ensure37=ensure37;C.pathVerification37=pathVerification37;C.serviceTargets37=serviceTargets37;C.targetReachabilitySignal37=targetReachabilitySignal37;C.reconcileConsumerLineage37=reconcileConsumerLineage37;C.compromiseChains37=compromiseChains37;C.artifactNeighborhood37=artifactNeighborhood37;
C.cardReachabilitySignal=targetReachabilitySignal37;
C.sanitizedCopy=function(state){return ensure37(oldSanitize(state));};
root.OBOL_CORE_V37={VERSION,ensure37,pathVerification37,targetReachabilitySignal37,reconcileConsumerLineage37,compromiseChains37,artifactNeighborhood37};
}

/* obol-core-delta: assets/core-v3.8.js */
{
// Obol v3.8 core overlay — pivot operational state, transition-aware compromise summaries, and proof-template depth.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.8.js');
const VERSION='3.8.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRanked=C.rankedApplicable,oldReadiness=C.reportReadiness35,oldSanitize=C.sanitizedCopy;
function ensure38(s){
 s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};s.networkModel=s.networkModel&&typeof s.networkModel==='object'?s.networkModel:{};s.networkModel.paths=Array.isArray(s.networkModel.paths)?s.networkModel.paths:[];s.networkModel.pathHistory=Array.isArray(s.networkModel.pathHistory)?s.networkModel.pathHistory:[];
 for(const p of s.networkModel.paths){p.sourceInterface=String(p.sourceInterface||'');p.listenerHealth=['unknown','healthy','degraded','down'].includes(p.listenerHealth)?p.listenerHealth:'unknown';p.listenerNote=String(p.listenerNote||'');p.lastHealthAt=String(p.lastHealthAt||'');}
 return s;
}
C.VERSION=VERSION;C.newState=function(){return ensure38(oldNew());};C.coerceState=function(raw){return ensure38(oldCoerce(raw));};C.migrateV1=function(raw){return ensure38(oldMigrate(raw));};
function pathById38(state,id){ensure38(state);return(state.networkModel.paths||[]).find(p=>p.id===id)||null;}
function pathEvent38(state,id,kind,note,extra){ensure38(state);const p=pathById38(state,id);if(!p)return null;const row={id:C.uid('path-event'),pathId:id,at:C.now(),kind:String(kind||'note'),note:String(note||'').slice(0,700),status:p.status||'',listenerHealth:p.listenerHealth||'unknown',sourceInterface:p.sourceInterface||'',...(extra||{})};state.networkModel.pathHistory.push(row);state.networkModel.pathHistory=state.networkModel.pathHistory.slice(-500);state.updatedAt=C.now();return row;}
function pathHistory38(state,id){ensure38(state);return state.networkModel.pathHistory.filter(x=>x.pathId===id).slice().sort((a,b)=>String(a.at).localeCompare(String(b.at)));}
function updatePivotOperations38(state,id,patch,opts){ensure38(state);patch=patch||{};opts=opts||{};const p=pathById38(state,id);if(!p)return null;
 if(patch.sourceInterface!=null)p.sourceInterface=String(patch.sourceInterface).trim();if(['unknown','healthy','degraded','down'].includes(patch.listenerHealth))p.listenerHealth=patch.listenerHealth;if(patch.listenerNote!=null)p.listenerNote=String(patch.listenerNote).trim().slice(0,700);if(patch.listenerHealth||patch.listenerNote!=null)p.lastHealthAt=opts.at||C.now();if(opts.verified)p.lastVerifiedAt=opts.at||C.now();p.updatedAt=C.now();state.updatedAt=C.now();pathEvent38(state,id,opts.kind||'operational-check',opts.note||patch.listenerNote||'',{});return p;
}
function pathOperational38(path,at){const verification=C.pathVerification37?C.pathVerification37(path,at):{state:'unverified',label:'not verified'};const health=path&&path.listenerHealth||'unknown';let healthScale=1;if(health==='unknown')healthScale=.85;else if(health==='degraded')healthScale=.5;else if(health==='down')healthScale=0;let verifyScale=1;if(verification.state==='aging')verifyScale=.65;else if(['stale','unverified','broken','inactive'].includes(verification.state))verifyScale=0;return{verification,health,healthScale,verifyScale,scale:healthScale*verifyScale,operational:!!path&&path.status==='active'&&health!=='down'&&verifyScale>0};}
function stripReason38(why,reason){why=String(why||'');reason=String(reason||'');if(!reason)return why;const suffix='; '+reason;if(why.endsWith(suffix))return why.slice(0,-suffix.length);return why===reason?'':why;}
function targetReachabilitySignal38(state,card,ctx){const base=C.targetReachabilitySignal37?C.targetReachabilitySignal37(state,card,ctx):(C.cardReachabilitySignal?C.cardReachabilitySignal(state,card,ctx):{service:'',delta:0,reason:'',targets:[]});const sig={...base,targets:(base.targets||[]).map(t=>({...t,operational:t.via?pathOperational38(t.via):null}))};const piv=sig.targets.filter(t=>t.state==='pivot'&&t.via),best=piv.reduce((m,t)=>Math.max(m,t.operational?t.operational.scale:0),0);
 if(piv.length&&sig.delta>0){sig.delta=Math.round(sig.delta*best);const bad=piv.filter(t=>t.operational&&t.operational.health==='down');const degraded=piv.filter(t=>t.operational&&t.operational.health==='degraded');if(bad.length&&!best)sig.reason='matching '+sig.service+' target is behind a pivot whose listener is recorded down';else if(degraded.length)sig.reason=(sig.reason?sig.reason+'; ':'')+'listener health is degraded';else if(best>0&&best<1)sig.reason=(sig.reason?sig.reason+'; ':'')+'operational confidence reduced by listener health or verification age';}
 return sig;
}
if(oldRanked)C.rankedApplicable=function(state,lanes,ctx,opts){const rows=oldRanked(state,lanes,ctx,opts||{}).map(r=>{const oldSig=r.reachability||{},sig=targetReachabilitySignal38(state,r.card,ctx),baseScore=r.score-(oldSig.delta||0),baseWhy=stripReason38(r.why,oldSig.reason);return{...r,score:baseScore+(sig.delta||0),reachability:sig,why:sig.reason?(baseWhy?baseWhy+'; ':'')+sig.reason:baseWhy};});return rows.sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));};
function transitionForEdge38(state,edge){const from=C.artifactById&&C.artifactById(state,edge.from),to=C.artifactById&&C.artifactById(state,edge.to),ids=[];for(const x of from&&from.consumedBy||[])if(x.cardId===edge.cardId&&x.activityId)ids.push(x.activityId);for(const x of to&&to.producedBy||[])if(x.cardId===edge.cardId&&x.activityId)ids.push(x.activityId);const uniq=[...new Set(ids)],a=uniq.length===1?(state.activities||[]).find(x=>x.id===uniq[0]):null;return{activityId:a&&a.id||'',transitions:a&&a.transitions||[],outcomeFacts:a&&a.outcomeFacts||[],result:a&&a.result||'',grounded:!!a};}
function compromiseTransitionChains38(state,ctx,opts){const chains=C.compromiseChains37?C.compromiseChains37(state,ctx,opts):[];return chains.map(c=>({...c,transitionSummary:c.edges.map(e=>({edge:e,...transitionForEdge38(state,e)}))}));}
function addReq38(row,q){if(!row.requirements.some(x=>x.id===q.id))row.requirements.push(q);}
function reportReadiness38(state,lanes){const base=oldReadiness?oldReadiness(state,lanes):{rows:[],missingBy:{}};for(const row of base.rows||[]){const a=row.activity||{},facts=a.outcomeFacts||[],trans=row.transitions||[];if(trans.includes('foothold'))addReq38(row,{id:'foothold-outcome',label:'Foothold outcome fact recorded',done:facts.some(f=>/^foothold\.|^shell\./.test(f)),manual:false});if(trans.includes('privilege'))addReq38(row,{id:'privilege-outcome',label:'Privilege outcome fact recorded',done:facts.some(f=>/^access\./.test(f)),manual:false});if(trans.includes('objective'))addReq38(row,{id:'objective-outcome',label:'Objective outcome fact recorded',done:facts.some(f=>/^objective\./.test(f)),manual:false});if(trans.includes('network')){const paths=C.networkPaths?C.networkPaths(state,a.context||state.activeContext,{}):[],ok=paths.some(p=>pathOperational38(p).operational);addReq38(row,{id:'path-operational',label:'Network path operationally verified',done:ok,manual:false});}row.missing=row.requirements.filter(x=>!x.done);row.ready=row.missing.length===0;}
 const rows=base.rows||[],missingBy={...(base.missingBy||{})};missingBy.operationalPath=rows.filter(r=>r.requirements.some(q=>q.id==='path-operational'&&!q.done)).length;return{...base,rows,readyRows:rows.filter(x=>x.ready),needsAttention:rows.filter(x=>!x.ready),ready:rows.filter(x=>x.ready).length,missingBy};}
C.ensure38=ensure38;C.pathById38=pathById38;C.recordPathEvent38=pathEvent38;C.pathHistory38=pathHistory38;C.updatePivotOperations38=updatePivotOperations38;C.pathOperational38=pathOperational38;C.targetReachabilitySignal38=targetReachabilitySignal38;C.compromiseTransitionChains38=compromiseTransitionChains38;C.reportReadiness38=reportReadiness38;C.cardReachabilitySignal=targetReachabilitySignal38;C.reportReadiness35=reportReadiness38;C.sanitizedCopy=function(state){return ensure38(oldSanitize(state));};
root.OBOL_CORE_V38={VERSION,ensure38,pathOperational38,targetReachabilitySignal38,compromiseTransitionChains38,reportReadiness38};
}

/* obol-core-delta: assets/core-v3.9.js */
{
// Obol v3.9 core overlay — Evidence coverage state, tool-family summaries, and current release state.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v3.9.js');
const VERSION='3.9.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure39(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const old=s.ui.evidence39&&typeof s.ui.evidence39==='object'?s.ui.evidence39:{};
  s.ui.evidence39={showCoverage:old.showCoverage!==false};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure39(oldNew());};
C.coerceState=function(raw){return ensure39(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure39(oldMigrate(raw));};
function norm39(s){return String(s||'').toLowerCase().replace(/\s+/g,' ').trim();}
function toolFamily39(command){
  const c=norm39(command);
  if(/(?:impacket-)?getnpusers|\bgetnpusers\.py\b/.test(c))return'impacket-asrep';
  if(/(?:impacket-)?getuserspns|\bgetuserspns\.py\b/.test(c))return'impacket-kerberoast';
  if(/(?:impacket-)?gettgt|\bgettgt\.py\b/.test(c))return'impacket-tgt';
  if(/impacket-secretsdump|\bsecretsdump\.py\b/.test(c))return'impacket-secretsdump';
  if(/impacket-(?:psexec|wmiexec|smbexec|atexec)|\b(?:psexec|wmiexec|smbexec|atexec)\.py\b/.test(c))return'impacket-remote-exec';
  if(/\blinpeas(?:\.sh)?\b/.test(c))return'peas-linux';
  if(/\bwinpeas(?:x64|x86)?(?:\.exe)?\b/.test(c))return'peas-windows';
  if(/\bsqlmap(?:\.py)?\b/.test(c))return'sqlmap';
  if(/\bcertipy\b/.test(c))return'certipy';
  if(/\brubeus(?:\.exe)?\b/.test(c))return'rubeus';
  if(/\bnxc\b|\bnetexec\b/.test(c))return'netexec';
  return'';
}
function evidenceCoverageSummary39(state,ctx){
  ensure39(state);const key=C.contextKey(C.normalizeContext(state,ctx||state.activeContext)),groups={};
  for(const a of state.activities||[]){if(a.contextKey!==key)continue;const family=toolFamily39(a.command);if(!family)continue;const g=groups[family]||(groups[family]={family,total:0,success:0,tried:0,failed:0,latestAt:'',cards:new Set()});g.total++;if(a.result==='success')g.success++;else if(a.result==='failed')g.failed++;else g.tried++;if(a.cardId)g.cards.add(a.cardId);if(String(a.at||'')>g.latestAt)g.latestAt=String(a.at||'');}
  return Object.values(groups).map(g=>({...g,cards:[...g.cards]})).sort((a,b)=>b.total-a.total||a.family.localeCompare(b.family));
}
C.ensure39=ensure39;C.toolFamily39=toolFamily39;C.evidenceCoverageSummary39=evidenceCoverageSummary39;
C.sanitizedCopy=function(state){return ensure39(oldSanitize(state));};
root.OBOL_CORE_V39={VERSION,ensure39,toolFamily39,evidenceCoverageSummary39};
}

/* obol-core-delta: assets/core-v4.0.js */
{
// Obol v4.0 core overlay — operator execution context, platform-aware Path signals, and execution provenance.
const C=root.OBOL_CORE_V2;if(!C)throw new Error('Obol v2 core is required before core-v4.0.js');
const VERSION='4.0.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRanked=C.rankedApplicable,oldRecord=C.recordActivity,oldSanitize=C.sanitizedCopy;
const MODES=new Set(['either','kali','windows']);
const KALI_TOOLS=new Set(['nxc','netexec','bloodhound-python','certipy','evil-winrm','xfreerdp','impacket-psexec','impacket-wmiexec','impacket-smbexec','impacket-atexec','impacket-secretsdump','impacket-getnpusers','impacket-getuserspns','impacket-gettgt','impacket-getst','ldapsearch','kerbrute','hashcat','john','hydra','responder','ffuf','feroxbuster','gobuster','sqlmap','smbclient','smbmap','enum4linux','enum4linux-ng','rpcclient']);
const WINDOWS_TOOLS=new Set(['rubeus','mimikatz','sharphound','powerview','powershell','cmd','wmic','reg','sc','msiexec','accesschk','psexec','godpotato','winpeas','winpeasx64','winpeasx86','setspn','nltest']);
function ensure40(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const old=s.ui.operator40&&typeof s.ui.operator40==='object'?s.ui.operator40:{};
  const by=old.byContext&&typeof old.byContext==='object'?old.byContext:{};
  s.ui.operator40={byContext:{...by},showCommandContext:old.showCommandContext!==false};
  for(const k of Object.keys(s.ui.operator40.byContext))if(!MODES.has(s.ui.operator40.byContext[k]))delete s.ui.operator40.byContext[k];
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure40(oldNew());};
C.coerceState=function(raw){return ensure40(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure40(oldMigrate(raw));};
function contextKey40(state,ctx){return C.contextKey(C.normalizeContext(state,ctx||state.activeContext));}
function operatorMode40(state,ctx){ensure40(state);return state.ui.operator40.byContext[contextKey40(state,ctx)]||'either';}
function setOperatorMode40(state,ctx,mode){ensure40(state);mode=MODES.has(mode)?mode:'either';state.ui.operator40.byContext[contextKey40(state,ctx)]=mode;state.updatedAt=C.now();return mode;}
function toolId40(cmd){return String(cmd&&cmd.tool||'').trim().toLowerCase().replace(/\.exe$/,'');}
function variantHint40(cmd){return String(cmd&&cmd.v||'').toLowerCase();}
function commandSurface40(cmd,card){
  if(cmd&&['kali','windows','target','either'].includes(cmd.operatorSurface40))return cmd.operatorSurface40;
  const tool=toolId40(cmd),v=variantHint40(cmd),run=String(cmd&&cmd.run||'').toLowerCase();
  if(KALI_TOOLS.has(tool)||['kali','remote','nxc','impacket','winrm','wmi'].includes(v))return'kali';
  if(WINDOWS_TOOLS.has(tool)||['win','windows','exe'].includes(v))return'windows';
  if(/\b(?:rubeus|mimikatz|sharphound)(?:\.exe)?\b|\bget-(?:net|domain)|\benter-pssession\b|\bnet\s+(?:user|group)\b|\bnltest\b|\bsetspn\b|[a-z]:\\/.test(run))return'windows';
  if(/\bimpacket-|\bnxc\b|\bnetexec\b|\bevil-winrm\b|\bxfreerdp\b|\bbloodhound-python\b|\bcertipy\b|\bproxychains|\/usr\/share\//.test(run))return'kali';
  if(tool==='linpeas'||/\blinpeas(?:\.sh)?\b/.test(run))return'target';
  if(tool==='sh'&&card&&String(card.lane||'').includes('linux'))return'target';
  return'either';
}
function executionSurfaceFromCommand40(command){
  const run=String(command||''),low=run.toLowerCase();
  if(/\b(?:rubeus|mimikatz|sharphound)(?:\.exe)?\b|\bget-(?:net|domain)|\benter-pssession\b|\bnet\s+(?:user|group)\b|\bnltest\b|\bsetspn\b|[a-z]:\\/i.test(run))return'windows';
  if(/\bimpacket-|\bnxc\b|\bnetexec\b|\bevil-winrm\b|\bxfreerdp\b|\bbloodhound-python\b|\bcertipy\b|\bproxychains|\/usr\/share\//.test(low))return'kali';
  return'either';
}
function cardSurfaceSummary40(card){
  const out={kali:0,windows:0,target:0,either:0,total:0,commands:[]};
  for(const [index,cmd] of (card&&card.commands||[]).entries()){const surface=commandSurface40(cmd,card);out[surface]=(out[surface]||0)+1;out.total++;out.commands.push({index,commandId:C.commandId?C.commandId(cmd,index):String(index),tool:cmd.tool||'',surface,variant:cmd.v||''});}
  return out;
}
function operatorSignal40(state,card,ctx){
  const mode=operatorMode40(state,ctx),summary=cardSurfaceSummary40(card);if(mode==='either')return{mode,delta:0,reason:'',summary,matchCount:summary.kali+summary.windows+summary.target+summary.either};
  const match=summary[mode]||0,neutral=(summary.target||0)+(summary.either||0),opposite=mode==='kali'?(summary.windows||0):(summary.kali||0);let delta=0,reason='';
  if(match){delta=8;reason=(mode==='kali'?'Kali-side':'Windows-side')+' implementation available';}
  else if(neutral){delta=0;reason='operator-side-neutral implementation available';}
  else if(opposite){delta=-8;reason='no '+(mode==='kali'?'Kali-side':'Windows-side')+' implementation is identified on this card; technique remains applicable';}
  return{mode,delta,reason,summary,matchCount:match+neutral};
}
if(oldRanked)C.rankedApplicable=function(state,lanes,ctx,opts){
  const rows=oldRanked(state,lanes,ctx,opts||{}).map(r=>{const sig=operatorSignal40(state,r.card,ctx),why=sig.reason?(r.why?r.why+'; ':'')+sig.reason:r.why;return{...r,score:r.score+sig.delta,operatorContext40:sig,why};});
  return rows.sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));
};
if(oldRecord)C.recordActivity=function(state,a){
  ensure40(state);a=a||{};const mode=a.operatorMode40||operatorMode40(state,a.context||state.activeContext),rec=oldRecord(state,a);if(rec){rec.operatorMode40=MODES.has(mode)?mode:'either';rec.executionSurface40=a.executionSurface40||executionSurfaceFromCommand40(rec.command||a.command||'');}
  return rec;
};
C.ensure40=ensure40;C.operatorMode40=operatorMode40;C.setOperatorMode40=setOperatorMode40;C.commandSurface40=commandSurface40;C.executionSurfaceFromCommand40=executionSurfaceFromCommand40;C.cardSurfaceSummary40=cardSurfaceSummary40;C.operatorSignal40=operatorSignal40;
C.sanitizedCopy=function(state){return ensure40(oldSanitize(state));};
root.OBOL_CORE_V40={VERSION,ensure40,operatorMode40,setOperatorMode40,commandSurface40,executionSurfaceFromCommand40,cardSurfaceSummary40,operatorSignal40};
}

/* obol-core-delta: assets/core-v4.1.js */
{
// Obol v4.1 core overlay — auditable Orange mindmap coverage, tool review, and explicit execution-metadata accounting.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V41;if(!C||!M)throw new Error('Obol v4.0 core and v4.1 methodology metadata are required before core-v4.1.js');
const VERSION='4.1.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure41(s){
  s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};
  const old=s.ui.mindmap41&&typeof s.ui.mindmap41==='object'?s.ui.mindmap41:{};
  s.ui.mindmap41={filter:['all','implemented','partial','gap','stale'].includes(old.filter)?old.filter:'all',showTools:old.showTools!==false,showSources:old.showSources!==false};
  return s;
}
C.VERSION=VERSION;C.newState=function(){return ensure41(oldNew());};C.coerceState=function(raw){return ensure41(oldCoerce(raw));};C.migrateV1=function(raw){return ensure41(oldMigrate(raw));};
function cardIndex41(lanes){const m={};for(const l of lanes||[])for(const c of l.cards||[])m[c.id]=c;return m;}
function nodeStatus41(node,cards){const refs=(node.cardIds||[]).map(id=>({id,exists:!!cards[id]}));if(node.coverage==='implemented'&&refs.length&&refs.some(x=>!x.exists))return'stale';return node.coverage||'gap';}
function mindmapCoverage41(lanes){
  const cards=cardIndex41(lanes),areas=[],tot={nodes:0,implemented:0,partial:0,gap:0,stale:0,mappedCards:new Set(),sourceFiles:new Set()};
  for(const area of M.areas||[]){const rows=[];for(const node of area.nodes||[]){const status=nodeStatus41(node,cards),linked=(node.cardIds||[]).filter(id=>cards[id]).map(id=>cards[id]);tot.nodes++;tot[status]=(tot[status]||0)+1;for(const c of linked)tot.mappedCards.add(c.id);if(area.sourceFile)tot.sourceFiles.add(area.sourceFile);rows.push({...node,status,linkedCards:linked.map(c=>({id:c.id,title:c.title,lane:c.lane})),missingCardIds:(node.cardIds||[]).filter(id=>!cards[id]),sourceFile:area.sourceFile,sourceUrl:M.sourceBase+area.sourceFile});}
    const counts={implemented:rows.filter(x=>x.status==='implemented').length,partial:rows.filter(x=>x.status==='partial').length,gap:rows.filter(x=>x.status==='gap').length,stale:rows.filter(x=>x.status==='stale').length};const status=counts.stale?'stale':counts.gap?'gap':counts.partial?'partial':'implemented';areas.push({...area,status,counts,nodes:rows,sourceUrl:M.sourceBase+area.sourceFile});
  }
  return{mindmapUrl:M.mindmapUrl,version:M.version,areas,nodes:tot.nodes,implemented:tot.implemented,partial:tot.partial,gap:tot.gap,stale:tot.stale,mappedCards:[...tot.mappedCards],sourceFiles:[...tot.sourceFiles],coveragePct:tot.nodes?Math.round(tot.implemented/tot.nodes*100):0,auditedPct:tot.nodes?Math.round((tot.implemented+tot.partial)/tot.nodes*100):0};
}
function toolAudit41(lanes){const coverage=mindmapCoverage41(lanes),rows=[];for(const a of coverage.areas)for(const n of a.nodes){const t=n.toolReview;if(!t)continue;rows.push({areaId:a.id,areaLabel:a.label,nodeId:n.id,nodeLabel:n.label,status:n.status,decision:t.decision||'review',mindmap:[...(t.mindmap||[])],preferred:[...(t.preferred||[])],note:t.note||''});}const counts={keep:0,supplement:0,replace:0,review:0};for(const r of rows)counts[r.decision]=(counts[r.decision]||0)+1;return{rows,counts};}
function executionMetadataAudit41(lanes){const cards=cardIndex41(lanes),ids=new Set((M.areas||[]).flatMap(a=>(a.nodes||[]).flatMap(n=>n.cardIds||[]))),rows=[];const totals={commands:0,explicit:0,fallback:0,kali:0,windows:0,target:0,either:0};for(const id of ids){const card=cards[id];if(!card)continue;for(const [index,cmd] of (card.commands||[]).entries()){const surface=C.commandSurface40?C.commandSurface40(cmd,card):(cmd.operatorSurface40||'either'),explicit=!!cmd.operatorSurface40,source=cmd.operatorSurface41Source||(explicit?'preexisting-explicit':'v4.0-fallback');totals.commands++;totals[explicit?'explicit':'fallback']++;totals[surface]=(totals[surface]||0)+1;rows.push({cardId:id,cardTitle:card.title,index,tool:cmd.tool||'',surface,explicit,source});}}
 return{rows,...totals,explicitPct:totals.commands?Math.round(totals.explicit/totals.commands*100):0};}
function priorityGaps41(lanes){const c=mindmapCoverage41(lanes),out=[];for(const a of c.areas)for(const n of a.nodes)if(['gap','partial','stale'].includes(n.status))out.push({areaId:a.id,areaLabel:a.label,nodeId:n.id,label:n.label,status:n.status,priority:a.priority||0,linkedCards:n.linkedCards,sourceUrl:n.sourceUrl,toolReview:n.toolReview||null});return out.sort((a,b)=>b.priority-a.priority||({stale:0,gap:1,partial:2}[a.status]??9)-({stale:0,gap:1,partial:2}[b.status]??9)||a.label.localeCompare(b.label));}
C.ensure41=ensure41;C.mindmapCoverage41=mindmapCoverage41;C.mindmapToolAudit41=toolAudit41;C.executionMetadataAudit41=executionMetadataAudit41;C.mindmapPriorityGaps41=priorityGaps41;
C.sanitizedCopy=function(state){return ensure41(oldSanitize(state));};
root.OBOL_CORE_V41={VERSION,ensure41,mindmapCoverage41,toolAudit41,executionMetadataAudit41,priorityGaps41};
}

/* obol-core-delta: assets/core-v4.2.js */
{
// Obol v4.2 core overlay — canonical Orange 2025.03 snapshot coverage and durable completion accounting.
const C=root.OBOL_CORE_V2,O=root.OBOL_ORANGE_AD_2025_03;if(!C||!O)throw new Error('Obol core and canonical Orange AD snapshot are required before core-v4.2.js');
const VERSION='4.2.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure42(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.mindmap42&&typeof s.ui.mindmap42==='object'?s.ui.mindmap42:{};s.ui.mindmap42={filter:['all','implemented','partial','gap','stale'].includes(old.filter)?old.filter:'all',file:old.file||'all',showSources:old.showSources!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure42(oldNew());};C.coerceState=function(raw){return ensure42(oldCoerce(raw));};C.migrateV1=function(raw){return ensure42(oldMigrate(raw));};
function cardIndex42(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function sectionStatus42(s,cards){if(s.status==='implemented'&&(s.cardIds||[]).length&&(s.cardIds||[]).some(id=>!cards[id]))return'stale';return s.status||'gap';}
function mindmapCoverage42(lanes){const cards=cardIndex42(lanes),files=[],tot={sections:0,implemented:0,partial:0,gap:0,stale:0,mappedCards:new Set()};for(const f of O.files||[]){const sections=(f.sections||[]).map(s=>{const status=sectionStatus42(s,cards),linked=(s.cardIds||[]).filter(id=>cards[id]).map(id=>({id,title:cards[id].title,lane:cards[id].lane})),missing=(s.cardIds||[]).filter(id=>!cards[id]);tot.sections++;tot[status]=(tot[status]||0)+1;for(const c of linked)tot.mappedCards.add(c.id);return{...s,status,linkedCards:linked,missingCardIds:missing,sourceFile:f.file,sourceUrl:f.sourceUrl};});const counts={implemented:sections.filter(x=>x.status==='implemented').length,partial:sections.filter(x=>x.status==='partial').length,gap:sections.filter(x=>x.status==='gap').length,stale:sections.filter(x=>x.status==='stale').length};files.push({...f,sections,counts,status:counts.stale?'stale':counts.gap?'gap':counts.partial?'partial':'implemented'});}const represented=tot.implemented+tot.partial;return{version:O.version,snapshotFormat:O.snapshotFormat,upstream:O.upstream,files,supportFiles:O.supportFiles||[],sections:tot.sections,implemented:tot.implemented,partial:tot.partial,gap:tot.gap,stale:tot.stale,mappedCards:[...tot.mappedCards],coveragePct:tot.sections?Math.round(tot.implemented/tot.sections*100):0,representedPct:tot.sections?Math.round(represented/tot.sections*100):0,sourceFileCount:(O.files||[]).length,snapshotFileCount:(O.files||[]).length+(O.supportFiles||[]).length};}
function snapshotIntegrity42(){const files=O.files||[],support=O.supportFiles||[],keys=new Set(),dupes=[];for(const f of files)for(const s of f.sections||[]){if(keys.has(s.key))dupes.push(s.key);keys.add(s.key);}return{ok:!!O.upstream&&!!O.upstream.commit&&!!O.upstream.adTree&&files.length===17&&support.some(x=>x.file==='conf.yml')&&dupes.length===0,methodologyFiles:files.length,supportFiles:support.length,sections:keys.size,duplicates:dupes,commit:O.upstream&&O.upstream.commit||'',tree:O.upstream&&O.upstream.adTree||''};}
function priorityGaps42(lanes){const c=mindmapCoverage42(lanes),rows=[];for(const f of c.files)for(const s of f.sections)if(['gap','partial','stale'].includes(s.status))rows.push({key:s.key,label:s.label,status:s.status,file:f.file,sourceUrl:f.sourceUrl,linkedCards:s.linkedCards,note:s.note||''});const rank={stale:0,gap:1,partial:2};return rows.sort((a,b)=>(rank[a.status]??9)-(rank[b.status]??9)||a.file.localeCompare(b.file)||a.label.localeCompare(b.label));}
function coverageBadge42(lanes){const c=mindmapCoverage42(lanes);return{label:'Orange AD '+c.version,value:c.coveragePct+'%',detail:c.implemented+'/'+c.sections+' canonical sections fully implemented',represented:c.representedPct+'% represented'};}
C.ensure42=ensure42;C.mindmapCoverage42=mindmapCoverage42;C.mindmapPriorityGaps42=priorityGaps42;C.orangeSnapshotIntegrity42=snapshotIntegrity42;C.orangeCoverageBadge42=coverageBadge42;C.sanitizedCopy=function(state){return ensure42(oldSanitize(state));};
root.OBOL_CORE_V42={VERSION,ensure42,mindmapCoverage42,priorityGaps42,snapshotIntegrity42,coverageBadge42};
}

/* obol-core-delta: assets/core-v4.3.js */
{
// Obol v4.3 core overlay — coverage reconciliation metrics, release delta, and audited hash-mode visibility.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V43;if(!C||!M)throw new Error('Obol core and methodology-v4.3 are required before core-v4.3.js');
const VERSION='4.3.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure43(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.mindmap43&&typeof s.ui.mindmap43==='object'?s.ui.mindmap43:{};s.ui.mindmap43={showAdvanced:old.showAdvanced!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure43(oldNew());};C.coerceState=function(raw){return ensure43(oldCoerce(raw));};C.migrateV1=function(raw){return ensure43(oldMigrate(raw));};
function reconciliation43(lanes){const c=C.mindmapCoverage42(lanes),rows=[];for(const f of c.files||[])for(const s of f.sections||[])if(s.advancedIn==='4.3')rows.push({key:s.key,file:f.file,label:s.label,status:s.status,linkedCards:s.linkedCards||[],note:s.note||''});return rows.sort((a,b)=>a.file.localeCompare(b.file)||a.label.localeCompare(b.label));}
function coverageDelta43(lanes){const c=C.mindmapCoverage42(lanes),b=M.baseline42||{};return{baseline:{...b},current:{implemented:c.implemented,partial:c.partial,gap:c.gap,stale:c.stale,coveragePct:c.coveragePct,representedPct:c.representedPct,sections:c.sections},implementedDelta:c.implemented-(b.implemented||0),partialDelta:c.partial-(b.partial||0),gapDelta:c.gap-(b.gap||0),staleDelta:c.stale-(b.stale||0),coveragePointDelta:c.coveragePct-(b.coveragePct||0),representedPointDelta:c.representedPct-(b.representedPct||0),advanced:reconciliation43(lanes)};}
function familySummary43(lanes){const c=C.mindmapCoverage42(lanes);return(c.files||[]).map(f=>({file:f.file,title:f.title,total:f.sections.length,implemented:f.counts.implemented,partial:f.counts.partial,gap:f.counts.gap,stale:f.counts.stale,coveragePct:f.sections.length?Math.round(f.counts.implemented/f.sections.length*100):0,representedPct:f.sections.length?Math.round((f.counts.implemented+f.counts.partial)/f.sections.length*100):0})).sort((a,b)=>a.coveragePct-b.coveragePct||b.gap-a.gap||a.file.localeCompare(b.file));}
function hashModeAudit43(){const rows=(M.hashModes||[]).map(x=>({...x}));return{rows,stock:rows.filter(x=>x.stock).length,external:rows.filter(x=>!x.stock).length,canonical:rows.filter(x=>x.canonical).length};}
function coverageBadge43(lanes){const c=C.mindmapCoverage42(lanes),d=coverageDelta43(lanes);return{label:'Orange AD '+c.version,value:c.coveragePct+'%',detail:c.implemented+'/'+c.sections+' canonical sections fully implemented',represented:c.representedPct+'% represented',delta:(d.implementedDelta>=0?'+':'')+d.implementedDelta+' implemented vs v4.2',coveragePointDelta:d.coveragePointDelta};}
C.ensure43=ensure43;C.coverageReconciliation43=reconciliation43;C.coverageDelta43=coverageDelta43;C.coverageFamilies43=familySummary43;C.hashModeAudit43=hashModeAudit43;C.orangeCoverageBadge43=coverageBadge43;C.sanitizedCopy=function(state){return ensure43(oldSanitize(state));};
root.OBOL_CORE_V43={VERSION,ensure43,reconciliation43,coverageDelta43,familySummary43,hashModeAudit43,coverageBadge43};
}

/* obol-core-delta: assets/core-v4.4.js */
{
// Obol v4.4 core overlay — Orange decision-path progress, context-safe recommendation signals, and Next Steps integration.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V44;if(!C||!M)throw new Error('Obol core and methodology-v4.4 are required before core-v4.4.js');
const VERSION='4.4.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldRanked=C.rankedApplicable,oldNext=C.nextStepsOverview34,oldSanitize=C.sanitizedCopy;
function ensure44(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.decision44&&typeof s.ui.decision44==='object'?s.ui.decision44:{};s.ui.decision44={showGuide:old.showGuide!==false,showTrail:old.showTrail!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure44(oldNew());};C.coerceState=function(raw){return ensure44(oldCoerce(raw));};C.migrateV1=function(raw){return ensure44(oldMigrate(raw));};
function cardIndex44(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function factFloor44(state,ctx){
 const c=C.normalizeContext(state,ctx||state.activeContext),has=id=>{try{return !!(C.hasFact&&C.hasFact(state,id,c));}catch(e){return false;}};
 if(has('hash.krbtgt')||has('loot.ntds'))return 70;
 if(has('access.system')||has('access.admin'))return 60;
 if(has('foothold.windows')||has('foothold.linux')||has('lateral.movement'))return 50;
 if(has('ad.control_paths')||has('credential.certificate')||has('relay.success')||has('ad.computer_added'))return 40;
 if(has('ad.attack_paths')||has('ad.graph.collected'))return 30;
 if(has('credential.available')||has('hash.ntlm')||has('credential.plaintext')||has('kerberos.ticket')||has('kerberos.tickets'))return 20;
 return 10;
}
function decisionProgress44(state,lanes,ctx){
 ensure44(state);const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c),cards=cardIndex44(lanes),trail=[],completedKeys=new Set(),completedCards=new Set();let order=factFloor44(state,c);
 for(const a of state.activities||[]){if(a.contextKey!==key||a.result!=='success')continue;const card=cards[a.cardId],stage=M.cardStage(card);if(!stage)continue;order=Math.max(order,stage.order);completedCards.add(a.cardId);for(const x of card.orange43||[])completedKeys.add(x.key);trail.push({activityId:a.id||'',cardId:a.cardId,cardTitle:card.title,stage:stage.id,stageLabel:stage.label,order:stage.order,at:a.at||''});}
 const stages=M.stages||[],stage=stages.filter(x=>x.order<=order).slice(-1)[0]||stages[0],next=stages.find(x=>x.order>stage.order)||null;
 trail.sort((a,b)=>String(b.at).localeCompare(String(a.at)));
 return{context:c,contextKey:key,stage,nextStage:next,order:stage.order,completedCanonical:[...completedKeys],completedCards:[...completedCards],trail:trail.slice(0,12)};
}
function decisionSignal44(state,card,ctx){
 const stage=M.cardStage(card);if(!stage)return{mapped:false,delta:0,reason:'',stage:null,progress:null,canonicalKeys:[]};const p=decisionProgress44(state,root.OBOL_LANES||[],ctx),status=C.statusFor?C.statusFor(state,card.id,p.context):'new';let delta=0,reason='';
 if(status==='done')reason='Orange path stage already succeeded in this context';
 else if(stage.order===p.stage.order){delta=3;reason='continues the current Orange decision stage';}
 else if(stage.order===p.stage.order+10){delta=7;reason='next Orange decision stage after recorded progress';}
 else if(stage.order===p.stage.order+20){delta=2;reason='near-future Orange branch with live prerequisites';}
 else if(stage.order<p.stage.order){reason='earlier Orange stage remains available as a fallback';}
 else reason='later Orange stage; normal evidence ranking remains authoritative';
 return{mapped:true,delta,reason,stage,progress:p,canonicalKeys:(card.orange44&&card.orange44.canonicalKeys)||[]};
}
if(oldRanked)C.rankedApplicable=function(state,lanes,ctx,opts){const rows=oldRanked(state,lanes,ctx,opts||{}).map(r=>{const sig=decisionSignal44(state,r.card,ctx),why=sig.reason&&sig.delta?(r.why?r.why+'; ':'')+sig.reason:r.why;return{...r,score:r.score+sig.delta,orangeDecision44:sig,why};});return rows.sort((a,b)=>b.score-a.score||a.card.title.localeCompare(b.card.title));};
function decisionQueue44(state,lanes,ctx,opts){opts=opts||{};const p=decisionProgress44(state,lanes,ctx),ranked=C.rankedApplicable?C.rankedApplicable(state,lanes,p.context,{showAll:!!opts.showAll}):[];return ranked.filter(r=>r.orangeDecision44&&r.orangeDecision44.mapped&&C.statusFor(state,r.card.id,p.context)!=='done').slice(0,Math.max(1,Math.min(12,+opts.limit||6))).map((r,i)=>({priority:i+1,cardId:r.card.id,title:r.card.title,lane:r.card.lane,score:r.score,stage:r.orangeDecision44.stage,delta:r.orangeDecision44.delta,reason:r.orangeDecision44.reason,canonicalKeys:r.orangeDecision44.canonicalKeys}));}
if(oldNext)C.nextStepsOverview34=function(state,lanes,ctx,opts){const out=oldNext(state,lanes,ctx,opts||{});out.orangeDecision44={progress:decisionProgress44(state,lanes,out.context),queue:decisionQueue44(state,lanes,out.context,{showAll:out.showAll,limit:6})};return out;};
C.ensure44=ensure44;C.orangeDecisionProgress44=decisionProgress44;C.orangeDecisionSignal44=decisionSignal44;C.orangeDecisionQueue44=decisionQueue44;
C.sanitizedCopy=function(state){return ensure44(oldSanitize(state));};
root.OBOL_CORE_V44={VERSION,ensure44,decisionProgress44,decisionSignal44,decisionQueue44};
}

/* obol-core-delta: assets/core-v4.5.js */
{
// Obol v4.5 core overlay — Orange run/evidence contract coverage and Next Steps readiness.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V45;if(!C||!M)throw new Error('Obol core and methodology-v4.5 are required before core-v4.5.js');
const VERSION='4.5.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldNext=C.nextStepsOverview34,oldSanitize=C.sanitizedCopy;
function ensure45(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.contract45&&typeof s.ui.contract45==='object'?s.ui.contract45:{};s.ui.contract45={showPathReadiness:old.showPathReadiness!==false,showEvidenceCoverage:old.showEvidenceCoverage!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure45(oldNew());};C.coerceState=function(raw){return ensure45(oldCoerce(raw));};C.migrateV1=function(raw){return ensure45(oldMigrate(raw));};
function contractFor45(card){const cmds=card&&card.commands||[],adjustable=cmds.filter(x=>(x.opts||[]).length||x.builder36).length,evidence=!!(card&&card.evidence45);return{cardId:card&&card.id||'',title:card&&card.title||'',stage:card&&card.orange44&&card.orange44.stage||'',commands:cmds.length,adjustableCommands:adjustable,fixedCommands:Math.max(0,cmds.length-adjustable),hasEvidenceProfile:evidence,evidenceFamily:evidence?card.evidence45.family:'',evidenceSource:evidence?card.evidence45.source:'',claims:evidence?[...(card.evidence45.claims||[])]:[],runReady:cmds.length>0,evidenceReady:evidence,loopReady:cmds.length>0&&evidence};}
function contractCoverage45(lanes){const rows=[];for(const l of lanes||[])for(const card of l.cards||[]){if(!card.orange44)continue;rows.push(contractFor45(card));}const totals={mappedCards:rows.length,runReady:0,evidenceReady:0,loopReady:0,commands:0,adjustableCommands:0,fixedCommands:0};for(const r of rows){if(r.runReady)totals.runReady++;if(r.evidenceReady)totals.evidenceReady++;if(r.loopReady)totals.loopReady++;totals.commands+=r.commands;totals.adjustableCommands+=r.adjustableCommands;totals.fixedCommands+=r.fixedCommands;}return{rows,...totals,evidencePct:totals.mappedCards?Math.round(totals.evidenceReady/totals.mappedCards*100):0,loopPct:totals.mappedCards?Math.round(totals.loopReady/totals.mappedCards*100):0,adjustablePct:totals.commands?Math.round(totals.adjustableCommands/totals.commands*100):0};}
function queueReadiness45(state,lanes,ctx){const q=C.orangeDecisionQueue44?C.orangeDecisionQueue44(state,lanes,ctx,{showAll:!!(state.ui&&state.ui.pathShowAll),limit:8}):[],map={};for(const l of lanes||[])for(const c of l.cards||[])map[c.id]=c;return q.map(x=>({...x,contract45:contractFor45(map[x.cardId])}));}
if(oldNext)C.nextStepsOverview34=function(state,lanes,ctx,opts){const out=oldNext(state,lanes,ctx,opts||{});out.orangeContracts45={coverage:contractCoverage45(lanes),queue:queueReadiness45(state,lanes,out.context)};return out;};
C.ensure45=ensure45;C.orangeContractFor45=contractFor45;C.orangeContractCoverage45=contractCoverage45;C.orangeQueueReadiness45=queueReadiness45;
C.sanitizedCopy=function(state){return ensure45(oldSanitize(state));};
root.OBOL_CORE_V45={VERSION,ensure45,contractFor45,contractCoverage45,queueReadiness45};
}

/* obol-core-delta: assets/core-v4.6.js */
{
// Obol v4.6 core overlay — SCCM branch progress, coverage delta, and Next Steps payload.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V46;if(!C||!M)throw new Error('Obol core and methodology-v4.6 are required before core-v4.6.js');
const VERSION='4.6.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldNext=C.nextStepsOverview34,oldSanitize=C.sanitizedCopy;
function ensure46(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.sccm46&&typeof s.ui.sccm46==='object'?s.ui.sccm46:{};s.ui.sccm46={showBranch:old.showBranch!==false,showTrail:old.showTrail!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure46(oldNew());};C.coerceState=function(raw){return ensure46(oldCoerce(raw));};C.migrateV1=function(raw){return ensure46(oldMigrate(raw));};
const ORDER=['recon','credentials','control','execution','cleanup'],LABELS={recon:'Reconnaissance',credentials:'Credential recovery',control:'Relay / takeover',execution:'Administrative execution',cleanup:'Cleanup / post mapping'};
const CARD_STAGE={'sccm-enum':'recon','sccm-credential-recovery':'credentials','sccm-relay-takeover':'control','sccm-admin-exec':'execution','sccm-cleanup-post':'cleanup'};
function sccmProgress46(state,ctx){ensure46(state);const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c),trail=[];let index=-1;for(const a of state.activities||[]){if(a.contextKey!==key||a.result!=='success')continue;const stage=CARD_STAGE[a.cardId];if(!stage)continue;index=Math.max(index,ORDER.indexOf(stage));trail.push({activityId:a.id||'',cardId:a.cardId,stage,label:LABELS[stage],at:a.at||''});}
 const has=id=>{try{return !!(C.hasFact&&C.hasFact(state,id,c));}catch(e){return false;}};
 if(has('sccm.credentials'))index=Math.max(index,1);if(has('sccm.control_path')||has('relay.success'))index=Math.max(index,2);if(has('sccm.execution_confirmed'))index=Math.max(index,3);if(has('sccm.cleanup_recorded')||has('sccm.post_map'))index=Math.max(index,4);
 trail.sort((a,b)=>String(b.at).localeCompare(String(a.at)));const current=index>=0?ORDER[index]:'recon',next=index+1<ORDER.length?ORDER[index+1]:null;return{context:c,contextKey:key,current,currentLabel:LABELS[current],next,nextLabel:next?LABELS[next]:'',completedIndex:index,trail:trail.slice(0,10)};}
function coverage46(lanes){const c=C.mindmapCoverage42?C.mindmapCoverage42(lanes):null;if(!c)return null;const baseline={implemented:42,partial:39,gap:46,stale:0,coveragePct:33,representedPct:64};return{current:c,baseline,implementedDelta:c.implemented-baseline.implemented,partialDelta:c.partial-baseline.partial,gapDelta:c.gap-baseline.gap,coveragePointDelta:c.coveragePct-baseline.coveragePct,representedPointDelta:c.representedPct-baseline.representedPct};}
if(oldNext)C.nextStepsOverview34=function(state,lanes,ctx,opts){const out=oldNext(state,lanes,ctx,opts||{});out.sccm46={progress:sccmProgress46(state,out.context),coverage:coverage46(lanes)};return out;};
C.ensure46=ensure46;C.sccmProgress46=sccmProgress46;C.sccmCoverage46=coverage46;C.sanitizedCopy=function(state){return ensure46(oldSanitize(state));};
root.OBOL_CORE_V46={VERSION,ensure46,sccmProgress46,coverage46};
}

/* obol-core-delta: assets/core-v4.7.js */
{
// Obol v4.7 core overlay — retroactive Orange reporting traceability and report-contract readiness.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V47;if(!C||!M)throw new Error('Obol core and methodology-v4.7 are required before core-v4.7.js');
const VERSION='4.7.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure47(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.report47&&typeof s.ui.report47==='object'?s.ui.report47:{};s.ui.report47={showTrace:old.showTrace!==false,showGaps:old.showGaps!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure47(oldNew());};C.coerceState=function(raw){return ensure47(oldCoerce(raw));};C.migrateV1=function(raw){return ensure47(oldMigrate(raw));};
function cards47(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function orangeReportTrace47(state,lanes){ensure47(state);const map=cards47(lanes),rows=[];for(const a of state.activities||[]){if(!a||a.result!=='success')continue;const card=map[a.cardId],contract=card&&card.report47;if(!contract)continue;let ready=null;try{ready=C.reportReadinessRow35?C.reportReadinessRow35(state,lanes,a):null;}catch(e){}rows.push({activity:a,card,contract,contextLabel:a.contextLabel||a.contextKey||'',ready:ready?ready.ready:false,missing:ready?ready.missing.map(x=>x.label):[],hasEvidence:!!String(a.evidence||'').trim(),hasCommand:!!String(a.command||'').trim()});}return rows.sort((a,b)=>String(a.activity.at||'').localeCompare(String(b.activity.at||'')));}
function orangeReportCoverage47(lanes){const map=cards47(lanes),rows=[];for(const c of Object.values(map)){if(!c.report47)continue;rows.push(c.report47);}return{rows,mapped:rows.length,traceable:rows.filter(x=>x.traceable).length,findings:rows.filter(x=>x.reportBearing).length,pathOnly:rows.filter(x=>!x.reportBearing).length,evidenceProfiles:rows.filter(x=>x.evidenceProfile).length};}
function orangeReportReadiness47(state,lanes){const rows=orangeReportTrace47(state,lanes);return{rows,total:rows.length,ready:rows.filter(x=>x.ready).length,needs:rows.filter(x=>!x.ready).length,findings:rows.filter(x=>x.contract.reportBearing).length,pathOnly:rows.filter(x=>!x.contract.reportBearing).length,missingEvidence:rows.filter(x=>!x.hasEvidence).length,missingCommand:rows.filter(x=>!x.hasCommand).length};}
C.ensure47=ensure47;C.orangeReportTrace47=orangeReportTrace47;C.orangeReportCoverage47=orangeReportCoverage47;C.orangeReportReadiness47=orangeReportReadiness47;C.sanitizedCopy=function(state){return ensure47(oldSanitize(state));};
root.OBOL_CORE_V47={VERSION,ensure47,orangeReportTrace47,orangeReportCoverage47,orangeReportReadiness47};
}

/* obol-core-delta: assets/core-v4.8.js */
{
// Obol v4.8 core overlay — domain-persistence progress, coverage delta, and Next Steps payload.
const C=root.OBOL_CORE_V2,M=root.OBOL_METHODOLOGY_V48;if(!C||!M)throw new Error('Obol core and methodology-v4.8 are required before core-v4.8.js');
const VERSION='4.8.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldNext=C.nextStepsOverview34,oldSanitize=C.sanitizedCopy;
function ensure48(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.persistence48&&typeof s.ui.persistence48==='object'?s.ui.persistence48:{};s.ui.persistence48={showBranch:old.showBranch!==false,showTrail:old.showTrail!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure48(oldNew());};C.coerceState=function(raw){return ensure48(oldCoerce(raw));};C.migrateV1=function(raw){return ensure48(oldMigrate(raw));};
const ORDER=['material','ticket','host','directory','cleanup'],LABELS={material:'Persistence material',ticket:'Forged ticket / certificate',host:'Host credential subsystem',directory:'Directory persistence',cleanup:'Cleanup / verification'};
const CARD_STAGE={'golden-ticket':'ticket','silver-ticket':'ticket','golden-certificate':'ticket','advanced-forged-tickets':'ticket','dsrm-persistence':'host','credential-subsystem-persistence':'host','dcshadow-persistence':'directory','acl-persistence-lifecycle':'directory'};
function persistenceProgress48(state,ctx){ensure48(state);const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c),trail=[];let index=-1;for(const a of state.activities||[]){if(a.contextKey!==key||a.result!=='success')continue;const stage=CARD_STAGE[a.cardId];if(!stage)continue;index=Math.max(index,ORDER.indexOf(stage));trail.push({activityId:a.id||'',cardId:a.cardId,stage,label:LABELS[stage],at:a.at||''});}
 const has=id=>{try{return !!(C.hasFact&&C.hasFact(state,id,c));}catch(e){return false;}};
 if(has('hash.krbtgt')||has('loot.ntds')||has('credential.certificate'))index=Math.max(index,0);
 if(has('persistence.silver_ticket')||has('persistence.golden_certificate')||has('persistence.diamond_ticket')||has('persistence.sapphire_ticket')||has('persistence.domain'))index=Math.max(index,1);
 if(has('persistence.dsrm')||has('persistence.skeleton_key')||has('persistence.custom_ssp'))index=Math.max(index,2);
 if(has('persistence.dcshadow')||has('persistence.acl'))index=Math.max(index,3);
 trail.sort((a,b)=>String(b.at).localeCompare(String(a.at)));const current=index>=0?ORDER[index]:'material',next=index+1<ORDER.length?ORDER[index+1]:null;return{context:c,contextKey:key,current,currentLabel:LABELS[current],next,nextLabel:next?LABELS[next]:'',completedIndex:index,trail:trail.slice(0,10)};}
function coverage48(lanes){const c=C.mindmapCoverage42?C.mindmapCoverage42(lanes):null;if(!c)return null;const baseline={implemented:48,partial:45,gap:34,stale:0,coveragePct:38,representedPct:73};return{current:c,baseline,implementedDelta:c.implemented-baseline.implemented,partialDelta:c.partial-baseline.partial,gapDelta:c.gap-baseline.gap,coveragePointDelta:c.coveragePct-baseline.coveragePct,representedPointDelta:c.representedPct-baseline.representedPct};}
function persistenceQueue48(state,lanes,ctx){const ids=new Set(['golden-ticket',...M.cards]),rows=C.rankedApplicable?C.rankedApplicable(state,lanes,ctx,{showAll:!!(state.ui&&state.ui.pathShowAll)}):[];return rows.filter(r=>ids.has(r.card.id)&&C.statusFor(state,r.card.id,ctx)!=='done').slice(0,6).map((r,i)=>({priority:i+1,cardId:r.card.id,title:r.card.title,score:r.score,contract45:C.orangeContractFor45?C.orangeContractFor45(r.card):null,report47:r.card.report47||null}));}
if(oldNext)C.nextStepsOverview34=function(state,lanes,ctx,opts){const out=oldNext(state,lanes,ctx,opts||{});out.persistence48={progress:persistenceProgress48(state,out.context),coverage:coverage48(lanes),queue:persistenceQueue48(state,lanes,out.context)};return out;};
C.ensure48=ensure48;C.persistenceProgress48=persistenceProgress48;C.persistenceCoverage48=coverage48;C.persistenceQueue48=persistenceQueue48;C.sanitizedCopy=function(state){return ensure48(oldSanitize(state));};
root.OBOL_CORE_V48={VERSION,ensure48,persistenceProgress48,coverage48,persistenceQueue48};
}

/* obol-core-delta: assets/core-v4.9.js */
{
// Obol v4.9 core overlay — unified North Star dashboard with hard numbers across methodology, operator-loop, execution, Next Steps, tool review, and reporting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V49;if(!C||!D)throw new Error('Obol core and dashboard-v4.9 metadata are required before core-v4.9.js');
const VERSION='4.9.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy;
function ensure49(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard49&&typeof s.ui.dashboard49==='object'?s.ui.dashboard49:{};s.ui.dashboard49={showContext:old.showContext!==false,showBacklog:old.showBacklog!==false,showTrend:old.showTrend!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure49(oldNew());};C.coerceState=function(raw){return ensure49(oldCoerce(raw));};C.migrateV1=function(raw){return ensure49(oldMigrate(raw));};
function cardIndex49(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function pct49(n,d){return d?Math.round(n/d*100):0;}
function mappedCards49(lanes){const out=[];for(const l of lanes||[])for(const c of l.cards||[])if(c.orange44)out.push(c);return out;}
function executionAudit49(lanes){const cards=mappedCards49(lanes),tot={commands:0,explicit:0,fallback:0,kali:0,windows:0,target:0,either:0,neutral:0};const rows=[];for(const card of cards)for(const [index,cmd] of (card.commands||[]).entries()){const explicit=!!cmd.operatorSurface40,surface=C.commandSurface40?C.commandSurface40(cmd,card):(cmd.operatorSurface40||'either');tot.commands++;tot[explicit?'explicit':'fallback']++;if(surface==='kali'||surface==='windows'||surface==='target'||surface==='either')tot[surface]++;else tot.neutral++;rows.push({cardId:card.id,cardTitle:card.title,index,tool:cmd.tool||'',surface,explicit,source:cmd.operatorSurface48Source||cmd.operatorSurface46Source||cmd.operatorSurface43Source||cmd.operatorSurface41Source||(explicit?'explicit':'fallback')});}return{rows,...tot,explicitPct:pct49(tot.explicit,tot.commands)};}
function canonicalPathAudit49(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):null,cards=cardIndex49(lanes);if(!coverage)return{linkedSections:0,stagedSections:0,stagedPct:0};let linked=0,staged=0;for(const f of coverage.files||[])for(const s of f.sections||[]){const ids=s.linkedCards&&s.linkedCards.length?s.linkedCards.map(x=>x.id):(s.cardIds||[]).filter(id=>cards[id]);if(!ids.length)continue;linked++;if(ids.some(id=>cards[id]&&cards[id].orange44))staged++;}return{linkedSections:linked,stagedSections:staged,stagedPct:pct49(staged,linked)};}
function toolReviewAudit49(lanes){const a=C.mindmapToolAudit41?C.mindmapToolAudit41(lanes):{rows:[],counts:{}};const total=(a.rows||[]).length,review=(a.counts&&a.counts.review)||0,reviewed=total-review;return{rows:a.rows||[],counts:a.counts||{},total,reviewed,pending:review,reviewedPct:pct49(reviewed,total)};}
function backlog49(coverage){const rows=[];for(const f of coverage&&coverage.files||[]){const gap=(f.sections||[]).filter(x=>x.status==='gap').length,partial=(f.sections||[]).filter(x=>x.status==='partial').length,stale=(f.sections||[]).filter(x=>x.status==='stale').length,implemented=(f.sections||[]).filter(x=>x.status==='implemented').length,total=(f.sections||[]).length;rows.push({file:f.file,title:f.title,total,implemented,partial,gap,stale,debt:gap+partial+stale,completePct:pct49(implemented,total)});}return rows.sort((a,b)=>b.gap-a.gap||b.partial-a.partial||a.file.localeCompare(b.file));}
function activeContext49(state,lanes,ctx){const c=C.normalizeContext(state,ctx||state.activeContext),key=C.contextKey(c),map=cardIndex49(lanes),activities=(state.activities||[]).filter(a=>a.contextKey===key),mappedSuccesses=activities.filter(a=>a.result==='success'&&map[a.cardId]&&map[a.cardId].orange44),decision=C.orangeDecisionProgress44?C.orangeDecisionProgress44(state,lanes,c):null,queue=C.orangeDecisionQueue44?C.orangeDecisionQueue44(state,lanes,c,{showAll:!!(state.ui&&state.ui.pathShowAll),limit:8}):[],trace=C.orangeReportTrace47?C.orangeReportTrace47(state,lanes).filter(r=>r.activity&&r.activity.contextKey===key):[],sccm=C.sccmProgress46?C.sccmProgress46(state,c):null,persistence=C.persistenceProgress48?C.persistenceProgress48(state,c):null;return{context:c,contextKey:key,label:C.contextLabel?C.contextLabel(state,c):key,activities:activities.length,mappedSuccesses:mappedSuccesses.length,decision,queue,reportRows:trace.length,reportReady:trace.filter(x=>x.ready).length,reportNeeds:trace.filter(x=>!x.ready).length,sccm,persistence};}
function northStarDashboard49(state,lanes,ctx){ensure49(state);const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{sections:0,implemented:0,partial:0,gap:0,stale:0,coveragePct:0,representedPct:0,files:[],sourceFileCount:0},contracts=C.orangeContractCoverage45?C.orangeContractCoverage45(lanes):{mappedCards:0,runReady:0,evidenceReady:0,loopReady:0,evidencePct:0,loopPct:0,commands:0,adjustableCommands:0,fixedCommands:0,adjustablePct:0},execution=executionAudit49(lanes),reporting=C.orangeReportCoverage47?C.orangeReportCoverage47(lanes):{mapped:0,traceable:0,findings:0,pathOnly:0,evidenceProfiles:0},pathAudit=canonicalPathAudit49(lanes),toolReview=toolReviewAudit49(lanes),active=activeContext49(state,lanes,ctx),backlog=backlog49(coverage),mapped=mappedCards49(lanes).length,tracePct=pct49(reporting.traceable,reporting.mapped),runPct=pct49(contracts.runReady,contracts.mappedCards),evidencePct=pct49(contracts.evidenceReady,contracts.mappedCards),loopPct=pct49(contracts.loopReady,contracts.mappedCards);const milestones=[...(D.milestones||[]),{release:'v4.9',implemented:coverage.implemented,partial:coverage.partial,gap:coverage.gap,stale:coverage.stale,coveragePct:coverage.coveragePct,representedPct:coverage.representedPct,label:'single North Star dashboard'}];const metrics=[
 {id:'methodology',label:'Fully implemented',value:coverage.coveragePct,detail:coverage.implemented+'/'+coverage.sections+' canonical sections',count:coverage.implemented,total:coverage.sections},
 {id:'represented',label:'Represented',value:coverage.representedPct,detail:(coverage.implemented+coverage.partial)+'/'+coverage.sections+' implemented or partial',count:coverage.implemented+coverage.partial,total:coverage.sections},
 {id:'operator-loop',label:'Run → Evidence loop',value:loopPct,detail:contracts.loopReady+'/'+contracts.mappedCards+' Orange-mapped cards',count:contracts.loopReady,total:contracts.mappedCards},
 {id:'evidence',label:'Explicit Evidence profiles',value:evidencePct,detail:contracts.evidenceReady+'/'+contracts.mappedCards+' mapped cards',count:contracts.evidenceReady,total:contracts.mappedCards},
 {id:'execution',label:'Explicit execution metadata',value:execution.explicitPct,detail:execution.explicit+'/'+execution.commands+' mapped commands',count:execution.explicit,total:execution.commands},
 {id:'decision-path',label:'Decision-path mapped',value:pathAudit.stagedPct,detail:pathAudit.stagedSections+'/'+pathAudit.linkedSections+' live canonical sections',count:pathAudit.stagedSections,total:pathAudit.linkedSections},
 {id:'reporting',label:'Reporting traceability',value:tracePct,detail:reporting.traceable+'/'+reporting.mapped+' mapped cards',count:reporting.traceable,total:reporting.mapped},
 {id:'tool-review',label:'Tool decisions reviewed',value:toolReview.reviewedPct,detail:toolReview.reviewed+'/'+toolReview.total+' audited tool-choice rows',count:toolReview.reviewed,total:toolReview.total}
];return{version:VERSION,source:D.source,categories:D.categories||[],coverage,contracts:{...contracts,runPct,evidencePct,loopPct},execution,reporting:{...reporting,tracePct},decisionPath:pathAudit,toolReview,active,backlog,milestones,mappedCards:mapped,sourceFiles:coverage.sourceFileCount||0,metrics};}
C.ensure49=ensure49;C.executionMetadataAudit49=executionAudit49;C.canonicalDecisionPathAudit49=canonicalPathAudit49;C.toolReviewAudit49=toolReviewAudit49;C.northStarBacklog49=backlog49;C.northStarDashboard49=northStarDashboard49;C.sanitizedCopy=function(state){return ensure49(oldSanitize(state));};
root.OBOL_CORE_V49={VERSION,ensure49,northStarDashboard49,executionAudit49,canonicalPathAudit49,toolReviewAudit49,backlog49};
}

/* obol-core-delta: assets/core-v5.0.js */
{
// Obol v5.0 core overlay — dedicated dashboard route, UI policy accounting, and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V50;if(!C||!D)throw new Error('Obol core and dashboard-v5.0 metadata are required before core-v5.0.js');
const VERSION='5.0.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard49;
function ensure50(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard50&&typeof s.ui.dashboard50==='object'?s.ui.dashboard50:{};s.ui.dashboard50={showContext:old.showContext!==false,showBacklog:old.showBacklog!==false,showTrend:old.showTrend!==false,compactHome:old.compactHome!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure50(oldNew());};C.coerceState=function(raw){return ensure50(oldCoerce(raw));};C.migrateV1=function(raw){return ensure50(oldMigrate(raw));};
// The Dashboard belongs in More, not primary navigation. Mutate the established v3 navigation object before app-v3.0 captures it.
const nav=C.NAVIGATION30;if(nav&&Array.isArray(nav.secondary)&&!nav.secondary.some(x=>x.id==='dashboard'))nav.secondary.unshift({id:'dashboard',label:'North Star Dashboard',href:'#/dashboard',help:'One project-health dashboard with hard methodology, Evidence, execution, Next Steps, tool-review, reporting, and UI/UX metrics.'});
function uiPolicyAudit50(){const primary=nav&&Array.isArray(nav.primary)?nav.primary:[],secondary=nav&&Array.isArray(nav.secondary)?nav.secondary:[],dashboardEntries=secondary.filter(x=>x.id==='dashboard').length;const checks=[
 {id:'single-dashboard',label:'Single project-health dashboard',pass:dashboardEntries===1,detail:dashboardEntries+'/1 Dashboard entries in More'},
 {id:'small-primary-nav',label:'Five-item primary navigation',pass:primary.length===5,detail:primary.length+'/5 primary workflow destinations'},
 {id:'brand-scope',label:'North Star brand scope contract',pass:Array.isArray(D.allowedBrandRoutes)&&D.allowedBrandRoutes.length===2&&D.allowedBrandRoutes.includes('dashboard')&&D.allowedBrandRoutes.includes('home'),detail:(D.allowedBrandRoutes||[]).join(' + ')+' are the only allowed brand surfaces'},
 {id:'current-version',label:'Current-version contract',pass:C.VERSION===VERSION,detail:'runtime '+C.VERSION},
 {id:'changelog',label:'Dedicated project history',pass:D.changelog==='CHANGELOG.md',detail:D.changelog}
 ];const passed=checks.filter(x=>x.pass).length;return{checks,passed,total:checks.length,pct:checks.length?Math.round(passed/checks.length*100):0};}
function northStarDashboard50(state,lanes,ctx){ensure50(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{},contracts:{},execution:{},reporting:{},decisionPath:{},toolReview:{},active:null,backlog:[]};const ui=uiPolicyAudit50(),metrics=(base.metrics||[]).filter(x=>x.id!=='ui-hygiene');metrics.push({id:'ui-hygiene',label:'UI / UX policy checks',value:ui.pct,detail:ui.passed+'/'+ui.total+' current architecture checks',count:ui.passed,total:ui.total});const milestones=[...(base.milestones||[])];if(!milestones.some(x=>x.release==='v5.0'))milestones.push({release:'v5.0',implemented:base.coverage&&base.coverage.implemented||52,partial:base.coverage&&base.coverage.partial||49,gap:base.coverage&&base.coverage.gap||26,stale:base.coverage&&base.coverage.stale||0,coveragePct:base.coverage&&base.coverage.coveragePct||41,representedPct:base.coverage&&base.coverage.representedPct||80,label:D.releaseMilestone.label});return{...base,version:VERSION,metrics,milestones,uiPolicy:ui,dashboardRoute:D.dashboardRoute,changelog:D.changelog,allowedBrandRoutes:[...(D.allowedBrandRoutes||[])]};}
C.ensure50=ensure50;C.uiPolicyAudit50=uiPolicyAudit50;C.northStarDashboard50=northStarDashboard50;C.sanitizedCopy=function(state){return ensure50(oldSanitize(state));};
root.OBOL_CORE_V50={VERSION,ensure50,uiPolicyAudit50,northStarDashboard50};
}

/* obol-core-delta: assets/core-v5.1.js */
{
// Obol v5.1 core overlay — dashboard delivery-debt drill-downs and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V51;if(!C||!D)throw new Error('Obol core and dashboard-v5.1 metadata are required before core-v5.1.js');
const VERSION='5.1.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard50;
function ensure51(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard51&&typeof s.ui.dashboard51==='object'?s.ui.dashboard51:{};s.ui.dashboard51={debtFilter:old.debtFilter||'all',debtQuery:old.debtQuery||'',showDeliveryDebt:old.showDeliveryDebt!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure51(oldNew());};C.coerceState=function(raw){return ensure51(oldCoerce(raw));};C.migrateV1=function(raw){return ensure51(oldMigrate(raw));};
function cardIndex(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function deliveryDebt51(lanes){const contracts=C.orangeContractCoverage45?C.orangeContractCoverage45(lanes):{rows:[]},exec=C.executionMetadataAudit49?C.executionMetadataAudit49(lanes):{rows:[]},report=C.orangeReportCoverage47?C.orangeReportCoverage47(lanes):null,map=cardIndex(lanes),execBy={};for(const x of exec.rows||[]){execBy[x.cardId]=execBy[x.cardId]||{total:0,explicit:0};execBy[x.cardId].total++;if(x.explicit)execBy[x.cardId].explicit++;}const traceable=new Set();if(report&&Array.isArray(report.rows))for(const r of report.rows)if(r.traceable)traceable.add(r.cardId);const rows=[];for(const r of contracts.rows||[]){const c=map[r.cardId]||{},e=execBy[r.cardId]||{total:r.commands||0,explicit:0},reportReady=!!(c.report47||c.report);const missing=[];if(!r.runReady)missing.push('run');if(!r.evidenceReady)missing.push('evidence');if(e.total&&e.explicit<e.total)missing.push('execution');if(!reportReady)missing.push('report');if(missing.length)rows.push({cardId:r.cardId,title:r.title||c.title||r.cardId,stage:r.stage||'',missing,commands:e.total,explicit:e.explicit,source:(c.orange43&&c.orange43[0]&&c.orange43[0].file)||''});}
 const counts={run:0,evidence:0,execution:0,report:0};for(const r of rows)for(const m of r.missing)counts[m]++;return{rows,counts,total:rows.length,mapped:(contracts.rows||[]).length,clean:Math.max(0,(contracts.rows||[]).length-rows.length)};}
function northStarDashboard51(state,lanes,ctx){ensure51(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[]},debt=deliveryDebt51(lanes),milestones=[...(base.milestones||[])];if(!milestones.some(x=>x.release==='v5.1'))milestones.push({...D.releaseMilestone,implemented:base.coverage&&base.coverage.implemented||52,partial:base.coverage&&base.coverage.partial||49,gap:base.coverage&&base.coverage.gap||26,stale:base.coverage&&base.coverage.stale||0});return{...base,version:VERSION,deliveryDebt51:debt,milestones};}
C.ensure51=ensure51;C.deliveryDebt51=deliveryDebt51;C.northStarDashboard51=northStarDashboard51;C.sanitizedCopy=function(state){return ensure51(oldSanitize(state));};
root.OBOL_CORE_V51={VERSION,ensure51,deliveryDebt51,northStarDashboard51};
}

/* obol-core-delta: assets/core-v5.2.js */
{
// Obol v5.2 core overlay — delivery-ready canonical accounting, build-next prioritization, and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V52;if(!C||!D)throw new Error('Obol core and dashboard-v5.2 metadata are required before core-v5.2.js');
const VERSION='5.2.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard51;
function ensure52(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard52&&typeof s.ui.dashboard52==='object'?s.ui.dashboard52:{};s.ui.dashboard52={showReadiness:old.showReadiness!==false,showBuildNext:old.showBuildNext!==false,buildNextFilter:old.buildNextFilter||'all',buildNextQuery:old.buildNextQuery||''};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure52(oldNew());};C.coerceState=function(raw){return ensure52(oldCoerce(raw));};C.migrateV1=function(raw){return ensure52(oldMigrate(raw));};
function pct(n,d){return d?Math.round(n/d*100):0;}
function cardIndex(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function cardReadiness52(lanes){const map=cardIndex(lanes),contracts=C.orangeContractCoverage45?C.orangeContractCoverage45(lanes):{rows:[]},exec=C.executionMetadataAudit49?C.executionMetadataAudit49(lanes):{rows:[]},execBy={};for(const x of exec.rows||[]){execBy[x.cardId]=execBy[x.cardId]||{total:0,explicit:0};execBy[x.cardId].total++;if(x.explicit)execBy[x.cardId].explicit++;}const rows=[],byCard={};for(const r of contracts.rows||[]){const c=map[r.cardId]||{},e=execBy[r.cardId]||{total:r.commands||0,explicit:0},reportReady=!!(c.report47||c.report),missing=[];if(!r.runReady)missing.push('run');if(!r.evidenceReady)missing.push('evidence');if(e.total&&e.explicit<e.total)missing.push('execution');if(!reportReady)missing.push('report');const row={cardId:r.cardId,title:r.title||c.title||r.cardId,stage:r.stage||'',source:(c.orange43&&c.orange43[0]&&c.orange43[0].file)||'',commands:e.total,explicit:e.explicit,runReady:!!r.runReady,evidenceReady:!!r.evidenceReady,executionReady:!e.total||e.explicit===e.total,reportReady,missing,ready:missing.length===0};rows.push(row);byCard[row.cardId]=row;}return{rows,byCard,ready:rows.filter(x=>x.ready).length,total:rows.length};}
function deliveryReadiness52(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{sections:0,implemented:0,partial:0,files:[]},cards=cardReadiness52(lanes),rows=[];let readyImplemented=0,implementedDebt=0,readyPartial=0,partialDebt=0;for(const f of coverage.files||[])for(const s of f.sections||[]){if(!['implemented','partial'].includes(s.status))continue;const linked=(s.linkedCards||[]).map(x=>x.id),tracked=linked.filter(id=>cards.byCard[id]),ready=tracked.filter(id=>cards.byCard[id].ready),missingCards=tracked.filter(id=>!cards.byCard[id].ready),isReady=ready.length>0;const row={key:s.key,label:s.label,file:f.file,status:s.status,linkedCardIds:linked,trackedCardIds:tracked,readyCardIds:ready,debtCardIds:missingCards,ready:isReady,note:s.note||''};rows.push(row);if(s.status==='implemented'){if(isReady)readyImplemented++;else implementedDebt++;}else{if(isReady)readyPartial++;else partialDebt++;}}
 const readyRepresented=readyImplemented+readyPartial;return{rows,cards,canonicalSections:coverage.sections,implementedCanonical:coverage.implemented,partialCanonical:coverage.partial,readyImplemented,implementedDebt,readyPartial,partialDebt,readyRepresented,implementedReadinessPct:pct(readyImplemented,coverage.implemented),deliveryReadyCoveragePct:pct(readyImplemented,coverage.sections),representedDeliveryPct:pct(readyRepresented,coverage.sections)};}
function buildNext52(lanes){const readiness=deliveryReadiness52(lanes),debt=C.deliveryDebt51?C.deliveryDebt51(lanes):{rows:[]},queue=[],coveredCards=new Set();for(const s of readiness.rows.filter(x=>x.status==='implemented'&&!x.ready)){for(const id of s.trackedCardIds||[])coveredCards.add(id);queue.push({id:'section:'+s.key,priority:1,kind:'implemented-quality',label:s.label,file:s.file,status:s.status,missing:[],detail:'Canonical section is implemented, but none of its tracked mapped workflows currently satisfies every Run, Evidence, execution-metadata, and reporting contract.'});}
 for(const r of debt.rows||[]){if(coveredCards.has(r.cardId))continue;queue.push({id:'card:'+r.cardId,priority:2,kind:'mapped-delivery',label:r.title,file:r.source||'',status:'mapped',missing:[...(r.missing||[])],detail:'Mapped workflow still has delivery debt: '+(r.missing||[]).join(', ')+'.'});}
 const gaps=(C.mindmapPriorityGaps42?C.mindmapPriorityGaps42(lanes):[]).filter(x=>x.status==='gap');for(const g of gaps)queue.push({id:'gap:'+g.key,priority:3,kind:'canonical-gap',label:g.label,file:g.file,status:g.status,missing:[],detail:g.note||'Canonical gap has no complete implementation yet.'});
 queue.sort((a,b)=>a.priority-b.priority||(b.missing||[]).length-(a.missing||[]).length||String(a.file).localeCompare(String(b.file))||String(a.label).localeCompare(String(b.label)));return{rows:queue,implementedQuality:queue.filter(x=>x.kind==='implemented-quality').length,mappedDelivery:queue.filter(x=>x.kind==='mapped-delivery').length,canonicalGaps:queue.filter(x=>x.kind==='canonical-gap').length,total:queue.length};}
function northStarDashboard52(state,lanes,ctx){ensure52(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},readiness=deliveryReadiness52(lanes),buildNext=buildNext52(lanes),metrics=(base.metrics||[]).filter(x=>x.id!=='delivery-ready-implementation');metrics.push({id:'delivery-ready-implementation',label:'Delivery-ready implementation',value:readiness.deliveryReadyCoveragePct,detail:readiness.readyImplemented+'/'+readiness.canonicalSections+' canonical sections are both fully implemented and backed by at least one clean mapped workflow',count:readiness.readyImplemented,total:readiness.canonicalSections});const milestones=[...(base.milestones||[])];if(!milestones.some(x=>x.release==='v5.2'))milestones.push({...D.releaseMilestone,implemented:base.coverage&&base.coverage.implemented||52,partial:base.coverage&&base.coverage.partial||49,gap:base.coverage&&base.coverage.gap||26,stale:base.coverage&&base.coverage.stale||0});return{...base,version:VERSION,metrics,milestones,deliveryReadiness52:readiness,buildNext52:buildNext};}
C.ensure52=ensure52;C.cardReadiness52=cardReadiness52;C.deliveryReadiness52=deliveryReadiness52;C.buildNext52=buildNext52;C.northStarDashboard52=northStarDashboard52;C.sanitizedCopy=function(state){return ensure52(oldSanitize(state));};
root.OBOL_CORE_V52={VERSION,ensure52,cardReadiness52,deliveryReadiness52,buildNext52,northStarDashboard52};
}

/* obol-core-delta: assets/core-v5.3.js */
{
// Obol v5.3 core overlay — implemented-quality delivery repair accounting and current release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V53,M=root.OBOL_METHODOLOGY_V53;if(!C||!D||!M)throw new Error('Obol core plus v5.3 dashboard/methodology metadata are required before core-v5.3.js');
const VERSION='5.3.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard52;
function ensure53(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard53&&typeof s.ui.dashboard53==='object'?s.ui.dashboard53:{};s.ui.dashboard53={showRepair:old.showRepair!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure53(oldNew());};C.coerceState=function(raw){return ensure53(oldCoerce(raw));};C.migrateV1=function(raw){return ensure53(oldMigrate(raw));};
function repairSummary53(lanes){const readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,cards=readiness&&readiness.cards&&readiness.cards.byCard||{},ids=[...(M.repairedCardIds||[])],rows=ids.map(id=>{const now=cards[id]||{},before=M.before[id]||{};return{cardId:id,title:now.title||id,ready:!!now.ready,missing:[...(now.missing||[])],profileAdded:!before.evidenceReady,executionAdded:M.surfaceAdds[id]||0,source:now.source||'',stage:now.stage||''};}),implementedSections=[];if(readiness)for(const s of readiness.rows||[]){if(s.status!=='implemented')continue;const matched=(s.readyCardIds||[]).filter(id=>ids.includes(id));if(matched.length)implementedSections.push({key:s.key,label:s.label,file:s.file,readyCardIds:matched});}return{rows,total:rows.length,ready:rows.filter(x=>x.ready).length,profileAdds:rows.filter(x=>x.profileAdded).length,executionAdds:rows.reduce((n,x)=>n+x.executionAdded,0),implementedSections,implementedSectionCount:implementedSections.length,currentImplementedDebt:readiness?readiness.implementedDebt:0,currentReadyImplemented:readiness?readiness.readyImplemented:0,currentDeliveryReadyCoveragePct:readiness?readiness.deliveryReadyCoveragePct:0};}
function northStarDashboard53(state,lanes,ctx){ensure53(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},repair=repairSummary53(lanes),metrics=[...(base.metrics||[]).filter(x=>x.id!=='v53-repair-wave')];metrics.push({id:'v53-repair-wave',label:'v5.3 repair set clean',value:repair.total?Math.round(repair.ready/repair.total*100):0,detail:repair.ready+'/'+repair.total+' targeted mapped workflows now satisfy Run, Evidence, execution metadata, and reporting',count:repair.ready,total:repair.total});const milestones=[...(base.milestones||[])];if(!milestones.some(x=>x.release==='v5.3'))milestones.push({...D.releaseMilestone,implemented:base.coverage&&base.coverage.implemented||52,partial:base.coverage&&base.coverage.partial||49,gap:base.coverage&&base.coverage.gap||26,stale:base.coverage&&base.coverage.stale||0});return{...base,version:VERSION,metrics,milestones,repair53:repair};}
C.ensure53=ensure53;C.repairSummary53=repairSummary53;C.northStarDashboard53=northStarDashboard53;C.sanitizedCopy=function(state){return ensure53(oldSanitize(state));};
root.OBOL_CORE_V53={VERSION,ensure53,repairSummary53,northStarDashboard53};
}

/* obol-core-delta: assets/core-v5.4.js */
{
// Obol v5.4 core overlay — canonical persistence progress, queue synchronization metadata, and current release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V54,M=root.OBOL_METHODOLOGY_V54;if(!C||!D||!M)throw new Error('Obol core plus v5.4 dashboard/methodology metadata are required before core-v5.4.js');
const VERSION='5.4.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard53;
function ensure54(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard54&&typeof s.ui.dashboard54==='object'?s.ui.dashboard54:{};s.ui.dashboard54={showCanonicalAdvance:old.showCanonicalAdvance!==false,showReadmeSync:old.showReadmeSync!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure54(oldNew());};C.coerceState=function(raw){return ensure54(oldCoerce(raw));};C.migrateV1=function(raw){return ensure54(oldMigrate(raw));};
function canonicalAdvance54(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline53.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline53.implemented,coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0};}
function northStarDashboard54(state,lanes,ctx){ensure54(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance54(lanes),buildNext=C.buildNext52?C.buildNext52(lanes):{rows:[],implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0},metrics=[...(base.metrics||[]).filter(x=>x.id!=='v54-canonical-advance')];metrics.push({id:'v54-canonical-advance',label:'v5.4 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after completing '+advance.delta+' previously partial persistence sections',count:advance.toImplemented,total:127});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.4').map(x=>x.release==='v5.3'?{...x,implemented:52,partial:49,gap:26,stale:0,coveragePct:41,representedPct:80}:x);milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance54:advance,buildNext54:buildNext,readmeQueueSync54:{script:D.sync.script,ciCheck:D.sync.ciCheck,markers:[...D.sync.readmeMarkers],authoritativeSource:'North Star Dashboard Build Next queue'}};}
C.ensure54=ensure54;C.canonicalAdvance54=canonicalAdvance54;C.northStarDashboard54=northStarDashboard54;C.sanitizedCopy=function(state){return ensure54(oldSanitize(state));};
root.OBOL_CORE_V54={VERSION,ensure54,canonicalAdvance54,northStarDashboard54};
}

/* obol-core-delta: assets/core-v5.5.js */
{
// Obol v5.5 core overlay — quality-debt cleanup, canonical progress, and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V55,M=root.OBOL_METHODOLOGY_V55;if(!C||!D||!M)throw new Error('Obol core plus v5.5 dashboard/methodology metadata are required before core-v5.5.js');
const VERSION='5.5.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard54;
function ensure55(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard55&&typeof s.ui.dashboard55==='object'?s.ui.dashboard55:{};s.ui.dashboard55={showQualityClear:old.showQualityClear!==false,showCanonicalAdvance:old.showCanonicalAdvance!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure55(oldNew());};C.coerceState=function(raw){return ensure55(oldCoerce(raw));};C.migrateV1=function(raw){return ensure55(oldMigrate(raw));};
function cardIndex(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function canonicalAdvance55(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline54.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline54.implemented,coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0,gap:coverage.gap||0};}
function qualityRepair55(lanes){const cards=C.cardReadiness52?C.cardReadiness52(lanes):{byCard:{}},map=cardIndex(lanes),rows=[];for(const id of M.repairedCardIds||[]){const c=map[id],r=cards.byCard&&cards.byCard[id];rows.push({cardId:id,title:c&&c.title||id,ready:!!(r&&r.ready),evidence:!!(c&&c.evidence45&&c.evidence45.source==='v5.5'),executionReady:!!(r&&r.executionReady),reportReady:!!(r&&r.reportReady)});}const buildNext=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{rows,targeted:rows.length,ready:rows.filter(x=>x.ready).length,implementedQualityRemaining:buildNext.implementedQuality||0,queue:buildNext};}
function northStarDashboard55(state,lanes,ctx){ensure55(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance55(lanes),repair=qualityRepair55(lanes),buildNext=repair.queue,metrics=[...(base.metrics||[]).filter(x=>!['v55-canonical-advance','v55-quality-clear'].includes(x.id))];metrics.push({id:'v55-canonical-advance',label:'v5.5 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after completing '+advance.delta+' previously partial sections',count:advance.toImplemented,total:127});metrics.push({id:'v55-quality-clear',label:'Implemented quality debt',value:repair.implementedQualityRemaining,detail:repair.ready+'/'+repair.targeted+' targeted repair workflows are delivery-ready',count:repair.implementedQualityRemaining,total:Math.max(repair.implementedQualityRemaining,repair.targeted)});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.5').map(x=>x.release==='v5.4'?{...x,implemented:57,partial:44,gap:26,stale:0,coveragePct:45,representedPct:80}:x);if(!milestones.some(x=>x.release==='v5.5'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance55:advance,qualityRepair55:repair,buildNext55:buildNext};}
C.ensure55=ensure55;C.canonicalAdvance55=canonicalAdvance55;C.qualityRepair55=qualityRepair55;C.northStarDashboard55=northStarDashboard55;C.sanitizedCopy=function(state){return ensure55(oldSanitize(state));};
root.OBOL_CORE_V55={VERSION,ensure55,canonicalAdvance55,qualityRepair55,northStarDashboard55};
}

/* obol-core-delta: assets/core-v5.6.js */
{
// Obol v5.6 core overlay — mapped-delivery cleanup, canonical progress, and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V56,M=root.OBOL_METHODOLOGY_V56;if(!C||!D||!M)throw new Error('Obol core plus v5.6 dashboard/methodology metadata are required before core-v5.6.js');
const VERSION='5.6.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard55;
function ensure56(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard56&&typeof s.ui.dashboard56==='object'?s.ui.dashboard56:{};s.ui.dashboard56={showDeliveryClear:old.showDeliveryClear!==false,showCanonicalAdvance:old.showCanonicalAdvance!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure56(oldNew());};C.coerceState=function(raw){return ensure56(oldCoerce(raw));};C.migrateV1=function(raw){return ensure56(oldMigrate(raw));};
function cardIndex(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function canonicalAdvance56(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline55.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline55.implemented,coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0,gap:coverage.gap||0};}
function deliveryRepair56(lanes){const cards=C.cardReadiness52?C.cardReadiness52(lanes):{byCard:{}},map=cardIndex(lanes),rows=[];for(const id of M.repairedCardIds||[]){const c=map[id],r=cards.byCard&&cards.byCard[id];if(!c)continue;rows.push({cardId:id,title:c.title||id,ready:!!(r&&r.ready),evidence:!!(c.evidence45&&c.evidence45.source==='v5.6'),executionReady:!!(r&&r.executionReady),reportReady:!!(r&&r.reportReady)});}const buildNext=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{rows,targeted:rows.length,ready:rows.filter(x=>x.ready).length,implementedQualityRemaining:buildNext.implementedQuality||0,mappedDeliveryRemaining:buildNext.mappedDelivery||0,queue:buildNext};}
function northStarDashboard56(state,lanes,ctx){ensure56(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance56(lanes),repair=deliveryRepair56(lanes),buildNext=repair.queue,metrics=[...(base.metrics||[]).filter(x=>!['v56-canonical-advance','v56-mapped-delivery'].includes(x.id))];metrics.push({id:'v56-canonical-advance',label:'v5.6 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after completing '+advance.delta+' previously partial sections',count:advance.toImplemented,total:127});metrics.push({id:'v56-mapped-delivery',label:'Mapped delivery debt',value:repair.mappedDeliveryRemaining,detail:repair.ready+'/'+repair.targeted+' v5.6 repair workflows are delivery-ready',count:repair.mappedDeliveryRemaining,total:Math.max(repair.mappedDeliveryRemaining,repair.targeted)});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.6').map(x=>x.release==='v5.5'?{...x,implemented:62,partial:39,gap:26,stale:0,coveragePct:49,representedPct:80}:x);if(!milestones.some(x=>x.release==='v5.6'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance56:advance,deliveryRepair56:repair,buildNext56:buildNext};}
C.ensure56=ensure56;C.canonicalAdvance56=canonicalAdvance56;C.deliveryRepair56=deliveryRepair56;C.northStarDashboard56=northStarDashboard56;C.sanitizedCopy=function(state){return ensure56(oldSanitize(state));};
root.OBOL_CORE_V56={VERSION,ensure56,canonicalAdvance56,deliveryRepair56,northStarDashboard56};
}

/* obol-core-delta: assets/core-v5.7.js */
{
// Obol v5.7 core overlay — canonical gap reduction and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V57,M=root.OBOL_METHODOLOGY_V57;if(!C||!D||!M)throw new Error('Obol core plus v5.7 dashboard/methodology metadata are required before core-v5.7.js');
const VERSION='5.7.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard56;
function ensure57(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard57&&typeof s.ui.dashboard57==='object'?s.ui.dashboard57:{};s.ui.dashboard57={showGapWave:old.showGapWave!==false,showCanonicalAdvance:old.showCanonicalAdvance!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure57(oldNew());};C.coerceState=function(raw){return ensure57(oldCoerce(raw));};C.migrateV1=function(raw){return ensure57(oldMigrate(raw));};
function canonicalAdvance57(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline56.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline56.implemented,fromGap:M.baseline56.gap,toGap:coverage.gap||0,gapDelta:M.baseline56.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function gapWave57(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{completed:[...(M.advancedKeys||[])],completedCount:(M.advancedKeys||[]).length,remainingGaps:q.canonicalGaps||0,queue:q};}
function northStarDashboard57(state,lanes,ctx){ensure57(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance57(lanes),wave=gapWave57(lanes),q=wave.queue,metrics=[...(base.metrics||[]).filter(x=>!['v57-canonical-advance','v57-gap-reduction'].includes(x.id))];metrics.push({id:'v57-canonical-advance',label:'v5.7 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after closing '+advance.delta+' canonical gaps',count:advance.toImplemented,total:127});metrics.push({id:'v57-gap-reduction',label:'Canonical gaps remaining',value:wave.remainingGaps,detail:wave.completedCount+' highest-priority gaps completed in v5.7',count:wave.remainingGaps,total:127});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.7').map(x=>x.release==='v5.6'?{...x,implemented:67,partial:34,gap:26,stale:0,coveragePct:53,representedPct:80}:x);if(!milestones.some(x=>x.release==='v5.7'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance57:advance,gapWave57:wave,buildNext57:q};}
C.ensure57=ensure57;C.canonicalAdvance57=canonicalAdvance57;C.gapWave57=gapWave57;C.northStarDashboard57=northStarDashboard57;C.sanitizedCopy=function(state){return ensure57(oldSanitize(state));};
root.OBOL_CORE_V57={VERSION,ensure57,canonicalAdvance57,gapWave57,northStarDashboard57};
}

/* obol-core-delta: assets/core-v5.8.js */
{
// Obol v5.8 core overlay — canonical gap reduction, release-contract accounting, and current-release state.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V58,M=root.OBOL_METHODOLOGY_V58;if(!C||!D||!M)throw new Error('Obol core plus v5.8 dashboard/methodology metadata are required before core-v5.8.js');
const VERSION='5.8.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard57;
function ensure58(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard58&&typeof s.ui.dashboard58==='object'?s.ui.dashboard58:{};s.ui.dashboard58={showGapWave:old.showGapWave!==false,showReleaseContract:old.showReleaseContract!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure58(oldNew());};C.coerceState=function(raw){return ensure58(oldCoerce(raw));};C.migrateV1=function(raw){return ensure58(oldMigrate(raw));};
function canonicalAdvance58(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline57.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline57.implemented,fromGap:M.baseline57.gap,toGap:coverage.gap||0,gapDelta:M.baseline57.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function gapWave58(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{completed:[...(M.advancedKeys||[])],completedCount:(M.advancedKeys||[]).length,remainingGaps:q.canonicalGaps||0,queue:q};}
function releaseContract58(){return{requiredBranch:'release/obol-v5.8',requiredStatusCheck:'test',prBodyRequired:true,duplicateBuildBranchBlocked:true,validator:'tools/validate-release-pr.js'};}
function northStarDashboard58(state,lanes,ctx){ensure58(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance58(lanes),wave=gapWave58(lanes),q=wave.queue,contract=releaseContract58(),metrics=[...(base.metrics||[]).filter(x=>!['v58-canonical-advance','v58-gap-reduction','v58-release-contract'].includes(x.id))];metrics.push({id:'v58-canonical-advance',label:'v5.8 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after closing '+advance.delta+' canonical gaps',count:advance.toImplemented,total:127});metrics.push({id:'v58-gap-reduction',label:'Canonical gaps remaining',value:wave.remainingGaps,detail:wave.completedCount+' additional canonical gaps completed in v5.8',count:wave.remainingGaps,total:127});metrics.push({id:'v58-release-contract',label:'Release PR contract',value:100,detail:'Required test now validates release branch naming and PR description before merge',count:1,total:1});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.8').map(x=>x.release==='v5.7'?{...x,implemented:72,partial:34,gap:21,stale:0,coveragePct:57,representedPct:83}:x);if(!milestones.some(x=>x.release==='v5.8'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance58:advance,gapWave58:wave,buildNext58:q,releaseContract58:contract};}
C.ensure58=ensure58;C.canonicalAdvance58=canonicalAdvance58;C.gapWave58=gapWave58;C.releaseContract58=releaseContract58;C.northStarDashboard58=northStarDashboard58;C.sanitizedCopy=function(state){return ensure58(oldSanitize(state));};
root.OBOL_CORE_V58={VERSION,ensure58,canonicalAdvance58,gapWave58,releaseContract58,northStarDashboard58};
}

/* obol-core-delta: assets/core-v5.9.js */
{
// Obol v5.9 core overlay — canonical gap reduction and generic release-quality accounting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V59,M=root.OBOL_METHODOLOGY_V59;if(!C||!D||!M)throw new Error('Obol core plus v5.9 dashboard/methodology metadata are required before core-v5.9.js');
const VERSION='5.9.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard58;
function ensure59(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard59&&typeof s.ui.dashboard59==='object'?s.ui.dashboard59:{};s.ui.dashboard59={showGapWave:old.showGapWave!==false,showReleaseQuality:old.showReleaseQuality!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure59(oldNew());};C.coerceState=function(raw){return ensure59(oldCoerce(raw));};C.migrateV1=function(raw){return ensure59(oldMigrate(raw));};
function canonicalAdvance59(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline58.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline58.implemented,fromGap:M.baseline58.gap,toGap:coverage.gap||0,gapDelta:M.baseline58.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function gapWave59(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{completed:[...(M.advancedKeys||[])],completedCount:(M.advancedKeys||[]).length,remainingGaps:q.canonicalGaps||0,queue:q};}
function releaseQuality59(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{implementedQuality:q.implementedQuality||0,mappedDelivery:q.mappedDelivery||0,canonicalGaps:q.canonicalGaps||0,clean:(q.implementedQuality||0)===0&&(q.mappedDelivery||0)===0,validator:'tools/validate-release-quality.js',requiredStatusCheck:'test'};}
function northStarDashboard59(state,lanes,ctx){ensure59(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance59(lanes),wave=gapWave59(lanes),q=wave.queue,quality=releaseQuality59(lanes),metrics=[...(base.metrics||[]).filter(x=>!['v59-canonical-advance','v59-gap-reduction','v59-release-quality'].includes(x.id))];metrics.push({id:'v59-canonical-advance',label:'v5.9 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after closing '+advance.delta+' canonical gaps',count:advance.toImplemented,total:127});metrics.push({id:'v59-gap-reduction',label:'Canonical gaps remaining',value:wave.remainingGaps,detail:wave.completedCount+' additional canonical gaps completed in v5.9',count:wave.remainingGaps,total:127});metrics.push({id:'v59-release-quality',label:'Release quality debt',value:quality.clean?100:0,detail:quality.clean?'0 implemented-quality and 0 mapped-delivery repairs':'higher-priority delivery debt blocks canonical expansion',count:quality.clean?1:0,total:1});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v5.9').map(x=>x.release==='v5.8'?{...x,implemented:77,partial:34,gap:16,stale:0,coveragePct:61,representedPct:87}:x);if(!milestones.some(x=>x.release==='v5.9'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance59:advance,gapWave59:wave,buildNext59:q,releaseQuality59:quality};}
C.ensure59=ensure59;C.canonicalAdvance59=canonicalAdvance59;C.gapWave59=gapWave59;C.releaseQuality59=releaseQuality59;C.northStarDashboard59=northStarDashboard59;C.sanitizedCopy=function(state){return ensure59(oldSanitize(state));};
root.OBOL_CORE_V59={VERSION,ensure59,canonicalAdvance59,gapWave59,releaseQuality59,northStarDashboard59};
}

/* obol-core-delta: assets/core-v6.0.js */
{
// Obol v6.0 core overlay — canonical gap reduction and release-quality accounting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V60,M=root.OBOL_METHODOLOGY_V60;if(!C||!D||!M)throw new Error('Obol core plus v6.0 dashboard/methodology metadata are required before core-v6.0.js');
const VERSION='6.0.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard59;
function ensure60(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard60&&typeof s.ui.dashboard60==='object'?s.ui.dashboard60:{};s.ui.dashboard60={showGapWave:old.showGapWave!==false,showReleaseQuality:old.showReleaseQuality!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure60(oldNew());};C.coerceState=function(raw){return ensure60(oldCoerce(raw));};C.migrateV1=function(raw){return ensure60(oldMigrate(raw));};
function canonicalAdvance60(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline59.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline59.implemented,fromGap:M.baseline59.gap,toGap:coverage.gap||0,gapDelta:M.baseline59.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function gapWave60(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{completed:[...(M.advancedKeys||[])],completedCount:(M.advancedKeys||[]).length,remainingGaps:q.canonicalGaps||0,queue:q};}
function releaseQuality60(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{implementedQuality:q.implementedQuality||0,mappedDelivery:q.mappedDelivery||0,canonicalGaps:q.canonicalGaps||0,clean:(q.implementedQuality||0)===0&&(q.mappedDelivery||0)===0,validator:'tools/validate-release-quality.js',requiredStatusCheck:'test'};}
function northStarDashboard60(state,lanes,ctx){ensure60(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance60(lanes),wave=gapWave60(lanes),q=wave.queue,quality=releaseQuality60(lanes),metrics=[...(base.metrics||[]).filter(x=>!['v60-canonical-advance','v60-gap-reduction','v60-release-quality'].includes(x.id))];metrics.push({id:'v60-canonical-advance',label:'v6.0 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after closing '+advance.delta+' canonical gaps',count:advance.toImplemented,total:127});metrics.push({id:'v60-gap-reduction',label:'Canonical gaps remaining',value:wave.remainingGaps,detail:wave.completedCount+' additional canonical gaps completed in v6.0',count:wave.remainingGaps,total:127});metrics.push({id:'v60-release-quality',label:'Release quality debt',value:quality.clean?100:0,detail:quality.clean?'0 implemented-quality and 0 mapped-delivery repairs':'higher-priority delivery debt blocks canonical expansion',count:quality.clean?1:0,total:1});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v6.0').map(x=>x.release==='v5.9'?{...x,implemented:82,partial:34,gap:11,stale:0,coveragePct:65,representedPct:91}:x);if(!milestones.some(x=>x.release==='v6.0'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance60:advance,gapWave60:wave,buildNext60:q,releaseQuality60:quality};}
C.ensure60=ensure60;C.canonicalAdvance60=canonicalAdvance60;C.gapWave60=gapWave60;C.releaseQuality60=releaseQuality60;C.northStarDashboard60=northStarDashboard60;C.sanitizedCopy=function(state){return ensure60(oldSanitize(state));};
root.OBOL_CORE_V60={VERSION,ensure60,canonicalAdvance60,gapWave60,releaseQuality60,northStarDashboard60};
}

/* obol-core-delta: assets/core-v6.1.js */
{
// Obol v6.1 core overlay — canonical gap reduction and release-quality accounting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V61,M=root.OBOL_METHODOLOGY_V61;if(!C||!D||!M)throw new Error('Obol core plus v6.1 dashboard/methodology metadata are required before core-v6.1.js');
const VERSION='6.1.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard60;
function ensure61(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard61&&typeof s.ui.dashboard61==='object'?s.ui.dashboard61:{};s.ui.dashboard61={showGapWave:old.showGapWave!==false,showReleaseQuality:old.showReleaseQuality!==false,showTrustWave:old.showTrustWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure61(oldNew());};C.coerceState=function(raw){return ensure61(oldCoerce(raw));};C.migrateV1=function(raw){return ensure61(oldMigrate(raw));};
function canonicalAdvance61(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,keys=[...(M.advancedKeys||[])],rows=[];for(const f of coverage.files||[])for(const s of f.sections||[])if(keys.includes(s.key))rows.push({key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===s.key&&x.ready):false});return{rows,total:rows.length,implemented:rows.filter(x=>x.status==='implemented').length,deliveryReady:rows.filter(x=>x.ready).length,fromImplemented:M.baseline60.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline60.implemented,fromGap:M.baseline60.gap,toGap:coverage.gap||0,gapDelta:M.baseline60.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function gapWave61(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0,rows:[]};return{completed:[...(M.advancedKeys||[])],completedCount:(M.advancedKeys||[]).length,remainingGaps:q.canonicalGaps||0,remainingKeys:(q.rows||[]).filter(x=>x.kind==='canonical-gap').map(x=>x.key),queue:q};}
function releaseQuality61(lanes){const q=C.buildNext52?C.buildNext52(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0};return{implementedQuality:q.implementedQuality||0,mappedDelivery:q.mappedDelivery||0,canonicalGaps:q.canonicalGaps||0,clean:(q.implementedQuality||0)===0&&(q.mappedDelivery||0)===0,validator:'tools/validate-release-quality.js',requiredStatusCheck:'test'};}
function northStarDashboard61(state,lanes,ctx){ensure61(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance61(lanes),wave=gapWave61(lanes),q=wave.queue,quality=releaseQuality61(lanes),metrics=[...(base.metrics||[]).filter(x=>!['v61-canonical-advance','v61-gap-reduction','v61-release-quality'].includes(x.id))];metrics.push({id:'v61-canonical-advance',label:'v6.1 canonical advance',value:advance.coveragePct,detail:advance.toImplemented+'/127 fully implemented after closing '+advance.delta+' canonical gaps',count:advance.toImplemented,total:127});metrics.push({id:'v61-gap-reduction',label:'Canonical gaps remaining',value:wave.remainingGaps,detail:wave.completedCount+' additional canonical gaps completed in v6.1',count:wave.remainingGaps,total:127});metrics.push({id:'v61-release-quality',label:'Release quality debt',value:quality.clean?100:0,detail:quality.clean?'0 implemented-quality and 0 mapped-delivery repairs':'higher-priority delivery debt blocks canonical expansion',count:quality.clean?1:0,total:1});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v6.1').map(x=>x.release==='v6.0'?{...x,implemented:87,partial:34,gap:6,stale:0,coveragePct:69,representedPct:95}:x);if(!milestones.some(x=>x.release==='v6.1'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance61:advance,gapWave61:wave,buildNext61:q,releaseQuality61:quality};}
C.ensure61=ensure61;C.canonicalAdvance61=canonicalAdvance61;C.gapWave61=gapWave61;C.releaseQuality61=releaseQuality61;C.northStarDashboard61=northStarDashboard61;C.sanitizedCopy=function(state){return ensure61(oldSanitize(state));};
root.OBOL_CORE_V61={VERSION,ensure61,canonicalAdvance61,gapWave61,releaseQuality61,northStarDashboard61};
}

/* obol-core-delta: assets/core-v6.2.js */
{
// Obol v6.2 core overlay — complete canonical breadth and begin source-depth accounting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V62,M=root.OBOL_METHODOLOGY_V62;if(!C||!D||!M)throw new Error('Obol core plus v6.2 dashboard/methodology metadata are required before core-v6.2.js');
const VERSION='6.2.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard61,oldBuildNext=C.buildNext52;
function ensure62(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard62&&typeof s.ui.dashboard62==='object'?s.ui.dashboard62:{};s.ui.dashboard62={showCanonicalCompletion:old.showCanonicalCompletion!==false,showSourceDepth:old.showSourceDepth!==false,sourceDepthFilter:old.sourceDepthFilter||'all',sourceDepthQuery:old.sourceDepthQuery||''};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure62(oldNew());};C.coerceState=function(raw){return ensure62(oldCoerce(raw));};C.migrateV1=function(raw){return ensure62(oldMigrate(raw));};
const FILE_PRIORITY={'adcs.md':100,'delegation.md':95,'acl.md':90,'mitm.md':85,'authenticated.md':82,'sccm.md':80,'admin.md':78,'no_creds.md':76,'lat_move.md':74,'low_access.md':72,'crack_hash.md':70};
function sectionIndex(){const out={};const O=root.OBOL_ORANGE_AD_2025_03||{};for(const f of O.files||[])for(const s of f.sections||[])out[s.key]={file:f.file,section:s};return out;}
function sourceDepth62(lanes){const idx=sectionIndex(),rows=[];for(const key of M.sourceDepthKeys||[]){const hit=idx[key]||{},s=hit.section||{},audit=s.sourceDepthAudit62&&typeof s.sourceDepthAudit62==='object'?s.sourceDepthAudit62:{status:'needs-audit'};const status=audit.status||'needs-audit',reviewed=['modeled','superseded','rejected'].includes(status);rows.push({key,label:s.label||key,file:hit.file||'',canonicalStatus:s.status||'unknown',auditStatus:status,reviewed,reason:audit.reason||'',cardIds:[...(s.cardIds||[])],priority:FILE_PRIORITY[hit.file]||50,detail:reviewed?(audit.reason||'Source-depth item reviewed.'):'Audit the pinned Orange subordinate nodes, decision edges, tool choices, failure states, artifacts, Evidence, Next Steps, cleanup, GUI controls, and reporting implications beneath this partial canonical section.'});}
 rows.sort((a,b)=>b.priority-a.priority||String(a.file).localeCompare(String(b.file))||String(a.label).localeCompare(String(b.label)));const reviewed=rows.filter(x=>x.reviewed).length,pending=rows.length-reviewed;return{rows,total:rows.length,reviewed,pending,reviewedPct:rows.length?Math.round(reviewed/rows.length*100):100,baseline:M.sourceDepthBaseline||rows.length,states:[...(M.sourceDepthStates||[])],statement:D.sourceDepthPhase.statement,plan:[...(D.sourceDepthPhase.plan||[])]};}
function buildNext62(lanes){const base=oldBuildNext?oldBuildNext(lanes):{rows:[],implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0},depth=sourceDepth62(lanes);if(base.total>0)return{...base,sourceDepth:depth.pending,sourceDepthReviewed:depth.reviewed,sourceDepthBaseline:depth.total,sourceDepthRows:depth.rows};const rows=depth.rows.filter(x=>!x.reviewed).map(x=>({id:'depth:'+x.key,priority:4,kind:'source-depth',label:x.label,file:x.file,status:x.canonicalStatus,missing:[],detail:x.detail,key:x.key,auditStatus:x.auditStatus}));return{...base,rows,total:rows.length,sourceDepth:rows.length,sourceDepthReviewed:depth.reviewed,sourceDepthBaseline:depth.total,sourceDepthRows:depth.rows};}
function canonicalAdvance62(lanes){const coverage=C.mindmapCoverage42?C.mindmapCoverage42(lanes):{},readiness=C.deliveryReadiness52?C.deliveryReadiness52(lanes):null,key='trusts.parent-child';let row=null;for(const f of coverage.files||[])for(const s of f.sections||[])if(s.key===key)row={key:s.key,label:s.label,file:f.file,status:s.status,cardIds:(s.linkedCards||[]).map(x=>x.id),ready:readiness?!!(readiness.rows||[]).find(x=>x.key===key&&x.ready):false};return{row,fromImplemented:M.baseline61.implemented,toImplemented:coverage.implemented||0,delta:(coverage.implemented||0)-M.baseline61.implemented,fromGap:M.baseline61.gap,toGap:coverage.gap||0,gapDelta:M.baseline61.gap-(coverage.gap||0),coveragePct:coverage.coveragePct||0,representedPct:coverage.representedPct||0,partial:coverage.partial||0};}
function releaseQuality62(lanes){const q=oldBuildNext?oldBuildNext(lanes):{implementedQuality:0,mappedDelivery:0,canonicalGaps:0};return{implementedQuality:q.implementedQuality||0,mappedDelivery:q.mappedDelivery||0,canonicalGaps:q.canonicalGaps||0,clean:(q.implementedQuality||0)===0&&(q.mappedDelivery||0)===0,validator:'tools/validate-release-quality.js',requiredStatusCheck:'test'};}
function northStarDashboard62(state,lanes,ctx){ensure62(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},advance=canonicalAdvance62(lanes),depth=sourceDepth62(lanes),q=buildNext62(lanes),quality=releaseQuality62(lanes),metrics=[...(base.metrics||[]).filter(x=>!['v62-canonical-completion','v62-source-depth','v62-release-quality'].includes(x.id))];metrics.push({id:'v62-canonical-completion',label:'Canonical representation',value:advance.representedPct,detail:advance.toImplemented+'/127 fully implemented · '+advance.partial+' partial · '+advance.toGap+' gaps',count:127-advance.toGap,total:127});metrics.push({id:'v62-source-depth',label:'Orange source-depth audit',value:depth.reviewedPct,detail:depth.reviewed+'/'+depth.total+' v6.2 partial-branch baselines reviewed; canonical representation does not imply source exhaustion',count:depth.reviewed,total:depth.total});metrics.push({id:'v62-release-quality',label:'Release quality debt',value:quality.clean?100:0,detail:quality.clean?'0 implemented-quality and 0 mapped-delivery repairs':'higher-priority delivery debt blocks source-depth expansion',count:quality.clean?1:0,total:1});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v6.2').map(x=>x.release==='v6.1'?{...x,implemented:92,partial:34,gap:1,stale:0,coveragePct:72,representedPct:99}:x);if(!milestones.some(x=>x.release==='v6.2'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,canonicalAdvance62:advance,sourceDepth62:depth,buildNext62:q,releaseQuality62:quality,sourceDepthPlan62:{...D.sourceDepthPhase}};}
C.ensure62=ensure62;C.sourceDepth62=sourceDepth62;C.buildNext62=buildNext62;C.canonicalAdvance62=canonicalAdvance62;C.releaseQuality62=releaseQuality62;C.northStarDashboard62=northStarDashboard62;C.sanitizedCopy=function(state){return ensure62(oldSanitize(state));};
root.OBOL_CORE_V62={VERSION,ensure62,sourceDepth62,buildNext62,canonicalAdvance62,releaseQuality62,northStarDashboard62};
}

/* obol-core-delta: assets/core-v6.4.js */
{
// Obol v6.4 core overlay — atomic Orange source-fidelity and North Star requirement accounting.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V64,M=root.OBOL_METHODOLOGY_V64,F=root.OBOL_ORANGE_FIDELITY_V64,O=root.OBOL_ORANGE_AD_2025_03;
if(!C||!D||!M||!F||!O)throw new Error('Obol core plus v6.4 dashboard/methodology/source-fidelity metadata are required before core-v6.4.js');
const VERSION='6.4.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard62;
function ensure64(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard64&&typeof s.ui.dashboard64==='object'?s.ui.dashboard64:{};s.ui.dashboard64={showSourceFidelity:old.showSourceFidelity!==false,fidelityFamily:old.fidelityFamily||'all',fidelityStatus:old.fidelityStatus||'all',fidelityQuery:old.fidelityQuery||''};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure64(oldNew());};C.coerceState=function(raw){return ensure64(oldCoerce(raw));};C.migrateV1=function(raw){return ensure64(oldMigrate(raw));};
function pct(n,d){return d?Math.round(n/d*100):0;}
function cardIndex(lanes){const out={};for(const l of lanes||[])for(const c of l.cards||[])out[c.id]=c;return out;}
function sourceFidelity64(lanes){const cards=cardIndex(lanes),terminal=new Set(['modeled','superseded','rejected']),dimensions=F.dimensions||[],rows=(F.units||[]).map(u=>{const review={...(u.review||{})},reviewed=terminal.has(u.auditStatus),complete=reviewed&&dimensions.every(d=>review[d.id]===true),existingOwners=(u.ownerCardIds||[]).filter(id=>!!cards[id]);return{...u,review,reviewed,complete,existingOwners,broadOwned:existingOwners.length>0};});const filesTotal=(O.files||[]).length,filesAtomized=(F.atomizedFiles||[]).length,baselineTotal=M.sourceDepthBaseline||0,baselineAtomized=(F.atomizedCanonicalKeys||[]).length,reviewed=rows.filter(x=>x.reviewed).length,complete=rows.filter(x=>x.complete).length,broadOwned=rows.filter(x=>x.broadOwned).length;const matrix=dimensions.map(d=>{const count=rows.filter(x=>x.review&&x.review[d.id]===true).length;return{id:d.id,label:d.label,phase:d.phase,count,total:rows.length,pct:pct(count,rows.length)};});const families=[...new Set(rows.map(x=>x.family))];return{version:VERSION,rows,total:rows.length,reviewed,complete,pending:rows.length-reviewed,reviewedPct:pct(reviewed,rows.length),completePct:pct(complete,rows.length),broadOwned,broadOwnedPct:pct(broadOwned,rows.length),families,dimensions,matrix,files:{total:filesTotal,atomized:filesAtomized,pending:filesTotal-filesAtomized,pct:pct(filesAtomized,filesTotal)},partialBaseline:{total:baselineTotal,atomized:baselineAtomized,pending:baselineTotal-baselineAtomized,pct:pct(baselineAtomized,baselineTotal)},adcs:{file:'adcs.md',units:rows.length,families:families.length,reviewed,complete,broadOwned,canonicalSections:baselineAtomized,sourceSha:(rows[0]&&rows[0].sourceSha)||''},statement:D.sourceFidelityPhase.statement,completionRule:D.sourceFidelityPhase.completionRule};}
function buildNext64(lanes){const base=C.buildNext52?C.buildNext52(lanes):{rows:[],implementedQuality:0,mappedDelivery:0,canonicalGaps:0,total:0},fidelity=sourceFidelity64(lanes),depth=C.sourceDepth62?C.sourceDepth62(lanes):{rows:[]};if((base.implementedQuality||0)>0||(base.mappedDelivery||0)>0||(base.canonicalGaps||0)>0)return{...base,sourceFidelity:fidelity.pending,sourceFidelityReviewed:fidelity.reviewed,sourceInventory:fidelity.partialBaseline.pending,sourceFidelityRows:fidelity.rows};const atomic=fidelity.rows.filter(x=>!x.reviewed).map((x,i)=>({id:'fidelity:'+x.id,priority:4,kind:'source-fidelity',label:x.label,file:x.sourceFile,status:x.auditStatus,missing:[],detail:'Atomic Orange source unit '+x.family+' under '+x.canonicalKey+' requires a full North Star fidelity audit.',key:x.id,canonicalKey:x.canonicalKey,family:x.family,order:i}));const atomized=new Set(F.atomizedCanonicalKeys||[]),inventory=(depth.rows||[]).filter(x=>!x.reviewed&&!atomized.has(x.key)).map((x,i)=>({id:'inventory:'+x.key,priority:5,kind:'source-depth-inventory',label:x.label,file:x.file,status:x.auditStatus,missing:[],detail:'Decompose this broad partial canonical section into meaningful source nodes, variants, decision edges, tools, and outcomes before claiming source fidelity.',key:x.key,order:i}));const rows=[...atomic,...inventory];return{...base,rows,total:rows.length,sourceFidelity:atomic.length,sourceFidelityReviewed:fidelity.reviewed,sourceFidelityBaseline:fidelity.total,sourceInventory:inventory.length,sourceInventoryAtomized:fidelity.partialBaseline.atomized,sourceInventoryBaseline:fidelity.partialBaseline.total,sourceFidelityRows:fidelity.rows};}
function northStarCompliance64(lanes){const f=sourceFidelity64(lanes);return{dimensions:f.matrix,inventoryCaptured:f.matrix.filter(x=>x.phase==='inventory').reduce((a,x)=>a+x.count,0),inventoryPossible:f.matrix.filter(x=>x.phase==='inventory').reduce((a,x)=>a+x.total,0),deliveryCaptured:f.matrix.filter(x=>x.phase==='delivery').reduce((a,x)=>a+x.count,0),deliveryPossible:f.matrix.filter(x=>x.phase==='delivery').reduce((a,x)=>a+x.total,0),terminalCaptured:f.matrix.filter(x=>x.phase==='completion').reduce((a,x)=>a+x.count,0),terminalPossible:f.matrix.filter(x=>x.phase==='completion').reduce((a,x)=>a+x.total,0)};}
function northStarDashboard64(state,lanes,ctx){ensure64(state);const base=oldDashboard?oldDashboard(state,lanes,ctx):{metrics:[],milestones:[],coverage:{}},fidelity=sourceFidelity64(lanes),compliance=northStarCompliance64(lanes),q=buildNext64(lanes),metrics=[...(base.metrics||[]).filter(x=>!['v64-source-inventory','v64-source-fidelity','v64-adcs-fidelity','v64-partial-atomization'].includes(x.id))];metrics.push({id:'v64-source-inventory',label:'Orange files atomized',value:fidelity.files.pct,detail:fidelity.files.atomized+'/'+fidelity.files.total+' methodology source files have an atomic node inventory',count:fidelity.files.atomized,total:fidelity.files.total});metrics.push({id:'v64-partial-atomization',label:'Partial baselines atomized',value:fidelity.partialBaseline.pct,detail:fidelity.partialBaseline.atomized+'/'+fidelity.partialBaseline.total+' frozen partial canonical sections have subordinate source-unit accounting',count:fidelity.partialBaseline.atomized,total:fidelity.partialBaseline.total});metrics.push({id:'v64-source-fidelity',label:'Atomic source fidelity complete',value:fidelity.completePct,detail:fidelity.complete+'/'+fidelity.total+' inventoried source units have a terminal disposition and every North Star delivery dimension complete',count:fidelity.complete,total:fidelity.total});metrics.push({id:'v64-adcs-fidelity',label:'AD CS atomic fidelity',value:fidelity.completePct,detail:fidelity.adcs.units+' atomic units across '+fidelity.adcs.families+' source families; '+fidelity.broadOwned+' have broad Obol owners but only '+fidelity.complete+' are fidelity-complete',count:fidelity.complete,total:fidelity.total});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v6.4');if(!milestones.some(x=>x.release==='v6.4'))milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,sourceFidelity64:fidelity,northStarCompliance64:compliance,buildNext64:q,sourceFidelityPlan64:{...D.sourceFidelityPhase}};}
C.ensure64=ensure64;C.sourceFidelity64=sourceFidelity64;C.buildNext64=buildNext64;C.northStarCompliance64=northStarCompliance64;C.northStarDashboard64=northStarDashboard64;C.sanitizedCopy=function(state){return ensure64(oldSanitize(state));};
root.OBOL_CORE_V64={VERSION,ensure64,sourceFidelity64,buildNext64,northStarCompliance64,northStarDashboard64};
}

/* obol-core-delta: assets/core-v6.5.js */
{
// Obol v6.5 core overlay — first atomic AD CS fidelity delivery wave.
const C=root.OBOL_CORE_V2,D=root.OBOL_DASHBOARD_V65,M=root.OBOL_METHODOLOGY_V65;
if(!C||!D||!M||!C.sourceFidelity64||!C.buildNext64||!C.northStarDashboard64)throw new Error('Obol core plus v6.4 fidelity model and v6.5 metadata are required before core-v6.5.js');
const VERSION='6.5.0',oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard64;
function ensure65(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard65&&typeof s.ui.dashboard65==='object'?s.ui.dashboard65:{};s.ui.dashboard65={showFidelityWave:old.showFidelityWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure65(oldNew());};C.coerceState=function(raw){return ensure65(oldCoerce(raw));};C.migrateV1=function(raw){return ensure65(oldMigrate(raw));};
function sourceFidelity65(lanes){return C.sourceFidelity64(lanes);}
function buildNext65(lanes){return C.buildNext64(lanes);}
function northStarDashboard65(state,lanes,ctx){ensure65(state);const base=oldDashboard(state,lanes,ctx),f=sourceFidelity65(lanes),q=buildNext65(lanes),metrics=[...(base.metrics||[]).filter(x=>x.id!=='v65-fidelity-wave')];metrics.push({id:'v65-fidelity-wave',label:'AD CS atomic delivery',value:f.completePct,detail:f.complete+'/'+f.total+' inventoried AD CS source units are fidelity-complete; '+f.pending+' remain pending',count:f.complete,total:f.total});let milestones=[...(base.milestones||[])].filter(x=>x.release!=='v6.5');milestones.push({...D.releaseMilestone});return{...base,version:VERSION,metrics,milestones,sourceFidelity65:f,buildNext65:q,fidelityWave65:{...D.fidelityWave}};}
C.ensure65=ensure65;C.sourceFidelity65=sourceFidelity65;C.buildNext65=buildNext65;C.northStarDashboard65=northStarDashboard65;C.sanitizedCopy=function(state){return ensure65(oldSanitize(state));};
root.OBOL_CORE_V65={VERSION,ensure65,sourceFidelity65,buildNext65,northStarDashboard65};
}

/* obol-core-delta: assets/core-v6.6.js */
{
// Obol v6.6 core overlay — one authoritative current project-status adapter.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V66;
if(!C||!P||!C.northStarDashboard65||!C.sourceFidelity65||!C.buildNext65)throw new Error('Obol v6.5 core and v6.6 project metadata are required before core-v6.6.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldDashboard=C.northStarDashboard65;
function ensure66(s){
  s=s||{};
  s.obolVersion=VERSION;
  s.ui=s.ui||{};
  const old=s.ui.dashboard66&&typeof s.ui.dashboard66==='object'?s.ui.dashboard66:{};
  s.ui.dashboard66={detailsOpen:old.detailsOpen===true};
  return s;
}
C.VERSION=VERSION;
C.newState=function(){return ensure66(oldNew());};
C.coerceState=function(raw){return ensure66(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure66(oldMigrate(raw));};
function dashboard66(state,lanes,ctx){
  ensure66(state);
  const base=oldDashboard(state,lanes,ctx);
  const milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);
  milestones.push({...P.releaseMilestone});
  return{...base,version:VERSION,milestones,consolidation66:{...P.phase},architecture66:{...P.architecture}};
}
function projectModel66(state,lanes,ctx){
  ensure66(state);
  const dashboard=dashboard66(state,lanes,ctx),coverage=C.mindmapCoverage42(lanes),fidelity=C.sourceFidelity65(lanes),buildNext=C.buildNext65(lanes);
  const recent=(dashboard.milestones||[]).slice(-3).reverse().map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));
  const next=(buildNext.rows||[])[0]||null;
  return{
    version:VERSION,
    release:P.release,
    phase:{...P.phase},
    architecture:{...P.architecture},
    docs:{...P.docs},
    canonical:{total:coverage.sections,implemented:coverage.implemented,partial:coverage.partial,gap:coverage.gap,stale:coverage.stale,completePct:coverage.coveragePct,representedPct:coverage.representedPct},
    source:{filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},
    quality:{implementedQuality:buildNext.implementedQuality||0,mappedDelivery:buildNext.mappedDelivery||0,canonicalGaps:buildNext.canonicalGaps||0,totalDebt:(buildNext.implementedQuality||0)+(buildNext.mappedDelivery||0)},
    buildNext:{total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},
    next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,
    recent,
    details:{dashboard,fidelity,buildNext}
  };
}
C.ensure66=ensure66;
C.northStarDashboard66=dashboard66;
C.projectModel66=projectModel66;
C.sourceFidelity66=function(lanes){return C.sourceFidelity65(lanes);};
C.buildNext66=function(lanes){return C.buildNext65(lanes);};
C.sanitizedCopy=function(state){return ensure66(oldSanitize(state));};
root.OBOL_CORE_V66={VERSION,ensure66,dashboard66,projectModel66};
}

/* obol-core-delta: assets/core-v6.7.js */
{
// Obol v6.7 core overlay — advance the consolidated current model through the next AD CS source-fidelity wave.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V67,M=root.OBOL_METHODOLOGY_V67,F=root.OBOL_ORANGE_FIDELITY_V67;
if(!C||!P||!M||!F||!C.projectModel66||!C.northStarDashboard66||!C.sourceFidelity65||!C.buildNext65)throw new Error('Obol v6.6 current model plus v6.7 methodology/fidelity metadata are required before core-v6.7.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel66,oldDashboard=C.northStarDashboard66;
function ensure67(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard67&&typeof s.ui.dashboard67==='object'?s.ui.dashboard67:{};s.ui.dashboard67={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure67(oldNew());};
C.coerceState=function(raw){return ensure67(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure67(oldMigrate(raw));};
function sourceFidelity67(lanes){return C.sourceFidelity65(lanes);}
function buildNext67(lanes){return C.buildNext65(lanes);}
function northStarDashboard67(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure67(state);return{...base,version:VERSION,milestones,sourceWave67:{...P.sourceWave},phase67:{...P.phase}};}
function projectModel67(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity67(lanes),buildNext=buildNext67(lanes),dashboard=northStarDashboard67(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure67(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave67:{...P.sourceWave}}};}
C.ensure67=ensure67;C.sourceFidelity67=sourceFidelity67;C.buildNext67=buildNext67;C.northStarDashboard67=northStarDashboard67;C.projectModel67=projectModel67;
// v6.6's overview-first UI intentionally calls the consolidated projection boundary by its original name. Keep that call site current without duplicating the Dashboard implementation.
C.projectModel66=projectModel67;
C.sanitizedCopy=function(state){return ensure67(oldSanitize(state));};
root.OBOL_CORE_V67={VERSION,ensure67,sourceFidelity67,buildNext67,northStarDashboard67,projectModel67};
}

/* obol-core-delta: assets/core-v6.8.js */
{
// Obol v6.8 core overlay — advance the consolidated current model through the ESC4/ESC7 source-fidelity wave.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V68,M=root.OBOL_METHODOLOGY_V68,F=root.OBOL_ORANGE_FIDELITY_V68;
if(!C||!P||!M||!F||!C.projectModel67||!C.northStarDashboard67||!C.sourceFidelity67||!C.buildNext67)throw new Error('Obol v6.7 current model plus v6.8 methodology/fidelity metadata are required before core-v6.8.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel67,oldDashboard=C.northStarDashboard67;
function ensure68(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard68&&typeof s.ui.dashboard68==='object'?s.ui.dashboard68:{};s.ui.dashboard68={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure68(oldNew());};
C.coerceState=function(raw){return ensure68(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure68(oldMigrate(raw));};
function sourceFidelity68(lanes){return C.sourceFidelity67(lanes);}
function buildNext68(lanes){return C.buildNext67(lanes);}
function northStarDashboard68(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure68(state);return{...base,version:VERSION,milestones,sourceWave68:{...P.sourceWave},phase68:{...P.phase}};}
function projectModel68(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity68(lanes),buildNext=buildNext68(lanes),dashboard=northStarDashboard68(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure68(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave68:{...P.sourceWave}}};}
C.ensure68=ensure68;C.sourceFidelity68=sourceFidelity68;C.buildNext68=buildNext68;C.northStarDashboard68=northStarDashboard68;C.projectModel68=projectModel68;
// Stable non-versioned current pointers keep tooling/docs from hard-coding a release-specific adapter name.
C.currentProjectModel=projectModel68;C.currentNorthStarDashboard=northStarDashboard68;
// The overview-first v6.6 Dashboard shell still calls this compatibility boundary. Keep it pointed at the current projection until that UI owner is compacted.
C.projectModel66=projectModel68;
C.sanitizedCopy=function(state){return ensure68(oldSanitize(state));};
root.OBOL_CORE_V68={VERSION,ensure68,sourceFidelity68,buildNext68,northStarDashboard68,projectModel68};
}

/* obol-core-delta: assets/core-v6.9.js */
{
// Obol v6.9 core overlay — advance the consolidated current model through the ESC5/ESC6/ESC11 source-fidelity wave.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V69,M=root.OBOL_METHODOLOGY_V69,F=root.OBOL_ORANGE_FIDELITY_V69;
if(!C||!P||!M||!F||!C.projectModel68||!C.northStarDashboard68||!C.sourceFidelity68||!C.buildNext68)throw new Error('Obol v6.8 current model plus v6.9 methodology/fidelity metadata are required before core-v6.9.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel68,oldDashboard=C.northStarDashboard68;
function ensure69(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard69&&typeof s.ui.dashboard69==='object'?s.ui.dashboard69:{};s.ui.dashboard69={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure69(oldNew());};
C.coerceState=function(raw){return ensure69(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure69(oldMigrate(raw));};
function sourceFidelity69(lanes){return C.sourceFidelity68(lanes);}
function buildNext69(lanes){return C.buildNext68(lanes);}
function northStarDashboard69(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure69(state);return{...base,version:VERSION,milestones,sourceWave69:{...P.sourceWave},phase69:{...P.phase}};}
function projectModel69(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity69(lanes),buildNext=buildNext69(lanes),dashboard=northStarDashboard69(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure69(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave69:{...P.sourceWave}}};}
C.ensure69=ensure69;C.sourceFidelity69=sourceFidelity69;C.buildNext69=buildNext69;C.northStarDashboard69=northStarDashboard69;C.projectModel69=projectModel69;
C.currentProjectModel=projectModel69;C.currentNorthStarDashboard=northStarDashboard69;
// The overview-first v6.6 Dashboard shell still calls this compatibility boundary. Keep it pointed at the current projection until that UI owner is compacted.
C.projectModel66=projectModel69;
C.sanitizedCopy=function(state){return ensure69(oldSanitize(state));};
root.OBOL_CORE_V69={VERSION,ensure69,sourceFidelity69,buildNext69,northStarDashboard69,projectModel69};
}

/* obol-core-delta: assets/core-v7.0.js */
{
// Obol v7.0 core overlay — complete AD CS atomic fidelity and advance Build Next into source inventory.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V70,M=root.OBOL_METHODOLOGY_V70,F=root.OBOL_ORANGE_FIDELITY_V70;
if(!C||!P||!M||!F||!C.projectModel69||!C.northStarDashboard69||!C.sourceFidelity69||!C.buildNext69)throw new Error('Obol v6.9 current model plus v7.0 methodology/fidelity metadata are required before core-v7.0.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel69,oldDashboard=C.northStarDashboard69;
function ensure70(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard70&&typeof s.ui.dashboard70==='object'?s.ui.dashboard70:{};s.ui.dashboard70={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure70(oldNew());};
C.coerceState=function(raw){return ensure70(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure70(oldMigrate(raw));};
function sourceFidelity70(lanes){return C.sourceFidelity69(lanes);}
function buildNext70(lanes){return C.buildNext69(lanes);}
function northStarDashboard70(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure70(state);return{...base,version:VERSION,milestones,sourceWave70:{...P.sourceWave},phase70:{...P.phase}};}
function projectModel70(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity70(lanes),buildNext=buildNext70(lanes),dashboard=northStarDashboard70(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure70(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave70:{...P.sourceWave}}};}
C.ensure70=ensure70;C.sourceFidelity70=sourceFidelity70;C.buildNext70=buildNext70;C.northStarDashboard70=northStarDashboard70;C.projectModel70=projectModel70;
C.currentProjectModel=projectModel70;C.currentNorthStarDashboard=northStarDashboard70;
// The overview-first v6.6 Dashboard shell still calls this compatibility boundary. Keep it pointed at the current projection until that UI owner is compacted.
C.projectModel66=projectModel70;
C.sanitizedCopy=function(state){return ensure70(oldSanitize(state));};
root.OBOL_CORE_V70={VERSION,ensure70,sourceFidelity70,buildNext70,northStarDashboard70,projectModel70};
}

/* obol-core-delta: assets/core-v7.1.js */
{
// Obol v7.1 core overlay — advance current project status through Kerberos delegation source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V71,M=root.OBOL_METHODOLOGY_V71,F=root.OBOL_ORANGE_FIDELITY_V71;
if(!C||!P||!M||!F||!C.projectModel70||!C.northStarDashboard70||!C.sourceFidelity70||!C.buildNext70)throw new Error('Obol v7.0 current model plus v7.1 methodology/fidelity metadata are required before core-v7.1.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel70,oldDashboard=C.northStarDashboard70;
function ensure71(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard71&&typeof s.ui.dashboard71==='object'?s.ui.dashboard71:{};s.ui.dashboard71={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure71(oldNew());};
C.coerceState=function(raw){return ensure71(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure71(oldMigrate(raw));};
function sourceFidelity71(lanes){return C.sourceFidelity70(lanes);}
function buildNext71(lanes){return C.buildNext70(lanes);}
function northStarDashboard71(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure71(state);return{...base,version:VERSION,milestones,sourceWave71:{...P.sourceWave},phase71:{...P.phase}};}
function projectModel71(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity71(lanes),buildNext=buildNext71(lanes),dashboard=northStarDashboard71(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure71(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave71:{...P.sourceWave}}};}
C.ensure71=ensure71;C.sourceFidelity71=sourceFidelity71;C.buildNext71=buildNext71;C.northStarDashboard71=northStarDashboard71;C.projectModel71=projectModel71;
C.currentProjectModel=projectModel71;C.currentNorthStarDashboard=northStarDashboard71;
C.projectModel66=projectModel71;
C.sanitizedCopy=function(state){return ensure71(oldSanitize(state));};
root.OBOL_CORE_V71={VERSION,ensure71,sourceFidelity71,buildNext71,northStarDashboard71,projectModel71};
}

/* obol-core-delta: assets/core-v7.2.js */
{
// Obol v7.2 core overlay — advance current project status through ACL / ACE source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V72,M=root.OBOL_METHODOLOGY_V72,F=root.OBOL_ORANGE_FIDELITY_V72;
if(!C||!P||!M||!F||!C.projectModel71||!C.northStarDashboard71||!C.sourceFidelity71||!C.buildNext71)throw new Error('Obol v7.1 current model plus v7.2 methodology/fidelity metadata are required before core-v7.2.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel71,oldDashboard=C.northStarDashboard71;
function ensure72(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard72&&typeof s.ui.dashboard72==='object'?s.ui.dashboard72:{};s.ui.dashboard72={showSourceWave:old.showSourceWave!==false};return s;}
function installBrowserSourceDelivery72(){if(root.OBOL_SOURCE_DELIVERY_V72)return root.OBOL_SOURCE_DELIVERY_V72;const M41=root.OBOL_METHODOLOGY_V41,M47=root.OBOL_METHODOLOGY_V47,lanes=root.OBOL_LANES||[];if(!M41)return null;function card(id){for(const l of lanes)for(const c of l.cards||[])if(c.id===id)return c;return null;}function marker(c,needle,value){if(!c)return;const cmd=(c.commands||[]).find(x=>needle.test(String(x.note||'')));if(cmd&&!String(cmd.run||'').includes(value))cmd.run=String(cmd.run||'')+' && echo '+value;}function ensureReport(c){if(!c||!M47||!M47.reportContract)return;const p=M.profiles&&M.profiles[c.id],r=M47.reportContract(c);if(!r)return;c.report47={...r,evidenceProfile:!!p,evidenceFamily:p&&p.family||'',evidenceSource:p?'v7.2':(c.report47&&c.report47.evidenceSource)||'',claims:p?[...(p.claims||[])]:[...((c.report47&&c.report47.claims)||[])]};const old=(M47.contracts||[]).find(x=>x.cardId===c.id);if(old)Object.assign(old,c.report47);else(M47.contracts||[]).push({...c.report47});}const area=(M41.areas||[]).find(x=>x.id==='acl');if(!area)return null;const all=['dcsync','acl-shadow-credential-72','acl-group-membership-72','acl-group-owner-dacl-72','delegation-rbcd-71','acl-user-password-72','acl-targeted-kerberoast-72','acl-user-logonscript-72','acl-ou-inheritance-72','acl-ou-gplink-72','acl-gmsa-read-72','laps-read','acl-gpo-control-72','acl-dnsadmin-72'];const summary=(area.nodes||[]).find(x=>x.id==='acl-control');if(summary){summary.coverage='implemented';summary.cardIds=all.filter(id=>!!card(id));summary.toolReview={mindmap:['PowerView','BloodHound','Impacket','Certipy','NetExec','native Windows tools'],preferred:['BloodHound','Impacket','Certipy','NetExec','PowerShell'],decision:'supplement',note:'v7.2 atomizes the pinned ACL / ACE source family and preserves distinct proof for rights, mutations, credential/ticket material, access, privilege, and cleanup.'};}const detail=[['dcsync','DCSync replication rights',['dcsync']],['shadow-credentials','Shadow Credentials / msDS-KeyCredentialLink',['acl-shadow-credential-72']],['group-control','Group object control',['acl-group-membership-72','acl-group-owner-dacl-72']],['computer-control','Computer object control / RBCD',['delegation-rbcd-71','acl-shadow-credential-72']],['user-control','User object control',['acl-user-password-72','acl-targeted-kerberoast-72','acl-shadow-credential-72','acl-user-logonscript-72']],['ou-control','OU control and inheritance',['acl-ou-inheritance-72','acl-ou-gplink-72']],['gmsa','ReadGMSAPassword',['acl-gmsa-read-72']],['laps','Read LAPS passwords',['laps-read']],['gpo','GPO control',['acl-gpo-control-72']],['dns-admin','DNS Admin abuse',['acl-dnsadmin-72']]];for(const [id,label,ids] of detail){let n=(area.nodes||[]).find(x=>x.id==='acl-'+id);if(!n){n={id:'acl-'+id,label,coverage:'implemented',cardIds:ids.filter(x=>!!card(x)),toolReview:{mindmap:['Orange acl.md'],preferred:['current v7.2 owners'],decision:'supplement',note:'Atomized in v7.2 with explicit Run, Evidence, Next Steps, reporting, and cleanup boundaries.'}};area.nodes.push(n);}else{n.coverage='implemented';n.cardIds=ids.filter(x=>!!card(x));}}marker(card('acl-group-owner-dacl-72'),/Restore the recorded original owner/i,'OBOL_ACL_OWNER_RESTORED');marker(card('acl-group-owner-dacl-72'),/Mandatory DACL cleanup/i,'OBOL_GROUP_DACL_RESTORED');marker(card('acl-ou-inheritance-72'),/Mandatory cleanup from the exact pre-test backup/i,'OBOL_OU_DACL_RESTORED');for(const id of M.cardIds||[])ensureReport(card(id));root.OBOL_SOURCE_DELIVERY_V72={version:'7.2.0',areaId:'acl',detailNodeIds:detail.map(x=>'acl-'+x[0]),cardIds:all.filter(id=>!!card(id)),browserFallback:true};return root.OBOL_SOURCE_DELIVERY_V72;}
installBrowserSourceDelivery72();
C.VERSION=VERSION;
C.newState=function(){return ensure72(oldNew());};
C.coerceState=function(raw){return ensure72(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure72(oldMigrate(raw));};
function sourceFidelity72(lanes){return C.sourceFidelity71(lanes);}
function buildNext72(lanes){return C.buildNext71(lanes);}
function northStarDashboard72(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure72(state);return{...base,version:VERSION,milestones,sourceWave72:{...P.sourceWave},phase72:{...P.phase}};}
function projectModel72(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity72(lanes),buildNext=buildNext72(lanes),dashboard=northStarDashboard72(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure72(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave72:{...P.sourceWave}}};}
C.ensure72=ensure72;C.installBrowserSourceDelivery72=installBrowserSourceDelivery72;C.sourceFidelity72=sourceFidelity72;C.buildNext72=buildNext72;C.northStarDashboard72=northStarDashboard72;C.projectModel72=projectModel72;
C.currentProjectModel=projectModel72;C.currentNorthStarDashboard=northStarDashboard72;
C.projectModel66=projectModel72;
C.sanitizedCopy=function(state){return ensure72(oldSanitize(state));};
root.OBOL_CORE_V72={VERSION,ensure72,installBrowserSourceDelivery72,sourceFidelity72,buildNext72,northStarDashboard72,projectModel72};
}

/* obol-core-delta: assets/core-v7.3.js */
{
// Obol v7.3 core overlay — advance current project status through MITM / relay source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V73,M=root.OBOL_METHODOLOGY_V73,F=root.OBOL_ORANGE_FIDELITY_V73;
if(!C||!P||!M||!F||!C.projectModel72||!C.northStarDashboard72||!C.sourceFidelity72||!C.buildNext72)throw new Error('Obol v7.2 current model plus v7.3 methodology/fidelity metadata are required before core-v7.3.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel72,oldDashboard=C.northStarDashboard72;
function ensure73(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard73&&typeof s.ui.dashboard73==='object'?s.ui.dashboard73:{};s.ui.dashboard73={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure73(oldNew());};
C.coerceState=function(raw){return ensure73(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure73(oldMigrate(raw));};
function sourceFidelity73(lanes){return C.sourceFidelity72(lanes);}
function buildNext73(lanes){return C.buildNext72(lanes);}
function northStarDashboard73(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure73(state);return{...base,version:VERSION,milestones,sourceWave73:{...P.sourceWave},phase73:{...P.phase}};}
function projectModel73(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity73(lanes),buildNext=buildNext73(lanes),dashboard=northStarDashboard73(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure73(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave73:{...P.sourceWave}}};}
C.ensure73=ensure73;C.sourceFidelity73=sourceFidelity73;C.buildNext73=buildNext73;C.northStarDashboard73=northStarDashboard73;C.projectModel73=projectModel73;
C.currentProjectModel=projectModel73;C.currentNorthStarDashboard=northStarDashboard73;
C.projectModel66=projectModel73;
C.sanitizedCopy=function(state){return ensure73(oldSanitize(state));};
root.OBOL_CORE_V73={VERSION,ensure73,sourceFidelity73,buildNext73,northStarDashboard73,projectModel73};
}

/* obol-core-delta: assets/core-v7.4.js */
{
// Obol v7.4 core overlay — advance current project status through authenticated source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V74,M=root.OBOL_METHODOLOGY_V74,F=root.OBOL_ORANGE_FIDELITY_V74;
if(!C||!P||!M||!F||!C.projectModel73||!C.northStarDashboard73||!C.sourceFidelity73||!C.buildNext73)throw new Error('Obol v7.3 current model plus v7.4 methodology/fidelity metadata are required before core-v7.4.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel73,oldDashboard=C.northStarDashboard73;
function ensure74(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard74&&typeof s.ui.dashboard74==='object'?s.ui.dashboard74:{};s.ui.dashboard74={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure74(oldNew());};
C.coerceState=function(raw){return ensure74(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure74(oldMigrate(raw));};
function sourceFidelity74(lanes){return C.sourceFidelity73(lanes);}
function buildNext74(lanes){return C.buildNext73(lanes);}
function northStarDashboard74(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure74(state);return{...base,version:VERSION,milestones,sourceWave74:{...P.sourceWave},phase74:{...P.phase}};}
function projectModel74(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity74(lanes),buildNext=buildNext74(lanes),dashboard=northStarDashboard74(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure74(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave74:{...P.sourceWave}}};}
C.ensure74=ensure74;C.sourceFidelity74=sourceFidelity74;C.buildNext74=buildNext74;C.northStarDashboard74=northStarDashboard74;C.projectModel74=projectModel74;
C.currentProjectModel=projectModel74;C.currentNorthStarDashboard=northStarDashboard74;C.projectModel66=projectModel74;
C.sanitizedCopy=function(state){return ensure74(oldSanitize(state));};
root.OBOL_CORE_V74={VERSION,ensure74,sourceFidelity74,buildNext74,northStarDashboard74,projectModel74};
}

/* obol-core-delta: assets/core-v7.5.js */
{
// Obol v7.5 core overlay — advance current project status through SCCM source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V75,M=root.OBOL_METHODOLOGY_V75,F=root.OBOL_ORANGE_FIDELITY_V75;
if(!C||!P||!M||!F||!C.projectModel74||!C.northStarDashboard74||!C.sourceFidelity74||!C.buildNext74)throw new Error('Obol v7.4 current model plus v7.5 methodology/fidelity metadata are required before core-v7.5.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel74,oldDashboard=C.northStarDashboard74;
function ensure75(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard75&&typeof s.ui.dashboard75==='object'?s.ui.dashboard75:{};s.ui.dashboard75={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;
C.newState=function(){return ensure75(oldNew());};
C.coerceState=function(raw){return ensure75(oldCoerce(raw));};
C.migrateV1=function(raw){return ensure75(oldMigrate(raw));};
function sourceFidelity75(lanes){return C.sourceFidelity74(lanes);}
function buildNext75(lanes){return C.buildNext74(lanes);}
function northStarDashboard75(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure75(state);return{...base,version:VERSION,milestones,sourceWave75:{...P.sourceWave},phase75:{...P.phase}};}
function projectModel75(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity75(lanes),buildNext=buildNext75(lanes),dashboard=northStarDashboard75(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure75(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave75:{...P.sourceWave}}};}
C.ensure75=ensure75;C.sourceFidelity75=sourceFidelity75;C.buildNext75=buildNext75;C.northStarDashboard75=northStarDashboard75;C.projectModel75=projectModel75;
C.currentProjectModel=projectModel75;C.currentNorthStarDashboard=northStarDashboard75;C.projectModel66=projectModel75;
C.sanitizedCopy=function(state){return ensure75(oldSanitize(state));};
root.OBOL_CORE_V75={VERSION,ensure75,sourceFidelity75,buildNext75,northStarDashboard75,projectModel75};
}

/* obol-core-delta: assets/core-v7.6.js */
{
// Obol v7.6 core overlay — advance current project status through admin source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V76,M=root.OBOL_METHODOLOGY_V76,F=root.OBOL_ORANGE_FIDELITY_V76;
if(!C||!P||!M||!F||!C.projectModel75||!C.northStarDashboard75||!C.sourceFidelity75||!C.buildNext75)throw new Error('Obol v7.5 current model plus v7.6 methodology/fidelity metadata are required before core-v7.6.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel75,oldDashboard=C.northStarDashboard75;
function ensure76(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard76&&typeof s.ui.dashboard76==='object'?s.ui.dashboard76:{};s.ui.dashboard76={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure76(oldNew());};C.coerceState=function(raw){return ensure76(oldCoerce(raw));};C.migrateV1=function(raw){return ensure76(oldMigrate(raw));};
function sourceFidelity76(lanes){return C.sourceFidelity75(lanes);}function buildNext76(lanes){return C.buildNext75(lanes);}
function northStarDashboard76(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure76(state);return{...base,version:VERSION,milestones,sourceWave76:{...P.sourceWave},phase76:{...P.phase}};}
function projectModel76(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity76(lanes),buildNext=buildNext76(lanes),dashboard=northStarDashboard76(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure76(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave76:{...P.sourceWave}}};}
C.ensure76=ensure76;C.sourceFidelity76=sourceFidelity76;C.buildNext76=buildNext76;C.northStarDashboard76=northStarDashboard76;C.projectModel76=projectModel76;C.currentProjectModel=projectModel76;C.currentNorthStarDashboard=northStarDashboard76;C.projectModel66=projectModel76;C.sanitizedCopy=function(state){return ensure76(oldSanitize(state));};
root.OBOL_CORE_V76={VERSION,ensure76,sourceFidelity76,buildNext76,northStarDashboard76,projectModel76};
}

/* obol-core-delta: assets/core-v7.7.js */
{
// Obol v7.7 core overlay — advance current project status through no-credentials source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V77,M=root.OBOL_METHODOLOGY_V77,F=root.OBOL_ORANGE_FIDELITY_V77;
if(!C||!P||!M||!F||!C.projectModel76||!C.northStarDashboard76||!C.sourceFidelity76||!C.buildNext76)throw new Error('Obol v7.6 current model plus v7.7 methodology/fidelity metadata are required before core-v7.7.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel76,oldDashboard=C.northStarDashboard76;
function ensure77(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard77&&typeof s.ui.dashboard77==='object'?s.ui.dashboard77:{};s.ui.dashboard77={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure77(oldNew());};C.coerceState=function(raw){return ensure77(oldCoerce(raw));};C.migrateV1=function(raw){return ensure77(oldMigrate(raw));};
function sourceFidelity77(lanes){return C.sourceFidelity76(lanes);}function buildNext77(lanes){return C.buildNext76(lanes);}
function northStarDashboard77(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure77(state);return{...base,version:VERSION,milestones,sourceWave77:{...P.sourceWave},phase77:{...P.phase}};}
function projectModel77(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity77(lanes),buildNext=buildNext77(lanes),dashboard=northStarDashboard77(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure77(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave77:{...P.sourceWave}}};}
C.ensure77=ensure77;C.sourceFidelity77=sourceFidelity77;C.buildNext77=buildNext77;C.northStarDashboard77=northStarDashboard77;C.projectModel77=projectModel77;C.currentProjectModel=projectModel77;C.currentNorthStarDashboard=northStarDashboard77;C.projectModel66=projectModel77;C.sanitizedCopy=function(state){return ensure77(oldSanitize(state));};
root.OBOL_CORE_V77={VERSION,ensure77,sourceFidelity77,buildNext77,northStarDashboard77,projectModel77};
}

/* obol-core-delta: assets/core-v7.8.js */
{
// Obol v7.8 core overlay — advance current project status through lateral-movement source depth.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V78,M=root.OBOL_METHODOLOGY_V78,F=root.OBOL_ORANGE_FIDELITY_V78;
if(!C||!P||!M||!F||!C.projectModel77||!C.northStarDashboard77||!C.sourceFidelity77||!C.buildNext77)throw new Error('Obol v7.7 current model plus v7.8 methodology/fidelity metadata are required before core-v7.8.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel77,oldDashboard=C.northStarDashboard77;
function ensure78(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard78&&typeof s.ui.dashboard78==='object'?s.ui.dashboard78:{};s.ui.dashboard78={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure78(oldNew());};C.coerceState=function(raw){return ensure78(oldCoerce(raw));};C.migrateV1=function(raw){return ensure78(oldMigrate(raw));};
function sourceFidelity78(lanes){return C.sourceFidelity77(lanes);}function buildNext78(lanes){return C.buildNext77(lanes);}
function northStarDashboard78(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure78(state);return{...base,version:VERSION,milestones,sourceWave78:{...P.sourceWave},phase78:{...P.phase}};}
function projectModel78(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity78(lanes),buildNext=buildNext78(lanes),dashboard=northStarDashboard78(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure78(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave78:{...P.sourceWave}}};}
C.ensure78=ensure78;C.sourceFidelity78=sourceFidelity78;C.buildNext78=buildNext78;C.northStarDashboard78=northStarDashboard78;C.projectModel78=projectModel78;C.currentProjectModel=projectModel78;C.currentNorthStarDashboard=northStarDashboard78;C.projectModel66=projectModel78;C.sanitizedCopy=function(state){return ensure78(oldSanitize(state));};
root.OBOL_CORE_V78={VERSION,ensure78,sourceFidelity78,buildNext78,northStarDashboard78,projectModel78};
}

/* obol-core-delta: assets/core-v7.9.js */
{
// Obol v7.9 core overlay — advance current project status through low-access frozen-baseline completion.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V79,M=root.OBOL_METHODOLOGY_V79,F=root.OBOL_ORANGE_FIDELITY_V79;
if(!C||!P||!M||!F||!C.projectModel78||!C.northStarDashboard78||!C.sourceFidelity78||!C.buildNext78)throw new Error('Obol v7.8 current model plus v7.9 methodology/fidelity metadata are required before core-v7.9.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel78,oldDashboard=C.northStarDashboard78;
function ensure79(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard79&&typeof s.ui.dashboard79==='object'?s.ui.dashboard79:{};s.ui.dashboard79={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure79(oldNew());};C.coerceState=function(raw){return ensure79(oldCoerce(raw));};C.migrateV1=function(raw){return ensure79(oldMigrate(raw));};
function sourceFidelity79(lanes){return C.sourceFidelity78(lanes);}function buildNext79(lanes){return C.buildNext78(lanes);}
function northStarDashboard79(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure79(state);return{...base,version:VERSION,milestones,sourceWave79:{...P.sourceWave},phase79:{...P.phase}};}
function projectModel79(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity79(lanes),buildNext=buildNext79(lanes),dashboard=northStarDashboard79(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure79(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave79:{...P.sourceWave}}};}
C.ensure79=ensure79;C.sourceFidelity79=sourceFidelity79;C.buildNext79=buildNext79;C.northStarDashboard79=northStarDashboard79;C.projectModel79=projectModel79;C.currentProjectModel=projectModel79;C.currentNorthStarDashboard=northStarDashboard79;C.projectModel66=projectModel79;C.sanitizedCopy=function(state){return ensure79(oldSanitize(state));};
root.OBOL_CORE_V79={VERSION,ensure79,sourceFidelity79,buildNext79,northStarDashboard79,projectModel79};
}

/* obol-core-delta: assets/core-v8.0.js */
{
// Obol v8.0 core overlay — complete frozen-baseline accounting and enter whole-file inventory.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V80,M=root.OBOL_METHODOLOGY_V80,F=root.OBOL_ORANGE_FIDELITY_V80,O=root.OBOL_ORANGE_AD_2025_03,L=root.OBOL_ORANGE_FIDELITY_V64;
if(!C||!P||!M||!F||!O||!L||!C.projectModel79||!C.northStarDashboard79||!C.sourceFidelity79||!C.buildNext79)throw new Error('Obol v7.9 current model plus v8.0 methodology/fidelity metadata are required before core-v8.0.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel79,oldDashboard=C.northStarDashboard79;
function ensure80(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard80&&typeof s.ui.dashboard80==='object'?s.ui.dashboard80:{};s.ui.dashboard80={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure80(oldNew());};C.coerceState=function(raw){return ensure80(oldCoerce(raw));};C.migrateV1=function(raw){return ensure80(oldMigrate(raw));};
function sourceFidelity80(lanes){return C.sourceFidelity79(lanes);}
const FILE_ORDER=P.sourceWave.fileInventoryQueue||[];
function wholeFileRows(){const atomized=new Set(L.atomizedFiles||[]),byName={};for(const f of O.files||[])byName[f.file]=f;return FILE_ORDER.filter(x=>byName[x]&&!atomized.has(x)).map((file,i)=>{const f=byName[file];return{id:'file-inventory:'+file,priority:6,kind:'source-file-inventory',label:(f.title||file)+' source inventory',file,key:file,status:'needs-audit',missing:[],order:i,detail:'Atomize the complete pinned '+file+' source into meaningful branches, variants, prerequisites, tools, outcomes, cleanup, Evidence, Next Steps, and reporting before marking the file fully inventoried.'};});}
function buildNext80(lanes){const base=C.buildNext79(lanes);if((base.total||0)>0)return base;const rows=wholeFileRows();return{...base,rows,total:rows.length,sourceFidelity:0,sourceInventory:rows.length,sourceFileInventory:rows.length,sourceInventoryPhase:'whole-file',sourceInventoryRows:rows};}
function northStarDashboard80(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure80(state);return{...base,version:VERSION,milestones,sourceWave80:{...P.sourceWave},phase80:{...P.phase},wholeFileInventory80:wholeFileRows()};}
function projectModel80(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity80(lanes),buildNext=buildNext80(lanes),dashboard=northStarDashboard80(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure80(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave80:{...P.sourceWave}}};}
C.ensure80=ensure80;C.sourceFidelity80=sourceFidelity80;C.buildNext80=buildNext80;C.northStarDashboard80=northStarDashboard80;C.projectModel80=projectModel80;C.currentProjectModel=projectModel80;C.currentNorthStarDashboard=northStarDashboard80;C.projectModel66=projectModel80;C.sanitizedCopy=function(state){return ensure80(oldSanitize(state));};
root.OBOL_CORE_V80={VERSION,ensure80,sourceFidelity80,buildNext80,northStarDashboard80,projectModel80,wholeFileRows80:wholeFileRows};
}

/* obol-core-delta: assets/core-v8.1.js */
{
// Obol v8.1 core overlay — project completed low_access.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V81,M=root.OBOL_METHODOLOGY_V81,F=root.OBOL_ORANGE_FIDELITY_V81;
if(!C||!P||!M||!F||!C.projectModel80||!C.northStarDashboard80||!C.sourceFidelity80||!C.buildNext80)throw new Error('Obol v8.0 current model plus v8.1 methodology/fidelity metadata are required before core-v8.1.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel80,oldDashboard=C.northStarDashboard80;
function ensure81(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard81&&typeof s.ui.dashboard81==='object'?s.ui.dashboard81:{};s.ui.dashboard81={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure81(oldNew());};C.coerceState=function(raw){return ensure81(oldCoerce(raw));};C.migrateV1=function(raw){return ensure81(oldMigrate(raw));};
function sourceFidelity81(lanes){return C.sourceFidelity80(lanes);}
function buildNext81(lanes){return C.buildNext80(lanes);}
function northStarDashboard81(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure81(state);return{...base,version:VERSION,milestones,sourceWave81:{...P.sourceWave},phase81:{...P.phase}};}
function projectModel81(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity81(lanes),buildNext=buildNext81(lanes),dashboard=northStarDashboard81(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure81(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave81:{...P.sourceWave}}};}
C.ensure81=ensure81;C.sourceFidelity81=sourceFidelity81;C.buildNext81=buildNext81;C.northStarDashboard81=northStarDashboard81;C.projectModel81=projectModel81;C.currentProjectModel=projectModel81;C.currentNorthStarDashboard=northStarDashboard81;C.projectModel66=projectModel81;C.sanitizedCopy=function(state){return ensure81(oldSanitize(state));};
root.OBOL_CORE_V81={VERSION,ensure81,sourceFidelity81,buildNext81,northStarDashboard81,projectModel81};
}

/* obol-core-delta: assets/core-v8.2.js */
{
// Obol v8.2 core overlay — project completed crack_hash.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V82,M=root.OBOL_METHODOLOGY_V82,F=root.OBOL_ORANGE_FIDELITY_V82;
if(!C||!P||!M||!F||!C.projectModel81||!C.northStarDashboard81||!C.sourceFidelity81||!C.buildNext81)throw new Error('Obol v8.1 current model plus v8.2 methodology/fidelity metadata are required before core-v8.2.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel81,oldDashboard=C.northStarDashboard81;
function ensure82(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard82&&typeof s.ui.dashboard82==='object'?s.ui.dashboard82:{};s.ui.dashboard82={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure82(oldNew());};C.coerceState=function(raw){return ensure82(oldCoerce(raw));};C.migrateV1=function(raw){return ensure82(oldMigrate(raw));};
function sourceFidelity82(lanes){return C.sourceFidelity81(lanes);}
function buildNext82(lanes){return C.buildNext81(lanes);}
function northStarDashboard82(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure82(state);return{...base,version:VERSION,milestones,sourceWave82:{...P.sourceWave},phase82:{...P.phase}};}
function projectModel82(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity82(lanes),buildNext=buildNext82(lanes),dashboard=northStarDashboard82(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure82(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave82:{...P.sourceWave}}};}
C.ensure82=ensure82;C.sourceFidelity82=sourceFidelity82;C.buildNext82=buildNext82;C.northStarDashboard82=northStarDashboard82;C.projectModel82=projectModel82;C.currentProjectModel=projectModel82;C.currentNorthStarDashboard=northStarDashboard82;C.projectModel66=projectModel82;C.sanitizedCopy=function(state){return ensure82(oldSanitize(state));};
root.OBOL_CORE_V82={VERSION,ensure82,sourceFidelity82,buildNext82,northStarDashboard82,projectModel82};
}

/* obol-core-delta: assets/core-v8.3.js */
{
// Obol v8.3 core overlay — project completed low_hanging.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V83,M=root.OBOL_METHODOLOGY_V83,F=root.OBOL_ORANGE_FIDELITY_V83;
if(!C||!P||!M||!F||!C.projectModel82||!C.northStarDashboard82||!C.sourceFidelity82||!C.buildNext82)throw new Error('Obol v8.2 current model plus v8.3 methodology/fidelity metadata are required before core-v8.3.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel82,oldDashboard=C.northStarDashboard82;
function ensure83(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard83&&typeof s.ui.dashboard83==='object'?s.ui.dashboard83:{};s.ui.dashboard83={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure83(oldNew());};C.coerceState=function(raw){return ensure83(oldCoerce(raw));};C.migrateV1=function(raw){return ensure83(oldMigrate(raw));};
function sourceFidelity83(lanes){return C.sourceFidelity82(lanes);}
function buildNext83(lanes){return C.buildNext82(lanes);}
function northStarDashboard83(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure83(state);return{...base,version:VERSION,milestones,sourceWave83:{...P.sourceWave},phase83:{...P.phase}};}
function projectModel83(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity83(lanes),buildNext=buildNext83(lanes),dashboard=northStarDashboard83(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure83(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave83:{...P.sourceWave}}};}
C.ensure83=ensure83;C.sourceFidelity83=sourceFidelity83;C.buildNext83=buildNext83;C.northStarDashboard83=northStarDashboard83;C.projectModel83=projectModel83;C.currentProjectModel=projectModel83;C.currentNorthStarDashboard=northStarDashboard83;C.projectModel66=projectModel83;C.sanitizedCopy=function(state){return ensure83(oldSanitize(state));};
root.OBOL_CORE_V83={VERSION,ensure83,sourceFidelity83,buildNext83,northStarDashboard83,projectModel83};
}

/* obol-core-delta: assets/core-v8.4.js */
{
// Obol v8.4 core overlay — project completed persistence.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V84,M=root.OBOL_METHODOLOGY_V84,F=root.OBOL_ORANGE_FIDELITY_V84;
if(!C||!P||!M||!F||!C.projectModel83||!C.northStarDashboard83||!C.sourceFidelity83||!C.buildNext83)throw new Error('Obol v8.3 current model plus v8.4 methodology/fidelity metadata are required before core-v8.4.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel83,oldDashboard=C.northStarDashboard83;
function ensure84(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard84&&typeof s.ui.dashboard84==='object'?s.ui.dashboard84:{};s.ui.dashboard84={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure84(oldNew());};C.coerceState=function(raw){return ensure84(oldCoerce(raw));};C.migrateV1=function(raw){return ensure84(oldMigrate(raw));};
function sourceFidelity84(lanes){return C.sourceFidelity83(lanes);}
function buildNext84(lanes){return C.buildNext83(lanes);}
function northStarDashboard84(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure84(state);return{...base,version:VERSION,milestones,sourceWave84:{...P.sourceWave},phase84:{...P.phase}};}
function projectModel84(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity84(lanes),buildNext=buildNext84(lanes),dashboard=northStarDashboard84(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure84(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave84:{...P.sourceWave}}};}
C.ensure84=ensure84;C.sourceFidelity84=sourceFidelity84;C.buildNext84=buildNext84;C.northStarDashboard84=northStarDashboard84;C.projectModel84=projectModel84;C.currentProjectModel=projectModel84;C.currentNorthStarDashboard=northStarDashboard84;C.projectModel66=projectModel84;C.sanitizedCopy=function(state){return ensure84(oldSanitize(state));};
root.OBOL_CORE_V84={VERSION,ensure84,sourceFidelity84,buildNext84,northStarDashboard84,projectModel84};
}

/* obol-core-delta: assets/core-v8.5.js */
{
// Obol v8.5 core overlay — project completed dom_admin.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V85,M=root.OBOL_METHODOLOGY_V85,F=root.OBOL_ORANGE_FIDELITY_V85;
if(!C||!P||!M||!F||!C.projectModel84||!C.northStarDashboard84||!C.sourceFidelity84||!C.buildNext84)throw new Error('Obol v8.4 current model plus v8.5 methodology/fidelity metadata are required before core-v8.5.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel84,oldDashboard=C.northStarDashboard84;
function ensure85(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard85&&typeof s.ui.dashboard85==='object'?s.ui.dashboard85:{};s.ui.dashboard85={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure85(oldNew());};C.coerceState=function(raw){return ensure85(oldCoerce(raw));};C.migrateV1=function(raw){return ensure85(oldMigrate(raw));};
function sourceFidelity85(lanes){return C.sourceFidelity84(lanes);}
function buildNext85(lanes){return C.buildNext84(lanes);}
function northStarDashboard85(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure85(state);return{...base,version:VERSION,milestones,sourceWave85:{...P.sourceWave},phase85:{...P.phase}};}
function projectModel85(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity85(lanes),buildNext=buildNext85(lanes),dashboard=northStarDashboard85(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure85(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave85:{...P.sourceWave}}};}
C.ensure85=ensure85;C.sourceFidelity85=sourceFidelity85;C.buildNext85=buildNext85;C.northStarDashboard85=northStarDashboard85;C.projectModel85=projectModel85;C.currentProjectModel=projectModel85;C.currentNorthStarDashboard=northStarDashboard85;C.projectModel66=projectModel85;C.sanitizedCopy=function(state){return ensure85(oldSanitize(state));};
root.OBOL_CORE_V85={VERSION,ensure85,sourceFidelity85,buildNext85,northStarDashboard85,projectModel85};
}

/* obol-core-delta: assets/core-v8.6.js */
{
// Obol v8.6 core overlay — project completed know_vuln_auth.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V86,M=root.OBOL_METHODOLOGY_V86,F=root.OBOL_ORANGE_FIDELITY_V86;
if(!C||!P||!M||!F||!C.projectModel85||!C.northStarDashboard85||!C.sourceFidelity85||!C.buildNext85)throw new Error('Obol v8.5 current model plus v8.6 methodology/fidelity metadata are required before core-v8.6.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel85,oldDashboard=C.northStarDashboard85;
function ensure86(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};const old=s.ui.dashboard86&&typeof s.ui.dashboard86==='object'?s.ui.dashboard86:{};s.ui.dashboard86={showSourceWave:old.showSourceWave!==false};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure86(oldNew());};C.coerceState=function(raw){return ensure86(oldCoerce(raw));};C.migrateV1=function(raw){return ensure86(oldMigrate(raw));};
function sourceFidelity86(lanes){return C.sourceFidelity85(lanes);}
function buildNext86(lanes){return C.buildNext85(lanes);}
function northStarDashboard86(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure86(state);return{...base,version:VERSION,milestones,sourceWave86:{...P.sourceWave},phase86:{...P.phase}};}
function projectModel86(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity86(lanes),buildNext=buildNext86(lanes),dashboard=northStarDashboard86(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure86(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave86:{...P.sourceWave}}};}
C.ensure86=ensure86;C.sourceFidelity86=sourceFidelity86;C.buildNext86=buildNext86;C.northStarDashboard86=northStarDashboard86;C.projectModel86=projectModel86;C.currentProjectModel=projectModel86;C.currentNorthStarDashboard=northStarDashboard86;C.projectModel66=projectModel86;C.sanitizedCopy=function(state){return ensure86(oldSanitize(state));};
root.OBOL_CORE_V86={VERSION,ensure86,sourceFidelity86,buildNext86,northStarDashboard86,projectModel86};
}

/* obol-core-delta: assets/core-v8.7.js */
{
// Obol v8.7 core overlay — project completed trusts.md inventory through stable current pointers.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V87,M=root.OBOL_METHODOLOGY_V87,F=root.OBOL_ORANGE_FIDELITY_V87;
if(!C||!P||!M||!F||!C.projectModel86||!C.northStarDashboard86||!C.sourceFidelity86||!C.buildNext86)throw new Error('Obol v8.6 current model plus v8.7 methodology/fidelity metadata are required before core-v8.7.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel86,oldDashboard=C.northStarDashboard86;
function ensure87(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure87(oldNew());};C.coerceState=function(raw){return ensure87(oldCoerce(raw));};C.migrateV1=function(raw){return ensure87(oldMigrate(raw));};
function sourceFidelity87(lanes){return C.sourceFidelity86(lanes);}
function buildNext87(lanes){return C.buildNext86(lanes);}
function northStarDashboard87(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure87(state);return{...base,version:VERSION,milestones,sourceWave87:{...P.sourceWave},phase87:{...P.phase}};}
function projectModel87(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity87(lanes),buildNext=buildNext87(lanes),dashboard=northStarDashboard87(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure87(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave87:{...P.sourceWave}}};}
C.ensure87=ensure87;C.sourceFidelity87=sourceFidelity87;C.buildNext87=buildNext87;C.northStarDashboard87=northStarDashboard87;C.projectModel87=projectModel87;C.currentProjectModel=projectModel87;C.currentNorthStarDashboard=northStarDashboard87;C.projectModel66=projectModel87;C.sanitizedCopy=function(state){return ensure87(oldSanitize(state));};
root.OBOL_CORE_V87={VERSION,ensure87,sourceFidelity87,buildNext87,northStarDashboard87,projectModel87};
}

/* obol-core-delta: assets/core-v8.8.js */
{
// Obol v8.8 core overlay — pinned Orange 2025.03 source inventory and fidelity complete.
const C=root.OBOL_CORE_V2,P=root.OBOL_PROJECT_V88,M=root.OBOL_METHODOLOGY_V88,F=root.OBOL_ORANGE_FIDELITY_V88;
if(!C||!P||!M||!F||!C.projectModel87||!C.northStarDashboard87||!C.sourceFidelity87||!C.buildNext87)throw new Error('Obol v8.7 current model plus v8.8 methodology/fidelity metadata are required before core-v8.8.js');
const VERSION=P.version,oldNew=C.newState,oldCoerce=C.coerceState,oldMigrate=C.migrateV1,oldSanitize=C.sanitizedCopy,oldProject=C.projectModel87,oldDashboard=C.northStarDashboard87;
function ensure88(s){s=s||{};s.obolVersion=VERSION;s.ui=s.ui||{};return s;}
C.VERSION=VERSION;C.newState=function(){return ensure88(oldNew());};C.coerceState=function(raw){return ensure88(oldCoerce(raw));};C.migrateV1=function(raw){return ensure88(oldMigrate(raw));};
function sourceFidelity88(lanes){return C.sourceFidelity87(lanes);}
function buildNext88(lanes){return C.buildNext87(lanes);}
function northStarDashboard88(state,lanes,ctx){const base=oldDashboard(state,lanes,ctx),milestones=[...(base.milestones||[])].filter(x=>x.release!==P.release);milestones.push({...P.releaseMilestone});ensure88(state);return{...base,version:VERSION,milestones,sourceWave88:{...P.sourceWave},phase88:{...P.phase}};}
function projectModel88(state,lanes,ctx){const base=oldProject(state,lanes,ctx),fidelity=sourceFidelity88(lanes),buildNext=buildNext88(lanes),dashboard=northStarDashboard88(state,lanes,ctx),next=(buildNext.rows||[])[0]||null;ensure88(state);const recent=[{...P.releaseMilestone},...(base.recent||[]).filter(x=>x.release!==P.release)].slice(0,3).map(x=>({release:x.release,implemented:x.implemented,partial:x.partial,gap:x.gap,coveragePct:x.coveragePct,representedPct:x.representedPct,label:x.label||''}));return{...base,version:VERSION,release:P.release,phase:{...P.phase},source:{...base.source,filesAtomized:fidelity.files.atomized,filesTotal:fidelity.files.total,filesPct:fidelity.files.pct,baselinesAtomized:fidelity.partialBaseline.atomized,baselinesTotal:fidelity.partialBaseline.total,baselinesPct:fidelity.partialBaseline.pct,atomicComplete:fidelity.complete,atomicTotal:fidelity.total,atomicPending:fidelity.pending,atomicPct:fidelity.completePct,broadOwned:fidelity.broadOwned,inventoryComplete:fidelity.files.atomized===fidelity.files.total&&fidelity.pending===0},buildNext:{...base.buildNext,total:buildNext.total||0,sourceFidelity:buildNext.sourceFidelity||0,sourceInventory:buildNext.sourceInventory||0,sourceFileInventory:buildNext.sourceFileInventory||buildNext.sourceInventory||0,rows:buildNext.rows||[]},next:next?{label:next.label,kind:next.kind,file:next.file||'',key:next.key||next.id||'',detail:next.detail||''}:null,recent,details:{...(base.details||{}),dashboard,fidelity,buildNext,sourceWave88:{...P.sourceWave}}};}
C.ensure88=ensure88;C.sourceFidelity88=sourceFidelity88;C.buildNext88=buildNext88;C.northStarDashboard88=northStarDashboard88;C.projectModel88=projectModel88;C.currentProjectModel=projectModel88;C.currentNorthStarDashboard=northStarDashboard88;C.projectModel66=projectModel88;C.sanitizedCopy=function(state){return ensure88(oldSanitize(state));};
root.OBOL_CORE_V88={VERSION,ensure88,sourceFidelity88,buildNext88,northStarDashboard88,projectModel88};
}

})(typeof window!=='undefined'?window:globalThis);
