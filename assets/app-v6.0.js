// Obol v6.0 UI overlay — surface canonical progress and current release state.
'use strict';
(function(){
function page60(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model60(){try{return C.northStarDashboard60(state,LANES,ctx());}catch(e){return null;}}
function stat60(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render60(){if(page60()!=='dashboard')return;const v=document.querySelector('#view'),d=model60();if(!v||!d||!d.canonicalAdvance60||v.querySelector('.advance60'))return;const a=d.canonicalAdvance60,w=d.gapWave60||{},r=d.releaseQuality60||{};const html='<section class="dash-panel50 advance60"><div class="dash-head50"><div><span>v6.0 canonical gap wave</span><h3>Five more canonical gaps completed with delivery quality still clean</h3></div><span>'+esc(String(a.coveragePct))+'% fully implemented</span></div><p class="advance-copy60">v6.0 follows the synchronized Build Next ordering and completes Java RMI, Log4Shell, Tomcat/JBoss manager, Veeam quick-win, and the MITM Kerberos relay branch. Each owner has explicit execution context, conservative copy/paste Evidence, decision-path placement, reporting traceability, and bounded proof semantics.</p><div class="dash-mini50">'+stat60('Fully implemented',a.toImplemented+'/127','up '+a.delta+' canonical sections in v6.0')+stat60('Canonical gaps',w.remainingGaps||0,'down '+a.gapDelta+' in this release')+stat60('v6.0 delivery ready',a.deliveryReady+'/'+a.total,'new canonical owners')+stat60('Release quality',r.clean?'clean':'blocked',(r.implementedQuality||0)+' implemented-quality · '+(r.mappedDelivery||0)+' mapped-delivery')+'</div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync60(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v6.0';document.title='Obol v6.0 — Offensive Box Operations Ledger';render60();}
window.addEventListener('hashchange',()=>setTimeout(sync60,140));setTimeout(sync60,640);setTimeout(sync60,1040);setTimeout(sync60,1440);
})();
