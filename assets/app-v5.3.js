// Obol v5.3 UI overlay — implemented-quality delivery repair visibility.
'use strict';
(function(){
function page53(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model53(){try{return C.northStarDashboard53(state,LANES,ctx());}catch(e){return null;}}
function stat53(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render53(){if(page53()!=='dashboard')return;const v=document.querySelector('#view'),d=model53();if(!v||!d||!d.repair53||v.querySelector('.repair-wave53'))return;const r=d.repair53,html='<section class="dash-panel50 repair-wave53"><div class="dash-head50"><div><span>v5.3 quality repair</span><h3>Implemented methodology gets delivery-ready owners first</h3></div><span>'+r.ready+'/'+r.total+' repair targets clean</span></div><p class="delivery-copy52">This release follows the v5.2 Build next rule: repair already-implemented canonical work before raising the methodology count. The targeted workflows now receive explicit Evidence profiles and audited execution-side metadata while preserving conservative proof boundaries.</p><div class="dash-mini50 repair-stats53">'+stat53('Profiles added',r.profileAdds,'explicit copy/paste Evidence contracts')+stat53('Execution mappings',r.executionAdds,'commands moved off fallback inference')+stat53('Implemented sections served',r.implementedSectionCount,'implemented canonical sections backed by repaired cards')+stat53('Implemented quality debt',r.currentImplementedDebt,'remaining after this repair wave')+'</div><div class="repair-list53">'+r.rows.map(x=>'<div class="repair-row53"><span><b>'+esc(x.title)+'</b><small>'+esc(x.stage||'mapped')+(x.source?' · '+esc(x.source):'')+'</small></span><span class="repair-status53 '+(x.ready?'ok':'needs')+'">'+(x.ready?'delivery-ready':'needs '+esc((x.missing||[]).join(', ')))+'</span></div>').join('')+'</div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync53(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v5.3';document.title='Obol v5.3 — Offensive Box Operations Ledger';render53();}
window.addEventListener('hashchange',()=>setTimeout(sync53,160));setTimeout(sync53,700);setTimeout(sync53,1120);setTimeout(sync53,1500);
})();
