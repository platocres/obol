// Obol v5.5 UI overlay — surface quality-debt cleanup and canonical progress.
'use strict';
(function(){
function page55(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model55(){try{return C.northStarDashboard55(state,LANES,ctx());}catch(e){return null;}}
function stat55(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render55(){if(page55()!=='dashboard')return;const v=document.querySelector('#view'),d=model55();if(!v||!d||!d.canonicalAdvance55||v.querySelector('.advance55'))return;const a=d.canonicalAdvance55,r=d.qualityRepair55||{},q=d.buildNext55||{};const html='<section class="dash-panel50 advance55"><div class="dash-head50"><div><span>v5.5 delivery + methodology advance</span><h3>Implemented quality debt cleared before the next canonical wave</h3></div><span>'+esc(String(a.coveragePct))+'% fully implemented</span></div><p class="advance-copy55">The v5.4 generated queue drove this release: the ten implemented canonical sections that were still blocked by missing delivery contracts now have clean mapped owners, while five mature partial branches moved to implemented with dedicated Evidence and reporting contracts.</p><div class="dash-mini50">'+stat55('Fully implemented',a.toImplemented+'/127','up '+a.delta+' canonical sections in v5.5')+stat55('Quality repairs',r.ready+'/'+r.targeted,'targeted mapped workflows delivery-ready')+stat55('Quality debt left',r.implementedQualityRemaining||0,'implemented canonical sections without a clean mapped workflow')+stat55('Build Next',q.total||0,(q.mappedDelivery||0)+' mapped · '+(q.canonicalGaps||0)+' gaps')+'</div><div class="readme-sync54"><b>README queue remains synchronized</b><span>North Star Build Next → generated README snapshot → CI --check</span><code>node tools/sync-readme-build-next.js --check</code></div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync55(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v5.5';document.title='Obol v5.5 — Offensive Box Operations Ledger';render55();}
window.addEventListener('hashchange',()=>setTimeout(sync55,140));setTimeout(sync55,620);setTimeout(sync55,1020);setTimeout(sync55,1420);
})();
