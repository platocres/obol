// Obol v5.8 UI overlay — surface canonical gap reduction and release-contract status.
'use strict';
(function(){
function page58(){return (location.hash||'#/home').replace(/^#\/?/,'').split('/').filter(Boolean)[0]||'home';}
function model58(){try{return C.northStarDashboard58(state,LANES,ctx());}catch(e){return null;}}
function stat58(label,value,detail){return '<div class="dash-stat50"><span>'+esc(label)+'</span><b>'+esc(String(value))+'</b><small>'+esc(detail||'')+'</small></div>';}
function render58(){if(page58()!=='dashboard')return;const v=document.querySelector('#view'),d=model58();if(!v||!d||!d.canonicalAdvance58||v.querySelector('.advance58'))return;const a=d.canonicalAdvance58,w=d.gapWave58||{},q=d.buildNext58||{},r=d.releaseContract58||{};const html='<section class="dash-panel50 advance58"><div class="dash-head50"><div><span>v5.8 canonical gap wave</span><h3>Five more canonical gaps completed and release PR quality is now CI-enforced</h3></div><span>'+esc(String(a.coveragePct))+'% fully implemented</span></div><p class="advance-copy58">v5.8 follows the generated v5.7 Build Next ordering: PrintNightmare, PrivExchange, ProxyNotShell, AppLocker bypass, and Kerberos relay now have explicit command, execution-side, Evidence, decision-path, reporting, and conservative proof contracts. The required test job also rejects malformed release PRs before merge.</p><div class="dash-mini50">'+stat58('Fully implemented',a.toImplemented+'/127','up '+a.delta+' canonical sections in v5.8')+stat58('Canonical gaps',w.remainingGaps||0,'down '+a.gapDelta+' in this release')+stat58('v5.8 delivery ready',a.deliveryReady+'/'+a.total,'new canonical owners')+stat58('Release contract',r.prBodyRequired?'enforced':'missing','required branch + PR body inside the required test job')+'</div><div class="readme-sync54"><b>README queue remains synchronized</b><span>North Star Build Next → generated README snapshot → CI --check</span><code>node tools/sync-readme-build-next.js --check</code></div></section>';const foot=v.querySelector('.dash-foot50');if(foot)foot.insertAdjacentHTML('beforebegin',html);else v.insertAdjacentHTML('beforeend',html);}
function sync58(){const tag=document.querySelector('.tagline');if(tag)tag.textContent='Offensive Box Operations Ledger · v5.8';document.title='Obol v5.8 — Offensive Box Operations Ledger';render58();}
window.addEventListener('hashchange',()=>setTimeout(sync58,140));setTimeout(sync58,640);setTimeout(sync58,1040);setTimeout(sync58,1440);
})();
