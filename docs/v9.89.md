# Obol v9.89

## What changed

- Mined `source-note-cluster-ad-enumeration-ldap-kerberos-bloodhound` into public-safe AD enumeration, LDAP/Kerberos discovery, BloodHound, and domain-context mechanics.
- Folded the cluster into the existing `ad-enumeration-bloodhound-collection` owner card instead of adding duplicate LDAP, Kerberos, BloodHound, or domain-context wrapper cards.
- Added action-spine command guidance for DC service fingerprinting, SMB/domain fingerprinting, LDAP naming contexts and scoped queries, Kerberos user enumeration, AS-REP/SPN boundary discovery, BloodHound.py, PowerView, and SharpHound.
- Added public-safe field notes for domain baselines, DC service surface, LDAP naming-context proof, user/group enumeration provenance, Kerberos identity boundaries, BloodHound collection proof, Windows-side AD context, and report redaction.
- Added `ad-enumeration-ldap-kerberos-bloodhound-v989` analyzer logic for pasted AD, LDAP, Kerberos, BloodHound, PowerView, SharpHound, SMB/domain fingerprint, and boundary/failure evidence.
- Advanced the source-note ledger to 458/556 reviewed, 98 pending, and 6 remaining public-safe cluster review items.
- Generated the next cluster queue so Product Build Next advances to `source-note-cluster-ad-credential-attacks-and-ticket-material`.

## Wrapper control

- Does not create AD enumeration, LDAP/Kerberos, BloodHound, or domain-context wrapper cards.
- Keeps `ad-enumeration-bloodhound-collection` as the single primary owner for this cluster.
- Keeps password-spray, pass-the-hash, remote-exec, Metasploit pivot, and RDP/SOCKS surfaces as related context rather than parallel v9.89 owners.
- Keeps source prose, lab paths, credentials, flags, target identifiers, screenshots, ticket material, hashes, and exact solution chains out of public artifacts.

## Proof boundary

- Treats LDAP, Kerberos, SMB, RPC, and BloodHound output as scoped discovery until the protocol, identity, query filter, artifact, and result boundary are proven.
- Separates enumeration from compromise: valid usernames, SPN rows, AS-REP candidates, graph edges, or readable directory objects only move the path after later credential or channel validation proves impact.
- Preserves BloodHound collection method, domain, resolver/DC, artifact path, import state, and channel-validation boundary for report-ready evidence.

## Validation

- Adds `tests/run-v9.89-tests.js` as the current product contract.
- Checks existing-card enrichment, absence of wrapper cards, field-note bindings, analyzer redaction, intake integration, Product Build Next handoff, and downstream cluster generation.
- Keeps the release gate pointed at v9.89 with `tools/validate-release-pr.js --repo-only --release-version=9.89`.
