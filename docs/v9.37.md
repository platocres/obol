# Obol v9.37 — Path three-mode rendering

v9.37 is a product-hardening release focused on `ux-path-three-mode`.

## Scope

- Keep `C.nextStepsOverview34(...)` as the single evidence-ranked Path source.
- Render Checklist, Simplified, and Live Map views through the stable `assets/operator-route-current.js` owner.
- Persist the selected view in the browser-local workspace.
- Add accessible mode controls and pan/zoom controls for the SVG map.
- Preserve Evidence, manual-outcome, queue, and report-proof boundaries.

## Contract

The public product contract is unchanged: a static, browser-local study tool with human-run commands, no backend, no telemetry, and evidence-based proof separate from manual workflow advancement.
