# Obol v9.60

## What changed

- Completed the next Product Build Next item after v9.59: `notes-remine-private-superseded`.
- Re-mined old `private-reference-only` and `superseded` note dispositions as a source-boundary class instead of leaving them as discarded private references.
- Added `data/product-hardening/private-only-superseded-remining-v9.60.js` to publish public-safe extraction mechanics without copying raw private note bodies, flags, exact targets, payload catalogs, screenshots, or walkthrough chains.
- Added four public-safe field-note mechanics for private-source redaction, recipe-catalog conversion, lab-outcome proof templates, and volatile tool-reference criteria.
- Marked `notes-remine-private-superseded` complete only through live queue/progress integration with a `private-only-superseded-remine` packet review marker.
- Corrected Product Build Next policy so note burn-down remains the concrete next phase until the notes tracker is complete; offline/performance work stays queued behind that gate.
- Added a generated `Next notes batch` handoff so future agents know the specific next notes package to work instead of choosing an arbitrary theme inside the note mountain.

## Product output

The new Product Hardening extension is `data/product-hardening/private-only-superseded-remining-v9.60.js`.

It publishes four public-safe field-note mechanics:

- `note-private-source-redaction-boundary` — private material can still produce public-safe mechanics.
- `note-recipe-catalog-to-control-axes` — recipe catalogs become builder/control axes, not copied commands.
- `note-lab-outcome-to-proof-template` — lab-specific outcomes become generic proof templates.
- `note-volatile-tool-reference-boundary` — volatile tool lists become selection criteria and fallback logic.

## Evidence and Next Steps boundary

This release does not add a new operator command, exploit path, proof-control UI, terminal-output analyzer, or path item that expects pasted lab output.

Because it does not introduce a new pasted-output shape, no new Evidence ingestion parser is required for v9.60 itself. The output is source-mining governance and field-note guidance: it tells future builds how to convert private recipes, lab answers, volatile catalogs, and superseded material into safe product mechanics.

The v9.58 Evidence rule still applies. If a future build uses this v9.60 guidance to add a command, builder toggle, proof control, analyzer, or Next Steps branch, that build is incomplete until pasted Evidence creates the expected activity and outcome facts.

## Source boundary

No private note body, course prose, target, flag, credential, screenshot, exploit recipe, callback/listener mechanic, or exact solution chain is copied into public Obol.

The source basis is the complete private review-packet route:

- `platocres/obol-source-notes@agent/review-packets`
- `data/review-packets/manifest.json`
- `reviewTextPolicy: complete_cleaned_text`
- `truncationPolicy: none`
- `expectedNoteCount: 556`
- `packetCount: 29`

## Queue impact

After Product Build Next sync, the next concrete item must not advance to `perf-service-worker` while the notes tracker still has pending source notes or old-rubric-only reviewed notes.

The generated queue should keep the active notes-first gates ahead of offline/performance work:

- `notes-mechanic-backfill` remains concrete while already-reviewed notes still need full-spectrum source re-mining.
- `notes-disposition-burn-down` remains concrete while any of the 556 source notes still need disposition/mining.
- `perf-service-worker` remains queued behind note burn-down until those gates clear.

## Next notes batch handoff

The generated README must name the immediate notes package as:

- `notes-batch-old-rubric-reviewed-remine-001` — Old-rubric reviewed source re-mining batch 1.

Future agents should work that notes batch first. The batch selector is the next set of already-reviewed notes that lack full-spectrum audit rows, selected from `platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json` in manifest/source order, excluding themes already closed by released re-mining proof.

The batch acceptance rule is explicit: every selected note needs a 16-dimension re-mining audit row and either a public-safe product output, a covered rationale, a queued product gap, or private-boundary proof. Do not move to `perf-service-worker` or any offline/performance item while this generated next notes batch or either notes-first gate remains active.

Generated release artifact sync updates the visible README, index, changelog, and current application owner from the v9.60 release identity and Product Hardening extension list.
