# Obol v7.1

v7.1 continues the Orange source-depth phase by atomizing the pinned `delegation.md` family and carrying its useful branches through Obol's human-run command, Evidence, Next Steps, cleanup, lineage, and reporting contracts.

## Release scope

- decompose Kerberos delegation discovery, unconstrained delegation, constrained delegation with protocol transition, constrained delegation without protocol transition, RBCD, and S4U2Self into explicit source-fidelity units;
- preserve the pinned Orange 2025.03 source provenance and the frozen v6.2 source-depth denominator;
- keep directory mutations, ticket material, ticket use, authenticated service access, execution, administrator access, privilege, and cleanup as separate Evidence boundaries;
- model temporary machine-account and RBCD changes as explicit lifecycle stages with independently evidenced restoration;
- improve the delegation operator surface instead of treating the legacy broad `delegation-abuse` card as source-fidelity completion;
- update the consolidated current-project model and generated Build Next accounting from one source of truth;
- remain delta-based and avoid redundant Dashboard or other no-op release shims.

## Accounting outcome

- canonical methodology: 102 implemented / 25 partial / 0 gaps / 0 stale, 80% fully implemented and 100% represented;
- methodology source files atomized: 2 / 17;
- frozen v6.2 partial baselines decomposed: 9 / 34;
- currently inventoried atomic source units fidelity-complete: 25 / 25;
- live Build Next queue: 25 source-inventory/decomposition items, beginning with ACL / ACE control paths.

## Historical regression hardening

v7.1 makes the v6.6, v6.7, v6.8, v6.9, and v7.0 historical suites future-safe where they had accidentally asserted mutable current source-accounting totals. Those suites now preserve their own historical milestones, source waves, and delivery invariants while allowing later source families to increase the current atomized-file, decomposed-baseline, and atomic-unit totals.

## Validation contract

The fully hardened candidate passed exact-head preflight on `a6c83e8d8da3c0b0071a252a6f2c2ba075464e00` in Actions run `33277626817`, including smoke, current-release regression, release quality, release contract, historical-test future-safety, and README synchronization.

The exact final release head must pass smoke, preflight, and the complete historical regression chain before this PR leaves Draft. Any commit after a validated final head invalidates that readiness and requires a fresh exact-head validation. The `[release-final]` commit is therefore intended to remain the final repository change for v7.1 before review readiness.
