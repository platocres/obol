# Obol v9.95

## What changed

- Completes the final `source-note-cluster-reference-index-and-course-map-private-heavy` queue item as a terminal private-heavy disposition.
- Keeps reference indexes, tables of contents, course maps, grading paths, flags, private targets, screenshots, and exact solution chains out of public UI.
- Folds the public-safe residue into the existing `flag-discipline` owner as source-boundary and queue-handoff guidance instead of creating reference-index or course-map wrapper cards.
- Advances source-note accounting to 556/556 reviewed and 0 pending.
- Clears the generated Product Build Next note batch handoff so the README and dashboard no longer imply more source-note review remains.
- Pivots the visible queue to post-notes product hardening: Next Steps/tool-card clarity, old-layer retirement, regression-speed cleanup, and browser/offline work.

## Product mechanics

- Adds `data/product-hardening/reference-index-course-map-private-boundary-cluster-v9.95.js`.
- Updates `flag-discipline` with final source-note disposition, public-safe extraction, post-notes queue pivot, and README stale-queue scan guidance.
- Adds `reference-index-course-map-private-boundary-router` as the v9.95 feature ID.
- Adds public-safe analyzer output as `OBOL_REFERENCE_INDEX_COURSE_MAP_PRIVATE_BOUNDARY_ANALYZER_V995`.
- Hooks Evidence intake so pasted course-map or source-index material can be classified without leaking flags, targets, credentials, screenshots, or copied course prose.
- Marks `OBOL_SOURCE_NOTE_CLUSTERS` complete with empty pending and review queues.
- Updates Product Build Next through runtime queue state so stale source-note cluster items do not appear in the generated README block after v9.95.

## Validation

- Adds `tests/run-v9.95-tests.js` as the current product contract.
- Updates `tools/scope-check.js` and `tools/run-historical-contracts.js` so the current-product gate runs the v9.95 test suite.
- The v9.95 suite checks final cluster completion, existing-card enrichment, no duplicate reference/index/course-map wrapper cards, field-note bindings, analyzer redaction, intake integration, empty source-note queues, post-notes queue activation, and README generated-block cleanup.
- The README assertion rejects stale generated handoff language such as `Burn down all 556`, `Next notes batch`, and old source-note cluster entries after final source-note completion.
- The release test invokes `tools/validate-release-pr.js --repo-only --release-version=9.95`.

## Compatibility

Obol remains static and browser-local. This release does not add a backend, account system, telemetry, install prompt, or automatic command execution. The final source-note material remains private-heavy; public output is limited to generalized source-boundary mechanics, redaction guidance, disposition accounting, and the post-notes build-queue pivot.
