# Obol v9.90 - AD credential attacks and ticket-material boundary

v9.90 mines the active Product Build Next cluster, `source-note-cluster-ad-credential-attacks-and-ticket-material`, from the complete private review-packet route into public-safe credential-attack mechanics.

The release folds the cluster into the existing `ad-password-spray-safety-workflow` owner card instead of creating separate Kerberoast, AS-REP, spray, ticket, or credential-attack wrapper cards. The owner now carries an action spine for password policy proof, lockout safety, target-list provenance, Kerberos user enumeration, AS-REP material handling, Kerberoast ticket requests, offline cracking status, scoped credential validation, and ticket/cache state.

## What changed

- Folded AD credential-attack and ticket-material guidance into the existing `ad-password-spray-safety-workflow` owner card instead of creating duplicate Kerberoast, AS-REP, spray, or ticket wrapper cards.
- Added a public-safe action spine for password policy proof, target-list provenance, Kerberos user enumeration, AS-REP material capture, Kerberoast ticket capture, offline cracking status, scoped validation, and ticket/cache review.
- Added field notes for lockout policy, target provenance, hash and ticket classification, cracking versus validation, redaction, and report boundaries.
- Added an analyzer and intake routing for pasted AD credential-attack evidence while redacting raw hashes, tickets, passwords, flags, lab names, and host IPs.
- Advanced the source-note cluster queue to pivoting, tunneling, proxying, and route proof with 76 pending notes remaining.

## Product mechanics

- Keeps spray safety, AS-REP roastability, SPN/TGS capture, cracking, validation, and ticket material typing as separate evidence states.
- Adds copy-only command rows for `crackmapexec`, `rpcclient`, `ldapsearch`, `kerbrute`, `GetNPUsers.py`, `GetUserSPNs.py`, `hashcat`, `john`, and `klist`.
- Adds field notes that explain lockout policy, target provenance, hash/ticket classification, cracking vs validation, redaction, and report boundaries.
- Adds an analyzer that recognizes AD credential-attack evidence from pasted terminal output while redacting raw hashes, tickets, passwords, flags, lab names, and host IPs.
- Routes recognized evidence through the existing intake pipeline to the folded owner card.

## Notes and queue state

- Completes `source-note-cluster-ad-credential-attacks-and-ticket-material`.
- Records 22 notes and 241,557 complete-packet review characters from the private packet route.
- Advances source-note accounting to 480/556 reviewed and 76 pending.
- Keeps the remaining 76 pending notes organized into five cluster review items.
- Advances Product Build Next to `source-note-cluster-pivoting-tunneling-and-route-proof`.

## Validation

- Adds `tests/run-v9.90-tests.js` as the current product contract.
- Updates `tools/scope-check.js` and `tools/run-historical-contracts.js` to run the v9.90 current-product suite.
- The v9.90 suite checks existing-card enrichment, wrapper-card absence, field-note binding, analyzer redaction, intake routing, source-cluster ledger progression, Product Build Next handoff, and downstream cluster generation.

## Compatibility

Obol remains static and browser-local. This release does not add a backend, telemetry, account system, install prompt, or automatic command execution. Commands remain copy-only guidance for authorized environments, and credential-attack facts are created only from pasted Evidence.
