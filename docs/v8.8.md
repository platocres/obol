# Obol v8.8

v8.8 completes Obol's whole-file and atomic source-fidelity pass over the pinned Orange Cyberdefense 2025.03 Active Directory methodology by finishing the final methodology-bearing source file, `valid_user.md`.

Pinned Orange commit: `6d16ca0d1434875e0617f2f3cfa825fad0bc7d7e`

Pinned `valid_user.md` blob: `fa9ba8715ec616b468f92c47b80dee498b2fae24`

## Source accounting

The release reviews eighteen terminal source units spanning password-policy review, fine-grained policy variants, user-equals-password validation, common-password spraying, AS-REP roastable-user discovery, AS-REP roasting, blind-Kerberoast source variants, and the pinned CVE-2022-33679 branch.

Nine units are represented directly through mature preferred Obol owners. Nine redundant, specialized, or brittle upstream variants are explicitly superseded with rationale instead of silently disappearing or being copied into duplicate operator surfaces.

v8.8 advances the permanent accounting milestones to:

- canonical methodology: 127/127 implemented, 0 partial, 0 gaps, 0 stale;
- strict completion: 100%;
- represented coverage: 100%;
- frozen v6.2 source-depth baseline: 34/34 decomposed;
- methodology-bearing source files atomized: 17/17;
- inventoried atomic units fidelity-complete: 334/334;
- live methodology/source Build Next queue: 0.

Under the current North Star audit contract, the pinned Orange 2025.03 methodology-bearing source set is now fully inventoried and every inventoried unit has a terminal reviewed disposition.

## Owner integration

The final source family is integrated through the existing `password-spray`, `asrep-roast`, `kerberoast`, and `bloodhound-collect` owners rather than adding duplicate release-only cards. v8.8 adds source lineage, terminal dispositions, source corrections, and proof-boundary metadata while preserving the mature human-run operator surfaces.

The source review also records command/source corrections without copying stale text blindly, including current Impacket naming, corrected fine-grained-policy terminology, and explicit supersession of narrow blind-roast and single-purpose CVE helper variants that would otherwise duplicate or weaken the primary operator workflow.

## Evidence boundaries

Password-policy output, a validated username, an authentication attempt, an authentication success, a captured AS-REP or TGS artifact, a recovered credential, vulnerability validation, ticket material, authenticated service access, execution, administrator/SYSTEM context, and privilege remain separate proof states.

Source accounting never creates engagement facts. A source node, tool reference, or terminal audit label is project metadata only. It does not prove the corresponding action happened in an engagement.

## Product and runtime

v8.8 remains static, offline-capable, browser-local, and human-run. There is no backend, telemetry, or automatic command execution. Existing browser-local state migration and sanitized-export behavior remain intact.

The release advances the stable current project boundary to `C.projectModel88(...)` / `C.currentProjectModel(...)`, exposes the source-complete milestone in North Star Dashboard, and keeps release scaffolding delta-based. No no-op Dashboard or Intake ownership layer is added.

## What comes next

With the pinned methodology/source queue complete, future work should not invent source debt merely to keep a queue non-empty. The next engineering phase is regression-equivalent historical runtime compaction, UX refinement, and other quality improvements, unless the Orange upstream snapshot is deliberately repinned or a real source-quality defect is discovered.

Any compaction must preserve the completed 127/127, 34/34, 17/17, and 334/334 denominators, Evidence semantics, Next Steps behavior, reporting lineage, workspace migration, and exact-head release validation.

## Validation

The v8.8 regression suite locks the eighteen-unit final-file ledger, terminal modeled/superseded dispositions, 17/17 whole-file inventory, 334/334 atomic completion, mature owner lineage, source corrections, proof boundaries, zero live methodology/source queue, stable current-model pointers, release wiring, README synchronization, quality-debt gates, and sanitized-export behavior.

The release remains subject to the exact-final-head rule in `BUILDING.md`: the `[release-final]` head must pass smoke, preflight, historical-test future safety, release-contract validation, complete historical regressions, quality-debt validation, and README synchronization before the PR is ready to leave Draft.
