# Raw notes Git LFS access

This document exists because source re-mining fails if an agent stops at a Git LFS pointer or uses a truncated packet artifact. The active raw note exports live in the private source repo:

`https://github.com/platocres/obol-source-notes/tree/main/sources/raw`

The ENEX files are not normal small Git blobs. They are Git LFS objects. A 134-byte file that begins with `version https://git-lfs.github.com/spec/v1` is only the pointer and is not usable for source re-mining.

## Required HTB proof

Before claiming direct access to `HTB - Penetration Tester.enex`, the agent must personally materialize the real LFS object in a shell/runtime it can read:

```bash
git clone git@github.com:platocres/obol-source-notes.git
cd obol-source-notes
git lfs install
git lfs pull --include="sources/raw/HTB - Penetration Tester.enex"
python scripts/verify_sources.py
```

The verifier must report this exact HTB identity:

```text
OK sources/raw/HTB - Penetration Tester.enex bytes=194191214 sha256=ceeab3da0770ecd3709bcd2693b7a26a6390ad45c5bbada0234079e6eb2ff06f
```

A focused shell proof is also acceptable:

```bash
test "$(wc -c < 'sources/raw/HTB - Penetration Tester.enex')" -eq 194191214
printf '%s  %s\n' 'ceeab3da0770ecd3709bcd2693b7a26a6390ad45c5bbada0234079e6eb2ff06f' 'sources/raw/HTB - Penetration Tester.enex' | sha256sum -c -
grep -a -m1 '<en-export' 'sources/raw/HTB - Penetration Tester.enex'
```

Do not paste credentials or tokenized raw URLs into Obol docs, PR descriptions, logs, or generated artifacts. Use the environment's authenticated GitHub access.

## Complete packet route for ChatGPT-style connectors

When the agent runtime cannot clone GitHub, resolve GitHub DNS, run Git LFS, or read large binary files directly, use the complete sequential packets committed in the private source repo instead of the raw binary.

Use this source:

```text
platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json
```

The complete packet route is generated by `scripts/extract_enex_review_packets.py` on the private source branch. The workflow `.github/workflows/build-review-packets.yml` checks out the private notes with `actions/checkout@v4` and `lfs: true`, runs `git lfs pull`, verifies raw source identity with `python3 scripts/verify_sources.py`, builds complete packets, proves the packet inventory, and commits the regenerated `data/review-packets/*.json` files back to `agent/review-packets`.

The required manifest identity is:

```text
schema_version=2
review_text_policy=complete_cleaned_text
truncation_policy=none
note_count=556
unique_note_count=556
resource_count=1326
review_text_chars=8725188
truncated_note_count=0
window_marker_count=0
packet_count=29
```

Current source split:

```text
HTB - Penetration Tester: 352 notes, 859 resources, 3949052 cleaned text chars, 18 packets
OffSec PEN-200: 204 notes, 467 resources, 4776136 cleaned text chars, 11 packets
```

A connector-based agent should retrieve the manifest, then read packet files listed in `manifest.packets[*].file`. These are sequential all-note packets, not themed search packets. They cover all 556 notes with complete cleaned note text and zero truncation. They are acceptable for text-based source re-mining when direct ENEX binary access is impossible in the current runtime.

## Do not use the old themed artifact as source of truth

The older `Private note review packets` workflow artifact named `obol-private-review-packets` is useful only for theme discovery. It is not exhaustive enough for source re-mining because it groups notes by theme search, omits unmatched notes, and may truncate note text. Do not use `review-packets-fulltext/*.json` from that artifact to mark all-note review or re-mining complete.

## Connector and network failure rule

The GitHub contents API and normal `raw.githubusercontent.com` fetches can return only the 134-byte LFS pointer for these files. That is not a download. If the agent runtime cannot resolve GitHub hosts, cannot authenticate to the private repo, cannot run `git lfs`, or cannot read binary files, the agent must say so and switch to the complete sequential packet route above.

Do not claim raw-ENEX access from:

- a GitHub contents listing;
- the 134-byte pointer blob;
- the `download_url` field returned by the contents API;
- a source manifest hash by itself;
- a prior CI run by itself;
- the old themed full-text artifact by itself.

Those facts can identify the expected object, but they do not prove this agent downloaded and read the HTB ENEX. A successful complete-packet workflow plus schema-2 packet manifest proves a different, narrower thing: GitHub Actions materialized and verified the raw ENEX files, then generated complete cleaned text packets that this connector can read.

## GitHub Actions evidence

The complete-packet source workflow must show these steps completed successfully:

```text
Checkout private notes with LFS
Materialize LFS sources
Verify raw source identity
Build complete review packets
Prove complete packet inventory
Commit generated packets
```

For run `33877189291`, the job logs showed the raw HTB and OffSec identities verified, then generated and proved complete packets:

```text
OK sources/raw/HTB - Penetration Tester.enex bytes=194191214 sha256=ceeab3da0770ecd3709bcd2693b7a26a6390ad45c5bbada0234079e6eb2ff06f
OK sources/raw/OffSec PEN-200.enex bytes=110367324 sha256=c02bf5958f2bf2aaa690b20e0a497b70eb83a8fc4276d2f1b52e11592e89acb1
Wrote 556 complete-text notes across 29 private review packets; truncated=0; review_text_chars=8725188.
Validated 29 complete private review packets for 556 notes with zero truncation.
```

## Required PR evidence

A PR that claims raw-source re-mining must include direct evidence that the agent's working environment used an acceptable source:

- direct raw ENEX route: `python scripts/verify_sources.py` output showing the HTB `OK` line above, or local `wc -c`, `sha256sum -c`, and ENEX marker checks; or
- complete packet route: the schema-2 manifest values above, the source branch/commit used, the packet file(s) read, and the resulting public-safe product outputs or negative findings.

Without one of those, the PR may improve handoff documentation, but it must not mark raw-source re-mining complete.
