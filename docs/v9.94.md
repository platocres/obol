# Obol v9.94

## What changed

- Mines the `source-note-cluster-exam-skills-assessments-private-heavy` queue item as a private-heavy disposition boundary instead of copying walkthrough or assessment material into public UI.
- Folds the public-safe residue into the existing `flag-discipline` primary owner so exam flags, solution chains, private targets, and assessment-specific steps remain out of product text.
- Keeps `report-discipline`, session, route, Metasploit, and AD cards as context only; they receive no duplicate skills-assessment or walkthrough wrapper cards.
- Adds a private-boundary analyzer that recognizes assessment, walkthrough, exact-solution-chain, grading-objective, flag, credential, host, and screenshot/prose leakage signals while redacting sensitive material.
- Adds three public Field Notes covering generalized skill extraction, private-only walkthrough disposition, and exam/report redaction gates.
- Advances Product Build Next to the final private-heavy reference index and course-map cluster, leaving 1 pending note.

## Product mechanics

- Adds `data/product-hardening/exam-skills-assessment-private-boundary-cluster-v9.94.js`.
- Updates `flag-discipline` with copy-only private-boundary review commands and proof expectations.
- Adds `exam-skills-assessment-private-boundary-router` as the v9.94 feature ID.
- Adds public-safe analyzer output as `OBOL_EXAM_SKILLS_ASSESSMENT_PRIVATE_BOUNDARY_ANALYZER_V994`.
- Hooks Evidence intake so pasted assessment or walkthrough-heavy source text can be classified as private-boundary evidence without leaking secrets or exact paths.
- Advances source-note cluster accounting to 555/556 reviewed and 1 pending.
- Keeps the final pending item explicitly private-heavy so future agents do not convert reference indexes or course maps into UI filler.

## Validation

- Adds `tests/run-v9.94-tests.js` as the current product contract.
- Updates `tools/scope-check.js` and `tools/run-historical-contracts.js` so the current-product gate runs the v9.94 test suite.
- The v9.94 suite checks existing-card enrichment, no duplicate skills/walkthrough wrapper cards, field-note bindings, analyzer redaction, intake integration, source-cluster ledger progression, Product Build Next handoff, and final private-heavy queue generation.
- The release test invokes `tools/validate-release-pr.js --repo-only --release-version=9.94`.

## Compatibility

Obol remains static and browser-local. This release does not add a backend, account system, telemetry, install prompt, or automatic command execution. The skills-assessment material remains private-heavy; public output is limited to generalized boundary mechanics, redaction guidance, and disposition accounting.