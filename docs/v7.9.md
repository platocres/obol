# Obol v7.9

v7.9 continues the pinned Orange Cyberdefense 2025.03 source-depth queue from v7.8 by decomposing the two remaining `low_access.md` parents that were still partial at the frozen v6.2 boundary: Windows local exploit paths and WebDAV coercion.

Pinned Orange commit: `6d16ca0d1434875e0617f2f3cfa825fad0bc7d7e`

Pinned `low_access.md` blob: `8744d9b137338bb1fa99d26bc257aa50ef490e5e`

The release models three atomic source units: SMBGhost/CVE-2020-0796 validation, HiveNightmare/SeriousSAM/CVE-2021-36934 validation, and the WebDAV/searchConnector-ms coercion chain. The local-exploit owner keeps vulnerability proof, exploit attempt, execution, and privilege separate. The WebDAV owner keeps DNS preparation, lure/open action, coercion trigger, inbound authentication, relay success, access, execution, privilege, and cleanup separate.

v7.9 does not mark the whole `low_access.md` file atomized yet. The other already-implemented low-access parents still need subordinate source inventory before the file-level denominator can advance. Canonical accounting advances only `low_access.local-exploit` and `low_access.webdav`, bringing the current canonical projection to 124/127 implemented and 3 partial while preserving zero gaps and zero stale mappings.

Obol remains static, offline-capable, browser-local, and human-run only. Commands are guidance for authorized environments and are never executed automatically by Obol.
