# Obol v9.33

v9.33 continues the Notes Impact and Themed Burn-down work package with the credentials and authentication packet.

## Build scope

- Review the private credentials/authentication note packet substantively rather than treating lexical matches as dispositions.
- Normalize durable guidance for credential material, hash/plaintext distinctions, ticket and certificate handling, authentication validation, failure modes, and cross-tool/path handoffs.
- Convert any note-discovered product gaps into code-level Tool Builder, Path, Evidence, report, or workflow changes with item-specific proof.
- Keep cross-theme privilege-escalation and AD/pivoting material owned by their later packets when credentials/authentication is incidental.
- Close `notes-packet-credentials-auth` only when candidate accounting, public output, product mechanics, queue state, and focused tests agree.

## Product boundaries

- Obol remains browser-local and human-run only.
- Credential material is not proof of successful authentication or privilege.
- Raw private source text, lab targets, credentials, flags, screenshots, and copied walkthrough material remain outside public Obol.
- The 556-note umbrella remains queued until every source note reaches a terminal disposition.
