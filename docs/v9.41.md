# Obol v9.41 — Domain ownership flattening

v9.41 replaces the runtime domain owner’s ordered execution of 103 versioned fragments with one stable current-domain snapshot while retaining the historical files only as the frozen equivalence ledger.

The release remains in progress. The final head will include semantic equivalence proof, queue/accountability updates, release synchronization, and the complete exact-head validation required by `BUILDING.md`.
