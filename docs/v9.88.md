# Obol v9.88

## What changed

- Mined `source-note-cluster-windows-privesc-services-and-local-admin` into public-safe Windows local privilege-escalation mechanics.
- Enriched the existing Windows privesc owner cards rather than creating duplicate wrapper cards.
- Added service-control, unquoted-path, scheduled-task, registry-policy, credential-trail, token/local-admin, cleanup, and report-boundary guidance.
- Added public-safe field notes for Windows identity baselines, service boundaries, unquoted path proof, scheduled task chains, registry policy checks, credential trails, token capability review, and cleanup/reporting.
- Added `windows-privesc-services-local-admin-v988` analyzer logic for pasted Windows service, task, registry, token, local-admin, credential-candidate, elevated-effect, and cleanup evidence.
- Advanced the source-note ledger to 428/556 reviewed, 128 pending, and 7 remaining public-safe cluster review items.
- Generated the next cluster queue so Product Build Next advances to `source-note-cluster-ad-enumeration-ldap-kerberos-bloodhound`.

## Wrapper control

- Does not create Windows service, scheduled-task, registry, local-admin, token, or generic Windows-privesc wrapper cards.
- Folds the mined mechanics into existing primary cards with full action-spine fields.
- Keeps source prose, lab paths, credentials, flags, target identifiers, screenshots, and exact solution chains out of public artifacts.

## Proof boundary

- Treats Windows service, path, scheduled-task, registry, credential, token, and local-admin findings as candidates until the precondition, execution context, elevated effect, and cleanup state are proven.
- Separates discovery from privilege movement: a service exists, a group is local admin, or a token privilege appears only becomes path-moving after supported pasted Evidence proves what it enables.

## Validation

- Adds `tests/run-v9.88-tests.js` as the current product contract.
- Checks existing-card enrichment, absence of wrapper cards, field-note bindings, analyzer redaction, intake integration, Product Build Next handoff, and downstream cluster generation.
- Keeps the release gate pointed at v9.88 with `tools/validate-release-pr.js --repo-only --release-version=9.88`.
