# Obol v8.5

v8.5 continues the Orange whole-file source-inventory phase by atomizing the pinned `dom_admin.md` family end to end.

Pinned Orange commit: `6d16ca0d1434875e0617f2f3cfa825fad0bc7d7e`

Pinned `dom_admin.md` blob: `4c234b0199ccba1457f1f203a67dc3345d7d851b`

The release inventories eight terminal source units across domain credential replication, offline NTDS acquisition/parsing, certificate-assisted synchronization, and domain DPAPI backup-key retrieval. Seven units are modeled through mature DCSync, credential-dump, and backup-key owners. The Meterpreter-coupled `windows/gather/credentials/domain_hashdump` branch is explicitly superseded by clearer direct NetExec/Impacket workflows rather than silently dropped.

v8.5 preserves the source while reviewing command quality instead of copying it blindly. The abbreviated NTDSUtil IFM line is expanded into an explicit activate-instance/IFM/create sequence, the offline `secretsdump` route drops the unnecessary pinned `-hashes lmhash:nthash` placeholder when a matching SYSTEM hive is supplied, and the DonPAPI `- H` spacing is corrected to `-H`.

Evidence remains conservative throughout. NTDS/SYSTEM snapshots, returned account hashes, certificate-assisted synchronization output, and DPAPI backup keys are sensitive credential/recovery material only. They do not by themselves prove later authentication, service access, execution, administrator/SYSTEM context, lateral movement, or privilege. Temporary target-side IFM artifacts carry an explicit cleanup obligation.

Canonical methodology remains 127/127 implemented with zero partials, gaps, or stale mappings. The frozen v6.2 source-depth baseline remains 34/34 complete. Whole-file source inventory advances from 13/17 to 14/17 files, the atomic ledger expands from 258/258 to 266/266 fidelity-complete units, and Build Next shrinks from four to three whole-file source inventories: `know_vuln_auth.md`, `trusts.md`, and `valid_user.md`.

The release remains delta-based. It adds only the v8.5 source-fidelity, methodology, project-model, core, UI/CSS, regression, documentation, README, and runtime/index wiring required for the new source wave. No no-op Dashboard or Intake layer is introduced.

Obol remains static, offline-capable, browser-local, and human-run only. Commands are guidance for authorized environments and are never executed automatically by Obol.
