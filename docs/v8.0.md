# Obol v8.0

v8.0 is the final frozen v6.2 source-depth baseline wave. It will decompose the remaining `crack_hash.pxe`, `low_hanging.zerologon`, and `persistence.add-da` rows, preserve conservative Evidence boundaries, and transition Build Next from frozen-baseline completion into whole-file Orange source inventory for the remaining unatomized methodology files.
