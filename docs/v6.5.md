# Obol v6.5 — AD CS source-fidelity delivery wave

v6.5 converts the first five atomized AD CS source units introduced in v6.4 from broad-owner representation into verified end-to-end Obol workflows.

The completed atomic units are:

- AD CS enumeration and routing
- ESC8 web-enrollment relay
- ESC1 enrollee-supplied identity template
- ESC2 any-purpose / SubCA routing into ESC3
- ESC3 enrollment-agent / on-behalf-of request

The release keeps canonical breadth and source fidelity separate. It only credits an atomic unit as complete after source structure, branch conditions, tool review, command/GUI surface, execution side, conservative Evidence, Next Steps, reporting/lineage, cleanup applicability, and terminal disposition are explicitly accounted for.

## Canonical accounting

v6.5 promotes only the two broad AD CS canonical parents whose useful atomized depth is fully exhausted in this release:

- `adcs.enumeration`
- `adcs.web-enrollment`

The broader certificate-template parent remains partial because ESC13, ESC15, and other distinct template branches remain open.

Current canonical state:

- 95 / 127 implemented
- 32 partial
- 0 gaps
- 0 stale
- 75% fully implemented
- 100% represented

## Atomic source fidelity

The AD CS source file remains the first atomized Orange methodology file. The 19-unit AD CS ledger now has:

- 5 / 19 fidelity-complete
- 14 / 19 pending
- 1 / 17 methodology source files atomized
- 7 / 34 frozen v6.2 partial baselines decomposed
- 27 frozen partial baselines still awaiting atomic source inventory

`data/source-delivery-v6.5.js` finishes the operator-side delivery contract for this wave. It adds the missing Windows/Kali enumeration alternatives from the pinned source, an explicit ESC3 enrollment-agent certificate stage, and frozen-baseline completion markers for the two exhausted broad parents.

## Evidence boundaries

Obol remains static, browser-local, and human-run. AD CS enumeration, relay listener startup, certificate requests, saved certificate material, and certificate authentication remain distinct proof boundaries.

- Enumeration routes Next Steps only from explicit CA/template/endpoint/ESC observations.
- ESC8 listener startup is not certificate issuance.
- ESC1/ESC2/ESC3 only produce certificate facts from explicit issuance or saved certificate/PFX output.
- ESC2 agent material is an intermediate artifact and does not establish the ESC3 target identity.
- Certificate possession or authentication artifacts never silently become administrator, SYSTEM, or compromise facts.

## Build Next

The synchronized queue falls from 46 to 41 items:

- 0 implemented-quality repairs
- 0 mapped-delivery repairs
- 0 canonical gaps
- 14 remaining AD CS atomic source-fidelity audits
- 27 source-inventory/decomposition items

The next atomic item is ESC13, followed by the remaining ESC15, ESC4, ESC7, ESC5, ESC6, ESC11, ESC9/10, ESC14, and related variants from the pinned AD CS source.
