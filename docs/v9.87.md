# Obol v9.87

## What changed

- Mined `source-note-cluster-linux-privesc-enumeration-and-proof` into public-safe product mechanics.
- Enriched `linux-privesc-boundary-sweep` with Linux privilege-escalation signal-routing mechanics rather than creating a duplicate primary card.
- Kept service, sudo, SUID, capability, scheduled-execution, and kernel-risk lessons inside the canonical privilege-escalation boundary.
- Added public-safe field notes for signal routing, sudo rule proof, SUID/capability candidate handling, service/scheduled execution proof, kernel/local-exploit risk, and cleanup/report boundaries.
- Added `linux-privesc-signal-router-v987` analyzer logic for pasted sudo, SUID, capability, systemd/cron, kernel, enum-script, cleanup, and identity evidence.
- Added a downstream cluster queue refinement so the next Product Build Next item advances to `source-note-cluster-windows-privesc-services-and-local-admin` and the remaining eight cluster items stay explicit.
- Advanced the source-note ledger to 404/556 reviewed, 152 pending, and 8 remaining public-safe cluster review items.

## Wrapper control

- Does not create Linux service, SUID, kernel, local-exploit, or signal-router wrapper cards.
- Keeps exploit-specific recipes, payload source, target version chains, lab answers, flags, credentials, and private note prose out of public artifacts.
- Treats sudo, SUID, capability, service/schedule, credential-like, and kernel findings as separate evidence boundaries until effect proof and cleanup are captured.

## Connector fallback note

- Documents the observed `Could not resolve host: github.com` shell failure pattern so future agents switch directly to connector-only work after one diagnostic instead of burning time on repeated shell retries.
- Preserves the rule that DNS failure does not weaken the exact-head green PR-check requirement.

## Validation

- Adds `tests/run-v9.87-tests.js` as the current product contract.
- Checks the enriched boundary-sweep action spine, no duplicate signal-router card, field-note bindings, analyzer redaction, intake integration, queue handoff, and downstream cluster generation.
- Keeps the release gate pointed at v9.87 with `tools/validate-release-pr.js --repo-only --release-version=9.87`.
