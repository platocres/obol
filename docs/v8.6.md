# Obol v8.6

v8.6 completes the pinned Orange `know_vuln_auth.md` whole-file source inventory as the next wave after v8.5.

Pinned Orange commit: `6d16ca0d1434875e0617f2f3cfa825fad0bc7d7e`

Pinned `know_vuln_auth.md` blob: `fb25cc7766f1d1f169b258c0800063946071b364`

The release reviews sixteen terminal source units across MS14-068, GPP/MS14-025, PrivExchange, noPac, PrintNightmare, Certifried, and ProxyNotShell. Fourteen units are modeled through mature Obol owners. The redundant Metasploit MS14-068 and GPP scanner routes are explicitly superseded rather than silently omitted.

Upstream command review is source-faithful without copying stale or risky defaults blindly. The GoldenPac and Get-GPPPassword routes are normalized to current Impacket packaging conventions, PrintNightmare remains detection-first with an operator-reviewed benign validation DLL rather than the pinned add-user payload, and ProxyNotShell keeps a bounded `whoami` validation command instead of exposing an arbitrary mutating command as the base action.

Canonical breadth remains 127/127 implemented and the frozen v6.2 source-depth baseline remains 34/34 decomposed. Whole-file source inventory advances from 14/17 to 15/17 and the atomic ledger expands from 266/266 to 282/282 fidelity-complete units. Build Next now contains only `trusts.md` and `valid_user.md`.

Evidence remains conservative. Vulnerability detections, platform clues, coerced authentication, recovered credentials, generated tickets/certificates, execution, privilege, and cleanup remain separate proof stages. Project/source accounting metadata never creates engagement facts.

Obol remains static, offline-capable, browser-local, and human-run only. Commands are guidance for authorized environments and are never executed automatically by Obol.
