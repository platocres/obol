# Obol v9.84

## What changed

Obol v9.84 mines the queued `source-note-cluster-credential-dumping-lsass-and-windows-secrets` cluster from the complete private review packet route and converts it into public-safe credential dumping, secret-source triage, parser-output classification, validation, cleanup, and dashboard handoff mechanics.

## Product surface

- Adds `windows-secret-source-triage-workflow` as a real action-spine card because selecting among LSASS memory, registry hives, DPAPI/vault material, application configuration, database rows, and copied secret files is its own operator decision point.
- Enriches `credential-dump-proof-chain` with LSASS PID/dump lineage, registry hive pairing, offline parser output, hash/crack boundaries, scoped validation, and cleanup evidence.
- Enriches `pass-the-hash-proof-chain` only as a downstream handoff, not as a duplicate credential-dump card.
- Adds `windows-credential-dumping-secrets-analyzer-v984` for pasted LSASS, hive, parser, DPAPI/vault, app-config, database-hash, cracking, validation, blocked-control, and cleanup evidence.
- Adds public-safe field notes for secret-source triage, LSASS lineage, hive pairing, parser material classification, DPAPI/vault context, application/database secret provenance, cracking/reuse boundaries, blocked-control evidence, cleanup/redaction, and dashboard handoff.

## Dashboard and queue handoff

v9.84 updates the source-cluster ledger and product-hardening queue together:

- Reviewed notes advance from 333/556 to 348/556.
- Pending notes move from 223 to 208.
- Pending clusters move from 12 to 11.
- The completed queue is `source-note-cluster-credential-dumping-lsass-and-windows-secrets`.
- Product Build Next advances to `source-note-cluster-pass-the-hash-and-remote-exec-artifacts`.

The v9.84 contract explicitly checks `OBOL_PRODUCT_HARDENING.nextNotesBatch`, `concreteBuildNext(1)`, and `OBOL_SOURCE_NOTE_CLUSTERS.reviewQueue[0]` so the README and Product Hardening Dashboard do not drift apart.

## Source basis

- Source route: `platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json`.
- Assigned cluster: `credential-dumping-lsass-and-windows-secrets`.
- Assigned source windows: HTB packet 01 and OffSec PEN-200 packet 04.
- Review policy: `complete_cleaned_text`.
- Review text chars: 521,935.
- Source notes reviewed: 15.

## Compatibility

- No backend, accounts, telemetry, install prompt, or automatic command execution.
- No LSASS, Mimikatz, secretsdump, DPAPI, registry-hive, hashcat, or app-config micro-card spray.
- New card creation is limited to the distinct secret-source triage action spine.
- Public source boundaries remain intact: no flags, credentials, target hosts, copied course prose, private lab answers, or raw solution chains are published.
