# Obol v9.73 - Pending source note disposition batch 1

v9.73 completes `notes-disposition-pending-review-001`, the first batch of unreviewed private source notes after the v9.72 old-rubric closeout. The release reads the first 20 pending notes in manifest/source order from the complete private review packet fallback and converts only reusable, public-safe value into Obol.

## Queue source

- Current release entering this build: v9.72.
- Source re-mining gate: 135/135 complete.
- Old-rubric-only reviewed notes remaining: 0.
- Pending private source notes entering the build: 421.
- Pending private source notes after the build: 401.
- Next notes batch: `notes-disposition-pending-review-002`.
- Source packet: `data/review-packets/htb-penetration-tester-01.json` from `platocres/obol-source-notes@agent/review-packets:data/review-packets/manifest.json`.

## Batch disposition

The 20 selected notes produced terminal dispositions without adding any new primary path cards.

- 14 notes were modeled into public-safe product mechanics.
- 4 notes were covered by existing cards or Orange/navigation scaffolding and marked as superseded for this pass.
- 2 notes were kept private-reference-only because the source value was dominated by assessment answer or obfuscation-recipe material.

The batch advances the notes ledger from `135/556` to `155/556` reviewed. The remaining pending-note queue moves from `421` to `401`.

## Product additions

### Existing card enrichment

v9.73 intentionally folds value into existing cards instead of creating duplicate path clutter.

- `credential-dump-proof-chain` receives Windows credential-artifact boundary guidance for LSASS-style artifacts, offline parsing, redaction, and separate validation.
- `burp-intruder-fuzzing-workflow` receives proxy transform-loop guidance for payload position, encoding/decoding, response deltas, and paste-back evidence.
- `web-upload-inclusion-proof-chain` receives inclusion poisoning guidance for read/write/include/execute/cleanup proof boundaries.
- `web-authz-boundaries` receives IDOR/object-reference replay guidance, HTTP method differential guidance, report-boundary notes, and private-boundary guidance for obfuscation/evasion material.

### Public-safe field notes

The release adds seven public-safe field notes:

- `note-windows-credential-artifact-boundary-v973`
- `note-web-proxy-transform-loop-v973`
- `note-lfi-poisoning-proof-chain-v973`
- `note-idor-object-reference-replay-v973`
- `note-http-method-differential-v973`
- `note-web-attack-reporting-boundary-v973`
- `note-web-evasion-tool-private-boundary-v973`

### Evidence analyzer

Adds `web-boundary-evidence-analyzer-v9.73`, which recognizes pasted evidence for:

- HTTP method surface and method-auth differential candidates
- IDOR/object-reference candidates
- file-inclusion/session/log poisoning candidates
- proxy transform chains such as encoding, hashing, and payload processing

The analyzer folds findings into existing web authorization, upload/inclusion, and Burp Intruder cards rather than creating new primary cards.

## Product gaps queued

The first pending batch surfaced two concrete future gaps that should not be jammed into existing cards without a proper action spine:

```text
xxe-proof-boundary-card
command-injection-proof-boundary-card
```

These are queued as product gaps. They should become cards only if a later batch supplies concrete commands or GUI steps, expected evidence, failure modes, next movement, dynamic why-now compatibility, and uniqueness proof.

## Privacy / derivation

The private source packet contains lab targets, answers, screenshots, and exact payload material. Public Obol receives generalized proof-boundary language, synthetic command templates, analyzer tags, field-note context, and report guidance only. Course prose, flags, credentials, target values, screenshots, exact payload recipes, and solution chains remain private.

## Validation

Focused release validation:

```bash
node tests/run-v9.73-tests.js
node tools/validate-card-action-spine-v9.71.js
node tools/validate-path-card-uniqueness-v9.72.js
node tools/validate-release-pr.js --repo-only
```

Browser validation keeps the v9.71/v9.72 card UI and uniqueness gates active while allowing the one additional v9.73 product-hardening extension in the consolidated runtime request budget.
