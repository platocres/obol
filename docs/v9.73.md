# Obol v9.73 - Pending source note disposition batch 1

v9.73 starts the next notes gate after the v9.72 old-rubric closeout. The queued work is `notes-disposition-pending-review-001`, the first batch of unreviewed private source notes from the complete review packet fallback.

## Queue source

- Current release entering this build: v9.72.
- Source re-mining gate: 135/135 complete.
- Old-rubric-only reviewed notes remaining: 0.
- Pending private source notes: 421.
- Next notes batch: `notes-disposition-pending-review-001`.
- Selector: select the next 20 unreviewed private source notes in manifest/source order.

## Build intent

This release should disposition the first 20 pending source notes without regressing the action-card cleanup that landed in v9.71 and v9.72.

Each selected note must resolve to one of these terminal outcomes:

- public-safe product output
- covered by existing card, analyzer, report mechanic, or field note
- queued product gap
- private-boundary proof

For any note that produces a card, analyzer, tool-builder change, path branch, report mechanic, or UI behavior, keep the full 16-dimension audit row:

```text
path-bindings
tool-cards
gui-controls
scripts-one-liners
command-templates
terminal-analyzers
evidence-expectations
path-movement
lesson-boxes
examples
troubleshooting
cleanup
report-guidance
product-mechanics
product-gaps
orange-baseline
```

## Guardrails to preserve

- Do not publish course prose, flags, credentials, target values, screenshots, exact exploit payloads, or solution chains.
- Extract reusable product mechanics, not wording.
- New primary cards must pass action-spine validation.
- New primary cards must pass the v9.72 uniqueness gate.
- Prefer merging note value into existing cards, field notes, analyzers, dynamic why-now metadata, report guidance, or tool cards before creating new path cards.
- If a new primary card is truly required, it needs concrete commands or GUI actions, evidence expectations, failure guidance, next movement, and dynamic why-now compatibility.

## Expected implementation shape

Likely files for the completed build:

```text
data/product-hardening/pending-disposition-batch-v9.73.js
docs/v9.73.md
tests/run-v9.73-tests.js
```

Additional files should be touched only when the mined notes justify product changes, route guards, analyzers, dashboard queue state, README projection, or validator coverage.

## Validation target

Focused release validation should include:

```bash
node tests/run-v9.73-tests.js
node tools/validate-card-action-spine-v9.71.js
node tools/validate-path-card-uniqueness-v9.72.js
node tools/validate-release-pr.js --repo-only
```

Before merge, the exact PR head must also pass the repository regression and browser smoke workflows.
